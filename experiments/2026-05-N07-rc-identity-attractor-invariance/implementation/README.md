# Implementation Trace

This directory records implementation notes for
`2026-05-N07-rc-identity-attractor-invariance`.

N07 starts after N06 closed at:

```text
strongest_supported_sc_level = SC6
strongest_claim_ceiling = artifact_only_semantic_route_choice_candidate
semantic_choice_claim_allowed = false
```

N07 does not continue semantic choice, trail memory, goal regulation, ACO, or
agency claims directly. It opens the identity question:

```text
Can LGRC evidence support the same RC identity attractor across cycles,
perturbations, and topology/lineage contexts?
```

Use this area for experiment-local notes such as:

- ID-ladder run plans,
- RC theory-to-LGRC observable mapping,
- Arc of Becoming method records: classification, bounded probes,
  withdrawal, naturalization, and integration,
- support-area and basin fixture design,
- attractor stability and flux-convergence controls,
- proper-time identity persistence probes,
- artifact-only identity replay reports,
- and closeouts.

This is not the global `implementation/` directory. If N07 discovers missing
native support for identity surfaces, attractor potentials, support-area
transport, basin compatibility, or artifact replay, stop and open a separate
Phase 8 implementation task.

## Claim Boundary

N07 may produce RC identity-attractor candidates. It must keep these blocked
unless separately validated in later experiments:

```text
agency
agentic-like behavior
memory or trail formation
goal-proxy regulation
semantic choice promotion
locomotion-like behavior
biological behavior
personhood
unrestricted identity acceptance
```

Identity continuity is a prerequisite for later agentic-like evidence. It is
not itself agency.

Start with the root experiment README:

```text
../README.md
```

Then use:

```text
RCIdentityAttractorInvarianceImplementationPlan.md
RCIdentityAttractorInvarianceImplementationChecklist.md
```
