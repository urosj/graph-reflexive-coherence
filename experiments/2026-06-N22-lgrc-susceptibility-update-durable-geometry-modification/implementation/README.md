# N22 Implementation

The implementation plan and checklist define the staged reconstruction path for
N22.

Start with:

```text
SusceptibilityUpdateDurableGeometryImplementationPlan.md
SusceptibilityUpdateDurableGeometryImplementationChecklist.md
```

Implementation artifacts should preserve the N20/N21 handoff boundary, use
repository-relative paths only, and keep all unsafe claim flags false unless a
later source-backed experiment explicitly changes the allowed claim boundary.
