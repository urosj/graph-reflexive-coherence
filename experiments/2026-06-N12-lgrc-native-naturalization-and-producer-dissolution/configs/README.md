# N12 Configs

This directory is reserved for frozen N12 configuration or schema fragments if
later iterations need them.

The primary planned schema artifact is intentionally listed under `outputs/`:

```text
outputs/n12_naturalization_schema_v1.json
```

No config artifacts have been generated yet.
