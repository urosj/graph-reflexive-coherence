# Hypothesis D - Composition And Bidirectional Handoff

N29 can define first runnable ecology probes only if it composes existing
primitives into bounded candidate mechanisms without overclaiming ecology. It
must also return unresolved primitive and agency-component debt to N30+ work in
this repository.

Expected support:

```text
N28 generative/extractive pattern compositions are classified
N08/N22 trace-aftereffect routes are considered as trace surfaces
N13/N24 support/surplus rows are considered as reserve/pressure surfaces
N16/N25.2 boundary/multi-basin rows are considered as bounded-unit surfaces
N17 loops are considered as perturbation-response candidates
N26 proxy collapse is considered as proxy-diagnostic candidate
N27 transfer is considered as substrate/configuration-transfer candidate
```

First probe contracts should specify:

```text
probe_goal
required_source_rows
runtime_surface_requirements
producer_residue
medium_debt
controls
expected failure modes
claim ceiling
handoff target in agentic ecology project
```

N30+ continuation records should specify:

```text
missing native mechanism
producer surface to naturalize
medium surface to strengthen
primitive robustness gap
agency-component follow-up target
source rows that exposed the gap
claim boundary
```

Failure conditions:

```text
first probe requires unsupported native ecology
composition hides medium debt
N28 composition is treated as semantic cooperation
trace/pressure/susceptibility terms are used without source surfaces
handoff lacks controls and claim ceiling
N29 exports ecology probes but omits N30+ continuation debt
N29 implies this repository is finished after ecology handoff
```
