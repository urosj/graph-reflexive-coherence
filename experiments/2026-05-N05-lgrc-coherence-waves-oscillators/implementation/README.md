# Implementation Trace

This directory records what is done for the
`2026-05-N05-lgrc-coherence-waves-oscillators` experiment family and why.

N05 starts after N04 closed the movement/topology-mutating tranche at:

```text
topology_mutating_movement_candidate
```

N05 does not continue movement claims directly. It opens a lower-level
coherence-circuit question:

```text
Can LGRC produce delayed, replayable, budget-valid coherence-wave and
oscillator cycles?
```

Use this area for experiment-local notes such as:

- O-ladder run plans,
- source/target/reservoir fixture design,
- pulse and return-lineage conventions,
- oscillator controls,
- observed runtime capability gaps,
- artifact-only replay reports,
- and closeouts.

This is not the global `implementation/` directory. If N05 discovers missing
native support, such as serialized custom potentials or flux-facilitated metric
maps, stop and open a separate Phase 8 implementation task.

## Claim Boundary

N05 may produce coherence-wave or oscillator candidates. It must keep these
blocked unless separately validated in later experiments:

```text
movement
semantic choice
agency
RC identity collapse
identity acceptance
memory or trail formation
goal-proxy regulation
agentic-like behavior
locomotion-like behavior
biological behavior
ant-colony behavior
unrestricted movement
```

## Current Status

Initial definition only. No N05 probes have been run yet.

Start with the root experiment README:

```text
../README.md
```

Then use:

```text
CoherenceOscillatorsImplementationPlan.md
CoherenceOscillatorsImplementationChecklist.md
```
