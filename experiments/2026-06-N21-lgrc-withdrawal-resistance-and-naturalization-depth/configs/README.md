# N21 Configs

Configuration records for N21 should define only declared thresholds, withdrawal
windows, support/scaffold surfaces, replay windows, and fail-closed control
settings.

Config records must not contain local absolute paths or machine-specific state.

