# Hypothesis C - Claim Boundary And Producer-Residue Control

N21 can test withdrawal resistance and naturalization depth without converting
contract definitions, diagnostic vocabulary, producer-mediated state, or
naturalization debt into evidence.

## Expected Support

Hypothesis C is supported only if N21 records:

```text
N20 I5 rows consumed without redefinition
primitive evidence separated from N20 contract evidence
producer_mediated fields not counted as substrate-carried state
naturalization_debt fields not counted as native support
blocked_relabel fields rejected when used as evidence
all positive rows source-backed
derived_report_only = true blocks positive primitive support
WR/ND/N21-C ladder rungs assigned only from N21 evidence rows
all replay/control rows fail closed when their blocker condition triggers
unsafe claim flags false in every row
no Phase 8 implementation opened
no ant-ecology implementation spec opened
```

## Failure Conditions

Hypothesis C fails if any N21 result:

```text
passes by changing the N20 continuation rule
passes by changing the N20 proxy-only success blocker
passes by treating producer-mediated fields as native support
passes by treating diagnostic vocabulary as implementation evidence
passes from report-only or synthetic rows with derived_report_only = true
opens agency, selfhood, identity, sentience, or Phase 8 claims
imports ant-ecology implementation requirements before N29
```

Hypothesis C is the claim-hygiene condition for Hypotheses A and B. If C fails,
positive withdrawal-resistance or naturalization-depth rows must be demoted or
blocked regardless of their local margins.

## Claim Ceiling

The strongest admissible claim is:

```text
claim-clean artifact-level becoming-primitive evidence candidate
```

This claim remains below agency, native support, sentience, and Phase 8.
