# N10 Implementation

This directory tracks implementation planning and run records for
`2026-05-N10-lgrc-agentic-like-integration`.

N10 is experiment-local unless a separate Phase 8/core implementation task is
opened. Do not change `src/*` for N10 without stopping and recording the
runtime gap that requires native absorption.

Use the implementation plan and checklist as the source of truth:

```text
AgenticLikeIntegrationImplementationPlan.md
AgenticLikeIntegrationImplementationChecklist.md
```

Expected local artifact directories:

```text
configs/
outputs/
reports/
scripts/
hypotheses/
```

N10 should cite N05-N09 artifacts as prerequisites and create new N10
integration artifacts. It should not rewrite older experiment results.

