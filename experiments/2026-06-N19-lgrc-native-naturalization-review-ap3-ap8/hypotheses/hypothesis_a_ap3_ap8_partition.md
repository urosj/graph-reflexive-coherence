# Hypothesis A - AP3-AP8 Partition

The N13-N18 AP3-AP8 artifacts can be partitioned using the N12 NAT ladder
without promoting artifact evidence into native support.

Expected support:

```text
source-backed rows for N13-N18
N12 NAT ladder replayed without schema drift
each AP row assigned exactly one primary disposition
artifact evidence remains artifact evidence
phase8_opened = false
native_support_opened = false
```

Failure condition:

```text
artifact-only AP evidence is relabeled as native support, agency, selfhood,
identity acceptance, semantic action/perception, or organism/life behavior
```

