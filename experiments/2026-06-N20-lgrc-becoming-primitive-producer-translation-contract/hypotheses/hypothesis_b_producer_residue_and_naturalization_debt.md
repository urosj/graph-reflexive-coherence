# Hypothesis B - Producer Residue And Naturalization Debt

N20 can separate substrate-carried geometry from producer-mediated residue
before later primitive tests begin.

Expected support:

```text
producer definition frozen
variable classification enum frozen
each variable receives exactly one classification
producer residue ledger defined
naturalization debt ledger defined
native function descriptor schema defined
continuation function descriptor alias defined
proxy metric definition schema defined
support/scaffold declaration schema defined
same-basin continuation criteria defined
proxy-only success blockers defined
```

Failure condition:

```text
a later-positive primitive could pass without stating what is carried by
source-current geometry and what is still supplied by producer code
```

Additional guard:

```text
later experiments cannot convert producer_mediated or naturalization_debt fields
into substrate_carried evidence without a source-backed naturalization result
```
