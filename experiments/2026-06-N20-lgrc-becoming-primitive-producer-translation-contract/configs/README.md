# N20 Configs

N20 starts as a contract/schema experiment. Config files may be added when the
translation schema, producer-residue ledger, or primitive inventory needs a
stable machine-readable policy.

Config records must use relative paths only and must not encode local machine
state.

