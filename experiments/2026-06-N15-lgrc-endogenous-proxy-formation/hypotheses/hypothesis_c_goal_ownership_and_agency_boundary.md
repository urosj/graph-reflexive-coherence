# Hypothesis C - Goal Ownership And Agency Boundary

## Statement

```text
Even if endogenous proxy formation is observed, semantic goal ownership,
intention, semantic choice, agency, identity acceptance, selfhood, personhood,
biological behavior, native support, fully native integration, and unrestricted
agency remain blocked unless separate theory and native implementation gates
are satisfied.
```

## N15-Specific Meaning

N15 is close to goal-ownership language because it asks whether targets can be
formed from runtime-visible state rather than supplied externally. This
hypothesis tests whether that result stays inside the AP5
agency-prerequisite boundary.

The N15 positive result, if achieved, can be only:

```text
artifact_level_ap5_endogenous_proxy_formation_candidate
```

not:

```text
semantic goal ownership
intention
semantic choice
agency
identity acceptance
selfhood
personhood
biological behavior
native support
fully native agentic-like integration
```

## Acceptance Requirements

Hypothesis C can be supported only if:

```text
semantic goal ownership relabel controls fail closed
intention relabel controls fail closed
semantic choice relabel controls fail closed
agency relabel controls fail closed
identity acceptance relabel controls fail closed
native support relabel controls fail closed
selfhood relabel controls fail closed
personhood relabel controls fail closed
biological behavior relabel controls fail closed
unrestricted agency relabel controls fail closed
all unsafe claim flags remain false
phase8_opened = false
native_supported_flags = false
fully_native_integration_opened = false
src_diff_empty = true
```

## Rejection Conditions

Reject or defer the hypothesis if:

```text
AP5 evidence requires semantic goal ownership language
runtime-derived target formation is described as intention
support-derived target formation is promoted into agency
support/identity-condition descriptors are promoted into identity acceptance
N12 NAT4 readiness records are treated as native support
native implementation is implied without Phase 8
fully native integration is claimed from artifact-only evidence
```

## Claim Boundary

```text
endogenous proxy formation != semantic goal ownership
runtime-derived target != intention
support-derived target != agency
support/identity-condition descriptor != identity acceptance
artifact-level AP5 != native support
N15 AP5 != fully native agentic-like integration
```
