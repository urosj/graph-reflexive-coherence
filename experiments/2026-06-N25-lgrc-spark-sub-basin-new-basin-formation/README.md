# N25 - LGRC Spark / Sub-Basin / New-Basin Formation

N25 is a becoming-primitive experiment after N24. It consumes the N20
`spark_sub_basin_new_basin_formation` contract and the N24 closeout as bounded
surplus-supported optionality context, then tests whether becoming pressure can
produce a distinguishable sub-basin or new-basin candidate rather than only
reinforcing or thickening an existing basin.

Current state:

```text
status = initialized
source_contract_row = n20_i4_row_06_spark_sub_basin_new_basin_formation
source_consumable_contract_row = n20_i5_row_06_spark_sub_basin_new_basin_formation
n20_contract_status = complete
n24_native_lane = AB5_N24-C5_surplus_supported_optionality
n24_native_flux_debt = flux_envelope_not_widened_above_1e-9
n24_producer_assisted_lane = producer_mediated_flux_conditioning_scaffold
target_primitive = spark_sub_basin_new_basin_formation
target_reading = spark / sub-basin / new-basin formation
local_ladder = BF0...BF6
closeout_ladder = N25-C0...N25-C6
next_experiment = N26_proxy_divergence_proxy_collapse
```

Core question:

```text
Can becoming pressure produce a new distinguishable basin or sub-basin, rather
than only reinforcing an existing basin?
```

N25 is not a semantic learning, semantic choice, agency, native support,
sentience, Phase 8, or ant-ecology experiment. `Spark` and `new basin` may be
used only as bounded LGRC readings for source-backed bifurcation or
basin-formation candidates:

```text
a distinguishable boundary, support/coherence floor, and replayable state that
is not merely old-basin thickening, label assignment, transient fluctuation,
merge/leakage, or hidden producer insertion
```

## Source Boundary

Primary source artifacts:

```text
experiments/2026-06-N20-lgrc-becoming-primitive-producer-translation-contract/outputs/n20_native_function_proxy_contract.json
experiments/2026-06-N20-lgrc-becoming-primitive-producer-translation-contract/outputs/n20_same_basin_continuation_contract.json
experiments/2026-06-N24-lgrc-abundance-surplus-supported-optionality/outputs/n24_closeout_and_n25_handoff.json
experiments/2026-06-N24-lgrc-abundance-surplus-supported-optionality/outputs/n24_producer_flux_conditioning_probe_i7c.json
experiments/N20-N29-LGRC-BecomingAgencyEcologyHandoff.md
experiments/N20-N29-LGRC-BecomingAgencyEcologyRoadmap.md
```

N25 must consume:

```text
N20 source contract row = n20_i4_row_06_spark_sub_basin_new_basin_formation
N20 consumable same-basin row = n20_i5_row_06_spark_sub_basin_new_basin_formation
N24 native lane = AB5 / N24-C5 bounded surplus-supported optionality
N24 producer-assisted lane = I7-C producer-mediated flux-conditioning scaffold
```

N24 evidence is prerequisite context only. It can provide bounded optionality,
stress-backed jointly admissible branches, and a separate producer-mediated
flux scaffold. It cannot satisfy N25 basin-formation evidence by itself and
must not be relabeled as new-basin formation, semantic choice, agency, native
support, sentience, Phase 8, or ant ecology.

The native lane must be tested first. The producer-assisted lane may be tested
only as a separate extension:

```text
producer-assisted success cannot overwrite native failure
producer-assisted success may identify a minimal missing native mechanism
```

## Required Source-Current Fields

Positive N25 rows must provide source-current evidence for:

```text
source_current_inputs
source_contract_row_digest
source_consumable_contract_row_digest
source_output_digest
run_artifact_id
runtime_config_digest
source_commit_or_source_digest
artifact_paths
artifact_sha256
artifact_paths_equal_manifest_paths
artifact_sha256_equal_manifest_sha256
output_digest
row_specific_thresholds_declared_before_use
lane
lane_success_can_upgrade_native
native_lane_failure_overwritten
producer_assisted_result_class
formation_class
formation_source
spark_sub_basin_new_basin_formation.bifurcation_trace
spark_sub_basin_new_basin_formation.new_boundary_candidate_trace
spark_sub_basin_new_basin_formation.new_basin_support_coherence_trace
spark_sub_basin_new_basin_formation.replayable_distinction_trace
formation_window
bifurcation_window
boundary_candidate_window
replay_window
old_basin_reference_window
bifurcation_window_order_valid
thresholds_declared_before_bifurcation_window
old_basin_signature_digest
candidate_basin_signature_digest
candidate_boundary_signature_digest
old_to_candidate_separation_digest
boundary_distinguishability_margin
support_floor_margin_new_region
coherence_floor_margin_new_region
old_basin_separation_margin
merge_leakage_margin
replay_distinction_persistence_ratio
native_flux_debt_bound
native_flux_debt_widened
native_flux_debt_status
```

Evidence must come from LGRC runtime or replay artifacts. Labels, basin IDs
assigned after the fact, split threshold schedules, seed insertion policies,
producer enumeration, hidden support, merge/leakage, or report-built rows are
not evidence.

## Primitive Reading

Spark / sub-basin / new-basin formation means:

```text
becoming pressure produces a distinguishable basin-like region with its own
boundary candidate, support/coherence floor, and replayable state while the
old-basin relation and flux/leakage accounting remain auditable.
```

Operational distinction:

```text
sub_basin_candidate =
  boundary-distinguishable region inside or attached to old basin
  old-basin relation remains auditable
  support/coherence floor is distinct from local old-basin thickening

new_basin_candidate =
  boundary-distinguishable region with replayable support/coherence floor
  not reducible to old-basin thickening
  merge/leakage controls clean
  persists under stress/threshold matrix
```

N25 must distinguish:

```text
reinforced old basin
reshaped old boundary
sub-basin differentiation
new basin formation candidate
transient fluctuation misread as basin
producer-assisted new-basin scaffold
```

Candidate rows must use:

```text
formation_class =
  reinforced_old_basin |
  reshaped_old_boundary |
  sub_basin_candidate |
  new_basin_candidate |
  transient_fluctuation |
  merge_leakage_artifact |
  producer_assisted_scaffold

formation_source =
  native_source_current_bifurcation |
  native_old_basin_thickening |
  native_merge_leakage |
  producer_flux_conditioned |
  hidden_producer_insertion |
  label_only |
  report_derived
```

Native `BF3+` rows require `formation_source =
native_source_current_bifurcation`. Producer-assisted `BF3+` rows require
`formation_source = producer_flux_conditioned` and must keep producer residue
and naturalization debt visible.

## Lane Ceilings

Every row must record:

```text
lane = native | producer_assisted
lane_success_can_upgrade_native = false
native_lane_failure_overwritten = false
producer_assisted_result_class =
  not_applicable |
  producer_mediated_scaffold_candidate |
  missing_native_mechanism_probe
```

Native rows may support native BF rungs only if all native gates pass.
Producer-assisted rows may support producer-assisted BF candidates and identify
naturalization targets, but cannot upgrade native BF, N24 native C6, native
support, Phase 8, semantic agency, or ant ecology.

Cross-field invariants:

```text
if lane = native:
  formation_source = native_source_current_bifurcation
  producer_assisted_result_class = not_applicable
  native_flux_debt_bound = 1e-9
  native_flux_debt_widened = false
  native_flux_debt_status = preserved

if lane = producer_assisted:
  formation_source = producer_flux_conditioned
  native_flux_debt_not_overwritten = true
  producer_flux_window_declared_before_use = true
```

## Artifact Roles

N25 artifacts must use explicit roles:

```text
bifurcation_trace
new_boundary_candidate_trace
new_basin_support_coherence_trace
replayable_distinction_trace
old_basin_relation_trace
merge_leakage_trace
native_flux_debt_trace
producer_intervention_ledger
formation_replay_trace
stress_boundary_trace
negative_control_trace
runtime_trace
threshold_record
active_null_trace
closeout
source_handoff
schema_control_freeze
```

## Local N25 Ladder

N25 uses a local basin-formation ladder:

```text
BF0 = no source-current basin-formation evidence
BF1 = run artifact with becoming-pressure / N24 optionality context
BF2 = bifurcation or spark trace observed, but distinguishable basin not yet stable
BF3 = source-current distinguishable sub-basin boundary/support candidate
BF4 = replay/control-backed sub-basin differentiation candidate
BF5 = stress/threshold-backed new-basin formation candidate with merge/leakage controls clean
BF6 = N26-ready bounded basin-formation evidence
```

Rows below `BF3` cannot support sub-basin/new-basin formation. Rows below
`BF4` cannot support replay/control-backed formation evidence. `BF6` is a
handoff rung for N26, not agency, semantic choice, native support, or Phase 8.

## N25 Closeout Ladder

N25 also uses a tranche-level closeout ladder:

```text
N25-C0 = contract-only closeout
N25-C1 = active-null/control discipline established
N25-C2 = spark/bifurcation partial
N25-C3 = source-current sub-basin candidate
N25-C4 = replay/control-backed sub-basin differentiation candidate
N25-C5 = stress/threshold-backed basin-formation candidate
N25-C6 = N26-ready bounded basin-formation evidence
```

The closeout ladder classifies the whole N25 tranche. It must not convert a
basin-formation candidate into semantic learning, choice, agency, native
support, sentience, Phase 8, or ant-ecology evidence.

## AP Gap Boundary

N25 must preserve the AP gap split inherited from N19/N20/N24:

```text
AP4:
  N23/N24 provide bounded selection/optionality context, but N25 must not claim
  final global AP4 reclassification.

AP5:
  AP5 is required only when proxy derivation, target formation, or proxy-based
  basin valuation participates. N25 itself should avoid proxy/target formation;
  N26 is the proxy divergence / proxy collapse experiment.
```

## Claim Boundary

N25 must keep these blocked:

```text
semantic learning
semantic choice
semantic intention
semantic action
semantic perception
semantic goal ownership
agency
selfhood
identity acceptance
native support
sentience
consciousness
organism/life
Phase 8 implementation
ant ecology implementation
native ant agency
native colony agency
unrestricted autonomy
```

## Expected Iteration Shape

```text
I1 = source and handoff inventory
I2 = basin-formation schema and control freeze
I3 = active nulls and failure baselines
I4 = native optional-branch bifurcation probe
I5 = native replay/control matrix
I6 = producer-assisted flux-conditioned formation probe
I7 = comparative stress and formation-boundary matrix
I8 = closeout and N26 handoff
```
