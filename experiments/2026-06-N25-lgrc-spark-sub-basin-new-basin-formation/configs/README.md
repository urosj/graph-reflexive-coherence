# N25 Configs

N25 configs will be added only when an iteration needs a durable runtime
configuration. Configs must use repo-relative paths and must not encode local
machine state.

Expected configuration families:

```text
native_optional_branch_bifurcation
producer_assisted_flux_conditioned_bifurcation
replay_control_matrix
stress_boundary_matrix
```

