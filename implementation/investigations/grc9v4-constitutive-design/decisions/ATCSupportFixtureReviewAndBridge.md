> Relocation-only maintenance: repository paths and dependent identities were
> rebuilt before first commit. Scientific statements, numerical results and
> review verdicts are unchanged; the new identities are not an independent
> reviewer rerun. Earlier byte-preservation statements describe the pre-move stage.

# Support-fixture review: native bridge and causal discrimination

**Status:** independent review supports the bounded research result. This note
integrates its claim corrections and specifies the next two gates; it does not
accept F2, admit a new potential, authorize production changes or close a
capability cell. The descendant and support records remain unchanged.
The subsequent review reports **PASS as a research design** and PASS for the
native-boundary evidence, with **Gate N and Gate C both OPEN**. Its architectural
and scientific pressure is incorporated below; no native campaign is inferred.
The final design/preregistration review independently confirms the pinned
binding, exact predictions and numerical comparison bounds, with no requested
mathematical redesign. **Design/preregistration independent review is complete.**
This is not user acceptance, runtime admission or closure of either native gate.

**Placement clarification and current result:** the user clarified that research
code belongs inside the investigation and is allowed; only substrate `src/*`
is frozen. No production exception is needed. The subsequent
[local realization and causal experiment](./ATCSupportLocalResearch.md) implements
and tests the mathematical binding here, without changing `src/*`. The native
gate language below describes later substrate conformance, not a prerequisite
for mathematical foundations or permission to create research code.
The subsequent independent review [closes N-R and bounded C-R](./ATCSupportLocalResearch.md#research-review-disposition).
N-P/C-P denote deferred production integration/reproduction. Earlier statements
that both gates are open describe their prior or native scope, not a remaining
research-realization or fixed-experiment debt. The next scientific work is the
[root-independent sufficient-condition proposal](./ATCAddressabilitySufficientConditions.md).

## 1. Review disposition and claim boundaries

The review independently reproduced the polynomial, interval bounds, equilibrium
bracket, trapping inequalities, response bounds and certificate digest in the
[continuation/support study](./ATCFissionContinuationAndSupport.md). It reported
no blocking mathematical error. The precise dispositions are:

| Evidence | Supported conclusion | Not established |
| --- | --- | --- |
| Native descendant attempt | F2 survived one further application after its own graph change and an intervening ordinary update | Unbounded repetition, installed automatic ATC, terminal stopping |
| Proposed multiwell certificate | A stable response-bearing child organization exists for the declared equations | Native multiwell admission, accepted A_OS support for this law |
| Organized versus neutral target preparation | The specified response depends on target resource organization | Fission caused acquisition of a capability absent from the source |
| Individual responses and joint-load return | A relationally supported operational pair in the stated domain | Non-factorizable joint continuation, two independent RC identities, spark |

The potential wells pre-exist the event. At source C = (1,3,1), all site
derivatives vanish, but graph coupling produces nonzero current. This is not
evidence that the parent became nonviable or lost a local minimum. Conversely,
site minima alone do not certify the complete source's viability. Those are
separate questions from the target return theorem.

Keep the hierarchy explicit: stable target; response-bearing target;
resource-organization-dependent response; formation-caused durable change.
No automatic implication crosses these levels. The last remains open.

## 2. Gate N: explicit potential and initializer bridge

The present native owners cannot execute this fixture merely by changing the
complete profile identity. The [bounded boundary probe](../scripts/probe_atc_support_native_boundary.py)
checks both source and target graphs with independently distinct current/reset
resources, equal charge 5 and all-one W. Matching zero-potential controls pass.
Changing only the declared site-potential ID, with recomputed profile identity,
is rejected at all relevant boundaries:

| Owner | Present restriction | Required successor |
| --- | --- | --- |
| [A current](../../../../src/pygrc/models/grc_v4_candidate_a.py) | Only the zero-derivative ID; no site derivative in `phi0` | Explicit new-ID dispatch and site contribution, including gauge and rounding |
| [Crossing admission](../../../../src/pygrc/models/grc_v4_lifecycle.py) | Rejects other A potential IDs before target numerics | Recognize only the reviewed successor binding, with full both-role readmission |
| [Reference-pass initializer](../../../../src/pygrc/models/grc_v4_initializer.py) | Computes the zero-derivative reference potential | Same new site law in the target-only reference pass, separately for both roles |
| [Initializer schema](../../../../src/pygrc/models/grc_v4_assets/grc-v4-a-initializer-schema.json) | Pins the old potential in both static policy and target profile | A revision-distinct policy/schema identity; preserve old records and replay |

The [retained boundary result](../evidence/autonomous-topology-change/ATCSupportNativeBoundaryProbe.json)
is **passing fail-closed pressure**, not native support evidence. It includes
two admitted zero-potential controls and twelve expected rejections: two owner,
two crossing, four current and four initializer checks. No ordinary update or
event is executed. A development correction changed the probe's expected
initializer exception from identity error to its actual schema error; no owner
or scientific criterion was relaxed.

### Proposed research binding, not an installed declaration

Reserve `research_atc_sup_mw_site_derivative_v1` for the already reviewed
homogeneous polynomial of ATC-SUP-MW-AOS-v1:

$$
p(c)=(c-1)(c-5/4)(c-3/2)(c-9/4)(c-3).
$$

Its identity must bind this exact polynomial, nondimensional resource units,
nonnegative finite inputs and the numerical evaluation recipe. The fixed
fixture retains unit measure, zero context, A_OS, all-one W, no carrier,
eta = 1/4, kappa_c = 1/1024, dt = 1/64 and all previously zeroed channels.
It does not declare all graphs, parameter values or ten realization families.

The proposed [machine-readable binding](../evidence/autonomous-topology-change/ATCSupportPotentialBinding.json)
now makes that requirement concrete. Its `identity_payload` includes the
polynomial in factored and coefficient form, units, domain, evaluation/rounding
recipe, work bounds and initializer-conformance requirements. Canonical UTF-8
JSON (sorted keys, compact separators, ASCII escapes, no nonfinite numbers;
exact rationals encoded as strings) is hashed with SHA-256. The research-only
symbolic-ID mapping is pinned to:

```text
grcv4-research-potential-sha256:9f4a7d3c69c1e52a4551029e7085d8123f68215647e63706397b3cd8bfcbfd8d
```

The [prediction checker](../scripts/certify_atc_support_causal_predictions.py)
requires that exact identity, not merely a self-consistent incoming hash.
Fourteen mutation checks reject altered polynomial, units, domain, rounding,
work limits, initializer evidence or symbolic ID, including recomputed hashes.
Admission of any replacement requires a reviewed successor mapping. This is a
proposed research contract, not a new entry in accepted runtime discovery.
The digest pins the declared law; conformance must still establish that actual
implementation follows it. A versioned-looking string alone is insufficient.

Propose exact rational evaluation on represented binary64 inputs, assemble
`kappa_c L C - p(C) + mean(p(C))` exactly on each connected component, then
round at the declared potential boundary. Form the reference flux using the
existing declared mobility and flux rounding boundary. This must be an explicit
new numerical contract, not an assumption that factored floating-point and
expanded polynomial evaluation are bit-identical. Nonfinite final outputs
must fail, not clamp. The exact-arithmetic return theorem is not automatically
a theorem for indefinitely rounded execution.

Work is explicitly bounded per current evaluation or reference pass: at most
16 finite nonnegative binary64 coordinates and 32 edges, simple loop-free graph
of maximum degree two, fixed degree-five polynomial and fixed rational roots.
There is one site evaluation per vertex, at most 80 exact subtractions and 80
exact multiplications for those evaluations, at most 16 exact component means
with divisors no larger than 16, and at most 16 potential/32 flux roundings.
Incidence assembly has fixed finite graph traversals, not an adaptive solve.
Exact component sums use live-node order. Finite binary64 input size, fixed
degree/coefficients and these graph limits bound rational arithmetic; arbitrary
precision inputs, arbitrary polynomials or graph-size growth are not admitted.
Oversized/out-of-domain inputs must be rejected before arithmetic. These are
evaluation limits, not a support theorem for every graph within them.

The initializer must bind the same potential and a new policy identity, not
relabel `grcv4-a-target-reference-pass-v1`. In this zero-exponent fixture,
`W_base = W_A_init = 1` even if a buggy initializer omits the potential.
Consequently **checking final W alone would be vacuous**: compare the derived
reference potential and flux, for current and reset, against independent
polynomial oracles. Include resources away from the polynomial roots; the
unperturbed source/target values alone also conceal an omitted site term.

This is a **required conformance condition of the proposed successor**, not a
private-state debugging convention. The existing public initializer already
exposes `W_base`, `Phi_base` and `J_ref` in
`to_record()["payload"]["derived"]` with `retain_derived=True`. Require an
equivalent public, role-qualified record in the successor. Missing/null
intermediates fail this conformance check. Keep historical policy and record
identities unchanged; this proposal does not mutate their schema or results.

Before a native run, resolve the new declaration through the investigation's
existing authority route, including any required initializer/event/snapshot
schema and replay bindings. Old accepted declarations, claim dispositions and
G2 support must remain unchanged. Do not monkeypatch the current class, change
the meaning of the old potential ID, or call an external integrator a native
owner. Production work still follows the reviewed proposal, paper and spec;
the review's native recommendation does not silently waive that ordering.

### Bounded native experiment after that bridge

Use the fixed source and target, unchanged F2 and fixed allocator. Admit distinct
current/reset roles; compare the reference-pass intermediates as above. Test
the supplied event and both-role readmission, ordinary continuation and reset
without inventing an endogenous onset history for this new fixture.

Observe sixteen ordinary updates. Fix encounter ages 0, 8 and 15 in advance;
from each unencountered checkpoint make separate matched baseline and loaded
branches, with epsilon = 1/4096, individual directions w0/w1 and the four signed
joint corners. Never turn these independent branches into a repeated-load
schedule. Check positivity, charge, C/W/Z, full currents, background-subtracted
individual Q, energy/box membership and a bounded post-encounter continuation.
The joint branches test return-domain containment, not independently positive
intake for every load combination. Native finite continuation cannot replace
the exact theorem or establish an infinite binary64 tail.

Keep the equal-charge neutral preparation as an operational sign control, not
a missing-history substitute. This is a fixed research experiment, not a
parameter search or a change to F2's source guard.

## 3. Gate C: decide the causal claim with matched controls

A directly prepared copy of the post-event scientific state is an essential
control, but **equal future responses would not refute a causal effect of
fission**. It would show that its future effect is mediated by the state it
produced, rather than by the historical fact or receipt of fission. There is
no extra lineage-memory variable in this fixture's ordinary law.

An observed scientific F/D mismatch under genuinely matched scientific inputs
is a failure requiring investigation: incomplete input matching, undeclared
causal input, metadata consumption or history-dependent initialization. Do not
reinterpret that mismatch as successful evidence that receipt history is
physical. Expected equality supports state mediation, not publication identity.

Use three branches from explicitly bound inputs:

1. **Fission:** the fixed source, prescribed split, then target continuation.
2. **Direct preparation:** the same complete target scientific state, graph,
   profile/reference, context, W and independent reset as branch 1, but no
   fission receipt. Match ordinary requests and encounter schedule. Predict
   equal scientific trajectories/readouts, not equal receipt IDs or snapshots.
   Admit it as a fresh independent research publication; never strip receipts
   from the fission snapshot or fabricate an equal authoritative publication.
3. **No fission:** the same source as branch 1 at the intervention time, continued
   on its original graph under the same homogeneous law. This deliberate
   event-withholding counterfactual is not a claim that it follows enabled K0.

For a comparison defined on both graphs, retain the fixed conservative map

$$
P(C_a,C_c,C_{v_0},C_{v_1})=(C_a,C_{v_0}+C_{v_1},C_c).
$$

It makes source C and projected post-event C identical at the intervention.
Project encounters too: P w0 = (1,-1,0), P w1 = (0,-1,1). These are two ways
to load the **same parent** in the no-fission branch, not two invented child
identities. Compare full projected resource trajectories and incremental
parent-versus-child-pair budget responses using the same normalization for
those projected loads. Report per-child Q separately; do not compare its
two-epsilon denominator to a one-epsilon parent deficit as if they were equal.

Predeclare success as a quantified change in these shared observables and
separately report child addressability and return. A difference would support
a bounded causal effect of the split on subsequent operation. If the source
already supports the compared operation, do not call that acquisition of an
absent capability. “Fission is necessary,” “only fission creates this state,”
“the source became nonviable,” and “two child identities” remain stronger,
unestablished claims even after such a difference. Durable acquisition needs
its own operational absence/presence criterion and matched return evidence.

### Preregistered exact predictions before native execution

The [new exact prediction record](../evidence/autonomous-topology-change/ATCSupportCausalPredictions.json)
checks the review's numbers by two routes: factored site polynomial with
incidence/flux assembly, and expanded polynomial with a dense Laplacian.
This is mathematical pressure on the proposed equations, not a native run.
It preserves the original certificate and the fail-closed boundary record.

At the intervention moment, P C+ = C- = (1,3,1), but

$$
f^- = (-3/2048,3/1024,-3/2048),\qquad
f^+ = (-1/4096,-1/4096,1/4096,1/4096),
$$

$$
P f^+ = f^-/6,\qquad
\Delta_{\rm baseline}=P f^+-f^-=(5/4096,-5/2048,5/4096).
$$

Thus the predicted first-update projected difference at dt = 1/64 is
(5/262144,-5/131072,5/262144). Direct preparation of the identical target is
predicted to reproduce the target field, not the source field. The causal
subject is the **whole fission intervention**: added coordinate, resource split,
connector and changed incidence/reference together. This does not isolate
incidence alone while holding every other structural feature fixed.

At epsilon = 1/4096, both projected source encounters give

$$
\Delta f_b^- = 2300284075180035/4611686018427387904 > 0.
$$

Both target encounters give the pair-total increment

$$
\Delta(f_{v_0}^++f_{v_1}^+)
=3940432218817/144115188075855872 > 0.
$$

Under the **same one-epsilon aggregate normalization**, the parent response is
approximately 0.03192285, and the target pair response approximately 0.00174990.
Each addressed child's own two-epsilon response is separately

$$
Q_i=140111477036065/72057594037927936>0.
$$

The source therefore already satisfies the simple positive-return criterion;
this experiment cannot establish acquisition from its complete absence. This
is not a source return/stability certificate, and it does not equate that one
parent with two children. The stronger candidate interpretation is changed
organization/addressability of participation, not increased aggregate response
or a new independent identity.

Retain the **full shared response vectors**, not only their signs:

$$
\Delta_{\rm pair}^{(i)}=
P\bigl[f^+(C^++\epsilon w_i)-f^+(C^+)\bigr]
-\bigl[f^-(C^-+\epsilon P w_i)-f^-(C^-)\bigr].
$$

For i = 0, this equals

$$
\frac{(1029566215626721,-2174190244177891,1144624028551170)}
{4611686018427387904}.
$$

For i = 1 the exterior coordinates interchange; the parent/pair coordinate is
the same and negative. Both vectors conserve charge and are nonzero. Future
interpretation must keep five observations distinct: baseline change; shared
response change; positive individual child Q; positive source response; and
F/D scientific equality. None alone closes all the others.

If native execution confirms them, keep changed projected ordinary dynamics,
changed shared operational response and two separately addressable positive
child channels as **three separate bounded successor-claim subjects**, not one
aggregate claim. No accepted claim IDs are minted here. The interpretation
candidate is differentiation of participation structure despite conserved
resource and reduced aggregate response—not greater capacity, source
nonviability, new wells or independent child identities. It is not a general
theorem of fission, nor a claim of factorized or non-factorizable continuation.

The record also freezes the stage-rounded potential/flux oracle and comparison
bounds **before native execution**. For these six fixed states (two unchallenged
and four individually loaded), the checked rounding error in f is zero; the
declared conservative per-coordinate bound is 2^-48. Allow at most 2^-45 on
the mathematical delta vectors and 2^-39 on normalized responses, derived from
at most six such coordinate errors and the dt/epsilon = 64 amplification.
Require native potential and flux to match the independent stage-rounded
oracle itself; those bounds do not license a different rounding recipe. They
are not global error bounds for the sixteen-step trajectory or a rounded
infinite-time stability theorem.

The direct-preparation lifecycle test and this matched native experiment are
**not run here**. Exact equations and stage-rounding predictions have been
checked, not promoted to native causal evidence. H5-style
non-factorizability would require further relational interventions; it is not
an extra label attached to the current joint-load theorem.

## 4. Unchanged evidence and next work

The two existing positive results stay separate: the native descendant uses the
old zero-potential control; the mathematical support certificate uses the new
proposed potential. Neither lends native or accepted authority to the other.
All thirty capability/family cells, general F2 justification, CAN-B and full
ATC composition remain open. No accepted claim/debt, paper, spec, production
code, discovery roster or gate is modified.

The next implementation dependency is **review/admission of Gate N's explicit
potential-plus-initializer contract**, not a sixteen-step run under an aliased
potential. Gate C has a distinct causal question and must not be marked passed
by Gate N's numerical conformance. The final independent review recommends
admitting and implementing the bounded research successor next; it requests
no further mathematical design pass. The user's placement clarification resolves
the earlier unnecessary scope blocker: implement research code within the
investigation, leaving `src/*` untouched. That local work is now
[executed](./ATCSupportLocalResearch.md). The production-owner integration still
follows proposal/paper/spec propagation, but does not gate local mathematical
work. Do not alias the old law or relabel local execution as native conformance.

Reproduce the bounded checks when needed; the final review integration does
not rerun unchanged numerical work:

```sh
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python implementation/investigations/grc9v4-constitutive-design/scripts/probe_atc_support_native_boundary.py
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python implementation/investigations/grc9v4-constitutive-design/scripts/certify_atc_support_causal_predictions.py
```
