# Paired A_CI+PC research — scoped claim/debt adjudication

Status: accepted bounded A_CI+PC research. The user authorizes all-28 scoped
adjudication and source admission after CIP-0–CIP-2 scientific PASS and local
retained-record integrity verification. The [review disposition](../evidence/atc-cip-review/README.md)
preserves the missing independent retained-record comparison. No new science
or numerical campaign is required; no native or aggregate ATC acceptance follows.

## Accepted conditional claims

The [ledger](../evidence/atc-cip-admission/ATCCIPClaimDebtLedger.json) admits:

| Claim | Earned scope |
| --- | --- |
| `ATC-CIP-ROOT-00` | Native ordinary staging plus the nonlinear paired coupled J/H root self-map, contraction, regularity and convex Z-domain invariant. The native staging fixture is not the nonlinear causal witness. |
| `ATC-CIP-CHAIN-01` | Frozen combined causal witness: fresh source-only selection, signed lossless both-role transfer/readmission, unsplit/control obstruction and conditional exact-real return with W/Z evolving. |
| `ATC-CIP-REFERENCE-02` | Independent full-root oracle, exact/proved carrier boundaries, signed covariance, research lifecycle/replay/rollback and finite represented C/W/Z/J/H conformance. |

ROOT binds the four accepted CI/PC lemma references recorded in CIP-0.
CHAIN retains its exact ROOT / CI-DOMAIN-02 / PC-CARRIER-03 ancestry.
REFERENCE retains ROOT and CHAIN. These edges preserve proof provenance,
not inheritance of another realization's dynamics, domain or authority.
The superseded PC-CARRIER-02 projection remains a control, never the selected
carrier law. Origin proposals remain proposals, not accepted backward support.

## Scientific and review boundaries

- Five-node paired source and six-node double-star target only; the reviewed
  parameter/request box has signed decorated star-supported Z radius 1/5000,
  total H radius 1/4096, kappa_H in [1/2,3/5], zeta=rho_inst=1 and fixed tau.
  These are sufficient domains, not the standalone CI and PC domains spliced.
- Each ordinary operation uses old Z in its coupled root, then continuity,
  post-C W writing, the same selected-S Z writer and a fresh complete root.
  Source selection uses that fresh root; reset is not a second selection vote.
- Exact-real whole-box transfer/return is conditional on an admitted,
  uniquely resolved source event. Whole-box event formation is not claimed.
  The analytic C1 potential envelope is broader than the executed quadratic
  reference. Root uniqueness is only on the certified neighborhood.
- Old W follows lineage; only bridge W=1 is seeded. The selected relational
  Z lift preserves signed coefficients and Frobenius norm without a PSD rule;
  combined H is SPD. Ordinary target writing can leave the event image.
  One-time zero-Z controls regenerate Z, so they are not CI trajectories.
- Exact rational inputs and execution-proved successor predicates do not
  derive authority from outward box widths or caller-supplied receipts.
  Numerical nonresolution fails closed. Private Python is not a security boundary.
- Finite represented conformance retains charge drift and does not confer
  exact charge, infinite numerical conservation or fully binary64 internals.
  Production numerical policy, K0 scheduling and native ATC are downstream
  Phase 9 after proposal → paper → specification, not missing CIP research gates.
- The independent reviewer did not receive the retained CIP-2 certificate.
  Local checks validate its digest and source bindings; no exact equality
  to the reviewer's unprovided raw rerun is claimed by this admission.

## All-28 realization-local debt decision

All origin meanings/statuses and accepted A_OS/A_PC/A_CI dispositions remain
unchanged. The new CIP-local field alone records:

| Disposition | Origin debt numbers |
| --- | --- |
| Closed within paired research scope | 02, 03, 04, 05, 06, 07, 08, 14, 15, 16, 19, 21, 24, 28 |
| Partial | 01, 10, 12, 13, 23, 26 |
| Not activated | 09, 11, 17, 18, 20, 22, 25, 27 |

DB-04 includes the additive ROOT → CHAIN consumer correction, not a rewrite
of CIP-0. DB-05 uses the combined-root proof, including the Z budget and
root sensitivity. DB-15 includes actual signed W/Z history policy.
DB-19 is restoration of the combined dynamics, not independent CI/PC runs.
DB-24 closes only for this pinned inventory, exact append-only rebuild and
typed reciprocal claim/debt audit. Every row retains earned scope and
remaining/reactivation conditions. All 28 global debts remain open.

## Exact source admission

`grcv4_explorer.atc_cip` and `atc-cip-query` extend the accepted CI context
without altering any earlier node, edge, certificate or admission pin.
Observe all CIP evidence/review/admission directories and transitive execution
bindings. New, changed or missing sources fail closed; discovery cannot grant
acceptance. Historical pending flags in reviewed files remain historical;
this successor ledger is the current bounded acceptance authority.

Exposed surfaces are typed Python and read-only CLI, not browser/notebook
or native runtime discovery. Further source additions require a successor.
Other realizations, active environments, repeated-event/non-Zeno behavior,
core identity/viability and aggregate ATC-2/ATC-3 closure remain outside scope.
