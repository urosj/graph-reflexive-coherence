> Relocation-only maintenance: repository paths and dependent identities were
> rebuilt before first commit. Scientific statements, numerical results and
> review verdicts are unchanged; the new identities are not an independent
> reviewer rerun. Earlier byte-preservation statements describe the pre-move stage.

# Localized sector fission: a positive bounded causal-chain witness

Status: independent review **PASS for both the original and lineage-preserving
bounded mathematics**. The successor review's status-typing and lineage-wording
corrections are applied with focused checks (Section 9); no new physical
experiment is needed. The sector-to-site postulate is not scientifically
adopted. This is positive evidence toward
autonomous splitting, **not ATC-2 closure, accepted fission physics, native
execution or all-family coverage**. The failed cross-edge branch stays stopped.

The [checker](../scripts/certify_atc_localized_sector_fission.py) and
[certificate](../evidence/autonomous-topology-change/ATCLocalizedSectorFissionCertificate.json)
test one source-selected operation, not a catalogue of target variants.
Sections 1–7 retain that original HRESET witness and its historical selection
semantics. **The current research choice is Section 8:** preserve old-edge
history and distinguish source pressure from transfer admission. Its
[successor checker](../scripts/certify_atc_sector_lineage.py) and
[certificate](../evidence/autonomous-topology-change/ATCSectorLineageCertificate.json)
do not alter or replace the reviewed checker/certificate.
Section 9 supplies the current research status adapter; it does not change the
reviewed physical prescription, transfer or continuation mathematics.

## 1. What changes, and why it passes the operation screen

Return to an isolated four-leaf star, with no edges among its exterior leaves.
Its high-degree shared vertex supplies the obstructing coupling; splitting
that vertex actually changes that support. The prior failed fixture's eight
unaffected cross edges are absent. No bridge or allocation tuning is used to
salvage that failed mechanism.

The new ingredient is **two distinct retained-mobility response sectors**,
each containing two contacts. Equality inside each sector and distinction
between sectors are physical state facts, not labels or spectral sign choices.
In the reduced law below, equal leaf resources and equal edge W remain equal
under ordinary evolution. These are exact invariant response sectors, not
approximate clustering or instantaneous least-squares groupings.

The proposed new constitutive choice is explicit: when the common parent
certifiably cannot sustain the declared positive continuation, with inward
participation and funded children, **separate its two exact response sectors
into two sites**. Ordinary V4 equations do not force that event law. Giving
these sectors fission authority is the hypothesis being tested, not an already
accepted consequence of their symmetry. The old degree-two CAN-F2 rule and
CAN-B's structural obligations are not relabeled as completed.
The design is informed by the earlier supplied 2+2 stability theorem; it is
not a blind or preregistered discovery of a preferred partition. The new test
is whether a fixed source-only prescription reaches a restorative target at
its actual boundary. That is distinct from claiming the prescription is the
unique physical law implied by the fixed-topology equations.

## 2. Fixed research law and causal domain

Use the already investigated affine A_OS control:

$$
p(c)=ac,\quad a=19/4,\quad\eta=\kappa_c=\zeta_A=1,
\quad\alpha=\beta=\gamma=\chi_A=0,
\quad h=1/8,\quad\tau_A=h/\log2.
$$

There is no carrier. Reference H0,H1 and edge weights are all one; geometry
gains may be one, but structural current is zero because chi_A=0. Thus the
ordinary fresh OS read equals the reference read exactly, not by a silent
K0-stage substitution. With B tail-positive,

$$
L_W=B\operatorname{Diag}(W)B^\top,\quad
J=\operatorname{Diag}(W)B^\top(aI-L_W)C,
\quad C^+=C-hBJ,
\quad W_e^+=\sqrt{W_e}.
$$

The writer tends to **one**, not to an edge's arbitrary reference weight.
The two sectors have different retained W, not a changed writer target or
invented species label. The first exact test uses

$$
C_0=(1,1,1,1,5),\quad
W_0=((19/20)^2,(19/20)^2,(77/80)^2,(77/80)^2).
$$

The reset baseline is this independently retained initial C/W. The ordinary
beat changes current only, so current and reset are genuinely distinct when
the event is evaluated. The source-only evaluator never receives reset.
The initial depressed W is **prepared history**: the zero-channel canonical
initializer would give W=1 and this law does not form the depression from
that initializer. Recovery and onset are endogenous conditional on this
prestate; formation of the prestate is not established here.

The proposed rule is defined on finite simple graphs up to 16 vertices and
32 edges, with positive resources and 1/2<=W<=1 in this reduced profile. It
looks only at isolated four-leaf-star components; other shapes produce no
event **under this selected law**, not a proof that no topology change is
needed. This includes the constructed double-star target, so reevaluation is
defined there. Loops, parallel edges, invalid numerical input or exceeded
bounds are outside this research evaluator's domain, not silent no-event cases.
Its scientific activation evidence is much narrower than generic V4 support.

## 3. Source-only prescription, before any target exists

For each eligible parent, reconstruct the present reference-stage J. Require:

1. The source-only Rayleigh certificate (5/4) sum_incident W > a.
2. Strict inward current at all four parent contacts. A homogeneous state
   therefore cannot fire merely because an unstable mode exists.
3. Exactly two distinct W classes, two edges each, and equal neighbour C within
   each class. Otherwise the physical partition is `unresolved`, without IDs,
   tolerances, approximate matching or partition enumeration.
4. In order of physical mobility u<v, let the corresponding neighbour values
   be r_u,r_v and the parent resource s. Require x=s-(r_u+r_v)/2>0 and
   y=(r_u-r_v)/2>=0. These are the cone conditions of the obstruction theorem,
   not an inferred universal fission criterion.
5. Reuse the activity allocator: A_u,A_v are sums of the absolute present
   currents in the two blocks; k=round_even(2^16 A_u/(A_u+A_v)), with shares
   k/2^16 and 1-k/2^16. Each assigned child resource must strictly exceed the
   neighbours in its own block. No target current or return score is consulted.

Zero active loci gives `no_event`; exactly one unambiguous active locus gives
its prescription; multiple active loci or an unresolved physical partition
give `unresolved`. There is no ranking, target lookahead or runner-up. Sorting
the two blocks by unequal physical W only names their output coordinates;
the physical partition remains unordered. Reversing an edge changes J and
its parent incidence sign together and cannot change inward participation.

These inequalities are a **new proposed sufficient event law**, not a theorem
that every state excluded by them can continue safely. The research execution
uses exact rational operands at its exact anchor. A separately retained binary64
input/rounding companion checks the same decision and exposes charge rounding;
it is not a native arithmetic-conformance certificate.

## 4. Why the unsplit source actually fails its declared continuation

The identical pairs form an invariant four-coordinate source system. Put
S=u+v, delta=v-u, and retain x,y as above. Its ordinary resource update is

$$
\begin{pmatrix}x^+\\y^+\end{pmatrix}
=\begin{pmatrix}
1+(5h/2)A&(5h/2)B_0\\
hB_0/2&1-hD/2
\end{pmatrix}
\begin{pmatrix}x\\y\end{pmatrix},
$$

$$
A=\frac52 S^2+\frac12\delta^2-aS,\qquad
B_0=\delta(3S-a),\qquad
D=aS-\frac12 S^2-\frac52\delta^2.
$$

At the selected post-beat source, u=19/20, v=77/80, S=153/80. Subsequent
square-root writes preserve u<v<=1, increase S, and preserve the two sectors.
For S>=153/80,

$$
A\ge153/2560,\quad B_0>0,\quad
1-hD/2\ge13/32>0.
$$

Hence x>0,y>=0 is preserved as long as ordinary resource proposals remain
positive, and

$$
x^+\ge\frac{8345}{8192}x.
$$

Charge 9 gives the mean leaf resource (9-x)/5. If all leaves stayed positive
indefinitely, x<9 would contradict geometric growth. This is a finite-amplitude
obstruction with evolving history, not just a frozen unstable eigenvalue.
More generally the same argument uses any initial S>2a/5, with its positive
lower bound S((5/2)S-a); the displayed constant belongs to this anchor.

The onset is genuinely endogenous. Initially lambda_max(L_W)<=5 max W0
=29645/6400<a, so all resource modes of this instantaneous ordinary Jacobian
are damped at h=1/8. After the successful first history write, the concentration
Rayleigh quotient is (5/2)(u+v)=153/32>a. The sector-constant top eigenvalue
is simple: its quadratic is lambda^2-3S lambda+5uv, and the two within-sector
leaf modes have eigenvalues u,v. This is a local stability crossing along
ordinary recovery, not an equilibrium bifurcation or a counter-based trigger.

These are also exactly the unstable mode's two contact-response classes. For
the top eigenvalue lambda>v and a parent normalization phi_parent=1, the leaf
equation gives phi_leaf=-W/(lambda-W), hence the incidence-rectified contact
response is lambda/(lambda-W). This is strictly increasing in W at fixed
lambda. Its two distinct values therefore have precisely the same level sets
as u and v, independent of eigenvector sign/normalization or edge orientation.
All signs are the same: this is **not** the stopped sign-split rule. The new
postulate lifts two exact response amplitudes to sites, without choosing a
clustering threshold or inventing a sign opposition.

The exact first resource commit is approximately

$$
C_1=(1.085596484375,1.085596484375,
1.07679039306640625,1.07679039306640625,4.6752262451171875).
$$

At this poststate both inward block currents are positive, approximately
0.1013578463 and 0.1137769046 per contact. The allocator gives k=30876 and
share 7719/16384. Both children are strictly funded. The initial state gives
no event; the poststate resolves the split without observing any target.

## 5. Declared transfer, including the cost to history

Use the existing **proposed** ATC-2 constructor conventions for this chosen
partition: retain old edges and their endpoint roles, replace the parent by
two children, and add one child bridge. All reference weights are one, so
CAN-REF-GM seeds that bridge with one without a tunable parameter. Apply the
same conservative resource map separately to actual current and reset; its
columns sum to one exactly.

Select CAN-HRESET explicitly. At alpha=beta=gamma=0, the abstract target
reference-pass formula gives W_init=1 on every target edge, independently in
both roles. Old A history is **lost and reinitialized**, not transported
losslessly. This includes the two source sectors that selected the operation;
their distinction survives in the new connectivity and resource allocation,
not as a promised conserved W memory. The mathematical initializations have
zero structural source and identity geometry. There is no carrier to reset.

The initializer's typed optional producer authority is queried, but its
existence does not admit this research affine potential or a new native event
consumer. Its shipped numerical recipe has a narrower site/backend domain.
The present work verifies the mathematical specialization and both-role
resource/current construction, not full production initializer admission,
payload identities, WLS rebinding, atomic publication or receipt conformance.
Those requirements remain attached; “lawful transfer” is a declared compatible
research recipe here, not completed native lifecycle authority.

With the physical low-mobility block first, the target current resources are
approximately (1.0855965,1.0855965,1.0767904,1.0767904,2.2026411,2.4725852).
The separately mapped reset is (1,1,1,1,2.3556518555,2.6443481445).
Both have exact charge 9 in the rational model. Rounded represented charge
must still be checked by a future admitted owner; no privileged-node repair
is inferred from exact column conservation.

This is unit-site creation, not conserved-measure subdivision. No birth-energy
or neutral-subdivision interpretation is claimed or needed by this proposed
non-energy-ranked law. Such an interpretation would require separate physics.

## 6. Restored continuation at the actual selected boundary

The target is the unit-weight double star D(2,2). Its eigenvalues are

$$
0,1,1,3,\frac{5-\sqrt{17}}2,\frac{5+\sqrt{17}}2.
$$

This is a proof of the selected operation's consequence, **not an input to
selection**. Its maximum is below a=19/4, unlike the source. With W=1 the
writer stays at one and the complete resource step is

$$
M=I+hL(L-aI).
$$

On the charge-zero tangent all eigenvalues of M are positive and at most

$$
q=\frac{53+\sqrt{17}}{64}<\frac{457}{512}<1.
$$

Thus Euclidean distance to C*=(3/2)1 contracts. The mapped current already has
squared distance about 2.14130<9/4 and is in an invariant positive ball. The
mapped reset is outside that sufficient ball initially, so it is **not**
admitted by copying the current's argument: direct exact calculation proves
its first target resource proposal positive. After one step the current and
reset squared distances are respectively about 0.2769704 and 0.3864793, both
below 2/5<4/9. They therefore enter the radius-2/3 invariant ball, where every
resource stays above 5/6 and both trajectories converge to C*.

This supplies indefinite positive mathematical continuation for both roles
at the **actual post-beat prescription**, not at an unrelated earlier fixture.
Strict margins also give a neighbourhood within the exact two-sector family
with the same dyadic allocation bin and positive entry; this is not robustness
to perturbations that destroy the sector equalities. No universal restoration
theorem for every guard-positive source is asserted by this anchor.

The operative term removed is the single high-degree incidence concentration,
not an untouched exterior interaction. The operation screen therefore succeeds
where the stopped cross-edge example failed. This does not prove that splitting
is the only possible remedy or derive the new sector-to-site postulate uniquely.

## 7. Causal controls and what this actually earns

The checker executes one exact source-only resolution and constructs its target
only afterward. It retains the following discrimination:

- No split: ordinary retained-history recovery exits positivity on attempt 10.
- History reset without splitting, from the same poststate: the unit-weight
  source still has eigenvalue 5>a and its nonzero concentration grows by 37/32;
  the finite control rejects attempt 7. History reset alone is not the remedy.
- Selected split: both actual current and reset have the positive contraction
  proof above; twelve-step diagnostics confirm the exact matrix recurrence.
- Homogeneous C with the same W: J=0 and no event, even after the spectral test
  becomes positive. Uniform-W accumulating source: partition unresolved, not
  ID-selected. A within-sector resource mismatch is likewise unresolved.
- Two active isolated stars: unresolved; no best-locus ranking. Signed edge
  reversals and vertex/edge permutations transport the prescription unchanged.
- Reevaluate on the double-star target: no eligible four-star component, hence
  no event under this law while ordinary positive continuation proceeds. This
  is defined changed-graph policy applicability, not repeated-fission evidence.

The exact and Decimal diagnostics are not native GRCV4 execution. The checker
does not run target searches, old campaigns, or a general benchmark suite.

| Causal link | New evidence / remaining ceiling |
| --- | --- |
| Ordinary source evolution | Exact positive first commit; actual W recovery changes the source certificate. |
| Endogenous condition | Occupied positive-cone continuation obstruction, with no-event homogeneous control. |
| Source-determined decomposition | Unique two-sector partition on an invariant, deliberately bounded response family. Giving it fission force is the new constitutive hypothesis. |
| Resource/history transfer | Conservative both-role map and explicit all-W loss/reinitialization tested mathematically; scientific adoption and native authority remain open. |
| Restored continuation | Proved indefinitely for both mapped roles at this actual source boundary. |
| Autonomous splitting / ATC-2 | First positive bounded source-selected mechanism witness here, not an accepted generic law or aggregate closure. |

The original next decision was **review this constitutive candidate and its
source-domain ceiling**, not optimize its target. In particular: does exact response-sector
separation justify physical site creation, and is explicit history loss the
intended mechanism? If not, say which constitutive link is missing. If it is
accepted as a bounded candidate, derive its admitted activation/restoration
domain and scientific authority before extending feedback, less symmetric
states or other products. Do not silently add an approximate sector tolerance.
CAN-F2, the stopped branch, formal debts/capability cells and all ten-family
requirements remain unchanged. Claims/debts → reviewed topology proposal →
paper → specs → native 7T remains the propagation order.

## 8. Review integration: retain history and keep pressure distinct from funding

The supplied independent review passed the bounded onset, source obstruction,
source-only decomposition, operation compatibility, conservative resource map
and both-role restoration mathematics. It independently executed `exact_stage()`
and `finite_runs()` in isolation and reproduced the original certificate digest
`66e7faa8899da3ab0aa636a5f943fcc97cd1451adeb2ff0a93cc09cd96cab0af`
and checker SHA-256
`9ac3737d302fb327adaac6633c746cd63cfbcb0835a1b09c9edc5187b15c12c6`.
That is not a reported independent execution of the repository/forensic `run()`.
The original evidence remains byte-identical. Its embedded pending-review status
is historical; this section records the later disposition, without rewriting it.

### 8.1 Current history choice: edge-lineage transport

History erasure is unnecessary for restoration. Keep exactly the same source
prescription, dyadic shares, target topology and resource map. Change only the
selected history recipe:

$$
W_{\mathrm{target,current}}=(W_1,1),\qquad
W_{\mathrm{target,reset}}=(W_0,1).
$$

The first four source edges map to successor edges by declared lineage, with
their scalar history values transported unchanged. The parent endpoint is
replaced by its assigned child: this is **not literal unchanged edge identity**,
even when the finite coordinate index is reused. Orientation roles are retained;
incidence changes. The last target edge is the newly created child bridge. Only
that new coordinate is seeded to one. Current and reset histories are transported
**independently**, just like their resources. No source W value is archived,
dropped or replaced by the other role's value. Reorientation does not change W.

One is compatible with the zero-channel abstract reference-pass value and the
unit bridge reference, but those are distinct roles. This is an explicit new
research transfer choice, **not** a claim that CAN-HRESET already admits partial
initialization or that a lineage map alone supplies native transport authority.
The old ATC-2 proposal and accepted initializer contract are not rewritten.
Native source/target binding, new-coordinate production and both-role admission
remain to be authorized through the scientific pipeline. No initializer or
native owner is invoked by this calculation.

There is a small correction to the review's reset pressure calculation. Its
reported reset norm squared, approximately 0.3504555063, is reproduced using
the **current** W1 on the reset resource vector. Actual reset lineage instead
retains W0. The correct independently transported roles give:

| Target role | Old-edge history | First-step squared distance to C* | Minimum first-step resource |
| --- | --- | --- | --- |
| Current | W1 | 0.2512835384 | 1.3570852730 |
| Reset | W0 | 0.3246804772 | 1.3418386029 |

Both remain strictly positive and enter the same radius-2/3 ball. The correction
strengthens rather than invalidates the bounded restoration result; it must not
be presented as if the predecessor review itself had checked the actual W0 reset
entry. The subsequent lineage review did independently confirm both actual roles.

### 8.2 A common contraction bound for evolving, retained histories

The target Laplacian now changes with every ordinary history write. Do not
reuse the constant HRESET matrix or assume simultaneous diagonalization.
For both roles, initially and after every square-root write,

$$
\mu=361/400\le W_e\le1,\qquad
\mu L_{\mathbf1}\preceq L_W\preceq L_{\mathbf1}.
$$

On the common charge-zero subspace, the unit double-star spectrum from Section 6
and sqrt(17)<33/8 imply

$$
\ell=2527/6400<\lambda(L_W)<73/16=r<a=19/4.
$$

Each symmetric resource-step matrix has multiplier

$$
m(\lambda)=1-\frac18\lambda(a-\lambda).
$$

Its lower bound is 151/512>0. Concavity of lambda(a-lambda) puts its minimum
over [ell,r] at an endpoint; direct rational comparison gives

$$
\frac{151}{512}\le m(\lambda)\le
1-\frac18\min\{\ell(a-\ell),r(a-r)\}
=\frac{1829}{2048}<1.
$$

Every step therefore contracts in the **same Euclidean norm** around the
common fixed vector C*=(3/2)1, regardless of changing eigenvectors. Multiplying
these operator-norm bounds proves uniform geometric contraction; merely saying
each instantaneous eigenvalue is below one would not supply this uniform proof.

Exact rational first-entry calculations give squared distance below 2/5<4/9
for each role. Subsequent resources stay above 3/2-2/3=5/6 indefinitely. The
square-root writer preserves the weight interval, completing the invariant
argument. Six focused Decimal steps (three per role, 96-digit precision) check
the changing-history recurrence against its matrix expansion. They are
diagnostics, not an interval proof or native execution; the rational bounds
and first-entry calculations carry the indefinite-continuation claim.

This bound is different from the original HRESET bound 457/512. Neither is a
universal claim about all source states satisfying the proposed activation law.
The no-split and reset-without-split failures remain in the original certificate
and are not rerun. Together the results show that history erasure is unnecessary
and topology change is effective in this controlled comparison—not that no
other physical remedy could exist.

### 8.3 Source pressure precedes transfer admission

The successor keeps the reviewed source diagnostics but no longer votes only
among funded loci. Its source evaluator receives the same physical operands:
graph, current C/W and the fixed research law, never reset or a target.

1. Record whether the source obstruction is certified by the bounded
   Rayleigh/inflow/sector/cone conditions. Failure to certify is not a universal
   claim of safe continuation or absence of all possible topology pressure.
2. Resolve the physical decomposition and unique locus **before funding**.
   An ambiguous partition or multiple pressure loci remain `unresolved`.
3. For a unique certified locus, apply the unchanged dyadic activity allocator
   and test the selected transfer. Failure yields `blocked_transfer`, with
   `obstruction_status=certified_present` and the resolved prescription retained.
   It does not become `no_event`, and no alternative partition or funded locus
   is searched. No target can be built for that blocked result.
4. Size-domain failure likewise preserves certified pressure while blocking
   construction. An admitted funding check is not full target readmission or
   a universal restoration certificate.

For example C=(1,1,1,1,11/10), W=W1 has strict inflows, the two exact sectors,
x=1/10,y=0 and the certified unsplit obstruction, but both child allocations
fail the strict funding margin. The old evaluator returned `no_event`; the
successor retains the obstruction and returns `blocked_transfer`. Combining
this star with the funded witness gives **two pressure loci**, not a license
to select the funded one. Two unfunded stars also remain unresolved. Tests
cover both, homogeneous nonfiring, ambiguous sectors, size rejection, changed
graph and twelve representative coordinate actions, including blocked status
and independent old-history transport. These are research classifications,
not a silent change to the accepted native K0 outcome schema.

### 8.4 What remains a scientific decision

The current candidate is ordinary reduced V4 dynamics **plus an explicit
localized sector-to-site postulate**, with lineage preservation and a separate
transfer-admission boundary. Ordinary V4 supplies the history, response sectors,
instability and obstruction; it does not imply that sectors must become sites.
Preserving existing edge memory removes an unnecessary loss assumption but does
not derive that remaining postulate or make unit-site creation measure-neutral.

The next substantive decision is whether this obstruction-linked sector-to-site
principle is physically justified on its stated domain. If selected, establish
its admitted activation/restoration domain and claim/debt consequences before
propagating it. Do not seek another prettier target, optimize the partition,
or smuggle in an approximate-sector tolerance. Prepared history remains an
explicit admissible-state assumption, not spontaneous formation from W=1;
exact symmetry, zero Read-Back, absent general feedback/formation and all-family
coverage remain ceilings. Independent review of the successor mathematics now
passes, as recorded below. ATC-2 remains open and the stopped branch stays
stopped; no production, normative specification or accepted claim is changed.

## 9. Lineage review PASS and status-typing corrections

The second supplied review independently executed `exact_checks()` and
`finite_checks()` using generic helper substitutions in an isolated environment.
It confirmed both-role entry, charge, the common spectral interval and uniform
contraction, all six positive changing-history updates, certificate digest
`787c4a1cf33b3152a228751db98363389a268158614ad7279af79c398adc2521`
and checker SHA-256
`207fece35ac40284ea0481d2b69a2d639d12ea8a4f06c24fa7308f242fe5aa07`.
It did not run the repository-dependent `run()` wrapper. That wrapper checks
retained bindings; it is not a fresh forensic API query. The independent PASS
therefore concerns the bounded mathematics and pressure/funding separation,
not native execution, a full repository rerun or acceptance of the physical law.
Both reviewed scripts and certificates remain byte-identical.

The current research status entry point is `prescribe()` in the
[status adapter/checker](../scripts/check_atc_sector_status.py). Its
[focused check record](../evidence/autonomous-topology-change/ATCSectorStatusChecks.json)
binds the reviewed evidence and corrects two semantic conflations without
changing outcomes, selected prescriptions, currents or certified pressure loci.
The old embedded status fields remain historical observations, not the current
typing contract. No target is built and no trajectory is rerun by these checks.

### 9.1 Partition resolution is not locus arbitration

If two pressure loci each have a resolved partition, the aggregate reports
`decomposition_status=resolved_per_locus` and
`locus_resolution=multiple_unresolved`. Its overall outcome stays `unresolved`
and no prescription is selected. The physical sectors are known; event
concurrency/arbitration is not authorized. This does not choose atomic versus
sequential execution or introduce a tie-breaker.

A genuinely ambiguous physical partition still reports
`decomposition_status=unresolved`. The checks include resolved plus ambiguous
loci and multiple certified loci plus an ambiguous one, so the new label does
not conceal a genuine partition problem. Funded/unfunded mixtures and their
reversed ordering do not affect locus selection.

### 9.2 Funding is not construction-domain admission

The ambiguous field `transfer_admission` is replaced in the current research
view by `funding_admission` and `construction_domain_admission`, both per locus
and for a uniquely selected prescription. The latter field here means only the
fixed finite graph-size domain, not full target readmission, native admission
or proof of restored continuation.

| Source case | Decomposition | Locus resolution | Selected funding | Selected construction domain | Overall outcome |
| --- | --- | --- | --- | --- | --- |
| Single funded anchor | resolved | single_resolved | admitted | admitted | resolved |
| Single unfunded obstruction | resolved | single_resolved | blocked | admitted | blocked_transfer |
| Single funded, size exceeded | resolved | single_resolved | admitted | blocked | uncertified |
| Single unfunded, size exceeded | resolved | single_resolved | blocked | blocked | blocked_transfer |
| Two resolved pressure loci | resolved_per_locus | multiple_unresolved | not_reached | not_reached | unresolved |

For multiple loci, local funding/domain diagnostics remain available, but
aggregate admission is `not_reached` because no unique operation was selected.
In the simultaneous funding/size failure, both failures remain visible; the
predecessor's overall funding-failure outcome is preserved. Neither successful
funding nor a fitting size domain is a hidden chooser or a physical safety
certificate. The review's unfunded example has charge Q=51/10, not 9; the
positive-leaf bound x<Q and the same growing cone still certify obstruction.

Thirteen focused source-status cases pass, including the no-event homogeneous
and invalid-source-domain controls. All physical outcomes/prescriptions agree
with the reviewed evaluator. The retained edge map is also checked as lineage
transport with a replaced parent endpoint, not as unchanged incidence.

### 9.3 Next scientific decision, not another refinement campaign

The next step is now prepared in the
[CAN-LSF constitutive proposal](./ATCSectorConstitutiveProposal.md): a concrete
bounded scientific recommendation with activation, authority and debt surfaces,
not a new target campaign. Its scientific choice/domain remain for review.

Retain the lineage-preserving candidate; do not revert to HRESET. The bounded
causal-chain mathematics is independently reviewed. These two typing fixes
clarify its meaning and do not demand another physical experiment or a new
scientific gate before discussing the postulate.

What remains is whether an obstructed organization's **exact persistent
response sectors warrant separate-site realization** as a bounded new
constitutive law. If selected, formalize its activation, authority and debt
surface through the existing investigation route. Do not infer that current
edge-lineage transport already has native consumer authority.

This is presently a **symmetry-sector law**, not a generic self-fission law.
Exact equalities are dynamically invariant once present but form a narrow
state family; generic small symmetry-breaking perturbations near the active
anchor leave the partition unresolved. No approximate tolerance is selected.
Any later generalization should justify a structural/dynamical equivalence or
invariant quotient rather than numerical near-equality clustering. That later
scope is not a prerequisite for recognizing this bounded mathematical PASS.
Constitutive adoption, general-state/family coverage, native authority and ATC-2
closure remain open; the failed branch and all counterexamples stay retained.
