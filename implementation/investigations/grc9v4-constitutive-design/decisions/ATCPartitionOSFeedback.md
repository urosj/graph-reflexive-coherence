> Relocation-only maintenance: repository paths and dependent identities were
> rebuilt before first commit. Scientific statements, numerical results and
> review verdicts are unchanged; the new identities are not an independent
> reviewer rerun. Earlier byte-preservation statements describe the pre-move stage.

# Supplied 2+2 versus 1+3 partitions with one-pass A_OS history/read/geometry feedback

Status: independent mathematical review PASS, with four scope clarifications
applied. Exact bounds and retained finite staged diagnostics pass; the review
independently reconstructs the mathematics, not the full execution. No
production code, accepted claim/debt graph, paper, specification or
topology-selection rule is changed. The reviewed checker, certificate and
two predecessor records remain unchanged.

The user authorized combining the [partition result](./ATCPartitionDependentStability.md)
with [retained history, Read-Back and consumed OS geometry](./ATCOSGeometryFeedbackContinuation.md).
The result is non-vacuous: under one common parameter box, the same funded
source's supplied 2+2 target enters a positive full-state return region, while
its supplied 1+3 target still expands over its first step and retains a negative
large-child restorative response. The geometry path is actually consumed.
This explains consequences of supplied maps, not an autonomous choice of map.

## 1. Reconcile the two predecessors before combining them

Use the partition study's affine potential derivative, not the degree-two
study's multiwell polynomial:

$$
p(c)=a c,\quad a=19/4,\quad\eta=\kappa_c=1,
\quad h=1/8,\quad\rho=1/2,\quad\tau_A=h/\log2.
$$

There are four source leaves with resource 1 and one parent with resource 5.
The exact supplied map keeps the leaves and allocates parent resource in
shares m/4,n/4 to children u,v, where (m,n) is (2,2) or (1,3). Attach the
corresponding old leaves to each child and add one unit-reference child bridge.
Both targets have six unit-measure vertices, five edges and charge 9:

$$
C_{22,0}=(1,1,1,1,5/2,5/2),\qquad
C_{13,0}=(1,1,1,1,5/4,15/4),\qquad C^*=(3/2)\mathbf1.
$$

Both children are strictly endowed relative to their attached leaves in
both maps. Minimum endowment margins are 3/2 and 1/4 respectively. The source
and resource allocation are the same comparison as the reviewed s=5 pressure,
now with the following genuine feedback law. The partition and allocation
change together; this is not an incidence-only comparison at fixed target C.

Reference H0,H1 and edge weights are identity/ones; the typed structural
adapter is identity on common overlap-normalized star-tensor coordinates.
This identity adapter is a load-bearing assumption: the PSD/trace and assembly
derivative bounds below are for these declared coordinates and cannot be
exported unchanged to another typed adapter. Such an extension needs its own
assembly, positivity, norm and derivative bounds.

There is no carrier. Set alpha=beta=0, zeta_A=1, W_floor=1/2. Thus
"one-pass A_OS history/read/geometry feedback" means the retained W writer,
explicit A Read-Back, structural assembly, Hodge pushforward and geometry
consumer. It does not mean that all Candidate-A descriptor channels are
enabled, or that a persistent-carrier realization is covered. Declare

$$
0\le\gamma\le2^{-16},\quad0\le\chi_A\le1/2,\quad
0\le\kappa_H,\kappa_{Ah}\le1,\qquad -b\le y_e=\log W_e\le0,
\quad b=2^{-8}.
$$

The gamma range is **not** the predecessor's [0,256]: the affine example has
much larger currents and a different time step. It is selected from the
explicit floor/contrast/retention inequalities below. Nor are the old
multiwell equilibrium or its return constants reused. This declaration is
`ATC-A-OS-AFFINE-PARTITION-FEEDBACK-v1_not_runtime_registered`.
Its exact residual tolerance is 2^-9 in reference-relative edge operator norm,
not the earlier research 2^-30 tolerance or admitted native A_OS authority.
The residual PASS is conditional on this new research-profile declaration;
native norm, tolerance and staged-arithmetic conformance remain separate.

The return/sign bounds are uniform on this box, including zero-gain faces.
Strict channel activation is demonstrated at the fully enabled corner and
stated prestates, not at every state or gain. All these are mathematical
research parameters. The affine potential and its initialization/transport
binding are not silently registered in the native evaluator.

## 2. Same one-pass law, on each declared graph

Let B be tail-positive incidence, L=B B-transpose, A=B-transpose B,
D_w=diag(exp y), L_w=B D_w B-transpose and z=C-C*. Write v0 for the
reference-geometry baseline and v1 for the fresh corrector baseline:

$$
v_0=D_wB^\top(aI-L_w)z,\qquad
q_e(v)=\tanh\!\left(\frac{y_e+\gamma v_e^2/2}{2}\right),\qquad
F_y(v)_e=\frac{v_e}{1-\chi_Aq_e(v)},\qquad r(v)=F_y(v)-v.
$$

The declared floor is proved inactive below. The predictor, one geometry
update, fresh corrector and single writer are

$$
J^{(0)}=F_y(v_0),\quad f^{(0)}=r(v_0),\quad
S^{(0)}=\mathcal A_\star(f^{(0)}),\quad H^{(1)}=I+\kappa_H S^{(0)},
$$

$$
v_1=v_0-\kappa_{Ah}D_w A(H^{(1)}-I)B^\top z,\quad
J^{(1)}=F_y(v_1),\quad C^+=C-hBJ^{(1)},\quad
y^+=\tfrac12 y-\tfrac\gamma4(J^{(1)})^{\odot2}.
$$

The pre-read uses its stage's baseline, not its selected current; only the
selected corrector writes C and y. The final-C descriptor refresh is
numerically inactive at alpha=beta=0. The read-only split defect uses the
corrector's new inverse pairing:

$$
f^{(1)}=(H^{(1)})^{-1}r(v_1),\qquad
R_h=\kappa_H[\mathcal A_\star(f^{(0)})-\mathcal A_\star(f^{(1)})].
$$

No second corrector is selected. The next beat rebuilds from reference
geometry with the new C,W; H1 is not retained state. Mobility remains D_w,
not the generated Hodge. The assembly is PSD with trace ||f|| squared on
either tree, so H1>=I and ||H1 inverse||<=1, including off-diagonal terms.

The [specification](../../../../specs/grc-v4-spec.md),
[paper](../drafts/2026-09-GRC-V4.md) and
[reviewed OS note](./ATCOSGeometryFeedbackContinuation.md#2-the-actual-one-pass-equations)
supply the equation/staging lineage. Six freshly queried typed forensic
traces match those retained in the bound OS predecessor byte-for-byte. Their
full source/edge/payload rows remain retrievable there, and their disposition
remains `indeterminate_requires_review`. They do not admit this new theorem.

## 3. Common finite-state admission bounds

Use an outer estimate domain X=||z||_2<=R_o=3 and -b<=y<=0, with Y=||y||_infinity.
This outer ball is **not** asserted to be positive or invariant. It encloses
the two particular positive targets, their tested load segments and the
smaller positive return region. The affine equations extend algebraically
outside positive C, allowing this convenient sufficient bound.

Both graphs have Laplacian spectrum in [0,6], since they are simple graphs
on six vertices. Consequently ||B||<=sqrt(6)<5/2. Also

$$
\|B^\top(aI-L)\|^2
\le\max_{0\le\lambda\le6}\lambda(a-\lambda)^2
=\max\{0,6(a-6)^2,4a^3/27\}<16.
$$

Since ||D_w-I||<=Y, 0<D_w<=I, and ||L-L_w||<=6Y,

$$
\|v_0-B^\top(aI-L)z\|\le(4+6\cdot5/2)YX=19YX,
\qquad \|v_0\|\le A_0X,
\quad A_0=4+19b=1043/256.
$$

Thus j_star=A_0 R_o=3129/256. Set Q=1/256 and d_min=511/512. Exact rational
checks give, in order (so there is no assumption of corrector admission),

$$
\frac{b+2^{-16}j_*^2/2}{2}<Q,\quad
\|f^{(0)}\|\le\frac1{511}\|v_0\|,
$$

$$
\|v_1-v_0\|\le E\|v_0\|,\quad
E=6(5/2)R_o j_*/511^2=20115/9549568,
$$

$$
\frac{b+2^{-16}[(1+E)j_*]^2/2}{2}<Q,\quad
\frac{1+E}{d_{\min}}<L_J=201/200.
$$

The corrector is rebuilt, not taken proportional to the predictor; these are
norm bounds. They yield ||J1||<=L_J A_0 X and
||J1-v0||<=(L_J-1)A_0 X. Both fresh pre-read log targets and the final writer
drive have magnitude at most

$$
2^{-16}(L_Jj_*)^2/2<0.001152<b.
$$

Hence all floors stay inactive, denominators exceed d_min and the writer
preserves [-b,0]. The common residual bound is

$$
\|R_h\|\le(j_*/511)^2+(\tfrac12 Q L_Jj_*)^2
<0.001148<2^{-9}.
$$

This is research residual admission, not zero residual, a second iteration,
or native norm/tolerance/rounding conformance.

## 4. The 2+2 target: entry, positive full-state return and one encounter

At W=1 without feedback the resource map is T0=I-hL(aI-L). The 2+2 nonzero
eigenvalues are 1,1,3 and the two roots of lambda squared-5 lambda+2=0.
For those last roots the Euler multiplier is 3/4+lambda/32; the other
multipliers are 17/32 and 11/32. All are positive and strictly below
q0=29/32. This sharper bound is derived here, not a change to the predecessor.

The full OS update differs from T0 z by at most delta X, where

$$
\delta=h(5/2)[19b+(L_J-1)A_0]=4843/163840<0.029560.
$$

Thus X+<=q_x X, q_x=q0+delta<0.935810. For X<=R_o the writer obeys

$$
Y^+\le\tfrac12Y+cX,\qquad
c=\frac{2^{-16}}4(L_J A_0)^2R_o<0.000192.
$$

On the **positive return rectangle** X<=1, -b<=y<=0, resource contraction
and the preceding writer-drive bound prove invariance. Every C coordinate
is then at least 1/2. With S=X+Y,

$$
S^+\le q S,\qquad
q=\max(q_x+c,1/2)
=\frac{643215122754347}{687194767360000}<0.936002<1.
$$

Consequently (C,W) tends to ((3/2)1,1); derived read/source/geometry increments
vanish. This is a uniform exact-real full-state return theorem, not frozen-W
stability or evidence that geometry causes stabilization.

The actual supplied target initially lies outside X<=1: X0=sqrt(3)<7/4.
It is exactly the lambda=3 reference mode, however, so its first reference
image has norm (11/32)X0. The perturbation bound proves

$$
X_1<(11/32+\delta)(7/4)<0.654,
$$

for **any** history in the declared box and every gain in the parameter box.
Thus the actual positive target enters the positive return rectangle after
one complete step. This step is proved, not assumed from local stability.

Keep the old fixed-reference loads w_i=-L e_i at children i=u,v. An encounter
has delta-C=A w_u+B w_v with independently signed |A|,|B|<=epsilon=1/64;
it does not change history. The exact Gram matrix [[12,-6],[-6,12]] gives
||delta-C||<=6 epsilon=3/32. If this one encounter occurs at age zero,

$$
S_{1,\mathrm{loaded}}
<(11/32)(7/4)+(29/32)(3/32)
+\delta(7/4+3/32)+b
=3905577/5242880<0.745<1.
$$

The loaded initial coordinates are positive directly from the declared
loads. If the encounter instead occurs after the entry step, unencountered
S has decreased and

$$
S_{\mathrm{loaded}}<(11/32+\delta)(7/4)+b+3/32<1.
$$

Either way, the loaded full state returns and resources stay positive.
This covers one encounter at any unencountered age, not arbitrary repeated
forcing, history-adaptive loads, event/reset semantics or independent identities.

## 5. The 1+3 control is not repaired by these feedback channels

At the homogeneous equilibrium, z=0 and y=0. The baseline is linear in z
to first order; changing W contributes only products y z. Read-Back is
O(y z,z cubed); the structural source starts at fourth joint order, and its
potential consumer multiplies B-transpose z again. The writer's drive is
quadratic in current. The complete linearization therefore is

$$
D\mathcal T(C^*,0)=\operatorname{diag}(I-hL(aI-L),\tfrac12 I).
$$

For 1+3, det(5I-L)=-80, so some eigenvalue exceeds 5. Its resource multiplier
is 1+h lambda(lambda-a)>37/32>1. This expanding linearized mode persists for
the entire declared gain box. Unlike the nonlinear return theorem above,
this particular argument is local; it does not by itself describe a positive
long-run trajectory from the supplied target.

The actual same-source target also has a separate finite-state pressure
result. Its zero-feedback exact squared distances are

$$
D_0^2=49/8<(5/2)^2,\qquad
D_{1,0}^2=66211/8192>(14/5)^2.
$$

The common full-OS perturbation estimate proves

$$
D_1>14/5-\delta(5/2)=893289/327680>2.726>5/2>D_0.
$$

The exact reference first image has minimum coordinate greater than
delta(5/2), so this full-OS image is still positive. This is **uniform
positive-resource first-step expansion**, not a claim of indefinite positive
divergence or absence of every other attractor. Funding alone does not settle
target viability; neither a stability test nor future target performance is
inserted into the source's event-selection rule.

## 6. Child response signs survive the actual fresh reconstruction

To avoid dividing a coarse endpoint bound by a tiny epsilon, differentiate
the whole staged current at fixed incoming y. For either stage,

$$
F_y'(v)_e=\frac1{1-\chi_Aq_e(v)}
+\frac{\chi_A\gamma v_e^2[1-q_e(v)^2]}{2[1-\chi_Aq_e(v)]^2}.
$$

The common stage bounds give ||DF-I||<t=1/384. Normalized star assembly
satisfies ||D A_star(f)[g]||<=2||f|| ||g||, by its sum-of-outer-products
form, the overlap identity and Cauchy-Schwarz. Put f_star=j_star/511. The
geometry correction to v0 has derivative norm at most

$$
G=6[2 f_* t A_0(5/2)R_o+f_*^2(5/2)]<0.031423.
$$

Consequently the derivative of the selected current minus the reference
linear current is bounded by

$$
D_J=19b+G+t(A_0+G)<0.116333.
$$

Both endpoint OS reads rebuild their own geometry. The integral derivative
along a load segment inside X<=3 gives, for child degree d_i>=2,

$$
|Q_i^{OS}-Q_i^0|
\le h(5/2)D_J\frac{\sqrt{d_i(d_i+1)}}{d_i}
<h(5/2)D_J(5/4)
=\frac{24961935409475}{549309137289216}<0.045443.
$$

Here Q_i is the next-step resource restoration **after subtracting the imposed
load**, divided by its child deficit d_i epsilon. All stated load segments
fit the outer domain. The bound does not require a 1/epsilon amplification
and also controls the corresponding infinitesimal response at fixed history.
The exact reference responses therefore imply the uniform signs:

| Supplied target | Reference child Q | Full-OS guarantee |
| --- | --- | --- |
| 2+2, either child | 7/24 | Q > 0.2462 |
| 1+3, small child | 15/32 | Q > 0.4233 |
| 1+3, large child | -3/16 | Q < -0.1420 |

The large child's negative response is a separate, precisely defined
operational failure, not just a spectral label. Positive responses in 2+2
are not general controllability or two dynamically independent identities.

## 7. Source participation, channel consumption and finite pressure

For the matched finite examples, all four incoming old edges have
y=-1/512 and the new target bridge has y=0. These are declared mathematical
prestates, not a newly admitted history-transport/event recipe. The return
theorem itself permits every target history in its larger box.

On the symmetric source, uniform old w in [exp(-b),1] gives equal baseline
inflows a_s=4w(5w-19/4)>0. The generated star has eigenvalue (5/2)f_s squared
on the uniform four-edge mode, so the corrector baseline is
a_s+50 kappa_Ah kappa_H w f_s squared. All four activities remain equal and
positive. Separate scalar bounds verify source denominator/floor/residual
admission; the target radius-3 estimate is not misapplied to the source.
Equal activity supports the declared m/4,n/4 shares, **not a choice between
labeled partitions**. No higher-degree CAN-F2 implementation is asserted.

At the fully enabled corner, the symmetric 2+2 target has outer edge
d=-3/2, w=exp(-1/512). An independent group-scalar oracle gives

$$
v_{0,e}=w(19/4-3w)d,\quad
f_e=\chi_A q_e(v_0)F_y(v_0)_e,\quad
v_{1,e}=v_{0,e}-(9/2)w f_e^2d.
$$

The old-edge source blocks have diagonal f squared and off-diagonal f squared/2;
the symmetric bridge current is zero. Because d<0, f is nonzero and F_y is
strictly increasing on the admitted interval, the corrector current exceeds
the predictor current (both are negative). The checker proves a strict
rational lower bound: |v0|>=(3/2)(1-1/512)(7/4), |v0|<3 and
|q0|>=(1/512-2^-16 times 9/2)/4; these bound f squared and the positive
baseline increment. This is not a Decimal subtraction used as a proof.
In particular, the magnitude of each outer resource effect is bounded below by
1104957273511/3465519913261596672, approximately 3.1884e-7. This separates the
exact witness's scale from the old near-ulp effect, without establishing native
staged-rounding conformance for either research law.

Decimal96 diagnostics at this declared corner give:

| Observation | Value, approximately |
| --- | --- |
| 2+2 predictor old-edge current | -2.6273931236 |
| 2+2 selected corrector old-edge current | -2.6273826310 |
| Geometry-on/off old-edge current gap | 1.04926e-5 |
| Corresponding magnitude of outer resource gap | 1.31158e-6 |
| Corresponding child resource gap | 2.62316e-6 |
| 2+2 individual child Q | 0.293821, 0.293821 |
| 1+3 individual child Q | 0.469429, -0.182590 |

The scale is much larger than the old near-cancelling multiwell witness,
but this is a **different declared affine research law**, not a correction
to that evidence or proof of its native binary64 behavior. Decimal96 results
are diagnostic, not certified rounding enclosures or production conformance.

Matched controls remove kappa_H, kappa_Ah or chi_A separately. Turning off
the consumer leaves geometry present but restores predictor current; turning
off geometry also restores predictor current. A gamma-off control changes
both the pre-read functional and the retained writer; it is not mislabeled
as an isolated writer intervention. A next-read frozen-history substitution
separately checks that the actual retained write affects continuation.

Twenty-one complete finite mathematical steps cover both targets, their
controls, exact zero-feedback oracles, individual loads, the symmetric source,
one continuation and an asymmetric/nonuniform-history coordinate action.
Signed edge reversal and vertex/edge reordering preserve transformed outputs.
Stale predictor contrast, wrong pre-read current, predictor/baseline writer,
premature new-W reuse and a second selected corrector are discriminated.
The last two are one unselected diagnostic stage each, not extra lawful steps.
No trajectory campaign, native step or topology event is run.

## 8. Evidence, debt scope and next question

The [checker](../scripts/certify_atc_partition_os_feedback.py) emits the
[certificate](../evidence/autonomous-topology-change/ATCPartitionOSFeedbackCertificate.json).
It checks the identities/source bindings of both reviewed predecessors without
rerunning them, verifies exact inequalities and graph algebra, then performs
the finite Decimal diagnostics and current forensic queries. Full unchanged
forensic traces are referenced by predecessor record digest, JSON pointer and
trace digest rather than copied into a second archive. Paths are repository-relative.

From the repository root:

```sh
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python implementation/investigations/grc9v4-constitutive-design/scripts/certify_atc_partition_os_feedback.py
```

This prints a fresh deterministic record; it does not overwrite retained
evidence. The reviewed checker/certificate preserve their historical
pre-review status. The current mathematical review disposition is recorded
below; it is not formal scientific-claim admission.

The bounded result strengthens partition/constitutive-feedback and support
evidence relevant to DB-07/27. It does not derive CAN-B's structural functional,
critical branch, selected mode or half-edge partition, nor close the autonomous
generator/transport debts or any all-family capability cell. The uniform
return box surviving geometry does not prove geometry causes stabilization,
enlarges the true basin or improves rates. The homogeneous resting state and
vanishing history do not establish persistent differentiated identities.

### Independent-review disposition and evidence boundary

The supplied independent review finds no blocking mathematical defect and
gives PASS to the common stage/admission bounds, 2+2 entry and positive
full-state return, its one bounded encounter, 1+3's expanding tangent and
uniform positive-resource first-step expansion, child-response sign
preservation, symmetric source participation and consumed geometry activation.
It independently checks these reviewed identities:

- Certificate canonical digest:
  `76e5ece4e49a64b73d53f61b198ac2117474bbb3c9b1416929aeadcd525c85c8`.
- Checker SHA-256:
  `252ad3e32e9aaca399dbdcd77bddf9cb9818db5f6874170b90f1ba0ee74ea570`.

The reviewer read and compiled the checker and independently reconstructed
central rational bounds, contraction/response margins and the activation
lower bound. It did **not** execute the full repository checker: its review
environment lacked the complete predecessor/forensic context. The Decimal96
outputs remain the retained original execution evidence, not an independent
numerical rerun. This is a review-environment boundary, not a finding that the
repository lacks those inputs. No campaign is rerun to integrate this review.

The four requested clarifications are explicit above: precisely named enabled
channels with alpha=beta=0 and no carrier; the research-only 2^-9 tolerance;
the load-bearing identity adapter; and uniform gain-box return/sign bounds
versus selected fully enabled activation witnesses. Neither this PASS nor the
larger activation scale promotes native admission or changes accepted claims.

The scientific distinction is now clear: source participation, the supplied
partition and post-partition support are three different subjects. The source
remains symmetric and equally active while the two supplied partition classes
have different consequences. Consequences can discriminate those classes
without providing lawful source information that chooses a labeled member.
They must not be fed back retrospectively as a source-side chooser.

With this bounded mathematical review complete, the next scientific question
is **source-side distinction and
equivariant partition selection**. The symmetric degree-four source still has
three equivalent labeled 2+2 partitions. A preference for the favorable size
class supplies no invariant choice among them. Any proposed generator needs
an explicit source-side physical distinction, a justified invariant set-valued
outcome, or honest unresolved symmetry; IDs and target-outcome lookahead do
not supply that authority. Leave CAN-F2 unchanged while addressing that gap.
Native potential/initializer/residual admission remains separately deferred
until claims/debts, topology proposal, extension paper and specification have
supplied the required authority.
