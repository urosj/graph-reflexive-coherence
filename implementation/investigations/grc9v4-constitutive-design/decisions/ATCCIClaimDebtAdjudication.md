# Paired A_CI research — scoped claim/debt adjudication

Status: accepted bounded A_CI research. The user authorizes this adjudication
after the [final independent CI-3 PASS](../evidence/atc-ci-admission/CI3-FinalIndependentReview.md).
CI-0 through CI-3 are scientifically complete at the declared paired scope.
No further CI discovery stage or numerical campaign is required for this
admission. This is not native ATC support or aggregate ATC-2/ATC-3 closure.

## Accepted conditional claims

The [machine ledger](../evidence/atc-ci-admission/ATCCIClaimDebtLedger.json)
admits four claims, without promoting origin proposals or another realization:

| Claim | Earned scope |
| --- | --- |
| `ATC-CI-STEP-01` | Actual CI ordinary-step ordering and full fresh poststate J/H root identity; stage evidence, not native autonomous execution. |
| `ATC-CI-CHAIN-01` | Reviewed full-feedback paired CI causal anchor: source obstruction, source-only fission, independently admitted target roles and evolving-W restoration. |
| `ATC-CI-DOMAIN-02` | Uniform paired source/target joint-root existence, uniqueness on the declared ball, regularity, obstruction, positive funding, transfer, entry, variable-request return and asymptotics. |
| `ATC-CI-REFERENCE-03` | Full-graph reference and independent joint-root oracle; covariance, lifecycle/replay/rollback and finite represented C/W/J/H bridge, including the reviewed boundary and derived-witness corrections. |

Ancestry is STEP → CHAIN → DOMAIN → REFERENCE, with the reference also
explicitly binding STEP and CHAIN. Provenance is not sufficient proof and
does not transfer a predecessor's domain or confer normative/native authority.
The exact source references and reciprocal debt edges are retained in the ledger.

## Scientific boundaries retained

- These are the paired five-vertex source and six-vertex double-star target,
  with the declared parameter/request box and a fixed profile/physical tau.
  The whole-box transfer/return theorem assumes an admitted event and a
  uniquely resolved source-only share; it is not whole-box event formation.
- The potential theorem covers its C1 slope envelope; the executed reference
  covers the quadratic subclass. The joint root includes both J and H and
  is unique/regular only on the certified radius-1/4096 neighborhood. Other
  roots are not classified. The zero-coupling homotopy identifies a branch;
  it does not admit a new runtime profile.
- W follows old-edge lineage; only the new bridge is initialized at W=1.
  There is no persistent Z carrier in CI and no PC policy inheritance.
- Exact rational predicates and operation-proved facts are separate from
  numerical enclosures. Only execution can grant a derived successor witness;
  supplied results/receipts cannot. Unresolved numerical predicates still
  fail closed. Private Python internals are not a security sandbox.
- The finite represented experiment preserves its measured charge drift.
  It is not exact stored conservation, an all-binary64 internal evaluator,
  or indefinite numerical conformance. Production conservation is downstream.
- Active environments, wider graphs, repeated-event/non-Zeno behavior,
  other realizations and native scheduling/transactions remain outside scope.

## Debt disposition

All 28 origin debts remain globally open, with their original meanings and
accepted A_OS/A_PC dispositions unchanged. The CI ledger separately records:

| CI-local disposition | Origin debts |
| --- | --- |
| Closed within paired research scope | 02, 03, 04, 05, 06, 07, 08, 14, 15, 16, 19, 21, 24, 28 |
| Partial; wider/native/numerical obligations remain | 01, 10, 12, 13, 23, 26 |
| Not activated | 09, 11, 17, 18, 20, 22, 25, 27 |

DB-05 now closes locally through the CI-specific joint-root and regularity
proof; PC's non-activation does not apply to CI. DB-24 closes only through
the pinned CI source admission, exact append-only graph rebuild and typed
claim/debt audit, not through the review verdict alone. The ledger supplies
earned scope and remaining/reactivation conditions for every origin debt.

## Source admission and preservation

`atc-ci-query` and `grcv4_explorer.atc_ci` observe and validate the CI successor
inventory and expose reconstruction/debt traces with source/edge references
and digests. They extend the accepted PC context, which itself preserves A_OS.
Added, missing or changed admitted material fails closed; discovery does not
automatically admit new sources. No accepted historical node/edge is rewritten.

Earlier pending/held flags in reviewed CI certificates and working notes are
historical. This explicit adjudication is the current acceptance authority;
the reviewed certificate bytes remain unchanged. Likewise, PC's historical
list of unaccepted CI handles remains true of that older admission, not this
successor. The old PC/A_OS loaders retain their original scopes and digests.

The remaining work is other explicitly authorized research and the later
topology proposal → paper → specification → Phase 9 path. This adjudication
does not start CI+PC/RG2b or widen the accepted scientific domain.
