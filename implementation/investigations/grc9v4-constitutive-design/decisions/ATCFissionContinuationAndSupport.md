> Relocation-only maintenance: repository paths and dependent identities were
> rebuilt before first commit. Scientific statements, numerical results and
> review verdicts are unchanged; the new identities are not an independent
> reviewer rerun. Earlier byte-preservation statements describe the pre-move stage.

# F2 continuation and a separate operational-support fixture

**Status:** research continuation authorized; no F2 law, new potential binding,
scientific acceptance, claim admission or production extension accepted here.
The user agreed to keep F2-v1 unchanged while investigating its next eligible
event and a separately defined support experiment. Useful continuation ideas
are developed below as explicit mathematical objects, not imported authority.

## 1. Next source-selected descendant event: native result

The [focused probe](../scripts/probe_atc_fission_descendant.py) replays only the
retained nonuniform-history split from the
[preceding study](./ATCFissionTargetDiscrimination.md). It reproduces that
event's exact request, receipts, mapped current and distinct reset. Its larger
child is immediately F2-active, but no recursive event is attempted there.

On the same owner, the next positive ordinary invocation commits with dt = 1/64.
Its state and receipts reproduce the first retained continuation frame. The
fresh source current then determines the next prescription, without a target
performance test, new refractory rule or modified allocation:

| Item | Result |
| --- | --- |
| Selected parent | `atc2:child:1`, the larger child of the first split |
| Singleton blocks | `(bc,tail)` and `(atc2:edge:0,head)` |
| Source-determined shares | (16791/32768,15977/32768) |
| Event outcome | Committed; graph grows from four vertices/three edges to five/four |
| Current/reset | Independently mapped, each exact charge 3; W reinitialized to four ones |
| Immediate target reread | `no_event` |

In target order (a,c,old child0,new child2,new child3), the new current C is
approximately

```text
(0.501394987106, 0.494698762894, 0.565024495125,
 0.737312730289, 0.701569024586).
```

The two newborn tendencies have opposite signs: +96005024613/1099511627776
and -527903589/1099511627776. The larger newborn still has two inflows, but its newly
evaluated allocation cannot endow both proposed further descendants above
their neighbors. The other degree-two loci lack two strict inflows. Thus this
particular immediate stop comes from the unchanged rule, not an event cap
masquerading as `no_event`. Nothing proves that later ordinary evolution will
leave the rule inactive.

The [record](../evidence/autonomous-topology-change/ATCFissionDescendantProbe.json)
contains one source admission, four explicit reads, one ordinary commit and
two supplied event commits: one predecessor replay and one new attempt.
All target currents agree with the independent literal oracle. The graph
stays simple and degree-at-most-two, within both the rule and constructor work
bounds. No earlier campaign or browser suite was rerun.

The ordering realizes the **one-new-attempt-after-one-positive-ordinary** part
of [K0](./ATC1Acceptance.md). The research driver reapplies the same F2 recipe
on the changed graph, but the runtime still requires a caller-registered target
catalogue. Only the prescribed target is prepared, and reconstruction from the
actual poststate must match it. This is not dynamic runtime registration,
installed policy transport, a serialized automatic dispatcher or arbitrary
repetition. The initial replay remains the historical supplied event, not a
newly claimed K0 invocation with an invented ordinary prefix.

## 2. Collective gain and operational support are distinct

The new review's exact identities are verified against all sixteen retained
split-target frames, without rerunning them. In their zero-channel all-one-W
target control, write C = (r,s,x,y), m0 = x-r, m1 = y-s and delta = x-y. Then

$$
f_{v_0}=\frac{3m_0-m_1+3\delta}{8},\qquad
f_{v_1}=\frac{3m_1-m_0-3\delta}{8},\qquad
f_{v_0}+f_{v_1}=\frac{m_0+m_1}{4}.
$$

With M = m0+m1 and D = 2(m0-m1)+3 delta, both children gain iff |D| < M.
Positive margins guarantee collective gain in this control, not the latter
inequality. Neither condition alone establishes organizational persistence.

History reset also is not historical neutrality. In the weighted branch,
source W determines J, J determines allocation, and the allocation survives
in target C even after target W becomes all-one. A control that resets W does
not erase this already formed resource consequence.

For stronger support, fix the following **assessment**, separate from F2:

- An operation is a balanced core/rim load followed by a measured incremental
  return to that core under the ordinary law, with background drift subtracted.
- The child's operation must remain available over the declared observation
  window; return after the encounter is certified separately.
- Child and pair resource budgets are explicit. Sibling and exterior support
  may participate; no autonomous-isolation claim follows.
- Two children may have symmetry-equivalent response functions. Distinct
  addressable sites with supported operations are not required to have unequal
  numerical outputs. Functional diversification is a stronger optional claim.
- An equal-charge neutral analytical preparation tests dependence on the
  target's resource organization, not merely whether any connected graph
  responds. It is not a same-source no-fission causal counterfactual.

Support here means a bounded **response-bearing organization**, not a proof of
two organisms, parent-identity succession, necessity of splitting or a spark.
Encounters are analytical preparations. Neither their scheduling nor repeated
external maintenance is added to the native trajectory or to F2's guard.

## 3. Why a distinct fixture is necessary

The old literal target dynamics are C_next = (I + dt L squared/8) C. They have
no restoring nonconstant mode. For a fixed additive encounter and linear
observation, their baseline-subtracted response is independent of the resource
background. Merely extending those runs cannot demonstrate formation-dependent
restoring support. They remain valuable negative/discrimination controls.

The production A and C evaluators currently implement only the declared
zero-derivative site potential. A new nonlinear potential must not be smuggled
through that evaluator ID. This motivates an explicitly **proposed mathematical
fixture**, not a production edit or a silent repair of any previous example.

### Proposed fixture ATC-SUP-MW-AOS-v1

Take the same three-node path and fixed unit measure, with source C = (1,3,1),
charge 5 and W = (1,1). Retain zero context, alpha = beta = gamma = chi_A =
zeta_A = kappa_Ah = 0, identity Hodge and no carrier. W remains exactly all-one.
Set eta = 1/4, kappa_c = 1/1024 and dt = 1/64. At **every** vertex use the same
dimensionless site-potential derivative

$$
p(c)=V'(c)=(c-1)(c-5/4)(c-3/2)(c-9/4)(c-3).
$$

The ordinary equations, with L = B B transpose, are

$$
\Phi=\kappa_c LC-p(C)+\operatorname{mean}(p(C))\mathbf1,
\qquad J=-\eta B^\top\Phi,
\qquad C^+=C-dt\,BJ.
$$

The mean term fixes the potential gauge without changing current. All resource
levels, coefficients and topology are part of this new mathematical binding;
no production complete-profile identity or native admission is asserted.

This is a homogeneous multiwell existence fixture: 1, 3/2 and 3 are local
potential minima, with barriers at 5/4 and 9/4. It lets the same fixed law
support a parent operating level and two lower child operating levels, without
node-ID-specific child potentials or changing the law at the event. These
levels are chosen for an exact constructive test; they are not derived natural
capacities, representative parameter values or a claim that F2 creates wells.

At the source all p(C_i) vanish. The exact currents are (3/2048,-3/2048), so
the **unchanged** F2 evaluator selects b, equal shares and positive margins 1/2.
The fixed split map gives target C0 = (1,1,3/2,3/2). Its initial exterior
currents are (1/4096,-1/4096) and its connector current is zero. This establishes
a mathematical source-to-map binding, not a new native event or onset history.

## 4. Exact support certificate for the proposed equations

The [rational certificate](../scripts/certify_atc_support_fixture.py) checks
whole-interval polynomial bounds, not just trajectories or sampled endpoints.
Its [output](../evidence/autonomous-topology-change/ATCSupportFixtureCertificate.json)
records all coefficients and inequalities. No numerical production step runs.

### Stable operating neighborhood

On the target graph define

$$
E(C)=\sum_i V(C_i)-\frac{\kappa_c}{2}C^\top LC.
$$

The update is C_next = C - dt eta L grad E. Charge is exactly conserved.
In the convex box with each exterior coordinate in [31/32,33/32] and each
child coordinate in [47/32,49/32], Bernstein coefficient bounds give

$$
p'_{\rm exterior}\in[193221,493253]/1048576,
\qquad p'_{\rm child}\in[109445,182789]/1048576.
$$

Since 0 <= L <= 4I, the energy Hessian obeys mu I <= Hess E <= M_E I, where

$$
\mu=105349/1048576>0,\qquad M_E=493253/1048576.
$$

A unique charge-5 equilibrium in this box has the form
C* = (1-t,1-t,3/2+t,3/2+t), with 0 < t < 1/256. Its equation is

$$
p(3/2+t)-p(1-t)=2\kappa_c(1/2+2t).
$$

The exact endpoint signs are negative and positive, and the derivative is
strictly positive throughout the bracket. Strict convexity establishes local
uniqueness on the complete charge plane, not just on the symmetric line.

The initial energy excess is at most 1/210698. Choose a trapping sublevel with
energy excess at most 105349/8589934592. Strong convexity bounds its distance
to C* by 1/64. C* is at least 7/256 from every box face. The checked inequality
(1 + 4 dt eta M_E)/64 < 7/256 keeps the next point and intervening segment in
the box; the energy-descent estimate then closes the trapping induction.

Specifically, for g = grad E,

$$
E(C^+)-E(C)\le
-dt\,\eta(1-2dt\,\eta M_E)g^\top Lg.
$$

The charge-tangent spectral gap is 2-sqrt(2) > 1/2. Strong convexity consequently
gives a strict geometric energy contraction, bounded by the factor
1 - dt eta mu (1 - 2 dt eta M_E). The whole target C returns to C*; W stays
one by its declared zero-exponent law. This is a local theorem for these
proposed equations, not a native binary64 or global-support certificate.

### Fixed encounters and individual response

At any age of the unencountered target trajectory use

$$
w_0=(1,0,-2,1),\qquad w_1=(0,1,1,-2),\qquad
\Gamma_{a,b}(C)=C+a w_0+b w_1,
$$

with independent signed amplitudes |a|,|b| <= 1/4096. They preserve total
charge and alter no law, W or geometry. Because the squared joint-load norm is
at most $20(1/4096)^2$, the loaded energy excess is bounded by

$$
9829997639869/926659603796590592
<105349/8589934592.
$$

Thus the loaded point is positive and in the same return component. This
certifies one encounter at any unencountered age, including simultaneous loads;
it is not a theorem for an arbitrary repeated-load schedule.

For an individual positive load epsilon, measure incremental delivered resource
fraction, subtracting the unchallenged ordinary drift:

$$
Q_i(C;\epsilon)=\frac{dt}{2\epsilon}
\bigl[f_i(C+\epsilon w_i)-f_i(C)\bigr],\qquad
f=-\eta L\nabla E.
$$

For this degree-two core,

$$
Q_i=\frac{dt\,\eta}{2}
\left(4D_i+D_{n_0}+D_{n_1}-19\kappa_c\right),
$$

where each D is the average p' on its load segment. The same fixed box gives
Q_i >= 360495/268435456 > 0 for either child. Both response channels remain
available as the state relaxes; they need not become unequal to count as two
addressable operations. This bound is for individual encounters; the return
theorem for joint loads does not assert uncoupled or positive individual intake
under every combination of loads.

### Formation-neutral control and claim ceiling

The matched analytical preparation C = (5/4,5/4,5/4,5/4) has the same graph,
charge, W, references and law. It removes the resource-level organization, not
its history through an imaginary erasure. The same positive core/rim encounters
have strictly **negative** incremental delivery, by the exact negative p' bound
near the barrier. Positivity is also checked for that one-update comparison.
Thus the organized target has the specified response-bearing property while
the neutral preparation does not. Here **formation-dependent** means dependent
on the prepared resource organization, not causally attributed to the fission
history. This is stronger than arbitrary perturbational efficacy but does not
yet establish fission-caused acquisition or durable uptake.

This control, the two positive response bounds and the full-charge return
component give the fixture a concrete support meaning. They do not establish
two separate RC identities, autonomy after sibling removal, parent succession,
novel basin creation or the necessity of a topology operation. The field may
support both operations relationally. The underlying potential wells pre-exist
the split. Separate positive responses and return under joint loads also do not
prove that joint continuation is non-factorizable from participant records;
no stronger H5-style claim is imported. No response criterion is fed back into
F2 selection.

The source's site values are themselves potential minima. Its nonzero current
comes from graph coupling, not disappearance of the parent site's minimum.
F2 remains a convergent-participation/endowment growth hypothesis, not a derived
instability or nonviability criterion. Full source-organization viability has
not been certified here either; its loss cannot be assumed from a split.

## 5. Next work and unchanged boundaries

The [independent review and bridge follow-up](./ATCSupportFixtureReviewAndBridge.md)
reports PASS for this proposed mathematical existence fixture and positive
bounded repeated-use evidence. This is not user acceptance or accepted claim
authority. It tightens the causal interpretation without changing either
retained record. A native follow-up needs
an explicitly admitted site-potential evaluator/binding, target/current/reset
admission and reference-initializer conformance; the existing zero-potential
ID cannot stand in for it. A proposed finite observation is sixteen ordinary
updates, with the fixed encounter family at declared checkpoints, matched
unchallenged histories and the analytical return bound. It has not been run.
The follow-up separates that native-bridge gate from a matched causal
discrimination gate; neither is discharged by the neutral preparation.
The subsequent design review keeps both gates open while pinning the proposed
potential/evaluation contract and exact causal predictions. Those equations
already predict positive response at the source and a reduced target pair
response. The intended comparison concerns changed organization/addressability,
not acquisition from complete absence; no native confirmation is claimed.

The native descendant result does not depend on this proposed support fixture.
Both preserve the old counterexamples. F2's general scientific justification,
all thirty operation/family cells, structural CAN-B, composition beyond the
bounded next attempt, runtime policy transport and native automatic ATC remain
open. No origin claim/debt, accepted forensic node, paper, specification or
production implementation changes in this follow-up. No commit is made.

Reproduce the two bounded checks from repository root:

```sh
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python implementation/investigations/grc9v4-constitutive-design/scripts/probe_atc_fission_descendant.py
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python implementation/investigations/grc9v4-constitutive-design/scripts/certify_atc_support_fixture.py
```

Both commands print fresh records without replacing retained evidence. The
first executes the bounded native path; the second checks exact mathematical
inequalities only. Neither runs an aggregate implementation campaign.
