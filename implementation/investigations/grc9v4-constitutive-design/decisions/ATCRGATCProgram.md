# A_RG2b ATC — reuse of the supplied theory-first package

2026-09-24. **Fits the investigation as proposed research.** This is a quick
compatibility/intake assessment, not independent mathematical acceptance.
The user requests reuse, so the supplied proofs, code and scientific
certificates are retained rather than recreated.

**Subsequent review: PASS, reported by the user on 2026-09-24.** The
[additive review record](../evidence/atc-rgatc/RGATC-Review.md) preserves the
bounded scope and unchanged package identities. The subsequently authorized
[scoped adjudication](./ATCRGATCClaimDebtAdjudication.md) and source admission
are now complete, closing the [five Candidate-A research programs](./ATCCandidateAResearchClosure.md).
No fresh full E/R execution is claimed.

## Fit and substantive differences

The architecture agrees with the V4 RG2b distinction: authoritative C/W only;
completion-relative bounded Lipschitz section; lagged invariant equation;
fresh target reconstruction rather than transport of H/Gamma; no Z carrier
and no CI root. The package reuses geometry-uniform fixed-H A inequalities,
not the CI dynamics. The error-certified finite ladder is an implementation
of the declared graph transform, not a hand-selected geometry.

T/A corrects the original [direction](../evidence/atc-rgatc/RGATC-Direction.md)
in three necessary ways:

- The positive beat is frozen at 2^-25 and gain at 2^-32. The old variable
  request interval and 71-beat obstruction are not inherited. These are
  conservative existence parameters, not a practical performance claim.
- A new globally Lipschitz, floor-preserving auxiliary completion reaches
  through C=0; negative auxiliary coordinates do not admit negative physical
  resources. This is a reviewed research extension, not a shipped native identifier.
- Small-beat target return uses its rederived rate and positive invariant
  ball, not the old first-beat entry below radius 2/3.

The paired source obstruction and target asymptotics remain exact-real
theorems. The concrete source-only share is k=32768. Nonneutral section
geometry and nonzero same-state CI residual are explicitly distinguished.
E/R adds finite section/oracle/lifecycle/covariance/representation evidence,
not general native ATC or uniform finite-work success everywhere.

## Retained work and local checks

Use the [package index](../evidence/atc-rgatc/README.md). All ten files in the
separate T/A package match the E/R package's embedded predecessor before
portability normalization, so only the embedded copy is retained.
Scientific code, theorem text and certificate bytes are unchanged.

Local checks pass: both manifests; T/A's supplied pressure checker (two exact
regenerations against its certificate); E/R retained identity/code bindings;
E/R retained consistency pressure. The full E/R 201-section-query campaign
was reported by the supplied package and **was not rerun locally**.
No internal pressure result is relabeled independent review.

Only packaging/navigation metadata is normalized: one temporary-output
command, upload-specific source IDs, reproduction attribution and the
corresponding manifest entries. The repository source map is a correspondence,
not proof that an earlier uploaded document had identical bytes. The bundled
arithmetic differs from the current repository script only in an unused
standalone evidence path and digest; its arithmetic primitives are unchanged.
Keep the bundled dependency so the scientific execution identities remain exact.

## Claim/debt discipline and next step

The [intake record](../evidence/atc-rgatc/RGATC-Intake.json) retains typed
RG2b/CI predecessor traces, seven proposed handles, and all 28 origin debt
routes as pending at intake. Accepted source authority is not transferred
merely by a citation. The separate successor ledger now admits the seven
bounded conditional claims and reconciles all 28 RG-local debts; the intake,
origin and four earlier realization ledgers remain unchanged.

1. Independent T/A and E/R review: PASS as reported by the user; no additional
   findings were supplied. Retain the additive review rather than rewrite inputs.
2. If independent execution is needed, use the supplied E/R `--compare`
   once; it requires full equality with the retained certificate. Never replace
   the original with `--write` merely to obtain a passing record.
3. All-28 scoped adjudication and exact source admission: complete.
   Earlier A_OS/CI/PC/CI+PC authority is preserved.
4. Only then use reviewed results through topology proposal → paper →
   specification → Phase 9 native work.

No C1 section claim, completion-independent uniqueness, arbitrary graph or
active environment, repeated-event/non-Zeno theorem, exact indefinite numerical
conservation, native support, or aggregate ATC-2 closure is inferred. Native
engineering is downstream, not a missing half of research-reference acceptance.
