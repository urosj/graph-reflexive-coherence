> Relocation-only maintenance: repository paths and dependent identities were
> rebuilt before first commit. Scientific statements, numerical results and
> review verdicts are unchanged; the new identities are not an independent
> reviewer rerun. Earlier byte-preservation statements describe the pre-move stage.

# CAN-LSF Read-Back lift — first bounded results

**Status:** new research results for review; no second claim/debt adjudication.
The [fixed proposal](./ATCSectorReadBackLiftProposal.md) and
[evidence record](../evidence/autonomous-topology-change/ATCSectorReadBackLiftChecks.json)
separate exact sufficient inequalities from 96-digit Decimal diagnostics.
The [zero-channel acceptance](./ATCSectorConstitutiveAcceptance.md) is unchanged.

## 1. The enabled point is nonvacuous

At chi_A=1/16, gamma=1/1024 and both geometry gains one, the first ordinary
beat actually consumes the new channels:

| First source beat | Diagnostic value |
| --- | --- |
| Squared reference Read-Back current change | 0.0000137796772791 |
| Squared fresh-minus-reference selected-current change | 0.0000000928003305 |
| Squared Frobenius generated edge-Hodge increment | 0.0000000000882516 |
| Squared consumed geometry-potential change | 0.0000000225698406 |
| Writer targets, one per sector | 0.9997726260; 0.9998167057 |

These effects are not merely nonzero parameter declarations. The same-input
chi=0 control has zero generated geometry/current-stage gap; the
kappa_Ah=0 control has no fresh-current change despite generated geometry.
The actual geometry consumer also changes the resource endpoint.
The gamma-dependent writer is not the zero-channel square-root update.

Initially the unchanged source guard is inactive. After one positive ordinary
beat it passes, using the **recomputed selected reference-stage current** at
that committed state. The paired sector equalities hold; their separation is
about 0.0125197943. The old spectral guard margin is about 0.0307594419 and
the smallest reference inflow about 0.0993413468. These guard observations do
not themselves promote the old zero-feedback cone proof to the full map.

The same allocator now gives **k=30779**, with funding margins about 1.11088
and 1.40344. The grid coordinate lies about 0.2665 from its nearest half-integer
tie. Different k is an expected consequence of changed source currents, not
a changed physical allocator. No candidate target was scored or searched.
The homogeneous source remains nonfiring even at the post-beat history.

## 2. Both roles enter; the counterfactuals fail physically

Use that single source-selected map on live and independently retained reset,
preserving each role's own old W and seeding only its bridge.

| Role | Squared distance from (3/2)1 after first target beat | Smallest resource |
| --- | --- | --- |
| Current | 0.2554528249 | 1.3566254217 |
| Reset | 0.3311149036 | 1.3410050814 |

Both squared distances are below 4/9 and both log-histories lie in [-1/8,0].
Three target beats per role remain positive and pass the fixed OS tolerance.
The unsplit enabled trajectory first proposes a nonpositive resource at
attempt 10 (minimum about -0.23692). Resetting W to one without splitting at
the live event boundary fails at attempt 7 (minimum about -0.16398). All
earlier beats pass; both rejecting attempts also pass the OS split-residual
test. Thus their **observed** failure is resource positivity, not a tolerance
failure. Rejected outputs are not advanced, and these controls are never
event-selection operands.

This is positive finite evidence for the original mechanism under genuine
feedback. It is not an interval-certified obstruction theorem, a generic
proof about all guard-positive states or native event execution.

## 3. Uniform target theorem: positive, but conditional on entry

The proposal's analytic estimates and exact rational checker give, throughout
the full declared nonzero parameter box,

$$
q_X=9209/10240=0.89931640625<0.9,
$$

with writer drive at most 4489/1280000<1/8 and split-residual upper bound
512416169/2056356000000<1/512. Once a role lies in the declared charge-nine,
radius-2/3, log-history domain, it stays positive, its resource deviation
decays uniformly, and its log-history tends to zero. This is an all-time
mathematical argument, not extrapolation from the three target beats.

It does not turn the point's Decimal entry observation into a certified entry
proof, nor does it prove source activation/obstruction across the box. Those
are separate premises. In particular, the old positive resource-scaling
corollary does not carry over at fixed nonzero gamma and geometry feedback.

## 4. What remains before a second adjudication

Review the staged equations, symmetry argument, nonvacuity diagnostics and
conditional return theorem. The next mathematical execution should close the
**source and entry implications**, not improve the target: enclose the actual
source/independent-reset trajectories and every dyadic prescription reached
over the proposed box, then combine those bounds with the return theorem.
Retain a failed implication if this box does not close. No silent parameter
shrinkage, sector tolerance or fresh-current consumer substitution is allowed.

A later acceptance may be narrower than full-box closure, but must say exactly
which point, conditional theorem or box implication was reviewed. No new
claims are accepted here; graph readmission, native lifecycle, formation,
other families, the capability matrix and aggregate ATC-2 remain open.

Reproduce only this focused research check from the repository root:

```sh
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python implementation/investigations/grc9v4-constitutive-design/scripts/check_atc_sector_readback_lift.py
```

The command prints a fresh record without overwriting retained evidence.
Its first development assertion incorrectly required exact Decimal charge
summation; it was corrected to the stated finite-arithmetic diagnostic check.
The exact conservative map was not changed. No physical parameters, guard,
allocator, target or acceptance tolerance were changed after observing results.
