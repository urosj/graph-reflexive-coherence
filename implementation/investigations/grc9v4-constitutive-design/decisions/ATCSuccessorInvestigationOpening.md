> Relocation maintenance: paths and dependent identities were mechanically rebuilt; scientific content and review verdicts are unchanged. New identities do not claim a new independent rerun. Original committed subjects remain in Git.

# ATC successor investigation opening

**Gate:** ATC-OPEN  
**Date:** 2026-09-20  
**Status:** research opening authorized by the user's commit-and-continue
instruction; opening artifacts prepared for review.  
**Scientific result:** none. No ATC law, claim, debt resolution, paper,
specification or runtime support is accepted by this opening.

## Purpose and predecessor

Investigate a graph-generic constitutive extension that generates complete
topology events from declared scientific inputs. The accepted substrate
already applies caller-supplied typed events; that does not determine an
autonomous generator. This is an extension, not a finding that accepted
Tranche 7 failed or an instruction to redo its numerical campaigns.

The [source intake](../evidence/autonomous-topology-change/ATCSourceIntake.json)
pins the reference-intake commit `0386568`, the accepted Tranche 7 closure
commit `e020ea7`, its merge `7d4e376`, current substrate paper/proposal,
specifications and releases, relevant runtime owners, and the current
forensic authority context. Whole Git commit/tree identities identify the
source subject; selected file hashes make the principal inputs inspectable.
They are not a dependency-complete ATC execution record.

The [G3 acceptance](../../../phase-9-grcv4/tranche-7/P9-7.8-G3Acceptance.md)
remains unchanged: ten exact generic declarations, one consumed G3 set,
Tranche 7 closed, and only P9-8.1a specialization source entry opened.
No ATC policy is attached to an unchanged accepted profile identity.

## Proposal intake and source reconciliation

Use [Draft 8](../drafts/autonomous-topology-change/2026-09-GRCV4-Autonomous-Topology-Extension-Proposal-Draft8.md)
as the scientific exposition and
[Draft 7](../drafts/autonomous-topology-change/2026-09-GRCV4-Autonomous-Topology-Extension-Proposal-Draft7.md)
for rationale and claim/debt lineage, especially its §14. The retained
[index](../evidence/autonomous-topology-change/proposal_index.json) contains
35 proposed claims, 28 proposal debts and 165 reciprocal links. Its stable
`ATC7-…` identifiers remain local proposal handles, not accepted graph nodes.
The [location map](../evidence/autonomous-topology-change/draft8_locations.json)
connects those handles to Draft 8 without changing their meaning.

The intake records two distinct things:

- **Pinned inputs:** exact repository files and four primary core texts at
  `urosj/geometric-reflexive-coherence` commit
  `5a8b01ae60165054da617db649c5a039755a18ec`, with content hashes and immutable
  URLs. No adjacent checkout or machine-local path is required by the record.
- **Scientific correspondence still to establish:** whether the current
  primary-source clauses support each proposed inheritance. The core pins
  are newly identified subjects, not an assertion that they equal the
  proposal's historical attachments or previously retrieved `main` pages.

Draft source labels S05–S07 now have current local paper/proposal/specification
subjects; S08 has the accumulated claim/debt/provenance records; S09 has the
preopening plan and current query guide. S10–S12 have current event,
representation and initializer contracts and the accepted initializer
authority. S13 has the accepted aggregate/G3 records and exact-profile
registry. S14 has the pinned runtime tree and selected lifecycle/geometry
owners. These are replacement subjects for new investigation checks, not
retroactive replacements for historical execution evidence.

The machine-readable intake stays with the proposal references, outside the
accepted `decisions/*.json` inventory. This is explicit staging, not an
exception to source discovery: a future authority record must undergo the
normal schema classification, adapter/readmission, graph validation and
acceptance route. Do not add a JSON decision and then suppress its discovery
to keep current queries green. No tool configuration or accepted inventory is
changed here; the tool does not yet expose ATC proposal nodes.

## Carry forward accumulated authority, not just D10.2 counts

The current typed context is pinned by its historical bundle/graph identity,
append-only authority-extension identity, and complete node/edge digests.
It contains 44 current claim nodes, 29 historical claim nodes, 34
debt-transformation nodes, 83 normative objects, 190 equation contracts and
19 verification obligations. These are graph counts, not counts of open debt,
accepted runtime capabilities or new ATC support.

All inherited nodes, classifications, negative boundaries and reciprocal
relations remain unchanged. The 28 proposal debts are additive inputs. They
do not replace inherited debts, reopen every resolved debt, or discharge any
verification obligation through document intake. Runtime evidence and later
acceptance records remain distinct from the earlier design graph.

Eight typed forensic queries are retained in the intake with classifications,
source/edge references and reproducible trace digests:

| Boundary | Current result | Consequence for ATC |
| --- | --- | --- |
| `D10.2-EC-PARENT-CORE-C-AUTHORITY` | `indeterminate_requires_review` | Reconcile exact resource and causal-state clauses; no permission to add hidden memory. |
| `D10.2-EC-PARENT-SPEC-COMPOSITION-PROFILE-IDENTITY` | `indeterminate_requires_review` | A causal ATC policy needs an explicit successor identity; no automatic callback on existing profiles. |
| `D10.2-EC-PARENT-L-TOPOLOGY-EVENT` and `D10.2-EC-EVENT-LIFECYCLE-TUPLE` | `indeterminate_requires_review` | Consume the current executable event specification and accepted lifecycle evidence separately; parent provenance does not supply a generator. |
| `P9-EC-A-INITIALIZER-REFERENCE-PASS` | required support from the accepted optional initializer claim | Reuse its bounded target-only construction independently for current/reset; do not infer arbitrary target admission. |
| `D11-C-EC-C-J0-DERIVATIVE` | accepted on the existing smooth fixed-rank SPD selector stratum | Structural analysis must retain the derivative domain and full baseline chain. |
| `P9-EC-ABUNDANCE-OBSERVATION` | required bounded support | Observation authority does not supply ATC actuation or a numeric viability law. |
| `GTRS-RG-DEBT-C1-SECTION-REGULARITY` | narrowed; activated by RG derivative/spectrum claims | Preserve the Lipschitz-only boundary; derivative-free ATC is not universally blocked. |

## First investigations and claim-local obligations

The source intake assigns each proposed claim to one primary work route;
cross-route scientific dependencies remain the reciprocal edges in the index.
These routes are not five new acceptance layers or a mandatory catalogue of
operation names.

1. **ATC-1 — causal inputs, witness stage and transaction composition.**
   Reconcile the primary-source clauses and earliest affected contracts;
   specify the proposed-source admission mapping. Define the permitted
   scientific projection, complete policy identity, present versus historical
   current, optional memory and eligible positive-time boundary. Compare
   ordinary-then-event with joint commit explicitly: under `CAN-TIME-AFTER`,
   rejected events preserve the already committed ordinary poststate, not the
   pre-beat state. Observation, save/load and zero-duration operations are not
   unannounced event triggers. This is the next substantive investigation;
   no choice is accepted merely because Draft 8 calls it the first proposal.
2. **ATC-2 — concrete candidate closure and discriminating research probes.**
   Evaluate assembled edge-birth, structural-refinement and coarsening laws
   with their claim-local dependencies. At least an increasing and a forward
   simplifying construction must challenge the shared design; this does not
   require acceptance of both. Close the graph, resource, reference/context,
   history and whole-target lifecycle inputs before claiming an executable
   law. Research probes may use frozen runtime owners, but may not change
   production code, accepted releases or advertise support.
3. **ATC-3 — claim/debt adjudication and reviewed synthesis.**
   Record bounded acceptance, restriction, replacement, rejection or remaining
   debt separately for constructors, triggers and stronger interpretations.
   Use explicit side-tool successor processing before presenting new records
   as queryable authority. Preserve useful mathematics even if an assembled
   law fails. All ten products receive exact applicability dispositions, not
   ten inferred positive results.

Three already identified pressures belong to those investigations:

- `ATC7-CL-29A` / debts 02, 07, 26: the basic OS reference Hodge is diagonal,
  so nonadjacent node-form coupling is zero. A positive relation-birth witness
  cannot come from that basic read. A fresh OS-pass witness must be an
  explicitly selected policy, not a cached-current substitution.
- `ATC7-CL-29C` / debts 02, 09, 14: CAN-C still needs an exact homogeneous
  score/ordering and complete target-reference choices. Calling the assembled
  candidate fully specified does not select those missing choices.
- `ATC7-CL-18` and assembled-law claims / debts 07, 15, 27: compare topology
  changes against identity-topology reconstruction with equivalent history
  treatment wherever effects are attributed to topology. Reinitializing W
  and resetting Z can itself change subsequent evolution.

These are opening review observations and proposed work routing, not new
accepted negative claims or amendments to the retained index. Complete-state
quotient, core topology correspondence, restoration, identity and spark
claims activate their own debts only when pursued. No universal all-family
theorem is an entry prerequisite for every bounded law.

## Propagation and Phase 9 dependency amendment

The required route is:

```text
accepted Tranche 7 baseline
  -> ATC investigation, research probes and accepted claim/debt transformations
  -> reviewed topology-only proposal
  -> separate topology-extension paper
  -> normative extension specification and release
  -> Tranche 7T implementation of the accepted scope
  -> dependent Tranche 8 specialization and reconciled Tranches 9–10
```

Reserve **7T** now; determine its detailed leaves from accepted scientific
results. It must ultimately demonstrate a fixed policy and initial state
whose ordinary evolution reaches a generated event, constructs and admits
the full target through the real lifecycle, and continues on the changed
graph. Manually supplied events or an empty generator interface do not close
that requirement. No-event, unresolved/uncertified, reset-only rejection,
history controls and transaction-correct rollback are part of the evidence.

The [Phase 9 plan](../../../Phase-9-GRCV4-ImplementationPlan.md) and
[checklist](../../../Phase-9-GRCV4-ImplementationChecklist.md) reserve this
dependency without changing historical machine permissions:

- P9-8.1a chart/port preparation remains independently eligible; it is not
  started by this opening. Other genuinely independent mathematical/source
  preparation can proceed under its existing scope.
- Topology-dependent trigger/Split/expansion runtime and integration must
  first identify and satisfy the generic contracts they consume. Do not
  freeze that architecture inside GRC9V4 before its generic dependency closes.
- Nine-port mechanics, chirality and capacity remain specialization-owned;
  generic binary refinement is not a replacement for D11-G9.
- Tranches 9–10 must distinguish the existing substrate, ATC-enabled successor
  and GRC9V4 support. Their 40 disabled-compatibility obligations remain;
  optional completed-spark/hierarchy claims are not silently selected.

No runtime permissions are widened or newly revoked by these documents.
All topology-dependent specialization leaves are already unopened; a future
execution-policy successor must bind their ATC prerequisites before opening
them. The current P9-8.1a authorization and accepted G2/G3 records remain exact.

## Completion boundary

ATC-OPEN prepares source identities, inherited-boundary queries, additive
proposal routing and dependency planning. Primary-source clause adjudication,
ATC source/schema readmission, candidate-law selection, native ATC evidence,
new paper/specification and 7T implementation are still pending. The user
authorized this first research step, not the scientific conclusions of those
later steps.

## Bounded validation at opening

- Verified the intake's canonical digest and 34 repository source pins against
  its recorded commit. The two plan pins intentionally describe their
  preopening versions; all other pinned repository inputs remain unchanged.
- Verified four core-source hashes against their pinned Git objects,
  unchanged retained draft/index hashes, complete 35-claim routing and all
  165 reciprocal links to the 28 proposal debts.
- Rebuilt the current forensic context, reproduced all eight query digests
  and checked their retained classifications and source/edge references.
  The inherited node/edge identities are unchanged; no ATC handle is admitted.
- The existing P9-7.8 retained-evidence checker passes: G3 accepted, Tranche 7
  closed, ten exact profiles, only P9-8.1a source entry authorized. This is an
  integrity recheck, not a numerical rerun or a complete live-status check.
- Checked repository-relative file links, absence of machine-local paths in
  changed artifacts, and Git whitespace hygiene. No native ATC run occurred.

Forensic queries are reproducible through the current loader and the exact
operation/query arguments recorded in the intake; compare the resulting full
trace digests, not a digest of the compact retained projection. Historical
plan bytes are available through `git show <repository_commit>:<path>`.
The existing bounded G3 check, from the repository root, is:

```sh
.venv/bin/python implementation/phase-9-grcv4/verification/verify_p978_specialization_review.py
```
