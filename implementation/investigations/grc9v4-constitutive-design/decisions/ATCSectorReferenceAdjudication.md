> Relocation-only maintenance: repository paths and dependent identities were
> rebuilt before first commit. Scientific statements, numerical results and
> review verdicts are unchanged; the new identities are not an independent
> reviewer rerun. Earlier byte-preservation statements describe the pre-move stage.

# CAN-LSF — sixth scoped adjudication: G7 and the A_OS G1–G7 program

**G7: PASS. A_OS G1–G7 research program: PASS**, within the bounded scopes
established by the accepted stages. This records the user-supplied
[independent review](../evidence/autonomous-topology-change/review/CAN-LSF-G7-Pass.md)
**with the user's explicit correction of its native-work disposition**.
The [companion record](../evidence/autonomous-topology-change/ATCSectorReferenceAdjudication.json)
is the current acceptance/status authority for this research sequence.

## 1. Correct the scope, not the scientific results

For this investigation, **G7 is the executable-reference and conformance work
previously called G7-R**. There is no outstanding G7-N half of the research
program. The review's scientific PASS stands; its G7-N/open framing is
superseded by the user's subsequent instruction.

The order is:

1. G1–G6 mathematics → G7 executable reference, representation and research
   lifecycle conformance: the bounded A_OS research program is complete.
2. Reviewed scientific result → topology proposal → separate extension paper
   → specification → later Phase 9 native implementation and conformance.

Native profile/event ownership, production numerical policy and K0 integration
belong to the second sequence. They are **not unmet G7 research criteria**.
The existing [reserved Phase 9 Tranche 7T](../../../Phase-9-GRCV4-ImplementationPlan.md#tranche-7t-reserved-autonomous-topology-extension)
and [handoff](../../../Phase-9-GRCV4-Handoff.md) receive those engineering
requirements without opening implementation permissions now.

The reviewed note, executable, checker, conformance record and earlier
adjudications remain byte-for-byte unchanged, including historical pending
fields and G7-R/G7-N terminology. Read those fields as historical; this
successor supplies their corrected current disposition. Scope statements
such as “native ATC not executed” remain true and useful, but are not blockers
to the accepted scientific program.

## 2. Accepted G7 scope

- Full-graph nonlinear A_OS reference for the stated concrete G5–G6 profile,
  with independent staged comparison: eight cases and 1,116 scalar checks.
- Source-reference-current dyadic prescription, failure-closed quantization,
  and one prescribed topology operation without target lookahead.
- Independent current/reset resource and history transfer, old-edge lineage,
  bridge-only seeding, and detached both-role readmission.
- Atomic local publication, reset semantics, deterministic event and
  split/step/reset replay, negative controls and whole-publication rollback.
- Variable-request behavior at the stated points and schedules, including
  independently reviewed split/replay pressure at h=3/25.
- The finite exact-real versus publication-rounded binary64 bridge, with
  represented h/tau and explicit numerical boundaries.

The full C¹ potential class and parameter domains remain the G5–G6 theorems;
G7's concrete nonlinear law and interior tuple do not replace them. G7 does
not claim all native intermediate rounding or an infinite binary64 theorem.

Independent resource rounding's observed nonzero charge discrepancy is an
**accepted G7 finding**, identifying a later production conservation/rounding
requirement. It is not an unresolved defect in this research milestone.
Likewise, admission from exact event-state preimages rather than a production
ordinary-evolution dispatcher is the defined conformance scope. Formation and
scheduling are outside this bounded task, not missing G7 components.

## 3. Profile identity precision — review correction applied here

The reviewed note's phrase “all physical and computational operands” must be
read more narrowly. The profile preimage binds the **evaluator/profile
operands**: graphs, reference decorations, potential, constitutive parameters,
tau and numerical recipe labels. `PROFILE_ID` alone does not bind the complete
event semantics.

Reference-stage consumption, allocator/domain predicates, sector-to-site
transfer, history lineage, readmission and transaction/replay semantics are
**additionally bound by the implementation/checker source identities and
outer conformance record**. That combined binding is sufficient for this
research acceptance. The later production contract identity must contain or
explicitly reference its event semantics. This clarification corrects the
claim without changing the reviewed source bytes or pretending the profile
hash already contains fields it does not.

## 4. What the independent review actually executed

The reviewer reproduced all eight point rows, all four unsplit/history-reset
source controls, both local lifecycle suites (twelve negative cases each),
lineage, bridge seed, event replay and split/step/reset behavior. They also
ran additional lower-endpoint split/replay checks on both graph classes;
those are reviewer-reported checks, not retroactively inserted into the
original conformance record.

For the four retained sixteen-step representation comparisons, the reviewer
independently reran **paired-current and embedded-current**, matching every
retained row. The two reset runs remain retained author-executed evidence;
do not report that the reviewer reran all four. The reviewer did not execute
the repository-binding `main()` wrapper or native code.

Verified reviewed identities:

- Conformance record canonical digest:
  `9a8624ff777911ad90ff68c88df2c3f8a886e9c61a876175e880a09adb33ad37`.
- Reference implementation SHA-256:
  `202fd964df2ea52415957c5cf1adccfc3c25f034e15b0166088fd47faa4954c5`.
- Checker SHA-256:
  `7651e34126ce76cfcae0afd600da6d609d6149b06575c979f6f7c1f741ba07d5`.
- Reviewed note SHA-256:
  `d842ad5e289b5309bb6c241218e4589ef1c0b574879e475a5182623ef4422c7d`.

This acceptance checks those bindings and claim/debt consistency; it does
not repeat the independently reviewed numerical campaign.

## 5. Claims, debts and the next scientific decision

Six local `ATC-LSF-G7-CL-*` claims accept the evaluator/stages; source/transfer;
research lifecycle; finite representation bridge; numerical/identity findings;
and bounded program completion. They are research handles, not newly admitted
forensic nodes. The original 35-claim/165-link intake and all 28 wider debts
remain intact, with earned portions separated from remaining scope.

DB-03/11/15/16/21/22 gain bounded local transfer/reset/replay/rollback evidence;
DB-10/13 gain finite representation and quantization findings; DB-23 gains a
research executable, not production execution; DB-26 gains completion of this
A_OS research program, not other-family support. Native portions are routed
to downstream Phase 9, never used to keep G7 open. Scientific formation,
other realizations, wider environments, repeated hybrid/composed operations,
and aggregate ATC-2/ATC-3 are not closed by this scoped acceptance.

The A_OS result is now a suitable **reference template for Candidate-A lifts**:
carry forward the source-only prescription, exact sectors, transfer/lineage,
independent roles and evidence discipline, while rederiving each realization's
current/geometry closure, obstruction, entry and continuation claims. Do not
copy the OS proof as authority for another realization.

The recommended next investigation is A_CI: separate what is inherited from
what must be proved for its implicit closure. This is a next-step recommendation,
not an A_CI acceptance or an experiment started by this adjudication. No further
A_OS widening is required merely to satisfy G1–G7. No code, paper/specification
propagation, runtime permission, other-family support or aggregate closure is
created by this acceptance.
