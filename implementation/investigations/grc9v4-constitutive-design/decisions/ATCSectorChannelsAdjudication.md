> Relocation-only maintenance: repository paths and dependent identities were
> rebuilt before first commit. Scientific statements, numerical results and
> review verdicts are unchanged; the new identities are not an independent
> reviewer rerun. Earlier byte-preservation statements describe the pre-move stage.

# CAN-LSF — fifth scoped adjudication: G5–G6

**Status: accepted bounded mathematical research.** The user supplied the
[G5–G6 PASS](../evidence/autonomous-topology-change/review/CAN-LSF-G5-G6-Pass.md)
and authorized continuation into investigation-local G7, with no `src/`
changes. The [companion record](../evidence/autonomous-topology-change/ATCSectorChannelsAdjudication.json)
accepts exactly the reviewed scope; it does not rewrite the
[proof](./ATCSectorChannelsAndRequests.md), its historical pending fields,
the [fourth adjudication](./ATCSectorStateDomainAdjudication.md), or any older evidence.

## Accepted scope

- Explicit fixed host-frame WLS references and proposed child-position
  inheritance; alpha and beta in [1/4096,1/2048], genuinely consumed at the
  declared source stages. Writer descriptors are rebuilt from post-resource C.
- The common, fixed C¹ law on [0,9] with |p'-19/4|<=1/2048, including the
  stated nonlinear subclass. No independent per-edge/per-beat potential choice.
- The paired source cone, actual-p event balance, independent reset domain,
  source-selected dyadic allocation, funding and both-role target entry.
- Paired and whole embedded target indefinite return, with resource factors
  below 9/10 and 49/50 and history limit exp(-alpha c). This is not a joint
  state contraction or the old W→1 claim.
- h=1/8 for G5 and every request sequence in [3/25,1/8] for G6, with fixed
  tau=1/(8 log 2), potential and constitutive parameters. No subdivision,
  continuous-time or zero-step equivalence.
- Embedded unsplit/history-reset-only rejection by attempts seven/ten,
  respectively; these are upper horizons, not identical failure times. The
  conditional survivor intersection is a proof operation, never a physical
  clamp. The second-parent environmental counterexample remains excluded.

The exact G6 paired contraction margin is small: about 0.000621 below 9/10.
Any later widening must recompute it, not inherit its sign by assertion.
The original feedback box and each stated source/environment/role domain are
unchanged except the explicitly reviewed actual-p event-balance condition.

## Evidence and attribution

The reviewer independently executed the mathematical `evidence()` and matched
the complete retained payload. They independently reconstructed the captured
binary64 native descriptor/writer results, but did **not** execute `pygrc` or
either repository-dependent `main()` wrapper in their environment. Attribute
those primitive results as consistent positive evidence, not native G7.

The reviewed mathematical record digest is
`d404e45a76bafad90a1a563b298ae01b1cc4866c125627cb6163bc417bd1afb1`;
the native-readiness record digest is
`87a2085de7c0ec11fa1aa89ec10b40f1ecdcd908d1bef9ccfa640df165918ec6`.
The companion binds the review, exact reviewed records/scripts/note and this
adjudication. Integration checks identities and reciprocal claim/debt links,
not another mathematical campaign.

## Local claim/debt decisions

The six `ATC-LSF-G56-CL-*` handles are local research dispositions, not forensic
graph nodes. They cover channel/reference binding; potential-class source and
entry; shifted-equilibrium return; variable requests; active embedding and its
exclusion; and scope/native ceilings. All 28 debts are carried forward with
reciprocal references; the original 35-claim/165-link intake is unchanged.

DB-07/08/10/19 gain only the reviewed source/sector/allocation/restoration
portions. DB-26 gains this broader A_OS mathematical domain, not other-family
support or a native profile. DB-20 formation, DB-23 native execution, DB-24
source readmission and DB-25 general behavioral quotient/merge remain open.
No blanket debt discharge or aggregate ATC-2/ATC-3 closure occurs.

## G7 now means two explicitly separated work products

The authorized immediate work is **G7-R: investigation-local reference and
represented-error/lifecycle research**. It may create local executable code,
independent checks and a proposed execution contract; it must not masquerade
as native potential/profile or event ownership. New G7-R results require their
own review, rather than inheriting this acceptance.

**G7-N remains native admission**, after reviewed claims/debts → topology
proposal → extension paper → specification. Its production implementation,
full numerical policy and public lifecycle integration remain open. This split
preserves the original G7 objective while allowing meaningful research now;
it does not replace native conformance with a paper-only gate.
