# A_RG2b research — scoped claim/debt adjudication

2026-09-24. **Accepted bounded A_RG2b research.** Following the
[user-reported audit PASS](../evidence/atc-rgatc/RGATC-Review.md), the user
authorizes adjudication and closure of all five Candidate-A realization
research programs. This decision admits the supplied T/A and E/R work without
rewriting its proofs, code, certificates or historical pending flags.

## Seven accepted conditional claims

The [successor ledger](../evidence/atc-rgatc-admission/ATCRGATCClaimDebtLedger.json)
retains the exact proposed handles:

| Handle | Accepted scope |
| --- | --- |
| RGATC-T-SECTION | Sufficient completion-relative bounded Lipschitz invariant-section theorem, covariance and conditional reconstructive topology continuation. |
| RGATC-A-COMPLETION | Explicit floor-preserving auxiliary completion and sufficient budgets for the paired source/target; auxiliary negative resources are not physical states. |
| RGATC-A-RATES | Rederived duration-dependent source obstruction and positive invariant target return from fixed-H inequalities, not inherited CI dynamics. |
| RGATC-A-ANCHOR | Whole-section-envelope source onset, unique source-only dyadic selection, independent both-role transfer and target entry; T/A did not execute section queries. |
| RGATC-A-NONCI | Nonneutral section and nonzero same-state CI residual along the target continuation; no global trajectory-separation claim. |
| RGATC-E-WITNESS | Executed frozen section-based onset, selection, both-role reconstruction, finite continuation and no-split/history-reset controls. |
| RGATC-R-REFERENCE | Error-certified finite evaluator, independent numerical oracle, representation/covariance and research lifecycle/replay/rollback conformance. |

The RG2b predecessor contract is architectural provenance, with its existing
support disposition preserved. CI and A_OS predecessors supply fixed-H lemmas
and transfer/rate provenance, not their realization dynamics or parameter
domains. The new completion is authorized only here at research scope.
Origin ATC proposals are references, never retrospectively accepted claims.

## Load-bearing boundaries

- Authoritative scientific state is C/W only. H, Gamma, inverse seeds and
  ladder history are derived; source geometry is never transported to target.
- The lagged equation is Gamma(Fhat(X,Gamma(X))) = Ghat(X,Gamma(X)),
  not the same-state CI root. No C1 section or completion-independent
  uniqueness is claimed.
- The concrete profile freezes Delta=2^-25, kappa_H=2^-32 and
  tau=1/(8 log 2). No CI/PC variable-duration interval, old 71-beat horizon,
  old large-step first-entry bound or native completion identifier is inherited.
- The paired four-star/double-star witness selects k=32768 from the fresh
  source poststate only. Both roles independently transfer resources and
  lineage W; only bridge W=1 is seeded. Both target sections are reconstructed.
- Exact-real source obstruction by the loose 536870912-proposal bound and
  indefinite target return are theorems, not executed long campaigns.
- The finite evaluator certifies e_H + L(e_X+w) + R q^N. Its fixed work
  budget may fail where the exact section exists; nonresolution is not
  physical absence. No arbitrary-graph or uniform finite-work guarantee.
- Ordinary commit precedes the optional zero-time event. Event failure
  preserves the successful ordinary publication; ordinary failure cannot
  be rescued by an event. One event is covered, not repeated scheduling.
- Four-step binary64 C/W publication uses high-precision derived stages
  and retains charge drift and the rounded-writer RG-defect bound. It is
  not all-binary64 execution or exact indefinite numerical conservation.
- Audit PASS is user-reported. Local retained identity checks and supplied
  full E/R execution remain separately identified; no unprovided reviewer
  raw rerun or fresh local full campaign is claimed.

## All 28 realization-local debts

| Disposition | Debt numbers |
| --- | --- |
| Closed within bounded research | 02, 03, 04, 05, 06, 07, 08, 14, 15, 16, 19, 21, 24, 28 |
| Partial | 01, 10, 12, 13, 23, 26 |
| Not activated | 09, 11, 17, 18, 20, 22, 25, 27 |

Every row preserves its origin meaning, earned scope, remaining/reactivation
condition and reciprocal claim links. DB-05 closes through the explicit
Lipschitz alternative, not differentiability. DB-15 excludes H/Gamma transport.
DB-21 uses the two distinct transaction boundaries. DB-24 closes only through
the exact admitted inventory, append-only rebuild and focused mutation checks.
All global origin debts remain open; native work is downstream, not a missing
research criterion.

## Source admission and five-realization closure

The additive `grcv4_explorer.atc_rgatc` / `atc-rgatc-query` context extends
the accepted CIP context. Its read-only queries preserve classifications,
source pointers, conditioned edges and trace identities. Missing, changed or
new unprocessed evidence holds the context; repinning arbitrary incoming
content cannot silently grant authority. Existing four realization records,
graphs and APIs remain unchanged.

See [Candidate-A research closeout](./ATCCandidateAResearchClosure.md).
This finishes the five bounded research programs, not universal Candidate-A
topology support, Candidate C, aggregate ATC-2/ATC-3, or native implementation.
The route remains reviewed research → topology proposal → extension paper →
specification → Phase 9 native implementation.

