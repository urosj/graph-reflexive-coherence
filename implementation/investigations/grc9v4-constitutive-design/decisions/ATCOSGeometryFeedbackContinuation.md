> Relocation-only maintenance: repository paths and dependent identities were
> rebuilt before first commit. Scientific statements, numerical results and
> review verdicts are unchanged; the new identities are not an independent
> reviewer rerun. Earlier byte-preservation statements describe the pre-move stage.

# Degree-two return with retained history, Read-Back and consumed OS geometry

Status: independent mathematical review PASS, with interpretation and numerical
scope clarifications applied. The bounded A_OS three-path conditional
mathematics is reviewed; this is not formal claim admission or native
conformance. The reviewed checker/certificate, paper, specification, substrate
implementation and topology policy remain unchanged.

The [reviewed history theorem](./ATCHistoryFeedbackContinuation.md) retained
Candidate A's evolving W but disabled its explicit Read-Back and generated
geometry. This successor keeps that writer and restores the full one-pass
OS path from Read-Back through structural assembly to an equation-level
geometry consumer. Changing kappa alone or merely reporting a nonzero tensor
would not establish this result.

## 1. Declared research domain and authority

Keep the predecessor's fixed four-node tree, vertex order (a,c,u,v), tail-positive
incidence B, charge 5, polynomial p=V', eta=1/4, kappa_c=1/1024, h=1/64,
W_floor=1/2 and exact rho=1/2 from tau_A=h/log 2. The reference H0 and H1
are identity; reference edge weights are one. The typed star-tensor adapter
is the identity in the common structural coordinates. There is no carrier.

The new parameter box is

$$
0\le\gamma\le256,\quad 0\le\chi_A\le\tfrac12,\quad
0\le\kappa_H\le1,\quad 0\le\kappa_{Ah}\le1,
\qquad \zeta_A=1,\quad \alpha=\beta=0.
$$

The return theorem is uniform over this entire box, including its zero-gain
faces. Strict non-vacuous channel activation is established by the selected
prestates at gamma=256, chi_A=1/2, kappa_H=kappa_Ah=1. It is not asserted for
every state or every parameter point; zero gains can remove a path.

All other parameters and the multiwell law remain the predecessor's. Its
equilibrium C* and coordinate bounds are reused:

$$
C=C^*+Bx,\quad X=\|x\|_2\le R=1/128,\qquad
-\bar b\le y_e=\log W_e\le0,\quad \bar b=1/128.
$$

This is conductance-depressed history, not arbitrary inherited W. Geometry
is reconstructed within each ordinary beat, not a new persistent coordinate.
The exact same-beat split tolerance is declared as 2^-30 in reference-relative
edge operator norm. These are research parameters, not a newly registered
complete runtime profile or generally admissible numerical defaults.
In particular, 2^-30 is this research declaration's tolerance, not an
inherited native A_OS tolerance or an admitted executable profile. Later
N-P work must bind and verify the exact residual norm, tolerance and staged
arithmetic under its separately admitted native contract.

The [specification](../../../../specs/grc-v4-spec.md),
[paper §§8 and 10.3.1](../drafts/2026-09-GRC-V4.md), and unchanged
[current/writer](../../../../src/pygrc/models/grc_v4_candidate_a.py),
[geometry](../../../../src/pygrc/models/grc_v4_geometry.py) and
[OS staging](../../../../src/pygrc/models/grc_v4_realizations.py) sources supply
the equation/stage semantics. The multiwell potential is still outside the
native evaluator and initializer domains. This declaration has its own name,
`ATC-A-OS-READBACK-GEOMETRY-HISTORY-v1_not_runtime_registered`; it does not
reuse the old gamma=0 or history-only binding as native authority.

Current forensic discovery remains `current_bundle_exact`. Six typed
`contract_provenance` traces are retained for A Read-Back, star assembly,
the affine Hodge update, the fresh A corrector, OS stage order and the
no-second-iteration boundary. Every trace retains its exact source/edge
references and `indeterminate_requires_review` support disposition. Querying
these sources does not turn this theorem into accepted claim authority.

## 2. The actual one-pass equations

Write D_w=diag(exp y), A=B-transpose B, d=B-transpose C and
L_w=B D_w B-transpose. Let a denote the reference-geometry **baseline** current:

$$
a=\eta D_wB^\top[p(C)-\kappa_cL_wC].
$$

For any stage baseline v, define the floor-inactive fresh reference and read:

$$
\widehat W_e(v)=e^{-\gamma v_e^2/2},\qquad
q_e(v)=\frac{W_e-\widehat W_e(v)}{W_e+\widehat W_e(v)}
=\tanh\!\left(\frac{y_e+\gamma v_e^2/2}{2}\right),
$$

$$
\mathcal F_y(v)_e=\frac{v_e}{1-\chi_Aq_e(v)},\qquad
r(v)=\chi_A\operatorname{Diag}(q(v))\mathcal F_y(v).
$$

The pre-read functional uses the stage's baseline, not its selected physical
current. The latter is used by the later retained writer. Confusing those
two current roles changes the law.

### Predictor and generated geometry

At reference geometry G_J=I, so

$$
J^{(0)}=\mathcal F_y(a),\qquad f^{(0)}=r(a),\qquad
S^{(0)}=\mathcal A_\star(f^{(0)}),\qquad
H^{(1)}=I+\kappa_H S^{(0)}.
$$

For the stated edge order (a to u, v to c, u to v), the overlap-normalized
assembly is explicitly

$$
\mathcal A_\star(f)=
\begin{pmatrix}
f_1^2&0&f_1f_3/2\\
0&f_2^2&f_2f_3/2\\
f_1f_3/2&f_2f_3/2&f_3^2
\end{pmatrix}.
$$

It is positive semidefinite because it is the sum of the normalized star
outer products. Its trace is ||f|| squared; thus its operator norm is no
larger than ||f|| squared. Since kappa_H>=0, H1>=I throughout, including
nonzero off-diagonal assembly. Geometry is never substituted for D_w mobility.

### Fresh corrector, continuity and retained writer

At the produced geometry the baseline is rebuilt as

$$
b=a-\eta\kappa_{Ah}D_wA(H^{(1)}-I)d,\qquad
J^{(1)}=\mathcal F_y(b).
$$

Both widehat-W and q are recomputed from b, not taken from a. Only this
corrector enters the single resource write and the retained writer:

$$
C^+=C-hBJ^{(1)},\qquad
y^+=\tfrac12y-\tfrac\gamma4(J^{(1)})^{\odot2}.
$$

The final-C descriptor refresh is prescribed by A's writer but numerically
inactive for alpha=beta=0. No conformance of nonzero descriptor channels is
claimed. New W is consumed only on the next beat.

For the read-only OS defect, the corrector's structural one-form must use
the **new inverse pairing**:

$$
f^{(1)}=(H^{(1)})^{-1}r(b),\qquad
R_h=\kappa_H[\mathcal A_\star(f^{(0)})-\mathcal A_\star(f^{(1)})].
$$

This is not a second geometry update. A second corrector using its regenerated
geometry would be a different realization. On the next ordinary beat the
predictor starts afresh at reference geometry with the new C,W.

## 3. A uniform bound through both current stages

The predecessor proves, on the same state rectangle,

$$
\|a\|_2\le\eta(A_jX+D_cY)\le j_*,\quad
j_*=\frac{494065}{134217728},\quad Y=\|y\|_\infty,
$$

with A_j=493285/262144, D_c=195/65536, and

$$
\|d\|_2\le T+4R=203/256=:D_*.
$$

Use a deliberately simple common contrast bound Q=1/200. Since |tanh z|<=|z|,

$$
\|q(a)\|_\infty\le\tfrac12(\bar b+256j_*^2/2)<Q.
$$

Therefore every predictor denominator is at least 399/400 and

$$
\|f^{(0)}\|_2\le\frac{\chi_AQ}{1-\chi_AQ}\|a\|_2
\le\frac1{399}\|a\|_2.
$$

Using ||A||<4, ||D_w||<=1 and the star norm estimate,

$$
\|b-a\|_2\le4\eta\kappa_{Ah}\kappa_HD_*\|f^{(0)}\|_2^2
\le E\|a\|_2,
$$

$$
E=\frac{4\eta D_*j_*}{399^2}
=\frac{14327885}{781443529703424}.
$$

The exact bounds also give

$$
\tfrac12[\bar b+256((1+E)j_*)^2/2]<Q.
$$

Thus the same contrast/denominator bounds hold at the corrector. The two
pre-read floors are inactive, since their log-target magnitudes are <bbar.
Define L=1003/1000. The exact inequality (1+E)/(399/400)<L then yields

$$
\|J^{(1)}\|_2\le L\|a\|_2,\qquad
\|J^{(1)}-a\|_2\le(L-1)\|a\|_2.
$$

For the difference estimate, separately bound b-a and
F_y(b)-b; together their coefficient is

$$
E+\left(\frac1{1-\chi_AQ}-1\right)(1+E)
=\frac{1+E}{1-\chi_AQ}-1.
$$

The L bound is not an assumed scalar proportionality between the currents;
the actual dense geometry can change their directions.

Both current blocks are nonsingular with denominator condition ratio at
most 401/399<1.01. These are exact-real bounds, not a claim of implemented
rounding/conditioning conformance for the unadmitted potential.

### OS residual admission is part of the domain

Since H1>=I, its inverse has operator norm <=1, and
||f1||<=chi Q L j*. Consequently

$$
\|R_h\|_2
\le (j_*/399)^2+(\tfrac12 Q L j_*)^2
<1.704\mathbin{\cdot}10^{-10}<2^{-30}.
$$

The exact rational value is retained in the certificate. This proves the
declared residual admission throughout the rectangle. It does not make the
defect zero or reinterpret it as a time-discretization error.

## 4. Full-state invariant return with both feedback paths active

Let q_x, B_c and c denote the reviewed history-only majorant constants, with
the same auxiliary weight K=1/1024. Add the above current difference to its
resource bound and use the selected current in the writer:

$$
\widetilde q_x=q_x+h\eta(L-1)A_j,\qquad
\widetilde B_c=B_c+h\eta(L-1)D_c,\qquad
\widetilde c=L^2c.
$$

Then

$$
X^+\le\widetilde q_xX+\widetilde B_cY,\qquad
Y^+\le\widetilde cA_jX+(\rho+\widetilde cD_c)Y.
$$

There is no constant forcing term: all extra current vanishes with the
reference baseline at (C*,W=1). The exact invariant margins are

$$
R-\widetilde q_xR-\widetilde B_c\bar b
=\frac{22466851}{27487790694400}>0,
$$

$$
\bar b-256(Lj_*)^2/2
=\frac{34157784212145279}{5629499534213120000}>0.
$$

The second inequality also proves the final writer's floor is inactive.
The first proves the outgoing resource state remains in the slope domain;
it is not assumed before taking the step. Positivity and charge preservation
follow as in the predecessor; every resource coordinate is >=251/256.

For S=X+KY,

$$
S^+\le\widetilde qS,\qquad
\widetilde q=
\max(\widetilde q_x+K\widetilde cA_j,
\widetilde B_c/K+\rho+\widetilde cD_c)
=\frac{90071326401578207669}{90071992547409920000}<1.
$$

Thus (C,W) returns to (C*,1) for the whole stated parameter box and state
rectangle. Read currents and structural sources vanish and the recomputed
H1 tends to I. This is genuine full-state return with consumed geometry,
not persistence of geometry as an independent state. The bound is slow and
conservative, approximately 0.9999926; no useful performance or actual basin
boundary is inferred from it.

The earned result is that the already established return domain **survives**
the addition of explicit Read-Back and causally consumed geometry. Geometry
is not shown to cause stabilization, enlarge the basin or improve the return
rate. The proof treats the added channels as a bounded nonlinear perturbation
of the already contracting history-only system. Its larger, more conservative
contraction bound is likewise not evidence that the actual dynamics is slower.

At the equilibrium a=0 and y=0. Locally q=O(y,a squared), the read is
O(y a,a cubed), and its star source begins at fourth order in general joint
state perturbations. The new geometry term therefore does not change the
linear tangent of the reviewed history theorem. Neither that absence nor
the small source size proves causal absence at finite amplitude.

## 5. One encounter and the specified child operations

For C0=(1,1,3/2,3/2), any incoming y in [-bbar,0] gives

$$
S_0<\tfrac32(1/256)+K\bar b=769/131072.
$$

This includes, but does not require, the unchanged mathematical reference-pass
preparation y0=(-a_init,-a_init,0), a_init=gamma/(2 times 4096 squared).
The [initializer formula](../../../../specs/grc-v4-a-initializer-spec.md)
uses a baseline-only auxiliary reference pass, not an OS corrector or a
Read-Back solve; its formula-level value is unchanged by the new gains.
Its native multiwell admission is still absent. Supplied inherited histories
in this rectangle are mathematical initial states, not claims of a particular
event/history-transport recipe.

Keep fixed-reference child loads w_i=-L0 e_i, independently signed amplitudes
at most epsilon=1/4096. Their edge-coordinate load has norm less than
(5/2) epsilon. At any unencountered age, one such joint load gives

$$
X_{\rm loaded}\le S_{\rm loaded}<849/131072<R.
$$

History is unchanged by the load. Hence the loaded full state remains in the
invariant domain and returns. This is not a theorem for arbitrary repeated
loads, history-adaptive -L_w e_i operations, or native event/reset semantics.

For the individual operation at the specified positive epsilon, define its
restorative response using the fully reconstructed OS resource field. Compare
that with the reviewed fixed-W baseline response Q_i^hist at the same C,W.
The two complete OS reads rebuild their geometry separately. Since the child
row of B has norm sqrt(2)<3/2,

$$
|Q_i^{OS}-Q_i^{hist}|
\le\frac{h}{\epsilon}\frac32(L-1)j_*
=\frac{889317}{838860800}.
$$

Subtracting from the predecessor's lower bound gives

$$
Q_i^{OS}\ge\frac{180242767}{858993459200}>0.
$$

This finite-difference estimate uses the declared epsilon; its 1/epsilon
bound is not a new uniform infinitesimal-response or derivative theorem.
The signed joint return statement above, separately, allows all amplitudes
up to epsilon. Positive individual restoration is not positive intake under
every combined load or evidence of independent differentiated identities.

## 6. Does generated geometry actually change the corrector?

At symmetric C0 and y=(-d,-d,0), let w=exp(-d). The independent scalar
calculation gives

$$
a_0=w^2/4096,\quad q_0=\tanh[(-d+\gamma a_0^2/2)/2],\quad
j_p=\frac{a_0}{1-\chi_Aq_0},\quad f_0=\chi_Aq_0j_p.
$$

Its generated H1 has outer entries 1+kappa_H f0 squared and bridge entry 1.
For this state the new baseline and corrector are exactly

$$
a_1=a_0+\eta w\kappa_{Ah}\kappa_Hf_0^2,
\qquad j_c=\frac{a_1}{1-\chi_Aq(a_1)}.
$$

At gamma=256, chi=1/2, kappa_H=kappa_Ah=1, both d=a_init=1/131072
and d=1/256 give q0<0 and f0 nonzero. At fixed y and positive v,

$$
\mathcal F_y'(v)
=\frac1{1-\chi_Aq(v)}
+\frac{\chi_A\gamma v^2(1-q(v)^2)}{2(1-\chi_Aq(v))^2}
\ge\frac1{1+\chi_AQ}>0.
$$

Thus j_c-j_p has a strict positive lower bound obtained from the positive
baseline increment. The certificate uses exp(-d)>=1-d and tanh(z)>=z/2
for the relevant 0<=z<=1 to retain rational lower bounds for both prestates.
For the prepared case |q0|>=a_init squared/2; for inherited d=1/256,
|q0|>=(d-a_init)/4. Together with
j_p>=j_ref(1-d) squared/(1+chi Q), these prove the gap without relying on a
floating-point subtraction to establish its sign.

The Decimal96 diagnostics illustrate the same result at the fully enabled
corner, not throughout the gain box:

| Same incoming state, geometry on versus off | Corrector-current increase |
| --- | --- |
| Mathematical reference-pass prepared history | approximately 5.048e-29 |
| Supplied d=1/256 depressed history | approximately 1.384e-14 |

The first effect is far below unit-Hodge binary64 spacing. Initialization
nearly cancels the read contrast, making the structural source especially
small. Exact-law activation is not guaranteed native numerical resolution or
physical significance. The second example is a distinct inherited state,
not an alternative initialization silently substituted into the first.

The inherited witness also needs an endpoint-level numerical ceiling.
After continuity, the magnitude of each geometry-on/off resource difference
is h times the current gap, approximately 2.163e-16. At the child endpoints
near 1.5000038, binary64 spacing is 2^-52 and this is about **0.974 ulp**.
At the actual outer endpoints near 0.9999962, spacing is 2^-53, giving about
**1.948 ulps**. The review's 0.97-ulp comparison therefore applies to the
children (and to the spacing at exactly 1), not to every coordinate.

These scale comparisons use the retained Decimal96 outputs; they are neither
certified rounding enclosures nor executions of the native staged arithmetic.
Resolving a current difference does not by itself establish a robust difference
in committed resource bytes. Such endpoint nonannihilation remains open for
N-P and cannot be discharged by rounding only the final high-precision answer.
No claim that particular native bytes must coincide or differ is made here.

The same-input controls discriminate actual consumption:

- kappa_H=0 removes the generated Hodge change and gives the predictor current.
- kappa_Ah=0 retains the nonzero generated Hodge but removes its potential
  consumer; it also gives the predictor current. Geometry as telemetry alone
  is therefore insufficient to reproduce the fully enabled result.
- chi_A=0 removes the explicit read and structural source, recovering the
  reviewed history-only current while keeping its gamma writer.

The finite checks additionally use asymmetric resources/nonuniform W so
off-diagonal star terms and nonidentity G_J lowering are exercised. An
independent factored-polynomial scalar oracle checks the symmetric cases.
Vertex/edge reorderings and signed reorientation preserve the corresponding
outputs. One next ordinary step rebuilds from reference geometry and the
new authoritative C,W, rather than retaining the previous H1.

Wrong current roles are distinguished: stale predictor q in the corrector,
selected instead of baseline current in the pre-read, and predictor or
baseline instead of selected corrector current in the writer. A diagnostic
second corrector changes the answer and is explicitly not selected. The
nonzero OS split residual remains within its declared bound without an extra
iteration. These are research arithmetic checks, not production transactions.

## 7. Evidence and remaining work

The [checker](../scripts/certify_atc_os_geometry_feedback.py) emits the
[certificate](../evidence/autonomous-topology-change/ATCOSGeometryFeedbackCertificate.json).
It verifies the unchanged reviewed history record and all its source bindings,
then evaluates the new exact rational inequalities. Ten finite mathematical
steps cover the declared states, same-input controls, a coordinate action,
one continuation and two individual loads; one unselected second-corrector
read pressures the OS boundary. There is no trajectory campaign, native
step or topology event. Decimal diagnostics are not certified rounding
enclosures; the theorem uses the analytic inequalities and exact rational
checks. The checker and full provenance traces are source-bound and portable.

From the repository root:

```sh
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python implementation/investigations/grc9v4-constitutive-design/scripts/certify_atc_os_geometry_feedback.py
```

The scientific advance is a uniform return box together with fully enabled
witnesses where retained mobility, explicit Read-Back and generated geometry
are all causally present. This establishes compatibility with return, not
stabilization caused by geometry. It strengthens constitutive-feedback/support
evidence relevant to DB-07/27, not CAN-B's structural functional, critical
branch, selected mode or partition-lift debts. Using the existing structural
assembly is not deriving that missing topology-changing machinery.

### Independent-review disposition

The supplied review finds no blocking mathematical error and gives PASS to
the coupled A_OS theorem, uniform exact-real parameter-box return, declared
OS split-residual admission, one bounded encounter with positive child
response, and demonstrable geometry consumption by the corrector. It
independently reconstructs the exact rational margins and verifies:

- Certificate canonical digest:
  `c78185f49678aeba71dbc2045cac9831ffaeefe44d9f741383bd4333ee1207e6`.
- Checker SHA-256:
  `1f69df96b16034117b28f0d1074d69c6828cdeabcb9303b44631547e206c069b`.

The reviewed bytes remain unchanged, including the certificate's historical
pre-review status. This note records the current mathematical disposition.
The four requested boundaries are now explicit: compatibility rather than
geometry-caused stabilization; uniform return versus selected activation
witnesses; unresolved native resource-endpoint nonannihilation; and research,
not native, residual tolerance authority. The ulp comparison is qualified by
the actual endpoint ranges above.

For evidence-count accuracy, the explicit premature-new-W mutation check
belongs to the reviewed history predecessor; it is not an additional mutation
case in this ten-step OS record. The latter checks the current-role and
extra-corrector substitutions listed in §6. No numerical campaign is rerun
or extra native coverage inferred from this review.

This closes independent review of the bounded three-path mathematics, not
the authority or native gates. Higher-degree composition, other
realizations, active descriptor channels, carriers, W>1, other retention factors,
repeated encounters and the thirty capability/family cells remain open. No
equation-level support result is inserted into CAN-F2's source-only guard.
The result does not establish fission necessity, autonomous dispatch, durable
resting memory, native potential/initializer conformance or an accepted new
scientific claim. Reviewed predecessors and the authority-to-paper-to-spec
dependency remain unchanged.
