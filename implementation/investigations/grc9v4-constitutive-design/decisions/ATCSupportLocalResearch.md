> Relocation-only maintenance: repository paths and dependent identities were
> rebuilt before first commit. Scientific statements, numerical results and
> review verdicts are unchanged; the new identities are not an independent
> reviewer rerun. Earlier byte-preservation statements describe the pre-move stage.

# Local support-law realization and matched causal experiment

**Date:** 2026-09-21  
**Status:** N-R PASS/CLOSED; C-R PASS/CLOSED for the bounded local causal claim
after independent review. Production integration is deferred; accepted-graph
claim admission and a user acceptance/commit are not inferred.

## Scope correction

The user clarified that the prohibition concerns the substrate's `src/*`, not
code creation within this investigation. The purpose is to establish the
mathematical foundations for the paper. The previous request for a production
exception was unnecessary for this work. No such exception is taken.

The [local model](../research/atc_support.py),
[focused checks](../research/test_atc_support.py), and
[experiment driver](../scripts/run_atc_support_research.py) implement the
reviewed binding without changing or monkeypatching `src/*`. They neither
register a pygrc evaluator nor imitate its receipt/schema release. This is a
reduced-equation research realization, not a copy of the production substrate.

The production meanings of Gates N/C in the
[bridge proposal](./ATCSupportFixtureReviewAndBridge.md) remain deferred.
Their scientific questions have been tested here, and native
implementation is **not a prerequisite for this investigation's mathematics**.
Local results must not be relabeled as production admission, lifecycle/replay
conformance or accepted discovery support. Later paper/spec propagation still
precedes substrate integration.

## Research review disposition

The independent reviewer ran all eight SupportTests, checked the model, tests,
driver, F2, binding and prediction source identities, and recomputed the retained
experiment digest. The review found the realization and the executed causal
triangle sufficient to close the two bounded research questions:

| Subject | Disposition | Exact scope |
| --- | --- | --- |
| N-R: research realization/conformance | PASS / CLOSED | Pinned reduced-law evaluation, numerical/domain boundaries, initializer evidence and local reset semantics |
| C-R: local causal comparison | PASS / CLOSED bounded | Fixed F/D/N intervention, shared observables, state mediation and observed addressable response |
| N-P: substrate integration | DEFERRED | pygrc evaluator, initializer/schema and native lifecycle conformance |
| C-P: production causal reproduction | DEFERRED | Same scientific comparisons through the eventual production owners |

These labels clarify existing subjects; they do not add another recurring
approval process. The original execution's preparation-time flags and bytes
remain unchanged. This review supplies the current research disposition, not
new accepted forensic nodes or production support.

Retain three distinct bounded scientific subjects below the synthesis:

1. The whole fission intervention changes projected ordinary dynamics despite
   equal initial projected resource state.
2. It changes the full shared operational response vector; it does not simply
   increase its aggregate magnitude.
3. Two separately addressable child-centered positive channels remain available
   at the observed ages, with finite loaded continuation toward return.

For this fixture, **fission differentiates the addressable organization of
participation without requiring increased aggregate response capacity**.
The source already responds positively. This is not creation of efficacy from
absence, a general formation theorem, or evidence for independent identities.

### Allocation-interface hardening

The review's one nonblocking item was a redundant `allocated_current` field
ignored by the original fixed constructor. Its actual recorded values were
correct. The new [checked successor interface](../research/atc_support_fission.py)
requires the complete F2 record and validates that field against the recomputed
allocation before returning a construction. New callers consuming a full F2
prescription should use `checked_fixed_fission`.

The [focused check](../scripts/check_atc_support_research_closure.py) reproduces
the reviewed construction and both initializer records exactly, rejects seven
missing/malformed/inconsistent allocation variants, and checks the original
evidence identities. Its [result](../evidence/autonomous-topology-change/ATCSupportResearchClosureChecks.json)
contains **zero ordinary updates**. The old model/driver are preserved as the
exact historical reproducer; this append-only hardening neither rewrites its
source hashes nor reruns the F/D/N campaign. The old interface should not be
treated as validating that diagnostic field.

## Fixed model and experiment

The [content-addressed binding](../evidence/autonomous-topology-change/ATCSupportPotentialBinding.json)
and [causal preregistration](../evidence/autonomous-topology-change/ATCSupportCausalPredictions.json)
are unchanged. The local evaluator computes the factored polynomial exactly,
uses exact component-wise gauge assembly, then rounds potential and flux at
their declared binary64 boundaries. An independent expanded-polynomial,
dense-incidence oracle checks the consumed results. The model enforces the
finite graph/resource domain, including disconnected components and isolates.

For this fixed A_OS reduction, all conductance exponents, readback gains and
geometry feedback vanish; W and its target remain all-one, the structural
increment is zero, H remains identity and there is no carrier. Consequently
OS predictor/corrector geometry is identical. This justifies the reduced
ordinary equation; it is not evidence for all A_OS parameters or other products.

Each resource update is RN64(C + f/64), with f formed exactly from the consumed
rounded edge currents. Both current and reset are nonnegative charge-5 inputs,
with a declared absolute charge check of 2^-40 and **no resource repair**.
Initializer records publicly expose role, binding, graph, C, W_base, Phi_base,
J_ref and W_A_init. Current/reset off-root intermediates are checked, rather
than relying on the nondiscriminating all-one W. These records are local research
constructions, not GRC publication or receipt records.

The unchanged F2 function reads source C = (1,3,1) and the new law's current.
It chooses b and equal shares. A fixed local conservative map constructs
target C = (1,1,3/2,3/2). Source reset is independently
(1+epsilon,3-2 epsilon,1+epsilon), giving target reset
(1+epsilon,1+epsilon,3/2-epsilon,3/2-epsilon), epsilon = 1/4096.
Both roles are initialized and evaluated separately. This is one prescribed
local split, not an automatic dispatcher or a newly established onset history.

The branches are:

- F: that local split followed by ordinary evolution;
- D: an independently constructed identical scientific target, with its own
  initializer records; no history or receipt is stripped from F;
- N: the same source continued without the split.

The ordinary law consumes only scientific state. Branch-origin labels remain
experiment metadata. Equality here does not test whether production receipt
machinery leaks into physical evolution: no such machinery is present.

Observe all three for sixteen updates. At unencountered target ages 0, 8 and
15, fork separate positive single-child loads and all four signed joint corners.
Each branch receives **one load only**, followed by sixteen updates. Target
baseline F/D is extended to age 31 solely to supply matching unencountered tails;
this does not change the sixteen-step primary observation. No law, input,
prediction or preregistered comparison tolerance was retuned.

## Results

The [retained result](../evidence/autonomous-topology-change/ATCSupportLocalExperiment.json)
records eight passing focused test methods, 662 small research updates across
the independent branches, and 731 independent field checks. This is one focused
experiment, not a substrate regression campaign. It retains the source/target
and initializer inputs, trajectories, encounter controls, energies, digests and
relative source bindings. Native admissions, updates and events are all zero.

The local experiment reproduces the exact preregistered baseline and response
vectors, including

$$
P C^+=C^-,\qquad P f^+=f^-/6,\qquad
\Delta_{\rm baseline}=(5/4096,-5/2048,5/4096).
$$

The first-update projected difference is exactly
(5/262144,-5/131072,5/262144). Both full shared-response difference vectors
also reproduce their preregistered fractions, without needing the conservative
tolerance. At the intervention, normalized parent response is approximately
0.0319228543, pair response 0.00174990343 and addressed-child response
0.00194443735. The source is already responsive; aggregate response decreases
after splitting. No acquisition-from-absence or increased-capacity claim follows.

F and D have identical scientific results throughout their 31-update baseline
and all matched loaded continuations. N differs. This realizes state-mediated
causation **in this declared local model**, for the whole fission intervention
(new coordinate, resource split, connector and incidence/reference change), not
incidence alone or a production-lifecycle test.

| Checkpoint age | Child 0 response | Child 1 response |
| --- | --- | --- |
| 0 | 0.00194443735 | 0.00194443735 |
| 8 | 0.00194504078 | 0.00194504078 |
| 15 | 0.00194556190 | 0.00194556190 |

All individual responses exceed the certificate's positive lower bound within
the declared comparison tolerance. The neutral preparation's two responses
are negative (approximately -0.00131846611). It remains a sign control, not a
substitute for N's matched no-fission history.

All eighteen loaded target cases remain inside the prescribed positive box
and decrease the exact polynomial energy evaluated on their represented states.
After sixteen updates, each squared distance to the tightly bracketed equilibrium
is about 94.4–94.5% of its initial value. This is finite movement toward return, not
complete relaxation. No positive individual-intake assertion is imposed on
joint corners; joint containment and energy behavior are separate observations.

At update 16, in their respective vertex orders:

```text
F = D: (0.9999397617645732, 0.9999397617645732,
        1.5000602382354271, 1.5000602382354271)
N:     (0.9997097973347264, 3.000580405330547,
        0.9997097973347264)
```

W stays one, readback current stays zero, and reset restores the independently
defined reset resources. Maximum represented charge drift is
17/9007199254740992, approximately 1.89e-15, below the declared check. It is
reported rather than repaired. Because rounded trajectories can leave the exact
charge hyperplane by this amount, the exact-arithmetic infinite-tail theorem
is not silently asserted for the rounded run. The theorem and finite numerical
evidence remain separate.

## Scientific meaning and remaining work

This supplies local computational foundations for the paper: faithful bounded
realization of the proposed equations; changed projected dynamics and shared
response under the fixed fission intervention; and two positive, separately
addressable response sites with observed finite return behavior. Independent
implementation/oracle agreement is stronger than simply replaying the prediction
calculator, but it remains a constructed, zero-channel A_OS existence fixture.

It does not justify the growth law universally, certify source nonviability,
create new wells, establish independent child identities or H5-style
non-factorizability, demonstrate infinite rounded return, or close all thirty
capability cells. Higher-degree partitioning, CAN-B, other products, operation
composition and autonomous policy transport remain open.

The research review is complete; do not rerun the same F/D/N comparison simply
to reopen C-R. The outward
[sufficient structure for addressable support](./ATCAddressabilitySufficientConditions.md)
does not depend on the exact old potential roots. Its theorem, uniform family
certificate and affine exclusion now have independent mathematical PASS,
with proof precision amendments applied and a necessary slope-softening
corollary added. This does not reopen the closed fixture or prove all-family
closure. Claim/debt successor mapping, feedback/higher-degree generalization,
stronger formation/identity interpretations and production integration remain
separate work. No commit is inferred.

Reproduce from repository root using the existing environment:

```sh
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python implementation/investigations/grc9v4-constitutive-design/scripts/run_atc_support_research.py
```

The driver runs the eight focused checks and this fixed experiment, prints a
fresh result, and does not overwrite the retained evidence or any substrate file.
