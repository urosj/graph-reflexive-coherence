# CI-2 / CI-3 successor evidence

CI-0–CI-3: final independent PASS and user-authorized
[scoped claim/debt admission](../atc-ci-admission/README.md). The final review
closes the derived-witness HOLD. This directory's reviewed certificate bytes
and earlier status fields are preserved; the new adjudication supplies the
current acceptance state. This directory
does not extend the accepted A_PC or A_OS source inventories. The shared
CI-1/PC checkpoint stays unchanged; this successor references its exact bytes.

- [Combined domain/reference note](../../decisions/ATCCIDomainsAndReference.md)
- [CI-2 rational domain certificate](ATCCI2DomainCertificate.json)
- [CI-3 joint-root/reference certificate](ATCCI3ReferenceCertificate.json)
- [Domain checker](../../scripts/check_atc_ci_domains.py)
- [Reference checker](../../scripts/check_atc_ci_reference.py)
- [Independent Decimal equations](../../research/atc_ci_oracle.py)
- [Independent CI-2/CI-3 review](CI2CI3-IndependentReview.md)
- [Follow-up boundary PASS / derived-witness review](CI3-DerivedWitness-IndependentReview.md)
- [Closed-domain and represented-pairing regressions](../../scripts/check_atc_ci_boundaries.py)

Run the checkers from repository root with `.venv/bin/python`. They print
records, never write evidence or alter production code. CI-2 checks uniform
rational bounds; CI-3 executes only its declared paired root/stage/lifecycle
and finite representation pressure. The latter is not a new parameter search
or a rerun of the accepted PC/OS campaigns.

The analytic C1 potential theorem and executed quadratic subclass stay
distinct. Whole-box return assumes an admitted resolved source event.
Native implementation, general formation, active environments, repeated
events/non-Zeno and aggregate ATC-2 remain outside this scope. Measured
floating-point charge drift is not exact conservation or silently repaired.

## Executed checks

CI-2 passes its uniform rational root/domain, obstruction, transfer and return
inequalities. CI-3 passes 2,153 independent scalar stage/root comparisons,
30 joint J/H coordinate-covariance comparisons and 21 lifecycle/admission
negative cases. The fresh event root equals the complete restart root after
canonical interval-value serialization. An H-only mutation with unchanged J
is rejected. CI-1's source share remains 33616/65536.

Both actual transported roles pass four continuation steps with publication
rounding. Maximum enclosed errors are below 2.070e-16 (C), 8.035e-17 (W),
3.425e-16 (reconstructed J) and 7.446e-23 (reconstructed H), all within the
declared 1e-10 budget. Maximum observed charge discrepancy is exactly
1/2251799813685248. This is finite diagnostic evidence, not exact stored
charge conservation or an indefinite binary64 theorem.

Two development corrections were local to this successor: a proposed 1e-40
root stopping budget lay below the inherited 2**256 squared-norm resolution,
so it was replaced by an explicitly declared 1e-35 floor; and full-root
equality must compare interval endpoints/canonical values, not Python object
identity. Neither changes the CI law, scientific domain or the independent
comparison requirement. The Banach residual bound remains the root certificate.

## Review correction

The exact-boundary mismatch is fixed with CI-only exact/operation-proved
predicate witnesses, not epsilon comparisons. Eight endpoint fixtures and
19 negative cases cover the reported boundaries plus target radius, values
outside by 2^-300, unequal represented pairs and overlapping boxes. Exact
predicate provenance is now included in joint-root and publication identities.
At that boundary checkpoint, the numerical campaign and CI-2 mathematical/
claim-routing payload were unchanged. Review digests refer to the prior unadmitted records; refreshed
identities include the correction and its retained review. Accepted A_OS/PC
source bytes are unchanged.

One local test fixture initially used binary64(24/25), which lies *below*
the exact history floor. Its rejection was correct, unlike the reviewed
exact-rational mismatch. The represented nonzero-charge control now uses
interior W=97/100; exact W=24/25 still passes via its rational witness.
Arbitrary unresolved derived predicates remain fail-closed rather than
being credited with an exact proof from interval overlap.

## Derived-witness correction

The follow-up review independently passes the boundary fix, but demonstrates
that `after_step(before,data)` could forge charge/history facts for arbitrary
paired output. The Q=8.4 and W=0.8 counterexamples were reproduced. This
entry point is removed. `certified_step(before,h,profile)` executes its own
root, continuity and writer, derives facts, then readmits the successor. It
accepts neither numerical output nor caller-supplied proof receipts.

The retained boundary checker now also passes three requested forgery cases
before certified-state construction, a serialized-receipt rejection, six
constructor contradiction checks and positive execution-binding checks.
The binding records before witness, full selected root, request, profile,
numerical result and theorem. These are provenance, not redeemable authority.
Compatibility with charge/history/x/y/radius is checked as defense in depth,
not mistaken for proof that the operation happened.

CI-2 additionally retains `funding_lower=1303/1600>0`, derived uniformly for
both roles. The previous mathematical bounds and claim routing are unchanged.
The same CI-3 campaign is refreshed once; its numerical J/H/C/W results and
finite-error observations are unchanged, while proof-bound identities change.
Accepted PC/A_OS evidence, production code and scientific equations stay fixed.
