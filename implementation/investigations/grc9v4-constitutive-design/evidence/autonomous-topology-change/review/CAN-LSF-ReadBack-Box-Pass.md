> Relocation-only maintenance: repository paths and dependent identities were
> rebuilt before first commit. Scientific statements, numerical results and
> review verdicts are unchanged; the new identities are not an independent
> reviewer rerun. Earlier byte-preservation statements describe the pre-move stage.

This is a **PASS for the original Read-Back parameter box**, with one important scope qualifier: it closes the causal chain uniformly for the **fixed prepared four-star family across the full declared feedback box**, not for arbitrary A_OS states satisfying the guard.

That is the result the previous reviews were aiming at.

I independently executed the uploaded box checker’s `exponential_probes()`, `algebra_checks()`, and complete `box_evidence()` path against the uploaded point kernel. The freshly generated box data, kernel-hardening data, and reduced-algebra data match the retained certificate exactly. I also recomputed the certificate canonical digest and its accepted point-adjudication predecessor digest.  

## The full-box causal chain now closes

For every fixed tuple

$$
\chi_A\in[1/32,1/16],\qquad
\gamma\in[1/2048,1/1024],
$$

$$
\kappa_H,\kappa_{Ah}\in[1/2,1],
$$

with the same prepared \(C_0,W_0\), the certificate now proves all of the finite premises that had remained open after the point result.

The initial source is uniformly inactive. The first complete OS beat is uniformly admitted. At its committed endpoint the source has strict sector separation, strict source-guard margin, positive selected-reference inflow, positive \(x,y\), and positive funding. Read-Back, geometry generation/consumption, and \(\gamma\)-dependent writing are uniformly nonvacuous rather than merely nonzero at one corner. 

The conservative source bounds are substantial:

$$
0.0124655 < v-u < 0.0125643,
$$

$$
0.0307578
<
\frac52(u+v)-\frac{19}{4}
<
0.0310047,
$$

$$
0.0991223 < J_{\rm ref,min}<0.1005624,
$$

with

$$
3.59461<x<3.59527,\qquad
0.0043029<y<0.0044348.
$$

Those reproduce directly from the retained dyadic endpoints. 

So:

$$
\boxed{\textbf{Uniform source activation across the declared box: PASS}}
$$

for this prepared-source family.

## The reduced source mathematics is sound

I independently reconstructed the reduced formulas from the full weighted-star incidence equations.

With

$$
r_u=\frac{9-x}{5}+y,\qquad
r_v=\frac{9-x}{5}-y,\qquad
s=\frac{9+4x}{5},
$$

the two incidence differences are exactly

$$
d_u=y-x,\qquad d_v=-y-x,
$$

and the reference baselines reduce to

$$
b_u
=
u[(3u+2v-a)x+(-3u+2v+a)y],
$$

$$
b_v
=
v[(2u+3v-a)x+(-2u+3v-a)y].
$$

I also independently checked the normalized-star reduction that produces

$$
t_u=\frac32f_u^2d_u+f_uf_vd_v,
\qquad
t_v=f_uf_vd_u+\frac32f_v^2d_v,
$$

and hence the displayed fresh-baseline corrections. The checker's three exact matrix comparisons therefore reflect a genuine algebraic reduction rather than a fitted lower-dimensional surrogate. 

This matters because the box proof is only credible if the dependency-preserving \(x,y,u,v\) system really is the same staged OS map on the invariant paired subspace. It is.

## The interval treatment is conservative, not optimistic

There is another useful strength here.

The proof places the *entire parameter box* into each interval computation. It does not subdivide \(\chi,\gamma,\kappa_H,\kappa_{Ah}\).

As a consequence, correlations between repeated appearances of the same physical parameter are forgotten. Across successive interval steps the enclosure is effectively even permissive enough to include combinations that would correspond to different admissible parameter values at different uses.

That can only make the reachable sets larger.

So obtaining strict signs despite this dependency loss is stronger than evaluating the fixed tuples individually.

The same is true on the target side: the eight \(k\)-cells contain **continuous share intervals**, even though the actual allocator only returns dyadic integer \(k\)'s. Thus every physical target is covered by an enclosure that contains additional nonphysical intermediate shares.

This is conservative pressure, not target optimization.

---

## The obstruction is now uniform over the whole box

The source counterfactual is particularly decisive.

For every parameter tuple in the box, the no-split source has nine admitted ordinary resource updates and then, on attempt 10,

$$
-0.28553
<
\min C_{\rm proposal}
<
-0.20014.
$$

The immediately preceding ninth state remains uniformly positive:

$$
0.00336<\min C_9<0.05058.
$$

Likewise, resetting only \(W\) at the event boundary gives six admitted updates and then attempt 7 satisfies

$$
-0.19655
<
\min C_{\rm proposal}
<
-0.14203.
$$

Its sixth state remains positive:

$$
0.07764<\min C_6<0.10644.
$$

All relevant OS residual upper bounds stay far below \(1/512\); I obtain a maximum source-side bound of only about

$$
2.77\times10^{-5}.
$$



Therefore:

$$
\boxed{
\textbf{Enabled unsplit obstruction across the complete proposed box: PASS}
}
$$

and

$$
\boxed{
\textbf{History reset without topology change remains insufficient across the box: PASS}.
}
$$

Again, the exact scientific statement is trajectory-family specific. This does **not** prove

$$
\text{old spectral guard}>0
\Longrightarrow
\text{full-feedback failure}
$$

for arbitrary states.

The documents preserve that distinction correctly. 

---

# The \(k\)-handling is correct and avoids the refinement trap

This was probably the part I wanted to pressure most carefully.

The source activity enclosure gives

$$
30377.9101
<
2^{16}\frac{A_u}{A_u+A_v}
<
31235.5512.
$$

The resulting conservative round-even hull is

$$
\boxed{30378\le k\le31236}.
$$

Crucially, the proof does **not** now search these \(859\) possibilities for a successful split.

Instead, it establishes positive funding uniformly over the entire hull:

$$
1.0818<\text{funding}_u,
$$

$$
1.3703<\text{funding}_v.
$$

It then partitions the mathematical outcome hull into eight contiguous proof cells solely to control interval wrapping. Every cell has to pass; none can replace another. 

That distinction is legitimate.

For each physical parameter tuple, CAN-LSF still generates exactly one source-derived \(k\). The eight cells are an exhaustive **case proof over the possible output**, not eight candidate targets.

Moreover, each cell proves a continuous share range, which is stronger than checking the contained integer \(k\)'s.

So:

$$
\boxed{\textbf{No target search or successful-runner selection introduced: PASS}}
$$

and

$$
\boxed{\textbf{Dyadic discontinuity handled correctly: PASS}}.
$$

I would keep the wording used in the report: this is a conservative \(k\)-hull, not the exact reachable set. The proof does not need the latter.

---

# Every possible prescribed target now enters the return theorem

All eight cells pass for both current and independent reset.

The worst target first-step squared deviation I reconstruct from the certificate is the reset role in the first cell:

$$
\|C^+-C_*\|^2
<
0.376993
<
\frac49.
$$

That still leaves a healthy margin of roughly \(0.06745\) below the return-domain boundary.

The smallest certified first-target resource over all cells/roles is about

$$
1.3364>0.
$$

And the largest target OS residual upper bound across all sixteen role/cell cases is only

$$
\approx 4.76083\times10^{-4}
<
\frac1{512}
\approx1.95313\times10^{-3}.
$$

Every target \(W\) also remains strictly above \(e^{-1/8}\) and at most \(1\). 

So every actual prescribed target satisfies the hypotheses of the already-reviewed uniform theorem.

Composition therefore yields, throughout the entire box,

$$
X_{n+1}
\le
\frac{9209}{10240}X_n,
\qquad
\frac{9209}{10240}<0.9,
$$

with positive resources thereafter and \(W\to1\).

Thus:

$$
\boxed{\textbf{Uniform both-role target entry over the complete box: PASS}}
$$

$$
\boxed{\textbf{Uniform indefinite restoration by composition: PASS}}.
$$

This closes the second missing implication from the previous stage.

---

## Uniform nonvacuity also passes

The box is not secretly collapsing back toward the zero-Read-Back case at one of its edges.

The lower bounds on the initial reference structural forms are strictly positive, the fresh-baseline geometry changes have strictly positive lower bounds, and the derived fresh selected-current gap is uniformly at least approximately

$$
8.672\times10^{-6}
$$

per affected contact.

The writer targets are also uniformly strictly below one.

So throughout the entire parameter box:

$$
J_{\rm ref}\neq b_0,
$$

generated geometry is nonzero,

$$
J_{\rm fresh}\neq J_{\rm ref},
$$

and current-dependent history writing is genuinely consumed.

The additional nonzero exponential regression probes also pass using a second rational construction, including \(-1/2048,-1/8,-3/2,-10,-100\). This addresses the minor regression-hardening point from my point-certificate review without altering the previously reviewed interval kernel. 

---

# What I would now accept

The previous adjudication deliberately accepted only the enabled point plus the conditional box target theorem.  

The new result supports a real scope enlargement:

$$
\boxed{
\begin{array}{l}
\textbf{For the fixed prepared paired four-star source, CAN-LSF's complete}\\
\textbf{source-selected causal chain is certified uniformly over the original}\\
\textbf{positive Read-Back/geometry/history parameter box.}
\end{array}}
$$

That means the earlier `whole_box_source_and_entry_accepted=false` can now receive a successor adjudication after this review.

I would give:

| Question                                               | Verdict                      |
| ------------------------------------------------------ | ---------------------------- |
| Reduced full-feedback source algebra                   | **PASS**                     |
| Full original parameter box enclosed without shrinking | **PASS**                     |
| Uniform genuine feedback consumption                   | **PASS**                     |
| Initial inactive → first-boundary active transition    | **PASS**                     |
| Uniform source guard/cone/inflow/separation            | **PASS**                     |
| Uniform funding                                        | **PASS**                     |
| Enabled no-split obstruction across box                | **PASS**                     |
| History-reset-only failure across box                  | **PASS**                     |
| Conservative \(k=30378\ldots31236\) outcome coverage   | **PASS**                     |
| No target selection / no fitted partition              | **PASS**                     |
| Current-role entry for every possible prescribed \(k\) | **PASS**                     |
| Reset-role entry for every possible prescribed \(k\)   | **PASS**                     |
| Infinite full-feedback restoration                     | **PASS by composition**      |
| Arbitrary-state guard ⇒ obstruction                    | **Still open / not claimed** |
| Generic symmetry-broken CAN-LSF                        | **Open**                     |
| Other A realizations / Candidate C                     | **Open**                     |
| Native event/source admission                          | **Open**                     |
| ATC-2 aggregate closure                                | **Open**                     |

## One scope phrase I would be strict about

I would **not** summarize this as “CAN-LSF is proved over the full A_OS state domain.”

It is instead:

$$
\boxed{
\text{a parameter-box theorem over one fixed prepared source family}
}
$$

with exact paired symmetry and fixed \(h_0=1/8\).

The parameter robustness is now impressive; the state-domain robustness is still intentionally narrow.

That distinction should be carried directly into the next claim/debt update, particularly DB-07, DB-08, DB-10, DB-19 and DB-26.

---

## Evidence integrity

I independently reproduce the new certificate's three substantive generated components exactly:

* `kernel_hardening`,
* `reduced_algebra`,
* `box`.

The certificate canonical digest recomputes to

$$
\texttt{63645b59e7d2f78516fa435d99e1f648df50ab4528c3b98b1ab8d20085f36fac}.
$$

The uploaded box checker hash is

$$
\texttt{8636581d8b0a3a5db1bbf81b8f4d5c414a3729d8f7b77950af52b8378bcf4a62},
$$

and the certification-note hash is

$$
\texttt{fec8776c6549375423f9ee7349ceb0cfc4f165fabcdeaf3bcc96a03edb42f81c},
$$

matching the certificate bindings. The point-adjudication predecessor canonical digest also recomputes to

$$
\texttt{47c49a4e6a055ec087ddb9401277b315276e63157a0f469e3552be22a897376b}.
$$

 

I did not run the repository-dependent `main()` wrapper. I did independently execute the complete new mathematical box computation from the uploaded checker itself.

### Overall disposition

$$
\boxed{\textbf{Original Read-Back box causal-chain certification: PASS}}
$$

This is a fairly important milestone: the positive CAN-LSF mechanism has now survived not just a nonzero-feedback point but a **compact, genuinely consumed feedback region**, without changing its source stage, partition rule, allocator, topology operation, history lineage, or target.

I would now consider the A_OS Read-Back/geometry/history conformance question **sufficiently answered for this bounded prepared-source family**. The next scientific value is no longer in making this same box prettier or wider. It is in testing the remaining V4 dimensions—other A realizations / feedback channels and eventually Candidate C—or moving this accepted mathematical scope into the authority/native-admission track while keeping the broader ATC-2 capability matrix open.
