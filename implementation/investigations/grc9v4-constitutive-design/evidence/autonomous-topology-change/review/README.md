> Relocation-only maintenance: repository paths and dependent identities were
> rebuilt before first commit. Scientific statements, numerical results and
> review verdicts are unchanged; the new identities are not an independent
> reviewer rerun. Earlier byte-preservation statements describe the pre-move stage.

# ATC retained independent reviews

## CAN-LSF constitutive acceptance and Read-Back direction

[Constitutive review](./CAN-LSF-Constitutive-Review.md) accepts the bounded
zero-channel research law, independently passes positive common scaling and
checks the six-claim/28-debt companion. It explicitly did not execute the
thirteen-case status checker. The
[acceptance addendum](../../../decisions/ATCSectorConstitutiveAcceptance.md)
records its two wording constraints and the user's scoped adjudication request.

[Read-Back review](./CAN-LSF-ReadBack-Lift-Review.md) motivates a separate
enabled point and positive compact parameter box. It is guidance, not a review
of the subsequently produced lift equations/checker/results. Those require
their own review before a second claim/debt adjudication. No native or
aggregate ATC-2 acceptance follows from either review.

## CAN-LSF qualified Read-Back PASS

[Qualified review](./CAN-LSF-ReadBack-Qualified-Pass.md) independently reruns
the isolated mathematical core with neutral helpers and passes genuine
feedback, finite causal discrimination and the uniform conditional target
theorem. It checks the retained identities, but does not report a native or
complete repository-wrapper execution. Decimal source/entry evidence is not
promoted to a theorem; nonzero scope acceptance and second adjudication stay
held. The old spectral guard is not a full-feedback eigenvalue, and an
uncertified distance from a rounding tie is not generic DB-10 closure.

The requested point-first
[successor certification](../../../decisions/ATCSectorReadBackPointCertification.md)
now supplies outward finite-premise enclosures before a full-box attempt.
That new checker/certificate is not covered by the supplied review and awaits
its own review. The original box must not be shrunk merely to freeze k.

## CAN-LSF fixed-point PASS and second adjudication

[Point review](./CAN-LSF-ReadBack-Point-Pass.md) passes the exact enclosure
kernel, complete fixed-point mathematics and composition with the infinite
target theorem. It independently runs kernel/symmetry/evidence and pressures
nonzero exponentials; it does not report running the repository wrapper.
The [second scoped adjudication](../../../decisions/ATCSectorReadBackPointAdjudication.md)
accepts the point and conditional target theorem, not the parameter-box chain.

Its suggested nonzero exp probes are added in the successor checker using
independent exact rational brackets, leaving the accepted checker unchanged.
The requested original-box work now has a
[new certificate](../../../decisions/ATCSectorReadBackBoxCertification.md),
pending its own review. Neither this point review nor the second adjudication
is represented as reviewing or accepting that later box result.

## CAN-LSF original-box PASS and state-domain direction

The later [original-box PASS](./CAN-LSF-ReadBack-Box-Pass.md) now independently
reproduces the complete box mathematics. Its
[third adjudication](../../../decisions/ATCSectorReadBackBoxAdjudication.md)
accepts the prepared-family box, not arbitrary A_OS states. The separate
[G1–G7 program](./CAN-LSF-State-Domain-Program.md) is research guidance, not
a review of the subsequently produced
[G1–G4 mathematics](../../../decisions/ATCSectorStateDomain.md).

## CAN-LSF G1–G4 PASS and fourth adjudication

The [G1–G4 review](./CAN-LSF-G1-G4-Pass.md) independently reproduces the complete
mathematical evidence payload, checks certificate/checker/note identities,
and analytically passes the exact-sector theorem. It does not report running
the repository wrapper or native ATC execution. The
[fourth scoped adjudication](../../../decisions/ATCSectorStateDomainAdjudication.md)
accepts the paired event/reset and obstruction domains, decorated-orbit
definition/equivariance, the active bounded environmental class and its
whole-target return, retaining the negative second-parent control.

Its three ceilings remain explicit: full dimension only within the paired
quotient, no arbitrary-orbit fission authority, and one environmental class
rather than arbitrary surroundings. G5 is the next constitutive investigation;
G6–G7 remain separate. Reviewed evidence and narrower predecessors stay fixed.

## CAN-LSF G5–G6 PASS and fifth adjudication

The [G5–G6 review](./CAN-LSF-G5-G6-Pass.md) independently executes and matches
the mathematical evidence payload, reviews the active channels/nonlinear
potential/fixed-tau request domain, and reconstructs captured native primitive
outputs. It does not execute pygrc or the repository-dependent wrappers.
The [fifth scoped adjudication](../../../decisions/ATCSectorChannelsAdjudication.md)
accepts six bounded research claims with all 28 wider debts retained, including
the small G6 contraction margin, shifted history equilibrium and environmental
exclusion. Reviewed files and earlier accepted scopes stay unchanged.

The user then authorized [G7 reference conformance](../../../decisions/ATCSectorReferenceConformance.md)
inside the investigation without src changes. Its independent review and
acceptance are recorded separately below, not inherited from the G5–G6 PASS.

## CAN-LSF G7 PASS and completion of the bounded A_OS research program

The [G7 review](./CAN-LSF-G7-Pass.md) passes the executable full-graph law,
independent stage oracle, source-only prescription, both-role transfer and
readmission, lineage/reset semantics, atomicity, replay, negative pressure,
variable requests and finite represented-error bridge. It independently
reruns eight point rows, four source controls, both graph lifecycle suites,
extra request pressure at h=3/25 and the two current-role continuations.
The reset-role continuations remain retained author-run evidence; the review
does not report executing those, the main wrapper or native ATC.

The [sixth adjudication](../../../decisions/ATCSectorReferenceAdjudication.md)
records the user's explicit correction of the review's G7-R/G7-N split:
**G7: PASS; bounded A_OS G1–G7 research program: PASS**. Native implementation
is downstream Phase 9 work after the scientific publication/specification
chain, not an unmet G7 criterion. Charge drift identifies a future numerical
policy requirement; event-state preimages delimit this reference task's
scope rather than requiring production K0 scheduling here.

The original review and reviewed evidence remain unchanged. The addendum
also corrects the identity wording: PROFILE_ID binds evaluator operands;
source/checker hashes and the outer record additionally bind event/lifecycle
semantics. Six local claims retain all 28 wider debts without making them
blockers of this completed bounded program. Other realizations and aggregate
ATC-2/ATC-3 remain open; the proposed next scientific direction is an A_CI lift.

## CAN-F2 benchmark review and native follow-up

The [scientific disposition and fixed-target study](../../../decisions/ATCFissionTargetDiscrimination.md)
retain the supplied review's favorable symmetric prediction and both asymmetric
counterexamples. The supplied review checked equations and retained evidence,
not native events; its linked archive was not attached. The follow-up performs
separate native research and does not claim to reproduce that archive. Initial
endowment remains a hypothesis, and general dynamic support is refuted within
the stated control. No scientific/user acceptance is inferred.

## ATC-2 review and corrected completion requirement

The [ATC-2 review, correction and reproducer](./ATC2-IndependentReview/README.md)
retain exact research inputs and reproduce thirteen independent non-native
groups. The correction prevents a bounded PASS from closing missing generic
capabilities. User acceptance of ATC-2 has not been recorded.

## ATC-1 accepted review

The [review](ATC1-IndependentReview/ATC1-Review.md), original reproducer,
results, manifest and exact inputs are retained unchanged as readable files.
The input copies identify the reviewed subject even if later research changes
the active documents. They are evidence inputs, not another source of authority.
The [acceptance addendum](../../../decisions/ATC1Acceptance.md) records the
user's decision, separate opening/reconstruction review and scope limits.

To rerun the eleven independent groups from the repository root without
overwriting the retained result, use the original script's read-only function:

```sh
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python - <<'PY'
import json, runpy
from pathlib import Path
review = Path('implementation/investigations/grc9v4-constitutive-design/evidence/autonomous-topology-change/review/ATC1-IndependentReview')
checks = runpy.run_path(str(review / 'pressure_checks.py'), run_name='review_readonly')
result = checks['run'](review / 'inputs')
retained = json.loads((review / 'pressure_results.json').read_text())
assert json.loads(json.dumps(result['results'])) == retained['results']
print(json.dumps(result, indent=2))
PY
```

This prints a fresh result; it does not reproduce native ATC execution or
replace the original review's environment and input identities. The CLI
example inside the frozen review writes its output path; do not point that
example at the retained result when reproducing it here.
