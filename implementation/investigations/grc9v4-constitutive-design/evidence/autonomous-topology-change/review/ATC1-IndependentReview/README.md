> Relocation maintenance: paths and dependent identities were mechanically rebuilt; scientific content and review verdicts are unchanged. New identities do not claim a new independent rerun. Original committed subjects remain in Git.

# ATC-1 independent review bundle

Read `ATC1-Review.md` for the disposition and scope. `pressure_results.json` is the output of `pressure_checks.py`; `source_inventory.json` separates identical available bytes from unavailable source pins.

The `inputs/` directory preserves the two submitted JSON files, the original Draft-7 index and Draft-8 location map extracted unchanged from their companion bundle, the old source manifest as an explicitly historical reference, and the byte-identical 2025 core paper. No full proposed-source adapter, verifier, live model, forensic graph or GRCV4 runtime was reconstructed.

This bundle is evidence for a reviewer. It is not an accepted source or an official side-tool input schema.
