# P9-6.5 — concrete realization reconciliation and lifecycle routing

2026-09-10. **Bounded coordination review PASS; accepted by the user.**
P9-6.1a–P9-6.4d are accepted. Their eight candidate realizations have no open
numerical leaf blocker. **Tranche 6 is accepted and closed in its declared scope**;
the remaining lifecycle and scientific obligations below retain their own gates.
The routing record's acceptance object preserves the earlier execution status
and results and records this later disposition. No numerical implementation
changed for this leaf or its acceptance.

The [routing record](./P9-6.5-RealizationRouting.json) is the current successor
to the historical [P9-1.4 register](../tranche-1/P9-1.4-SupportAndDependencies.json)
for these results and routes. It does not rewrite that register's execution-time
statuses or create public support. Each row binds its candidate implementation,
applicable shared audit and a reproducible concrete profile seed. Seeds identify
internal provisional recipes; the reviewed parameter/graph domains and pressure
fixtures remain in the linked sources. A seed is neither the entire family nor
an authenticated lifecycle snapshot. Future lifecycle entry must bind its exact
profile, reference/graph/context, initializer, live/reset states and policy.

## Reconciled results

| Family | Accepted evidence | Numerical scope and next owner |
| --- | --- | --- |
| C_CI | [6.1a/b corrections](./P9-6.1ab-AuditFollowup.json), [6.1c](./P9-6.1c-ExecutionRecord.json) | Reference-connected unique root on a certified Frobenius ball; analytic joint residual; C selector stays in one strict-gap stratum. Route to C_CI lifecycle. |
| A_CI | Same accepted records, independent A oracles | In-root conductance/current refresh, selected-current continuity and one A writer; whole-ball floor/current conditions and reset/final-root readmission. Route to A_CI lifecycle. |
| C_PC | [6.2a/b/c](./P9-6.2abc-AuditFollowup.json) | Old independent Z, whole-chart source bound and one scalar ZOH write; C selector certified over the carrier geometry ball. Route to C_PC lifecycle. |
| A_PC | Same accepted record, independent A oracles | Authority (C,W,Z); refreshed W preserves old Z until the held-source write. Loop endpoint bounds include repeated incidence. Route to A_PC lifecycle. |
| C_CI_PC | [6.3a/b/c](./P9-6.3abc-AuditFollowup.json) | Frozen old Z within the root; full B_2R geometry coverage, separate uniform source and composite contraction bounds; exactly the selected root's source feeds one ZOH write. Route to C_CI_PC lifecycle. |
| A_CI_PC | Same accepted record, independent A oracles | Same-source gain two with complete A trial refresh and W-before-Z ownership. Route to A_CI_PC lifecycle. |
| C_RG2b | [6.4a/b foundation](./P9-6.4ab-ExecutionRecord.json), [6.4c graph run](./P9-6.4c-ExecutionRecord.json), [6.4d acceptance](./P9-6.4d-AuditFollowup.json) | Finite-graph completion-relative Lipschitz section; retained/filtering matrices, physical conditioning, whole-chart inverse/self-map/contraction/containment and matrix error certification. Route the generalized recipe to C_RG2b lifecycle. |
| A_RG2b | Same accepted records, independent A oracles | Full (C,W) base space and matrix geometry, active descriptor/WLS/current/writer channels; same computed certificate obligations. Route the generalized recipe to A_RG2b lifecycle. |
| A_OS (context) | [Tranche 5 claim review](../tranche-5/P9-5.4-Review.md) | Accepted provisional dynamics/claim separation; existing P9-7.1-A_OS onward remains pending. No new result assigned here. |
| C_OS (context) | [Exact G2 acceptance](../tranche-4/P9-4.8B-G2Acceptance.json) | Only the recorded exact C_OS profile has generic public support. Reuse its scoped lifecycle/conformance evidence; P9-G3[C_OS] remains a separate review. |

The common admitted Tranche 6 constructions use unit vertex measure, positive
diagonal reference Hodge and frozen graph/context, with candidate-specific
regularity and computed sufficient domains. They do not cover every graph,
parameter or selector merely because the schema can represent it. Certificates
are stronger than success at the submitted point. Analytic existence does not
ensure that a requested binary64 residual/error tolerance is representable.
Rejection at a domain, conditioning or numerical-budget boundary is retained.

For CI, the certified analytic residual is not a global root-error or stability
bound. For PC/CI+PC, carrier-ball invariance under the held-source ODE does not
establish base-state invariance or matched-forcing contraction when the sources
differ. RG2b remains Lipschitz-only and relative to the declared completion;
one-beat containment does not prove indefinite residence in the entry chart.
The scalar RG2b foundation does not substitute for the accepted matrix result.
The graph run covers repeated 8/16-vertex paths, a 12-vertex star and rich mixed
graphs. The 32-vertex campaign remains optional on explicit request: full A/C
completion is unestablished, and the recorded skip is not a pass.

## Lifecycle and G2 entry

Each of the eight rows independently routes to P9-7.1 (snapshot/load/replay,
reset, rebase, duplication), P9-7.2a (profile migration), P9-7.2b (mapped topology),
P9-7.3 (history and resource/charge dispositions), P9-7.4 (full target admission),
P9-7.5 (failure atomicity), P9-7.6 (receipt ownership), then P9-7.7 / P9-G2[p].
The record names family children for planning. Before executing a child, bind
the exact reviewed profile/fixture product; migration and event children also
need exact source/target identities and map scope. These are pending routes,
not execution permission or G2 admissions. No family depends on an unrelated
sibling's completion. An operation requiring two endpoints depends on both
particular endpoints, not on all Tranche 6 families becoming supported.

Use the existing owners and conformance harness, with profile-specific adapters
and independent expectations. In particular, retain distinct live/reset states,
reset-only target admission failure, total authority/receipt rollback, canonical
identity and duplicate independence. A's W and PC's Z are separate history
channels: A_PC/A_CI_PC have both, C_PC/C_CI_PC only Z, A_CI/A_RG2b only W,
C_CI/C_RG2b neither. Derived geometry, selected roots and RG2b sections are not
new persistent history. C reference transport and target reconstruction remain
applicable even where there is no carrier; A needs its own target/current/writer
admission. Reassess the documented CI/PC private cross-import ownership at
composite lifecycle integration, preserving one root owner and one ZOH writer.

The seven [registered migration classes](../tranche-1/P9-1.4-SupportAndDependencies.json)
remain visible: same-candidate nonhistory→nonhistory, nonhistory→history,
history→nonhistory, PC→CI+PC, CI+PC→PC, A→C and C→A. Here history classification
for the same-candidate realization crossings concerns PC carrier history;
candidate W disposition is an independent channel. For each proposed support
set, record positive, negative, pending and justified nonapplicable cases
separately. Unsupported-source construction rejection is not migration evidence.
Do not silently waive an unavailable positive crossing. Bind the full applicable
catalog product from P9-1.4, not just ordinary-step cases.

PC/CI+PC carrier-space event semantics are already defined by D9 and
[the lifecycle specification](../../../specs/grc-v4-spec.md#lifecycle-migration-and-topology-events): preserve history only through
an admitted typed bounded covariant carrier map, or explicitly archive/drop it
and zero the target carrier with a loss receipt. Implement and pressure that
contract at lifecycle entry; fixed-topology numerical execution is not its
runtime evidence. RG2b likewise needs a newly bound target completion and target
admission for topology/context changes. These pending runtime scopes neither
enlarge an exact G2 support set nor hold unrelated families.

## Scientific obligations assigned to P9-6.5

The [P9-1.3 routing](../tranche-1/P9-1.3-VerificationRouting.json) assigns two
forward obligations here. Neither is discharged in full by this leaf:

| Obligation | Evidence reconciled | Retained boundary / next owner |
| --- | --- | --- |
| D11-C-VERIFY-IMPLEMENTATION-COVARIANCE-AND-SOLVER-CONFORMANCE | Accepted C baseline/current/OS work and CI/PC/CI+PC/RG2b native signed-coordinate covariance, independent equations, physical conditioning and residual/error checks. The routing record links representative existing methods. | Scoped numerical evidence is available. Exact lifecycle/migration/event covariance and the complete applicable product remain at P9-7.2a/b–P9-7.7. No all-profile conformance declaration. |
| D9-VERIFY-QUANTITATIVE-PARAMETER-ENVELOPES | OS control pressure; CI whole-ball root certificates; PC source/current bounds, tau and signed/extreme ZOH release; CI+PC B_2R/composite bounds; RG2b computed whole-chart and finite-error bounds. | P9-EVID-ENVELOPES remains pending: preregistered cross-realization quantitative envelopes and release/source-closure evidence exceed finite implementation parameter tests. Concrete admission constants do not settle design-wide numeric instantiation. |

The [debt inventory](../tranche-1/P9-1.2-DebtInventory.json) remains authoritative
for all deferred gates. In particular, retain formation (P9-EVID-FORMATION),
complete-chain committed A/C endpoint witnesses (P9-EVID-ENDPOINT), formed-profile
comparison (P9-EVID-COMPARISON), spectral/metric analysis (P9-EVID-ANALYSIS and
P9-EVID-A-METRIC), and RG2b regularization (P9-SCI-RG-REGULARITY). Their scientific
evidence closure is separate from numerical and lifecycle conformance, with
P9-10.5 reconciliation where routed. Units/physical attribution, singular
continuation and reserved Candidate B keep their existing owners. No G3 or
specialization claim is made.

The older GTRS-PC-v1 **five-live-debt list is a historical snapshot**, not the
current design state. This also qualifies earlier Tranche 6 prose about
unresolved PC synthesis/topology: those records retain their historical bytes,
but downstream routing must use the successor dispositions:

| Original PC question | Current disposition and remaining work |
| --- | --- |
| Comparative synthesis / orthogonal axes / profile-kernel quotient | **Resolved by [COMP](../../investigations/grc9v4-constitutive-design/decisions/GeometryTemporalRealizationComparativeSynthesis.json).** Enabled affine PC has no nontrivial exact profile-kernel quotient. This is not absolute reachable-state minimality, a preferred architecture, or runtime ranking. |
| Carrier-domain / regularity / writer / numeric parameters | Superseded by COMP's parametric-envelope debt, then **design-resolved by [D9](../../investigations/grc9v4-constitutive-design/decisions/D9ResidualDebtLedger.json)**. Numeric evidence goes to D9-VERIFY-QUANTITATIVE-PARAMETER-ENVELOPES / P9-EVID-ENVELOPES, as reconciled above. Scalar ZOH is the admitted representative, not a universally unique law. |
| Carrier-space topology interspace semantics | **Design-resolved by D9.** Typed carrier mapping or explicit archive/zero/loss semantics are available; Tranche 7 still owes concrete live/reset execution and failure evidence. No missing-design hold is imposed. |
| A and C complete-chain base-state witnesses | Carried separately into the current debt inventory and P9-EVID-ENDPOINT. Distinct carrier/geometry or a root-level source does not establish a nonzero committed future base-state effect. |

The routing record binds the COMP/D9 disposition rows. P9-6.5 closes no design
debt itself and does not turn a resolved design contract into executed lifecycle
coverage.

## Reconstruction and focused verification

The routing record binds the accepted Git subject, five acceptance records,
historical registers and exact fixture seeds. Source/test bytes remain in Git.
Its verifier reuses the existing Tranche 6 source/acceptance chain and live
side-tool contract checks; it does not implement another numerical validator.
It reconstructs the eight seed identities and checks independent A/C routing,
shared-audit coverage, separate history channels, pending lifecycle/G2 and the
unchanged exact C_OS support. Mutation pressure challenges incomplete/duplicate
rows, swapped candidate evidence, missing shared audits, false support promotion,
history conflation, reopening resolved PC design debts and treating the optional
campaign as complete.

From the repository root:

```sh
uv sync --frozen --extra v4 --extra dev
PYTHONPATH=src:. .venv/bin/python implementation/phase-9-grcv4/verification/verify_p965_routing.py --check
```

The existing native numerical results are reused with their original source
subjects and execution counts; overlapping campaigns are not added together as
unique tests. No full repository run or optional RG2b campaign is needed for this
coordination-only change. Verification passed: eight reconstructed seeds,
13 rejected routing mutations, inherited acceptance/source checks, actual
API-to-browser validation and all 33 existing browser-validator tests. Lint,
relative-link/disposition checks and diff whitespace checks also passed.
The user accepted P9-6.5 and closed Tranche 6 on 2026-09-10, authorizing this
commit. The next handoff task is P9-7.1-A_OS. Acceptance verification checks the
preserved reviewed digest and bounded closure without repeating numerical runs.
Tranche 7 implementation, branch merging and G2/G3 reviews remain separate work.
