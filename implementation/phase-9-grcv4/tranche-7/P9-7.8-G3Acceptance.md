# P9-7.8 acceptance and Tranche 7 closure

The user accepted P9-7.8 and Tranche 7 on 2026-09-15. The
[decision](./P9-7.8-G3Acceptance.json) binds review checkpoint `c6925b5`.
The original review, all G2 decisions and numerical evidence remain unchanged;
current views project this separate acceptance.

P9-7.1–7.7 are accepted, including both 7.2 children and the 305-cell aggregate.
P9-7.8 is accepted. **Tranche 7 is closed.** G3 admits the single consumed set
of ten exact generic declarations, not ten conformant GRC9V4 runtime models.

The scoped execution successor makes only P9-8.1a eligible: fixed chart/port
graph in the existing planned V4 topology module and its paired test file.
No runtime files are created by this decision. Every future edit still needs
its exact work-manifest hash and owning leaf. This does not open the shared
module's later row-differential, trigger, coarse/Split or expansion work.
Legacy files, public integration and all other specialization targets remain
closed. No blanket Tranche 8/9 permission is granted.

P9-8.3A.1 owns independent oracle selection/construction/review, and P9-8.3A.2
depends on that accepted oracle and applicable runtime prerequisites. Both
remain pending. Genuine generic authority gaps return to a bounded Tranche 7
correction. All forty disabled-compatibility cells, deep expansion/covariance
and mandatory specialization lifecycle evidence remain Tranche 8/9 work.
Optional completion capabilities and a stronger handoff scope are not selected.

Next: P9-8.1a when implementation is requested. No specialization
implementation, all-pairs claim or new numerical campaign was started here.

Validation: eight focused P9-7.8 methods, one historical generic-permission
preservation regression and 42 affected browser tests passed. The scoped
current boundary passes with only P9-8.1a newly ready. A fresh-process import
regression covers the API's previously missing verification-helper search path.
The complete live status projection was interrupted after a prolonged run
without a result; it is not recorded as passed. Its end-to-end verification
remains visible in the main handoff, separately from this user acceptance.
