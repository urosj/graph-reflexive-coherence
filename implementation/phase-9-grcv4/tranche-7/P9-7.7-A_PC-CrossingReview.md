# P9-7.7 — A_PC crossing reconciliation

Status: bounded local/crossing reconciliation accepted by the user on 2026-09-14.
The explicit “accept and commit, then do G2” decision covers the combined
21 local cells and seven crossing dispositions. Original execution flags and
identities remain unchanged. This acceptance does not accept A_PC G2, G3,
all-pairs support or aggregate P9-7.7; public support stays C_OS/A_OS/A_CI/C_CI.

The [local product](./P9-7.7-A_PC-LocalReview.md) covers 21 cells. This
[crossing record](./P9-7.7-A_PC-Crossings.json) reconciles the remaining seven:
A migration-history receipt, reset after migration, reset after event, whole
lifecycle tuple map, migration-class reconciliation, history disposition, and
target-readmission failure. Together they cover all 28 required catalog cells
for the exact A_PC nomination
`grcv4-profile-sha256:058ae6b1f923c85952ffdfa083af74e3b56dd450f309190f307c3ea56ac2aa75`.
Class reconciliation is not universal positive execution of all ordered pairs.

## Ordered scope and retained evidence

Seven aliases bind original record identities, source/target declarations,
pre/poststate digests, receipt deltas and literal current/reset maps. Old runs
are not edited or rerun. Five migration witnesses directly involve the exact
nomination; two witnesses remain explicitly separate endpoints.

| Class | Evidence and boundary |
| --- | --- |
| Nonpersistent → nonpersistent | Retained A_OS → A_CI, separate endpoints; no A_PC execution credit. |
| Nonpersistent → persistent | Retained A_CI → exact A_PC; W preserved, fresh zero Z, no loss. |
| Persistent → nonpersistent | Exact A_PC → retained scalar A_RG2b declaration; W preserved, explicit carrier loss. This is not the generic RG2b nomination or its G2. |
| PC → CI+PC | Exact A_PC → retained A_CI+PC; both W and Z preserved under exact carrier-contract identity. |
| CI+PC → PC | Reverse retained A_CI+PC → exact A_PC; both histories preserved. |
| A → C | Exact A_PC → retained C_PC; candidate-history loss and whole-carrier reset/loss are separate channels. |
| C → A | A new operation rejects the unselected initializer on the exact nomination. Retained positive C_PC → initializer-selected A_PC is a different target, not nomination support. |

Preserving PC pairs have distinct nonzero live/reset carriers. Both directions
preserve the exact graph/K4/weights/context/common/candidate/geometry/carrier
contract; adding the immediate CI path is not permission to change that contract.
No CI+PC support gate follows automatically from these pair witnesses.

## New native execution

Three focused methods produce five cases, passing in 40.678 seconds:

- An explicitly selected affine topology event swaps resources and adds
  `(-1/8, 0)`. It independently initializes target W for current/reset using
  the rational reference-pass oracle, zeros target Z with explicit loss, and
  readmits both targets. Independent dense equations check the actual target
  readmission inputs/current/geometry. The negative increment keeps this fixed
  initializer target inside its admitted W chart; it changes charge by -1/8,
  not time. Source W loss and carrier reset are not native exponential release.
  Missing transition archives reject; reset preserves the mapped reset authority,
  clock/charge, archives, persistent ledger and independent snapshot ownership.
- Current-as-reset control succeeds against a C_PC target resource radius 3.
  A source-admissible reset `(3, 1)` alone exceeds that target chart while the
  same live state passes. Rejection is at target readmission, before publication,
  with the whole publication unchanged. Both source roles fit radius 4.
- Incoming C → exact nominated A_PC rejects its unselected initializer at actual
  operation admission. Changing `tau_PC` on the otherwise preserving A_PC →
  A_CI+PC target likewise rejects at operation admission; it cannot silently
  replace exact carrier transport with a reset. Both failures preserve the
  entire scientific/lifecycle publication.

## Integrity, access and limits

The checker binds source bytes, raw content-addressed snapshots/requests/results,
exact nomination and predecessor, native test roster, typed forensic traces,
two-role equations, loss channels and ordered scope. Retained receipts without
a solver outcome remain explicitly `not_recorded`; no outcome is inferred.
PC provenance retains its original conditional authority status. The accepted
reference-pass contract is an optional selected policy, not a universal producer.

Thirty rehashed evidence mutations cover missing cases/classes/cells, borrowed
endpoints, erased loss, event/reset equations, readmission role substitution,
initializer/contract boundaries, authority drift and acceptance/support widening.
All 30 controls passed, as did 11 shared registry/materializer tests. One actual
status evaluation, reused across notebook/HTTP/browser, passed in 146.953
seconds with 90 browser rejection controls and explicit pending A_PC rendering.
The pinned `a_pc_crossings` materializer depends on `a_pc_local_product`; shared
Python/API/notebook/browser paths need no new profile-specific dispatch branch.
Public support remains exactly C_OS/A_OS/A_CI/C_CI. Original local execution
flags stay unchanged; neither aggregate P9-7.7, all-pairs, A_PC G2 nor G3 closes.

From repository root, check retained evidence without a numerical rerun:

```bash
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_a_pc_crossings.py
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python -m unittest test_p977_a_pc_crossing_evidence -v
```

Use the first command with `--run` only for a deliberately new native execution;
its stdout is a new execution record, not a replacement for the retained run.
Next: integrated A_PC G2 review, with G2 acceptance a separate decision.
