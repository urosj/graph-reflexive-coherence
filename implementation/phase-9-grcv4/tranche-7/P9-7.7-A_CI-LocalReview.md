# P9-7.7 — A_CI local-product reconciliation

Status: **bounded local evidence accepted with the crossing reconciliation
by the user on 2026-09-14**. Integrated A_CI G2 review remains. Exact accepted
discovery is still C_OS and A_OS only. No aggregate P9-7.7 or G3 acceptance follows.

Subsequent progress: the [separate crossing reconciliation](./P9-7.7-A_CI-CrossingReview.md)
links those seven cells with explicit endpoint dispositions and records the
user's bounded acceptance separately.
This local execution and its capture-time coverage remain unchanged.

## Exact subject and source

Keep the original nomination from the [initial review](./P9-7.7-Review.md):

`grcv4-profile-sha256:16ed65f7f65d4716e1be3e384f6fa0f957d26dd7b7a3f7e1b43ad1aa3f250946`.

The [local execution record](./P9-7.7-A_CI-LocalProduct.json) binds the lossless
profile, graph, reference, differential backend, distinct live/reset authority,
requests, results, content-addressed snapshots, observed numerical values and
independent expectations. It does not substitute a family label for identity.

The specification's [coupled-implicit contract](../../../specs/grc-v4-spec.md#coupled-implicit-ci),
[catalog](../../../specs/grc-v4-conformance-fixtures.json) and
[paper §10.2.1](../../investigations/grc9v4-constitutive-design/drafts/2026-09-GRC-V4.md#1021-candidate-a-ci-root)
are the authority spine, with the accepted [CI reconciliation](../tranche-6/P9-6.1c-ExecutionRecord.json)
and numerical tests supplying retained source-owned equations and oracles.
Current typed `contract_provenance` traces are retained for
`D10.2-EC-CI-A-ROOT`, `D10.2-EC-CI-A-CONTRACTION` and
`P9-EC-RECEIPT-PARENT-CEILING`. The first two still say
`indeterminate_requires_review`; runtime success does not rewrite their
accepted support semantics. Parent-ceiling authority remains separately typed.

## What executed

| Product | Cells | Bounded evidence |
| --- | ---: | --- |
| Common | 8 | Actual ordinary/zero/negative operations, fresh second beat, typed consumed-solve faults, postsolve charge failure and whole-publication rollback. |
| Candidate A | 8 | Positive supplied W, exact trial J0-dependent W-hat, distinct chi/zeta-zero profiles, post-continuity descriptors, logarithmic writer, old-W consumption and no formation inference. |
| CI | 1 | Independent root equations, declared bounded self-map/contraction, certified joint residual, fresh trial currents, one continuity and one writer, separate reset/final readmission. |
| Local lifecycle | 4 | Save/load/replay, reset after ordinary steps, previous-primary receipt ownership, duplicate/export/rebase independence. |

The three focused methods passed in **95.676 seconds**, without failures,
errors or skips. A preceding focused equation check also passed; it is a
development check, not additional coverage credit. The retained record digest is
`9a7e34069d40b391d71a47c89a184a445e757383409747bd03cdd4d2a9c350d3`.

This is not an OS substitution. The exact CI nomination has nonzero gamma;
the test observes W-hat changing with trial baseline current. Independent
matrix/scalar equations locate the root, and independent Decimal descriptor
and logarithmic-writer equations check the consumed and written values.
Every trial uses incoming W. The writer rejects a mismatched selected trial,
refreshes descriptors using post-continuity C, and runs exactly once. The
restart admits written W but does not feed it backward into this beat.

The root certificate is local to the declared reference ball. A separate,
identity-bound one-iteration/zero-tolerance control rejects with
`no_admitted_root`, without fallback. That control is **root-construction
rejection**, not a public operation or an additional supported profile. The
nomination does not establish global uniqueness, arbitrary parameters/graphs,
full-transition stability or physical formation.

Domain/nonfinite consumed-solve and postsolve charge faults are labeled
test-only injections. Reset admission is allowed to pass before the consumed
solve fault. Common charge mismatch and atomic failure share one execution;
replay and receipt ownership also share an execution. Provisional equation rows
remain `not_invoked`, with no invented solver/commit/result. Numerical recipe
objects alone are not the observations: selected inputs, actual J/H, residual,
certificate, trial W-hat, writer and restart values are retained separately.

## Remaining work

The seven unresolved catalog rows remain:

- `A-MIGRATION-HISTORY-RECEIPT`;
- `RESET-AFTER-MIGRATION`;
- `RESET-AFTER-EVENT`;
- `WHOLE-LIFECYCLE-TUPLE-MAP`;
- `ALL-MIGRATION-CLASSES`;
- `HISTORY-DISPOSITION`;
- `TARGET-READMISSION-FAILURE`.

The retained A_CI reconstruction target is
`grcv4-profile-sha256:7e90f24e123cbaa2571aa5071489a8b4fdec815098925cba51ed01be09b1fe2b`,
not the nomination. Keep incoming/outgoing migrations, representation and
reconstruction endpoints distinct. Reconcile their retained assertions before
adding only missing crossing/reset/failure checks. Do not borrow A_OS's new
G2 acceptance, infer all-pairs coverage, or silently replace this nomination.
Integrated public-facade/G2 review and explicit user acceptance follow that work.

## Reproduction and access

From the repository root:

```bash
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_a_ci_local.py
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python -m unittest test_p977_a_ci_local_evidence test_p977_a_ci_surfaces -v
```

The default checker validates retained evidence and current source/authority
without numerical execution. `--run` emits a new execution to stdout; it does
not replace this run. No machine-local evidence paths or external artifacts
are required. Save/load temporary files are disposable test inputs only.

The existing API, Phase 9 notebook and browser expose `a_ci_local_product`.
The focused surface test obtains one actual checked status, then reuses that
same result through notebook/HTTP transport and browser validation, avoiding
three repetitions of the retained predecessor chain. This tests orchestration,
not three independent numerical executions. Rehashed evidence and browser
controls reject missing rows, borrowed identities, invalid branch evidence,
source drift, premature acceptance and widened support.

Verification passed: **20 rehashed evidence controls** (31.879 seconds) and
the one-check API/notebook/HTTP/browser test with **10 browser controls**
(64.031 seconds). Whitespace and machine-local-path checks also passed.
No broad numerical or legacy suite was rerun for this continuation.

Original execution records, accepted support declarations, scientific sources,
paper/specification text and production numerical code remain unchanged.
