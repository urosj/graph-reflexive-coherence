# P9-7.7 — C_RG2b local reconciliation

Status: bounded local/crossing reconciliation accepted by the user on 2026-09-15;
see the [acceptance statement](P9-7.7-C_RG2b-CrossingReview.md). The original
execution record is unchanged. No new G2 support; nine accepted declarations
remain unchanged.

The original P9-7.1 nomination is
`grcv4-profile-sha256:413497bec4f219ec402d82d5cd2aced01dca25a58d2ab906c348472b98d596b0`.
This is the four-node, three-edge graph with nonuniform reference weights,
nonzero C deformation/current coupling, and independently declared current/reset
resources. It has neither W history nor a persistent carrier.

The local product covers 26 catalog cells: eight common, thirteen Candidate C,
one RG2b realization and four fixed-profile lifecycle cells. Shared rows are
coverage aliases, not independent executions. The seven crossing cells
(reference-map lifecycle, reset after migration/event, whole-lifecycle tuple
map, all migration classes, history disposition and target-readmission failure)
are addressed in the separate [crossing review](P9-7.7-C_RG2b-CrossingReview.md).
This local execution record is unchanged. Integrated G2, aggregate P9-7.7
and G3 remain separate from the bounded acceptance.

## Scientific scope

Independently calculate two graph transforms with inverse points checked inside
the cutoff-one core. Compare current/reset/restart section geometry using the
two-transform contraction tail, finite certificate error, and a separately
stated binary64 allowance. This is an independent discriminator, not a second
certified evaluator. Recalculate the complete C selector, sector, retained
Hodge, potential, baseline, identifications, readback and current at every
selected role; verify continuity, generated geometry and restart invariance.
No A writer or carrier write is admitted.

The nomination retains all four selector modes. A separately labeled partial-
selector control exercises the fixed-stratum baseline derivative; analytic
directions are compared with centered differences. Edge orientation reversal
checks baseline and derivative covariance. Neither calculation differentiates
the RG section: its accepted ceiling remains bounded, completion-relative and
Lipschitz-only. No C1, spectrum, CI-root, indefinite base-chart or graph/parameter
sweep claim follows.

Distinct chi-zero, zeta-zero and kappa-M-zero declarations test baseline/source
and deformation controls without granting additional support. A changed-Hodge
control distinguishes stiffness dependence from frozen mobility authority.
An exact selector boundary is deliberately outside the completion chart and
is a fixed-stage rejection, not a lifecycle operation. The conditioning
counterexample is algebra-only, alongside actual matrix-point certificates.

Native common paths cover ordinary, zero/negative duration, injected domain,
nonfinite and charge failures, and stale-cache reconstruction. Additional
pressure covers the wrong frozen positive beat, construction budget/reset-chart
rejections, injected reset/restart readmission and a late publication exception.
Every failed operation preserves the entire publication and its owner pointer.
Reset after ordinary steps, save/load replay, duplication independence, receipt
delta/ledger ownership and coherently rehashed missing-parent rejection execute
on this exact nomination.

## Validation

Five native methods passed in the final source-bound capture. All 51 rehashed
semantic mutations were rejected; the final capture preserves exactly the
same fixture results, evidence objects and readmission-pressure payloads used
in that semantic check. Thirteen focused methods passed for the actual retained
checker, shared browser projection, registry/discovery and materializer guards.
The phase boundary check passed. No production modules or accepted records
were changed, and no historical numerical campaign was rerun.

Development corrections were confined to the new tests/checker: Candidate C's
fault hook takes one input rather than A's backend argument; control profile
changes legitimately rederive lifecycle identities; failure receipts are
emitted without being appended to the unchanged persistent ledger. None required
weaker numerical tolerances or a production fix.

## Sources and reproduction

Authority comes from the [V4 specification](../../../specs/grc-v4-spec.md),
[conformance catalog](../../../specs/grc-v4-conformance-fixtures.json),
[paper](../../investigations/grc9v4-constitutive-design/drafts/2026-09-GRC-V4.md)
and [Tranche 6 graph evidence](../tranche-6/P9-6.4c-Review.md).
Eleven forensic provenance traces preserve RG parent/invariance/contraction/
determinism/claim ceilings, C reference/mobility/baseline/derivative/covariance,
and the receipt-parent ceiling. Passing runtime checks does not promote their
claim classifications or support dispositions.

The [retained record](P9-7.7-C_RG2b-LocalProduct.json) binds exact inputs, outputs,
source hashes, environment, test roster and predecessor review. The historical
predecessor is checked through its pinned accepted A_RG2b linkage and current
source bindings; unchanged P9-7.1–7.6 numerical campaigns are not rerun.

Run from the repository root using the existing environment:

```bash
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_c_rg2b_local.py
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_c_rg2b_local.py --run
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python -m unittest test_p977_c_rg2b_local_evidence test_profile_g2_registry test_profile_materializers
```

Default mode checks retained evidence and equations. `--run` emits a fresh
five-method execution record to stdout; it does not overwrite accepted history.
The same pinned `c_rg2b_local_product` materializer exposes the result through
the API, notebook and generic browser view, with no bespoke profile dispatch.
