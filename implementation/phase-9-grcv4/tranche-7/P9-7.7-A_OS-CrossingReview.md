# P9-7.7 — A_OS crossing reconciliation

Status: bounded local/crossing reconciliation accepted by the user on
2026-09-13 through “accept and commit.” This accepts the 21-cell local evidence
and seven crossing evidence/dispositions, including their stated limitations.
No independent audit is invented by this decision. Integrated G2 applicability
review, G3, public-support change and aggregate P9-7.7 closure remain separate.
Original execution records and their capture-time flags remain unchanged.

This continues the [21-cell local product](./P9-7.7-A_OS-LocalReview.md).
The nomination remains
`grcv4-profile-sha256:e4c04a83240a33d77c50a26ca6effbb6ce142774bd01f0966861dd517a94e2c4`.
Its initial resources are not a universal fixture: the new crossings use the
same complete profile with the explicitly recorded near-equilibrium, distinct
current/reset inputs from 7.2a. Exact inputs and graph/reference identities
remain bound separately from the profile ID.

## Evidence reused and added

The [crossing record](./P9-7.7-A_OS-Crossings.json) references five unchanged
retained cases. Two already have the exact nominated A_OS source: A_OS→A_CI
preserves W, and A_OS→C_OS explicitly loses W. Their request, original run,
ordered endpoints, both history channels, receipt delta and reset are directly
inspectable. The projection does not invent a solver outcome absent from the
old receipt; its null field is explicitly labeled unrecorded, not failed or
unexecuted migration. The other three aliases remain separate-profile witnesses.

Three focused methods add six execution cases, not six independent runs:

- A_OS→A_PC and A_PC→the exact nominated A_OS, retaining W independently in
  both roles while initializing Z to zero or explicitly losing it. Each is
  followed by reset and snapshot restore. The returning PC source carries
  nonzero, distinct current/reset Z; this is not a vacuous zero-carrier loss.
- A mapped event from nominated A_OS to the separately identified,
  initializer-enabled A_OS. A resource swap plus 1/8 increment is calculated
  independently for both roles. The existing independent reference-pass oracle
  supplies expected W for each role. W loss, absent Z, charge, clock/index,
  reset, ledger/archive retention and missing-archive rejection are checked.
- A native reset-only target-readmission rejection and matching successful
  current-as-reset control, plus a well-formed C→nominated-A request rejected
  for the target's unselected initializer. Rejected operations preserve the
  whole publication pointer and emit a failure without appending to the ledger.

The event's changed target coefficients and history policy are deliberate,
explicit endpoint inputs. They do not replace the nomination or earn local
numerical-product credit for that different A_OS target. No production code,
specification, paper, claim or accepted run changes are needed.

## Seven catalog cells and the migration boundary

| Catalog obligation | Reconciled evidence |
| --- | --- |
| A migration-history receipt | Exact retained A→A/A→C and new independent W/Z crossings |
| Reset after migration | Retained reset endpoints and new persistent crossing resets |
| Reset after event | New mapped event and independent target reset |
| Whole lifecycle tuple map | Both resource/history roles, target reference, charge, clock/index, archive and ledger |
| All migration classes | Explicit seven-class disposition matrix below; **not** an all-pairs execution claim |
| History disposition | Candidate/carrier channels independently; preservation, initialization, explicit loss and absence |
| Target-readmission failure | Exact nominated source, failing reset versus passing current-as-reset control |

| Migration class | Exact scope and disposition |
| --- | --- |
| Same candidate NP→NP | Retained positive nominated A_OS→A_CI |
| Same candidate NP→P | New positive nominated A_OS→A_PC |
| Same candidate P→NP | New positive A_PC→nominated A_OS |
| PC→CI+PC | Retained A_PC→A_CI+PC; neither endpoint is A_OS, no nomination credit |
| CI+PC→PC | Retained reverse pair; neither endpoint is A_OS, no nomination credit |
| A→C | Retained positive nominated A_OS→C_OS |
| C→A | New rejection of C→nominated A_OS; retained positive targets the different initializer-enabled A_OS |

The nominated profile selects supplied reference history, not the C→A target
initializer. A positive incoming C→this nomination is therefore not claimed.
Nor are PC↔CI+PC witnesses relabeled as A_OS executions. The matrix records
these boundaries for integrated applicability review; it does **not** silently
waive a mandatory positive row or assert all ordered pairs are supported.
If integrated G2 review requires a positive C→A target in the supported set,
the initializer-enabled profile needs its own exact product/nomination review.
That is a support-scope decision, not permission to rewrite the current profile.

Thus all seven previously unlinked crossing obligations now have explicit
evidence/dispositions. The accepted bounded reconciliation still requires
independent, integrated G2 review and a separate G2 decision. Other eight held profiles,
the accepted C_OS singleton and G3 are unchanged.

## Reproduction

From the repository root:

```bash
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_a_os_crossings.py
```

The default checks retained evidence only. `--run` emits a new run to stdout,
without overwriting the original. The record binds current source files,
environment, original aliases, required result fields and content-addressed
full pre/post/reset snapshots, requests and results. No machine-local path or
external archive is required. Scope mutations are tested separately without
rerunning the numerical suite.

Execution: all three methods passed in 79.634 seconds, with no failures,
errors or skips. Record digest:
`2322b1a2f8b6b4904b38200f2f9018356db2698fbaafd53fe6c9ef8497afc351`.
During retention, a JSON formatting round trip changed numeric token spelling
and correctly failed the record digest check. Formatting was corrected to
preserve the captured tokens; the original execution digest was not replaced
and no numerical rerun was needed.

The read-only evidence test passed 15 rehashed mutation controls. The focused
API/notebook/HTTP/browser integration test passed in 156.702 seconds, including
52 browser rejection controls (six added for crossing scope). These status
checks did not rerun the numerical suite. The plans, checklists and maintenance
boundary include this continuation; runtime permissions and accepted support
remain unchanged.
