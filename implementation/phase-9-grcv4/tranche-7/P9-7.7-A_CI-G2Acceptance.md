# Exact A_CI G2 acceptance

The user accepted the exact A_CI G2 scope on 2026-09-14 and authorized committing
the reviewed checkpoint before applying acceptance. Commit `fa94cd2` preserves
the [integrated review](./P9-7.7-A_CI-G2Review.md), its JSON subject, the interface
capture and shared registry before acceptance. The
[acceptance record](./P9-7.7-A_CI-G2Acceptance.json) uses the common
`phase9_exact_profile_g2_acceptance_v1` adapter and binds that Git subject,
review JSON/text hashes, declaration, releases and predecessor support.

Accepted additional support is only
`grcv4-profile-sha256:16ed65f7f65d4716e1be3e384f6fa0f957d26dd7b7a3f7e1b43ad1aa3f250946`:
the supplied-history A_CI declaration and its reviewed 28-cell finite product.
The original C_OS/A_OS acceptances are unchanged. Public discovery now contains
those three exact declarations; parameter variants, initializer-enabled A_CI,
separate PC endpoints and arbitrary ordered crossings are not promoted.
No global CI branch, G3, aggregate P9-7.7 closure or new runtime permissions
are accepted. Seven other profile G2 decisions remain open.

Original review/run flags and evidence bytes are preserved, not rewritten to
claim they executed after this acceptance. The
[finite source-reuse record](./P9-7.7-A_CI-G2SourceReuse.json) binds exact
before/after discovery, assertion and retained-checker bytes to `fa94cd2`.
It composes before the existing A_OS bridge. Changed or unrelated identities
fail closed; fresh runs still bind current bytes. The runtime work manifest
updates only existing file hashes and exact accepted support, not ownership.

The first development discovery check rejected the declaration serialized by
ordinary JSON: the wire decoder requires exact binary64 canonical JSON.
Using the production canonical encoder corrected the representation; neither
the accepted declaration nor its profile identity changed.
One pressure fixture also used typed parameters at a JSON-only resolver;
it now supplies the JSON preimage and recomputed parameter identity. This
corrected the test fixture, not the runtime or accepted numerical evidence.

## Review comment and bounded follow-up

The review correctly identifies a remaining implementation seam: local-product
and crossing **presentation** checks still have profile-specific branches.
Evaluate sharing those views when the next profile follows the same shape;
keep numerical/scientific evidence checks profile-specific. This is not an
A_CI G2 blocker and does not require another refactor before acceptance.

Historical records intentionally keep their schemas, but the shared validator
checks their explicit runtime/specialization ceilings as well as normalizing
the view. Discovery remains checked against accepted records; it has never
been the source of acceptance authority. Per-profile counts now come from
the pinned review, not a universal 28-cell assumption.

## Verification

Use the focused acceptance/source-reuse and registry tests, exact discovery
tests, and one current-status API/notebook/HTTP/browser check. These validate
the acceptance transition, not another numerical campaign. The accepted six
local/crossing methods are not rerun; reproduction remains a distinct run.

The initial 15-test acceptance/registry/A_OS pass had only that fixture error;
the other 14 methods passed. After correction, all seven focused A_CI
acceptance, public-discovery and actual API/notebook/HTTP/browser methods
passed in 82.494 seconds. The browser exercised 36 coherently rehashed
rejections and rendered three accepted exact declarations. Original review,
interface, local/crossing evidence and prior acceptances were checked against
their Git subjects. No numerical campaign was rerun.
