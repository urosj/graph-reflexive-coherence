# P9-7.2a — initializer integration into the paper

Status: **paper revision accepted by the user** through the commit-and-continue
request on 2026-09-11. The exact reviewed paper bytes are retained, including
their drafting-stage status line; this record supplies the acceptance disposition.
The user accepted the initializer-integrated proposal by requesting commit and
continuation; `448e420` records that decision. This step propagates it to the
[paper](../../investigations/grc9v4-constitutive-design/drafts/2026-09-GRC-V4.md).
The accepted proposal, scientific authority records, specs, packaged release,
runtime and original execution records remain unchanged.

## Source and transfer

The accepted proposal SHA-256 remains
`09c080fd835d096372cff6204e42ddf0baba2b2da674fee905c7b5df2c83ed84`.
The paper candidate SHA-256 is
`6060e28698c07068f91a5e40d5335d1cf8ae2c4292baf3a7c817a36f1ede57a9`.

Six sections transfer exactly from that accepted source: §8.11, §12.5, §12.6,
§14.4, §15.2 and Appendix E.8. As in the prior paper extraction, detailed
source-register blockquotes in §§12.5–12.6 are omitted from the main text;
Appendix G.5 retains the new claim, contract, object, debt and forward-obligation
references. The actual copied equations, numerical staging, lifecycle rules and
claim ceilings are unchanged. The abstract adds a bounded initializer summary;
the historical D10/D11 census is explicitly separated from later P9 authority.
Unrelated paper-specific corrections, Appendix A's specialization and the
bibliography are preserved.

The current forensic API confirms projection digest
`fa59abda356446daca516b33028a07a48ec63aaacb8f961cd274a425dca48dd1`.
`P9-7.2a-CL-O-INIT-001` remains optional; its contract has required support for
the selected design. The D10.2 initializer retains
`indeterminate_requires_review`. The debt trace separates the design-resolved
producer choice from forward integration work. No historical association or
supplied-flux execution is promoted to implementation of this producer.

## Verification subjects and checks

The existing `verify_p972a_proposal.py` now distinguishes three document subjects:

- released proposal and paper bytes at `f36b3ba`, with their original release hashes;
- the accepted proposal at `448e420`;
- this exact paper revision, now user-accepted.

The executable release remains
`grcv4-spec-release-sha256:e2acd9df0cc02c5fd4bbed4989ff5d7da3a819adeb2950d922b8a6ef4bf35f24`.
Every other release member/source, generator, packaged asset and codec pin
remains current-byte exact. The original release builder is unchanged and can
reconstruct its output at the original checkout. Current verification reads
the declared Git subjects without creating a checkout, archive or new execution.
Missing history holds rather than substituting the revised documents.

The migration checker still verifies original paper/source hashes at their
historical subject and retains the 25/17-test evidence. Its current-paper check
now belongs to the exact candidate and accepted-proposal transfer validation;
it no longer requires today's paper to equal the old migration input. No runtime
or specification exemption is added.

Seven focused methods in the existing
[`test_p972a_proposal.py`](../verification/test_p972a_proposal.py) cover document,
source, release, asset and codec drift; missing/substituted Git history; accepted
proposal identity; and independent mutations of copied equations, pass count,
fixed target charts and pending migration claims. Identity/transfer checks do
not automate semantic paper review or prove executable migration.

Scoped commands, from the repository root:

```sh
.venv/bin/python implementation/phase-9-grcv4/verification/test_p972a_proposal.py -v
PYTHONPATH=src:. .venv/bin/python implementation/phase-9-grcv4/verification/verify_p972a_proposal.py --check
```

Next: bind the closed
policy/construction payload and release applicability in V4 specifications,
followed by the common producer and positive C→A implementation. Aggregate
P9-7.2a, wider G2/G3, generic events and exact GRC9V3 binding remain separate.
