# P9-7.7 C_CI crossing reconciliation

Status: bounded local/crossing reconciliation accepted by the user on 2026-09-14.
The explicit “accept, commit, then do C_CI G2” decision covers the combined
26 local cells and seven crossing dispositions. It does not accept G2.
Original execution flags and evidence identities remain unchanged. This closes
the seven reconciliation rows, not C_CI G2, all-pairs support or aggregate
P9-7.7. Public support remains exactly C_OS/A_OS/A_CI.

The [local product](./P9-7.7-C_CI-LocalProduct.json) retains its original 26-cell
capture and seven remaining-row snapshot. This [crossing record](./P9-7.7-C_CI-Crossings.json)
supplies their subsequent dispositions without rewriting that historical view.
The retained crossing digest is
`0bb8587f208e05121d517670468611c5f0c82f8935886f6e0b2c3b0976beec21`:
three passing methods, five captured cases, no failures/errors/skips.
The exact nomination remains
`grcv4-profile-sha256:a56ef981821478cc50a3551a914dd6240e0dc62c9ca69d52305bd59a3405f69e`.

## Seven obligations

| Obligation | Bounded evidence |
| --- | --- |
| `C-LIFECYCLE-REFERENCE-MAP` | Retained exact incoming C_OS/A_CI_PC migrations, new C_PC return, and a new mapped event whose renamed target edge has its own complete W_C_tr before both roles are numerically admitted. |
| `RESET-AFTER-MIGRATION` | Exact retained incoming/outgoing migrations and new C_PC return/C→A initializer executions; distinct current/reset resources, reset authority and persistent ledger checked. |
| `RESET-AFTER-EVENT` | New affine event maps live/reset resources independently; reset restores the target baseline without changing clock or target charge. |
| `WHOLE-LIFECYCLE-TUPLE-MAP` | New event maps charge/resources, reconstructs C from target authority, retains the transition archive, reproduces exact replay, and rejects missing archival evidence. |
| `ALL-MIGRATION-CLASSES` | Seven explicitly scoped class dispositions below; separate PC/CI+PC pairs are not nomination executions. |
| `HISTORY-DISPOSITION` | Candidate and carrier channels checked separately: retained incoming A_CI_PC drops both histories; C_PC return loses carrier only; outgoing C→A initializes W without loss; C event rederives without W/Z. |
| `TARGET-READMISSION-FAILURE` | Same live C_CI resources and target in a passing current-as-reset control and failing reset-only resource-chart case; failure precedes publication and preserves the whole publication pointer/payload. |

## Ordered scope and reuse

Five aliases refer directly to [P9-7.2a migrations](./P9-7.2a-Migrations.json):

| Migration class | Witness and exact-nomination relationship |
| --- | --- |
| Nonpersistent → nonpersistent, same candidate | `C_NH_NH`: C_OS → nominated C_CI. |
| Nonpersistent → persistent, same candidate | `C_NH_PC`: nominated C_CI → C_PC, zero carrier initialization. |
| Persistent → nonpersistent, same candidate | New `persistent_C_PC`: C_PC → nominated C_CI; distinct nonzero live/reset carriers explicitly lost. |
| PC → CI+PC | `C_PC_CIPC`: separate endpoints, no positive C_CI nomination credit. |
| CI+PC → PC | `C_CIPC_PC`: separate endpoints, no positive C_CI nomination credit. |
| A → C | `A_C_DROP`: A_CI_PC → nominated C_CI; candidate and carrier loss. |
| C → A | New `C_to_A`: nominated C_CI → separately declared initializer-enabled A_OS; independent reference-pass equations for both roles, no source W history to lose. |

Retained aliases preserve original execution identities and only the fields
actually recorded. In particular, a solver disposition absent from the old
migration receipt remains absent; no root outcome is inferred. Existing
acceptances and execution records are unchanged, and none of these aliases is
counted as a new numerical run.

## New execution and scientific boundaries

Three focused methods capture five new cases: the returning carrier and
outgoing initializer migrations, one mapped event, and a readmission
control/rejection pair. These are not five independent test methods.

The event reconstructs a renamed one-edge C_CI target with W_C_tr keyed by
`target-e`, not the source's `e`. Its resource map exchanges the two components
and adds 1/8 to the first, with corresponding charge transport. Both live and
reset target admissions are observed while the old publication is still live.
Every captured target C point is compared against independent selector,
retained-Hodge, potential, baseline, current and read-back equations. Selected
CI roots are also compared with independent 200-digit scalar bisection. There
is no A initializer pair, W history or carrier in the C event.

The renamed target and initializer-enabled A_OS target have distinct exact
profile identities. They are crossing witnesses, not additional supported
profiles. The scalar CI test uses the same bounded zero-deformation stratum;
it is not an arbitrary-topology/parameter sweep or global root theorem. The
previously accepted changed-graph evidence is not relabeled as a new C_CI run.

The retained record includes complete input/output preimages, test/source
identities and typed forensic traces. Optional initializer authority and D10.2
review dispositions keep their original status. Passing this bounded runtime
suite neither rewrites source claims nor grants G2/G3 support.

Development validation passed all three methods before record assembly exposed
a wrong retained-alias lookup in the new harness. The lookup was corrected and
moved ahead of numerical execution. No production source or tolerance changed;
only the subsequently complete capture is cited as retained execution evidence.

## Checking and next step

From the repository root:

```bash
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_c_ci_crossings.py
```

This validates retained evidence; `--run` emits a new numerical execution on
stdout without replacing the retained run. Mutation tests cover endpoint and
source substitutions, erased losses, altered equations/maps, role collapse,
missing cases/aliases, and premature acceptance/all-pairs claims.

API/notebook/browser expose `c_ci_crossings` through the existing shared
bounded-view registry. The evidence table shows 26 local cells plus seven
crossing dispositions, separately from the three accepted G2 declarations.
One checked status is reused across transport/rendering tests.

Next: perform the integrated exact-profile G2 product/applicability/public-facade review.
G2 acceptance remains a separate decision; no new numerical campaign is
needed merely to display or review these retained results.
