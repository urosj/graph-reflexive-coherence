# P9-7.7 — A_RG2b local reconciliation

Status: bounded local/crossing reconciliation accepted by the user on 2026-09-15;
see the [acceptance statement](P9-7.7-A_RG2b-CrossingReview.md).
The execution record is unchanged and G2 remains separate. The original P9-7.1 nomination is
`grcv4-profile-sha256:12abb2946bfaa616df2a42bd571732e2be3000736f5078d45f8dbf53682b212b`.

The local product covers 21 catalog cells: eight common, eight Candidate A,
`RG2B-SECTION`, and four fixed-profile lifecycle cells. The seven crossing cells
are addressed separately in the subsequent
[crossing review](P9-7.7-A_RG2b-CrossingReview.md); this local record is unchanged.
Shared rows are coverage aliases, not independent executions.

## Scientific scope

Use the original four-node, three-edge nomination with nonuniform weights,
nonzero descriptor/current coupling and distinct supplied current/reset C/W.
An independent dense two-transform inverse calculation checks section geometry
for current, reset and restart. Every inverse stays inside the cutoff-one core;
comparison includes the two-transform contraction tail and recorded finite
section error, plus a separately stated binary64 oracle allowance. This is not
an alternative certified evaluator or a parameter/graph sweep.

Independently calculate selected current, pre-read descriptors/W-hat, continuity,
post-continuity writer descriptors and logarithmic W update. Verify one consumed
current, one continuity evaluation and one W writer; reconstructed current/reset/
restart reads are not additional writes. Geometry is reconstructed, never Z
history. Compare generated geometry and restart section against the admitted
invariance error bound. Preserve bounded, frozen-completion-relative Lipschitz
authority: no CI-root interpretation, classical Jacobian/spectrum, indefinite
base-chart invariance, formation authentication or G2 support.

Separate chi-zero and zeta-zero profile declarations recover baseline current
and reference geometry. They do not nominate additional support. Wrong positive
beat rejects in the actual facade. Exhausted budget and out-of-inner-chart reset
controls are construction rejections, not claimed lifecycle executions. Explicit
test-only reset/restart numerical and late-publication faults check complete
rollback and zero/one prospective W writes at the appropriate stage.

Save/load, replay, independent duplication, reset after ordinary steps, ledger
versus emitted delta, and coherently rehashed missing-parent rejection execute
on A_RG2b. No crossing or migration evidence is borrowed into the local product.

## Sources and reproduction

Sources are the [V4 spec](../../../specs/grc-v4-spec.md#reconstructed-geometry-rg2b),
[catalog](../../../specs/grc-v4-conformance-fixtures.json),
[paper](../../investigations/grc9v4-constitutive-design/drafts/2026-09-GRC-V4.md),
and [Tranche 6 graph evidence](../tranche-6/P9-6.4c-Review.md).
Six current forensic traces retain RG2b parent, invariance, contraction,
determinism, claim ceiling and receipt-parent ceiling with their original
classifications/source references. Tests do not promote those classifications.
The five D10.2 traces retain `indeterminate_requires_review`; the P9 parent
ceiling retains its required-support semantics.

The [retained product](P9-7.7-A_RG2b-LocalProduct.json) binds source hashes,
nomination, predecessor review, test roster and content-addressed operation
preimages. Its checker independently recalculates equations and rejects
rehashed scope, numerical, ownership and lifecycle mutations.

From repository root:

```bash
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_a_rg2b_local.py
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python -m unittest test_p977_a_rg2b_local_evidence -v
```

Use `--run` for a deliberately new native execution; stdout is a new record,
not a replacement for the original. Eight exact G2 declarations remain unchanged.

The four native methods passed in 162.639 seconds. The retained run digest is
`669ca8eec0cee1e3621f077bbea723f0861ef4060c6254627b1f84463920f14d`.
Development corrected oracle/test-fixture wiring and a class-replacement fault
double that interfered with a runtime type check. No production law, numerical
tolerance or admission guard was relaxed. The original predecessor review was
checked once for capture; focused surface checks reuse its bound identity and
do not claim another execution of the full status chain.

All 40 rehashed evidence mutations rejected. The focused checker/browser
projection and shared registry/materializer tests passed except for one old
display-test assertion: it incorrectly prohibited *any* remaining crossings
after a different profile had finished. The assertion is now profile-specific,
and checked that A_RG2b's then-pending seven cells remained visible. This is a test
correction, not a display or authority change; its focused rerun is recorded
below. No full status-chain or unrelated numerical campaign was rerun.

The 14-method focused batch took 149.470 seconds (13 passed; the display-test
assertion above was the sole failure). Its corrected isolated rerun passed in
0.012 seconds. The real A_RG2b checker/registry view, shared browser render and
nine browser rejection controls were included in the passing focused tests.
