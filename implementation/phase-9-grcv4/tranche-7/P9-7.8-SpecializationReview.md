# P9-7.8 — specialization admission review

Status: **reviewed; recommended for acceptance**. User acceptance, G3 admission
and tranche-7 closure remain pending. No specialization implementation starts
in this review.

## Decision and exact scope

Recommend `P9-G3[S]` for the ten exact accepted generic declarations in
[the checked review](./P9-7.8-SpecializationReview.json), not just the historical
optional `C_OS` singleton. Each nomination retains its complete-profile ID,
accepted parameters/domains, review and acceptance identities. This is a
proposed consumed **generic** set, not ten implemented combined GRC9V4 models.
A nine-port graph, fixed-row backend or event target changes identity and must
receive its own concrete target admission and applicable runtime evidence.
No arbitrary parameterization, all-pairs migration, or initializer/event target
is promoted into advertised support.

The predecessor is [accepted P9-7.7](./P9-7.7-AggregateAcceptance.md) at
`25b80f4`: ten accepted G2 decisions and 305 reconciled catalog cells. The
original HOLD and all accepted numerical records remain unchanged. This review
checks identities and obligations; it does not rerun or add numerical credit.

## Authority and mechanics

The primary normative source is [the GRC9V4 specification](../../../specs/grc-9-v4-spec.md),
with the [generic specification](../../../specs/grc-v4-spec.md),
[interface extension](../../../specs/grc-common-interface-v4-ext.md),
[paper](../../investigations/grc9v4-constitutive-design/drafts/2026-09-GRC-V4.md)
and typed investigation provenance. The review binds their exact bytes.

All 73 specialization contracts are queried through `contract_provenance`:
13 D10.2 parents, 20 D11-G9 contracts and 40 disabled-profile contracts.
The record preserves source references, edge identities/support semantics,
classification and original trace digest. These are compact projections, not
new forensic traces. D10.2 `indeterminate_requires_review` remains unchanged;
D11-G9 `accepted_bounded_GRC9V4_successor` supplies bounded design authority,
not completed runtime conformance. The dihedral-covariance and lifecycle
contracts explicitly retain their distinct design-level/runtime-pending
dispositions; the legacy-domain contract explicitly excludes a GRC9/V3 rewrite.
They are not flattened into the general D11 successor label.
RG2b C1 debt and every accepted generic
scope/endpoint restriction remain attached to their original reviews.

| Forward owner | Required evidence before its own closure |
| --- | --- |
| P9-8.1a–d | Ordered ports/chart; independent fixed-row differential and stage-specific weight bridge; saturation **and** semantic-degeneracy trigger; actual coarse-graining/Split. |
| P9-8.2 | Reserve-first D11-G9-P4a allocator, chiral same-port spine, conditional phase, deterministic recursive tree/capacity and stable IDs. No caller-supplied target allocation. |
| P9-8.3C-OS / C-PC | Distinct C rederivation/absent-carrier and nonnull whole-carrier-reset cases; complete target reference maps, independent current/reset resources and target readmission. |
| P9-8.3A.1 | Select/validate the specialization A-history/initialization binding and construct an independent numerical oracle from accepted contracts, outside the frozen release. Exact A G2/G3 entry; no production-runtime prerequisite. |
| P9-8.3A.2 | After accepted `.1` and applicable P9-8.1a–c/P9-8.2 implementation, implement/test expansion against the pinned oracle for the same exact A scope. Verify current/reset targets, W/Z channels, readmission, receipts, rollback and replay. |
| P9-8.4–8.6 | Every D30/D31/D45/D52 chirality/phase vector, covariance, occupancy, charge, atomic rollback and deep recursive runtime evidence. D37/D44 probes are additional, not replacements. |
| P9-9.1b | Mandatory specialization lifecycle, independently of optional completion: separate beat/event transactions, readmission, receipt ownership, reset/replay and duplication. |
| P9-9.2–9.4 | Forty independent disabled surfaces and both branch-crossing directions, each with exact delegate/input/oracle/result; no inferred rows or columns. |
| P9-9.5–9.6 | Legacy regressions and full applicable specialization review, not an enabled-only conformance claim. |

The machine-readable matrix links every retained expansion, metamorphic and
G9 atomic-failure vector to its exact repository JSON pointer. Seventeen
expansion and three metamorphic vectors are **preimplementation oracles**.
P9-8.3A.1 owns production of the missing A expansion oracle; it is not an
unassigned external prerequisite. It must bind concrete source/target identities,
old-edge history and new-edge initialization, fixed-row/incoming-reference-current
recipes, distinct current/reset resources, independent expected W and receipt/
readmission results, and negative rollback cases. Persistent scope needs its
separate whole-carrier policy. Generic reference-pass/mapped-event acceptance
does not supply this specialization oracle, and production output may not
serve as its own expected result. The parent P9-8.3A closes only after both
children are accepted for the same exact A scope.

If oracle construction exposes genuinely absent graph-generic authority,
hold the affected child and return the exact missing contract to a bounded
Tranche 7 correction through the established authority/paper/spec/runtime
propagation process. Do not invent a specialization workaround or reopen
unrelated claims. Missing specialization evidence alone does not invalidate
accepted generic G2. Neither child blocks independent C work.
Other exact profiles
still need their own applicable specialization target/lifecycle evidence;
C_OS/C_PC vectors are not blanket certification of the C family.

## Legacy and optional-capability boundary

Use the five V4-owned module/test surfaces from the accepted
[ownership review](../tranche-1/P9-1.5-OwnershipAndLegacyBaseline.json).
GRC9/GRC9V3 code and tests stay unchanged. The review checks their retained
legacy bindings; disabled execution delegates to the unchanged GRC9V3 model.

Exact disabled compatibility is restricted to `D_V3,defined`. The saturated
port-5 conflict must return `legacy_expansion_target_undefined` in the V4
wrapper with the entire lifecycle unchanged; never repair V3 or run the
enabled V4 allocator and relabel its result legacy-compatible. Current/reset
resource transport, candidate/carrier losses and branch snapshots must be
checked independently in both migration directions.

No optional stabilization, completed-spark or hierarchy capability, and no
stronger handoff requirement, is selected here. P9-9.1a remains visibly
unselected. This does not waive triggers, expansion mechanics, charge,
mandatory lifecycle or the four disabled surfaces for full conformance.

## Closure and next step

After acceptance, record the exact G3 consumed set and tranche-7 closure
without changing historical G2 evidence. Wire that accepted decision into a
bounded successor of the execution policy before any specialization source
edit: the current policy deliberately still rejects Tranches 8/9. Do not
simply remove that guard or infer permission from the review status.

Then begin P9-8.1a (fixed chart/port graph), not A expansion or optional
completion. Later leaves retain their own prerequisites and review gates.
This review adds no `src/` or runtime `tests/` changes.

## Recheck

From the repository root, using `.venv`:

```bash
.venv/bin/python implementation/phase-9-grcv4/verification/verify_p978_specialization_review.py
PYTHONPATH=implementation/phase-9-grcv4/verification .venv/bin/python -m unittest test_p978_specialization_review
```

The checker rebuilds the source-exact review, predecessor and trusted G2
bindings, forensic queries, forward matrix and browser projection. It checks
retained evidence integrity, not new specialization execution. The normal
Phase 9 API/notebook/browser status exposes the same checked pending review
and clears it on a failed current boundary.

Validation: six focused Python tests passed (including twenty-three rehashed
mutations), twelve neighboring aggregate/materializer regressions passed,
and 42 browser tests passed (38 shared, two aggregate, two specialization).
The current implementation boundary and diff hygiene checks passed. API
materializer import/binding, actual notebook query-cell transport/cleanup and
HTTP asset delivery are covered without a full numerical rerun.

Development corrections preserved Python JSON numeric types when retaining
the review and replaced an overbroad test expectation with the three exact
D11 covariance/lifecycle/legacy-domain dispositions. Neither change altered
accepted inputs, scientific evidence or admission criteria.
