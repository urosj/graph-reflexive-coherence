# P9-7.2b representation wire completion and joint package

The user authorized completion of the representation wire contract followed
by joint packaging, after accepting the operation scope at `8594d9d`.
This record describes contract/declaration work, **not runtime execution or
aggregate P9-7.2b acceptance**. No new constitutive law is selected.

The [representation wire supplement](../../../specs/grc-v4-representation-transport-spec.md)
defines complete ordered bijections, explicit signs and loop/parallel-edge
correspondence, request/operation/receipt identities and a separate mixed-version
snapshot layout. It preserves coordinate-sensitive IDs and verifies declared
equivalence rather than equating different serializations or rewriting history.
Its fixed coordinate law comes from the accepted parent contract; new wire
definitions do not imply executable covariance or arbitrary backend support.

The [joint release](../../../specs/grc-v4-event-contract-release.json) binds
both operation schemas, synthetic vectors, source documents and the inherited
initializer policy. The package decoder uses installed resources and explicit
release/manifest/schema pins. It admits wire shapes and checks graph-map
declarations only. It is not wired into model lifecycle restoration and cannot
execute, publish or advertise either operation.

The accepted fallback schema and its pair, policy cases and receipt examples
stay byte/semantically exact; only document-source bindings evolve. Earlier
base/initializer releases, codec, archived runs and 7.2a acceptance are unchanged.
The retained-run verifier explicitly distinguishes the new decoder's source
from the older captured execution, which never imported it. It does not turn
packaging into a new initializer run or conceal unrelated source changes.

Focused checks cover offline inherited-schema resolution; identity preimages;
map order, coverage, duplicate targets, missing endpoints, signs, loops and
parallel edges; disjoint fallback/representation receipts and initializer rules;
mixed-version wire archives; unknown releases; missing/altered assets; exact
old-loader rejection; and package lookup from a fresh process outside the
repository working directory. These are contract checks, not actual current/
reset transport, numerical readmission, rollback or historical attestation.

## Pre-acceptance package audit corrections

The independent package audit passed the policy/declaration architecture but
identified two receipt-contract ambiguities, not executed lifecycle defects.
R1 now explicitly selects exact coordinate-action zero delta for this new
receipt only; the four embedded current/reset source/target charge evaluations
retain the existing ordered-tree admission and can differ numerically. R2
selects exactly one representation primary, with embedded charge/history
evidence, a complete commit/envelope vector and previous-primary head rules.
No dummy map hashes, legacy auxiliary-core relaxation or old receipt
reinterpretation is introduced. The user accepted the corrected contract
package on 2026-09-12 through “commit changes”. This accepts the wire/package
work and R1/R2 corrections, not runtime execution or aggregate P9-7.2b closure.

The durable audit substance is converted into focused repository tests:
the unit-scale charge permutation witness (including zero-tolerance rejection),
complete charge evidence and signed-zero rejection, singleton cardinality,
canonical IDs/backlinks, coherent endpoint mismatch and parent-head controls.
The standalone attachment checker used partial inherited-schema bridges;
its positive results are not recorded here as full installed-package or
runtime validation. Native focused tests use the actual pinned inherited
resources. No attachment staging paths or duplicate audit bundle are needed.

Commands from repository root:

```sh
PYTHONPATH=src:. .venv/bin/python implementation/phase-9-grcv4/verification/test_p972b_event_release.py JointPackageTests -v
PYTHONPATH=src:. .venv/bin/python implementation/phase-9-grcv4/verification/build_p972b_event_release.py --check
```

Next: instantiate exact reconstruction and representation execution children,
integrate operation-specific lifecycle owners and archive/release dispatch,
then test independent current/reset transport, target readmission, inverse/
covariance, no-loss versus explicit-loss behavior, F1/F2 failures, publication
rollback and mixed migration/event/evolution/restore/reset paths. The wire
archive examples do not discharge those runtime paths. Preserve existing
finite accepted C_OS evidence, and do not widen G2/G3 through packaging.
General topology-history transport remains deferred.

Carry the audit's runtime pressure forward explicitly: inverse transport must
recover scientific/reset/reference preimages while retaining two successful
groups (not a restored old ledger); identity representation preserves nontrivial
histories while identity reconstruction loses/reinitializes them. The mixed
sequence must include preserving migration → representation → evolution →
lossy event → restore/reset, with operation-specific pairs and historical IDs
intact. Include charge-order/signed-zero controls and incompatible W_C,tr,
K4, backend, context and chart targets even with valid graph declarations.
These remain runtime obligations, not discharged by the synthetic group.
