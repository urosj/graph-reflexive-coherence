# P9-7.2a — initializer source and tool admission

The user accepted the clarified
[target-only reference-pass design](../../investigations/grc9v4-constitutive-design/decisions/P9CandidateAInitializerReferencePassProposal.md)
by requesting commit and continuation on 2026-09-11. Commit `49b83ba` records
that decision. This continuation admits its structured authority and exposes it
through existing tools; it does not implement the producer or C→A migration.

## Source and claim boundary

The [authority record](../../investigations/grc9v4-constitutive-design/decisions/P9CandidateAInitializerReferencePassAuthority.json)
binds the exact accepted design bytes and the four D10.2 initializer/migration/
reset/atomicity provenance contracts. It selects policy
`grcv4-a-target-reference-pass-v1` and numerical recipe
`grcv4-a-reference-pass-binary64-v1`. The ordering is new bounded authority,
not an inference that D10 already specified the producer.

The [admission](../../investigations/grc9v4-constitutive-design/tools/exploratory-side-tool/records/P972aInitializerAdmission.json)
pins source, design, predecessor bundle and graph identities. Six nodes are
appended: source, optional claim `P9-7.2a-CL-O-INIT-001`, debt
`P9-7.2a-DEBT-A-INITIALIZER-SOURCE`, object
`P9-O-A-INITIALIZER-REFERENCE-PASS`, contract
`P9-EC-A-INITIALIZER-REFERENCE-PASS`, and forward obligation
`P9-7.2a-VO-A-INITIALIZER-INTEGRATION`. Historical nodes and edges are unchanged.
Required contract support is design authority, not runtime evidence. The debt
trace has a distinct forward-verification row; reconstruction excludes forward
obligations from backward evidence.

| Obligation | Current status |
| --- | --- |
| Reference-current producer choice | Resolved by the accepted design |
| Structured source and forensic tool admission | Implemented and focused checks passed |
| Reviewed proposal/paper/spec propagation and closed payload binding | Pending |
| Producer implementation and positive C→A through all five A realizations | Pending |
| Aggregate P9-7.2a | Pending evidence and reconciliation |
| Additional G2/G3 or specialization support | None |

The current adapter validates the exact full source inventory. Changed, missing,
unknown or coherently rehashed sources/admissions hold; there is no current-to-
historical fallback. Explicit historical abundance/parent/D11 queries remain
available with their original identities. Existing current-loader imports
delegate to the new initializer successor.

## Visible access and focused checks

`initializer_authority` returns source-exact typed claim/debt/object/contract
traces. The real `/api/a-initializer` handler, executable
`a-initializer-authority` notebook cell, and browser's **Load initializer
authority** button with four selectors expose that same projection. Failure
clears stale values; a delayed older response cannot repopulate held output.
Parent and abundance views retain their source policies under the new context.

The seven focused methods in
[`test_p972a_initializer.py`](../../investigations/grc9v4-constitutive-design/tools/exploratory-side-tool/tool/scripts/test_p972a_initializer.py)
cover append-only/classification/forward-debt semantics, coherent source and
admission rewrites, changed design and unknown discovery, historical-evidence
relabels, actual notebook/HTTP-handler identity, browser-validator overclaims,
and desktop/mobile controls including refresh races. Browser transport is
intercepted in that focused test; the actual HTTP handler is tested separately.
Chromium required execution outside the sandbox; the initial launch denial was
an environment restriction, not a scientific failure. Both existing five-method
parent and abundance suites also pass. No numerical campaign was rerun.

The current check is:

```sh
PYTHONPATH=src:. .venv/bin/python implementation/phase-9-grcv4/verification/verify_p972a_initializer_authority.py --check
```

The normal tool verification and existing migration `--check` route to it.
It checks current design-only authority and the maintenance boundary while
retaining the unchanged 25/17-test records. Their exact sources are recovered
from Git at `49b83ba` plus existing original-source correction spans. The 7.1
predecessor boundary is retained with batched Git reads. A new source admission
does not relabel negative C→A evidence or replace an accepted execution with a
rerun. No new archive or runtime permission is introduced.

## Next content step

Update **GRCV4-proposal §12.6** using these typed traces and obtain its review
before paper → V4 specification → implementation. The accepted design document
and historical execution records remain byte-identical. GRCV4-proposal, paper,
specs, runtime source and repository runtime tests were not changed by this
source/tool admission. The numerical and lifecycle regressions in the forward
obligation remain tasks for the actual producer, not evidence supplied here.
