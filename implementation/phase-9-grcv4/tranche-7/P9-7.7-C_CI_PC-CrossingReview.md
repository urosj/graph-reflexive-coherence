# P9-7.7 — C_CI+PC crossing reconciliation

Status: bounded local/crossing reconciliation accepted by the user on 2026-09-14.
The explicit “accept, commit then complete g2” decision accepts all 26 local
cells and seven crossing dispositions. Original execution records and their
flags remain unchanged. This does not accept C_CI+PC G2, all-pairs, aggregate
P9-7.7 or G3. The seven accepted exact G2 declarations are unchanged.

The [local product](P9-7.7-C_CI_PC-LocalReview.md) covers 26 cells. The
[crossing record](P9-7.7-C_CI_PC-Crossings.json) addresses the remaining seven:
the C lifecycle reference map, reset after migration/event, whole-lifecycle
tuple mapping, migration classes, history disposition and target-readmission
failure. The exact nomination is
`grcv4-profile-sha256:3a7a084788c59a55b4232fb98e9c5529c1aa4c7c71074c9d3288982fa2fd2a5b`.
Class reconciliation does not mean all ordered pairs have executed.

## Ordered evidence

Three retained aliases preserve original record identities, ordered endpoints,
two-role maps and emitted receipt deltas. Only the two PC pairs involve the
nomination; the nonpersistent pair remains a separate class witness.

| Class | Evidence and boundary |
| --- | --- |
| Nonpersistent → nonpersistent | Retained C_OS → C_CI; no nomination execution credit. |
| Nonpersistent → persistent | New C_CI → exact C_CI+PC; rederive C, initialize zero Z, no history loss. |
| Persistent → nonpersistent | New nomination → C_CI; rederive C, explicitly lose Z. |
| PC → CI+PC | Retained C_PC → nomination; exact carrier-contract preservation. |
| CI+PC → PC | Reverse retained nomination → C_PC; changed `tau_PC` must reject rather than silently reset. |
| A → C | New A_CI+PC → nomination; explicit W loss and whole-carrier reset/loss. C has no W slot. |
| C → A | New nomination → separate initializer-selected A_CI+PC target; independently initialize W, reset/lose Z, no candidate-history loss. This is not the accepted A_CI+PC nomination. |

The preserved PC pair has distinct nonzero live/reset carriers. Its exact
graph/K4/weights/context/common/candidate/geometry/carrier identity is checked
in both directions. Retained migration receipts contain no solver disposition;
none is invented. Removal/reset of historical Z is not native exponential release.

## New native pressure

Four methods produce eight cases:

- Four migrations verify independent current/reset C/W/Z maps, full target
  readmission before publication, exact replay and subsequent reset. C targets
  have a full dense-oracle reconstruction chain at their selected implicit root;
  the separate A target uses the accepted rational reference-pass W oracle and
  an independent coupled-root calculation. Candidate and carrier losses are
  distinct, with no loss invented for fresh initialization.
- A renamed-edge C_CI+PC event swaps resources and adds `(-1/8, 0)`.
  `W_C_tr` is rebuilt for the target edge, both roles rederive C, and the selected
  policy resets Z with explicit carrier loss. Independent coupled equations and
  the full C chain verify that zero Z does not erase immediate geometry. The
  negative increment keeps both role sources measurably nonzero without altering
  the target's constitutive parameters or solver tolerance. Charge changes by
  -1/8; time does not. Missing archives reject, and replay/duplicate/reset retain
  the complete lifecycle ownership and persistent ledger.
- Nomination → C_PC with changed `tau_PC` rejects at exact-contract admission,
  preserving the entire owned publication. No implicit lossy fallback is allowed.
- A current-as-reset control passes a separate C_CI+PC target chart of radius 3.
  With the same current and target, source-admissible reset `(3, 1)` alone fails
  target readmission. Both source roles fit radius 4. Publication is never called
  on that failure, and the complete scientific/lifecycle snapshot is unchanged.

These are fixed declared endpoints and a renamed one-edge event, not arbitrary
graph, parameter, migration-pair or lossless-lineage coverage.

## Authority, integrity and access

Sources are the [V4 specification](../../../specs/grc-v4-spec.md),
[catalog](../../../specs/grc-v4-conformance-fixtures.json),
[common-interface extension](../../../specs/grc-common-interface-v4-ext.md),
[paper](../../investigations/grc9v4-constitutive-design/drafts/2026-09-GRC-V4.md),
and retained migration evidence. The typed forensic API retains 14 traces:
the local C composition/contraction/carrier/baseline/receipt boundaries plus the
accepted reference-pass initializer. Their classifications and support ceilings
remain unchanged; numerical execution does not promote claim authority.

Exact sources, predecessor identity, test roster, content-addressed snapshots,
requests/results, independent equations, loss channels and scope are checked.
One `c_ci_pc_crossings` materializer depends on `c_ci_pc_local_product`; generic
API/notebook/browser projections need no new dispatch/rendering branch.

The four native methods/eight cases passed in 206.518 seconds. The capture digest is
`c15ae17c287fe89aab2f41cff778fa7a086d88f1e9713ed04e9564dacf173c5b`.
The record retains 63 content-addressed objects. All 55 rehashed mutations
rejected (123.224 seconds), and 12 shared registry/materializer tests passed
(9.685 seconds). One actual status check, reused across notebook/API/browser,
passed in 725.457 seconds with 162 browser rejection controls. It
checks retained evidence and does not rerun the earlier numerical campaigns.
At zero target Z, the event's current/reset geometry shifts are approximately
`2.3413e-8` and `1.0406e-8`; both are discriminated by the independent
coupled-root comparison at absolute tolerance `2e-11`.

An initial fixture check used a mandatory `initializer_pair` key for a migration
schema that legitimately omits it when no initializer runs. The fixture was
corrected to test absence/null; no production behavior or criteria were relaxed.

From repository root:

```bash
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_c_ci_pc_crossings.py
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python -m unittest test_p977_c_ci_pc_crossing_evidence -v
```

Default verification checks retained evidence. `--run` emits a new native record
to stdout; it does not replace the original run. Shared status checks full
ancestry once and reuses that result across transports. The next step after
this bounded acceptance is the separate integrated G2 review and decision.
