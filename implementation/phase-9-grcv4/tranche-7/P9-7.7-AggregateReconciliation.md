# P9-7.7 — Aggregate reconciliation of accepted G2 declarations

Status: **305/305 catalog cells reconciled; aggregate review and acceptance
pending.** All ten exact child G2 declarations are already accepted. This
successor adds no execution credit, public support, runtime permission or G3
admission. P9-7.8 is not performed here.

## Result

The [reconciliation record](P9-7.7-AggregateReconciliation.json) joins the
[original 305-cell review](P9-7.7-ProfileReview.json) to the ten accepted child
decisions at checkpoint `ffbcabf`. Every nomination matches its original exact
profile declaration; there is no substitution by family name. It checks every
cell pointer against the accepted child product and its retained evidence,
including control identities and actual source/target identities.

| Family | Cells | Accepted decision |
| --- | ---: | --- |
| C_OS | 33 | [P9-4.8B alias](../tranche-4/P9-4.8B-G2Acceptance.json) |
| A_OS | 28 | [G2](P9-7.7-A_OS-G2Acceptance.json) |
| A_CI | 28 | [G2](P9-7.7-A_CI-G2Acceptance.json) |
| C_CI | 33 | [G2](P9-7.7-C_CI-G2Acceptance.json) |
| A_PC | 28 | [G2](P9-7.7-A_PC-G2Acceptance.json) |
| C_PC | 33 | [G2](P9-7.7-C_PC-G2Acceptance.json) |
| A_CI+PC | 28 | [G2](P9-7.7-A_CI_PC-G2Acceptance.json) |
| C_CI+PC | 33 | [G2](P9-7.7-C_CI_PC-G2Acceptance.json) |
| A_RG2b | 28 | [G2](P9-7.7-A_RG2b-G2Acceptance.json) |
| C_RG2b | 33 | [G2](P9-7.7-C_RG2b-G2Acceptance.json) |
| **Total** | **305** | **Ten exact declarations** |

The original 33 C_OS aliases retain the P9-4.8 HOLD → P9-4.8B successor →
acceptance lineage. They are not rerun or credited twice. The other 272 cells
now resolve through their separately accepted products. Each of the nine child
reviews satisfies G2-EXACT-PRODUCT, G2-ORDERED-ENDPOINTS and
G2-INTEGRATED-REVIEW; the corresponding C_OS obligations retain their original
accepted schema and scope. There are no unresolved child catalog obligations.

The initial HOLD JSON, its review text, all child review/acceptance records and
all execution evidence remain unchanged. The initial `272 unresolved` value
is historical, not the current aggregate result. Its status field remains
available as `profile_conformance_review`; the checked successor is
`profile_aggregate_reconciliation` in the same API, notebook and browser status.

## Scope that is preserved, not closed by counting cells

- All seven migration classes retain the child's actual disposition: positive,
  negative, separate endpoints or other explicitly bounded evidence. Ten
  accepted endpoints do not establish every ordered pair. C_OS's earlier
  unavailable-target boundaries remain historical evidence, not claims about
  the newly expanded discovery set.
- Controls, scalar witnesses, initializer-selected targets and mapped-event
  targets keep their own exact identities. Acceptance of a nominal A profile
  does not silently select the reference-pass history policy for that profile.
  The shared reference-pass producer applies to all five A realizations; it
  does not replace CI's root or RG2b's section reconstruction.
- Candidate A supplied history is not authenticated formation. Candidate C
  retains its reference/mobility/baseline distinction and has no independent W_A.
  Carrier loss, initialization, exact transport and native release stay distinct.
- CI and CI+PC retain their local root/certificate and same-source boundaries;
  PC contraction or indefinite chart invariance is not inferred. RG2b retains
  its fixed completion/beat, Lipschitz-only section and K versus K-minus
  distinction. The C fixed-stratum derivative does not close the RG C1 debt.
- Each row links its accepted forensic authority/debt material. This successor
  neither reclassifies claims nor treats forward obligations as executed proof.
  Unrelated debts keep their existing owners; GRC9 specialization, stronger
  regularity and optional completion campaigns are not silently accepted.

## Verification and next decision

The checker uses the existing trusted acceptance adapters and discovery check,
verifies input bytes against the accepted checkpoint, and checks catalog,
nomination, result-pointer and ordered-class correspondence. It does not rerun
the numerical suites or replace the existing per-child scientific checkers.
The normal status path adds this reconciliation after those child checkers have
completed; no partial aggregate view is published on failure.

From the repository root:

```bash
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_aggregate.py
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python -m unittest test_p977_aggregate test_profile_materializers -v
```

Focused validation covers ten Python aggregate/materializer methods, eighteen
rehashed aggregate mutations and two browser tests with fourteen rejection
controls. The real HTTP asset route, bounded HTTP/notebook transport and
Python-to-browser projection agree. The transport test initially omitted the
notebook's `current_boundary` fixture field; that test-only fixture was corrected
and the affected method passed on its targeted rerun. No numerical campaign or
full status/predecessor campaign was run. The standalone aggregate check and
phase boundary pass.

The shared `phase9-web/verification.test.mjs` fixture-maintenance gap is now
fixed as part of P9-7.7. The initial run found 23/35 failures, also present at
`ffbcabf`: the old singleton fixture failed the newer registry validator before
reaching its intended assertions. The corrected synthetic fixture supplies the
current pinned profile views, separate historical C_OS alias, initializer/event
acceptance and permission shape, and aggregate projection. Held-state fixtures
explicitly remove current evidence without erasing historical G1 acceptance.
No production validator was relaxed and no accepted execution was rewritten.

All original 35 browser tests now pass. Three added regressions check detached
fixture ownership, each profile's missing/altered acceptance and product views,
targeted rejection from a valid starting payload, and complete held-state
cleanup. The shared suite passes **38/38**, alongside **2/2** aggregate browser
tests, using the managed Node toolchain. These are validation/UX tests, not new
scientific execution credit or aggregate acceptance.

Independent aggregate review and user acceptance remain the only P9-7.7
closure decision. P9-7.8 then reviews `P9-G3[S]` for an explicitly consumed
support set and its specialization boundaries; it is not implied by this record.
