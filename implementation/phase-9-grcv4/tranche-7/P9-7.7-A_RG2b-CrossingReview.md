# P9-7.7 — A_RG2b crossing reconciliation

Status: bounded local/crossing reconciliation accepted by the user on 2026-09-15.
The explicit “accept, commit, then continue with g2” decision accepts the
21 local cells and seven bounded crossing dispositions. Original execution
records and flags remain unchanged. This does not accept G2, all-pairs,
aggregate P9-7.7 or G3; the eight accepted G2 declarations remain unchanged.
The original four-node, three-edge nomination remains
`grcv4-profile-sha256:12abb2946bfaa616df2a42bd571732e2be3000736f5078d45f8dbf53682b212b`.

Together with the [21-cell local product](P9-7.7-A_RG2b-LocalReview.md), this
reconciles the seven remaining catalog obligations at explicitly bounded
endpoint scope. It is not all-pairs coverage or a waiver of future G2 review.

## Exact endpoints, not family labels

The old `A_PC_NH` migration targeted a *scalar* A_RG2b declaration, not this
graph nomination. Retain it as a separate witness; do not relabel it as graph
coverage. The two retained PC↔CI+PC cases and the old positive initializer
case also have separate endpoints. Their original records, receipt identities
and missing historical solver-disposition fields remain unchanged.

Three native methods supply nine cases:

| Case | Bounded evidence |
| --- | --- |
| Graph A_OS→nomination | Preserve C and W independently for current/reset; no carrier. |
| Nomination→graph A_PC | Preserve C/W, initialize zero Z in both roles without inventing loss. |
| Graph A_PC→nomination | Preserve C/W, explicitly lose distinct nonzero current/reset Z. |
| Nomination→graph C_OS | Preserve C, explicitly lose W, no carrier; independently check the C baseline/current. |
| Renamed-graph A event | Reverse the four resources and add 1/128 to the first target resource; independently initialize both W roles, record W loss, reconstruct the target section, then reset/restore. |
| Current-as-reset control | Incoming A_OS→nomination succeeds with the same live authority and target as the rejection case. |
| Reset-only K rejection | Reset W=2.3125 is outside the target core chart; reject at target readmission before publication. |
| K but not K-minus | Reset W=2.1875 is admitted as state and can be reset/restored, but ordinary entry at zero or the frozen positive beat rejects without mutation. |
| Incoming C→nomination rejection | The nomination does not select the initializer. A well-formed initializer declaration cannot silently change that choice. |

Each companion is explicitly declared on the same source graph, not a new
publicly supported profile. The event uses renamed graph elements, the accepted
reference-pass history policy, and a *separate* frozen completion whose W chart
is centered on one. Its initializer outputs fit that chart; the nomination's
supplied-W chart centered on two is not altered or bypassed. Neither event
target nor the historical scalar initializer target receives local nomination
credit or G2 support.

The complete companion/target identities are retained with their full
declarations in the record, not inferred from a family label:

| Declaration | `grcv4-profile-sha256:` suffix |
| --- | --- |
| Graph A_OS companion | `114582ee070c38df4e5e94bcf04727da53bdb4957764b1f4ed4515cdaae8cf00` |
| Graph A_PC companion | `81a400db5e93c6382b81647b8fb265e070582aa96351a09ccb34877c1f83fcd6` |
| Graph C_OS companion | `1a5579c1f6fa550f0629ba2cc0373d7ab147f1ca8d72a1bfcb79da1586fa4c82` |
| Separate renamed initializer/event target | `f5109dde24eef932707a6c79d04af89ea338c6ec343e954132221adc6243370e` |

## Seven catalog obligations

`A-MIGRATION-HISTORY-RECEIPT`, `RESET-AFTER-MIGRATION`, `RESET-AFTER-EVENT`,
`WHOLE-LIFECYCLE-TUPLE-MAP`, `ALL-MIGRATION-CLASSES`, `HISTORY-DISPOSITION`, and
`TARGET-READMISSION-FAILURE` have explicit evidence references in the
[crossing record](P9-7.7-A_RG2b-Crossings.json).

The seven-class matrix uses exact incoming/outgoing graph witnesses for NP→NP,
NP→P, P→NP and A→C. PC↔CI+PC stays a separate pair, outside the nomination's
nonpersistent realization. C→A has a negative exact nomination target and a
separate retained positive initializer target. Integrated G2 review must assess
these dispositions; this record neither upgrades the nomination to an
initializer-enabled profile nor claims all publicly supported ordered pairs.

## Scientific and lifecycle checks

Before publication, observe actual target reconstruction for both roles.
Compare selected current with independent dense equations. For RG2b, compare
each reconstructed section with independent two-transform inverse equations,
the contraction tail and finite error bound. Crucially, readmission has dt=0
but the section oracle uses the *target completion's frozen positive beat*.
No ordinary continuity or W writer is credited to a crossing. No section value
is persistent history, and no classical derivative/spectrum or indefinite
base-chart invariance is inferred.

Migration receipts keep candidate and carrier dispositions separate. Reset
preserves charge, clock, target reference, archives and earlier ledger entries.
Migrations replay exactly; event duplication, reset/restore and missing archive
rejection are checked. Failure emits a failure receipt without appending it to
the persistent ledger or replacing the owned publication.

## Sources and reproduction

Authority comes from the [V4 spec](../../../specs/grc-v4-spec.md),
[event spec](../../../specs/grc-v4-topology-event-spec.md),
[initializer spec](../../../specs/grc-v4-a-initializer-spec.md),
[paper](../../investigations/grc9v4-constitutive-design/drafts/2026-09-GRC-V4.md),
and current typed forensic traces. Preserve the six local traces plus the
initializer and D11-C baseline provenance; D10.2's indeterminate support is not
promoted by numerical success.

The record contains source hashes, complete object preimages, target identities,
ordered endpoints, per-role numerical observations and exact retained aliases.
The local product is checked against its registry pin and current equations/
sources; its already checked predecessor chain and native local campaign are
not rerun. All artifact references are repository-relative.

```bash
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_a_rg2b_crossings.py
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python -m unittest test_p977_a_rg2b_crossing_evidence -v
```

`--run` emits a distinct new native record, never overwriting this one.
The registry's `a_rg2b_crossings` materializer uses the existing generic
API/notebook/browser projection. Eight accepted G2 declarations, runtime
permissions, G3 and aggregate P9-7.7 are unchanged.

Development corrected the test's Candidate C baseline field access and two
retained-receipt projection details. The first fully passing run (three methods,
203.895 seconds) exceeded the output channel's record-size limit, so capture
was repeated with compact transport. The retained record is that distinct new
run, not a reconstructed or relabeled copy of the truncated output. No production
law, admission guard or numerical tolerance was changed.

The retained run passed all three methods (nine cases) in 203.132 seconds.
Its record digest is
`73d4c7823d18e71464e61f377a1cb8a93c9654c47cd5b2ff4727d20b0b7093a2`.

The focused evidence/registry/materializer batch passed all 14 methods in
91.499 seconds, including all 52 rehashed evidence mutation controls and
11 browser rejection controls. It exercised the actual maintenance-bound
crossing checker, generic browser rendering, pinned dispatch/dependency order,
cleanup and the unchanged eight-profile discovery roster. The display test
checks both the new seven-cells-reconciled state and a synthetic earlier
local-only state. These projection controls are not new numerical execution
credit. No full status-chain or unrelated native campaign was rerun.
