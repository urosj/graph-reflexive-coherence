# P9-G2[A_PC] integrated review

Verdict: **PASS proposal for the exact declared scope**, pending explicit G2
acceptance. This is an integrated review, not an independent audit or a G2
decision. The user accepted the bounded local/crossing result first, committed
as `4a0d1e8`. Public support remains exactly C_OS/A_OS/A_CI/C_CI.

Proposed complete profile:
`grcv4-profile-sha256:058ae6b1f923c85952ffdfa083af74e3b56dd450f309190f307c3ea56ac2aa75`.

The [machine-readable review](./P9-7.7-A_PC-G2Review.json) binds the exact
nomination, current source/release identities, accepted bounded reconciliation,
all 28 catalog cells, ordered applicability, facade supplement and typed
contract/debt traces. Four local methods and three crossing methods are reused,
not rerun or presented as a second independent numerical campaign.

## Three G2 obligations

| Obligation | Integrated result |
| --- | --- |
| `G2-EXACT-PRODUCT` | 28 unique catalog IDs resolve to 21 local rows and seven crossing dispositions. Zero controls are distinct exact declarations; provisional-stage observations are not committed operations. |
| `G2-ORDERED-ENDPOINTS` | Exact PC↔CI+PC pairs preserve both histories only under identical carrier contracts. Candidate changes, carrier loss/reset, unselected initializers and separate nonhistory endpoints retain their individual dispositions. |
| `G2-INTEGRATED-REVIEW` | Specs, paper, forensic contract/debt traces, native lifecycle evidence, independent equations and the narrow public facade agree for this declaration. Passing does not promote scientific authority classes or forward verification obligations. |

Sources are the [V4 specification](../../../specs/grc-v4-spec.md), the
[common-interface extension](../../../specs/grc-common-interface-v4-ext.md),
unchanged common interface, initializer/event specifications and the
[substrate paper](../../investigations/grc9v4-constitutive-design/drafts/2026-09-GRC-V4.md).
The [local review](./P9-7.7-A_PC-LocalReview.md) and accepted
[crossing review](./P9-7.7-A_PC-CrossingReview.md) carry detailed evidence.

Typed queries use the current forensic API for the PC parent, ZOH writer,
native release, matched-forcing, initializer, receipt-parent, abundance and
lifecycle contracts, plus initializer/parent/abundance debt lifecycles. Original
`indeterminate_requires_review` dispositions and optional-policy status remain
attached to their source and trace identities. Design debt resolution is not
backward evidence of a runtime property.

## PC scientific and ordered-scope limits

- Read geometry from old committed Z, compute current and held source there,
  then perform continuity, refreshed A history writing and exactly one ZOH
  carrier write. Prospective W/Z do not feed the same beat's consumed current.
  Both complete current/reset targets must readmit before publication.
- The nomination uses supplied W, not authenticated W formation. A same-profile
  signed-carrier companion and distinct zero-chi/zero-zeta declarations expose
  nonvacuous carrier behavior without adding profiles to public support.
- Native zero-source release is exponential decay. Reset, migration drop and
  event-driven zeroing are not native release. Candidate W and carrier Z are
  independent authority/loss channels; PC is not Candidate B.
- The admitted carrier envelope is not a claim of matched-forcing trajectory
  contraction or indefinite base-chart invariance. No parameter/graph sweep,
  CI+PC numerical product, or RG2b conformance follows from this PC nomination.
- The exact PC↔CI+PC pair witnesses preserve both histories and contract
  identity. A changed `tau_PC` target rejects rather than silently resetting.
  Other pair contracts remain unreviewed; endpoint support is not pair support.
- Incoming C→nominated A_PC rejects its unselected initializer. The retained
  positive initializer-selected A_PC and affine event target are different
  declarations, not proposed public additions. The explicit affine event loses
  source W and Z, initializes W independently for current/reset, zeros Z,
  transports charge without advancing time, and readmits both complete targets.
- G3, GRC9V4 specialization, aggregate P9-7.7 and other profile decisions remain
  separate. This proposal adds no implementation permission.

## Public-facade supplement and access

The [one-method facade capture](./P9-7.7-A_PC-G2Interface.json) passed in
14.305 seconds. It checks detached C/Z configuration, frozen resolved parameters,
unchanged assignment, local versus public discovery, missing defaults and
malformed inputs, required A backend, strict/external zero-duration equivalence,
and preservation of a prior commit when a request iterator fails. Distinct
current/reset Z and reset authority survive the sequence. Zero-duration calls
do not supply positive-step numerical credit; the local product owns that.

The existing registry receives one proposed `exact_profile_v1` row and one
dependent G2 materializer. Shared Python, API, notebook and browser paths expose
four accepted declarations plus this proposal without profile-specific dispatch
or rendering code. Original execution and acceptance records are untouched.

## Decision and reproduction

Validation includes 17 rehashed proposal mutations, 10 interface mutations and
11 shared registry/materializer tests. The shared roster regression initially
assumed four total rows; it now preserves four accepted profiles while allowing
separately checked proposals. One status attempt correctly held because that
bound test changed during verification; the follow-up uses frozen inputs.
Neither correction changed scientific code, tolerances or accepted evidence.
The frozen-input follow-up passed in 157.401 seconds: one actual status reused
across notebook/HTTP/browser, with 100 browser rejection controls. It renders
four accepted G2 declarations and the separate, unaccepted A_PC proposal.

Accept or return this exact G2 proposal. A later acceptance would add only the
nominated A_PC declaration, not the separate controls/targets or all crossings.

From repository root, validate the retained integrated review:

```bash
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_a_pc_g2.py
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python -m unittest test_p977_a_pc_g2.ReviewTests -v
```

`--emit` reconstructs the review without a numerical campaign rerun.
`--capture-interface` emits a deliberately new facade execution on stdout;
neither command replaces the retained evidence.
