# P9-7.7 — integrated A_CI G2 review

**Verdict: PASS proposal for the exact declared scope; G2 acceptance pending.**
The bounded local/crossing reconciliation was accepted in `e883ede`.
This integrated review is not an independent audit, an acceptance act, a new
numerical campaign, aggregate P9-7.7 closure, or a discovery update.

## Exact proposed scope

Propose only
`grcv4-profile-sha256:16ed65f7f65d4716e1be3e384f6fa0f957d26dd7b7a3f7e1b43ad1aa3f250946`.
The [integrated record](./P9-7.7-A_CI-G2Review.json) binds the complete declaration,
finite inputs, graph/reference/context, release identities, exact evidence
pointers and typed authority. This is the supplied-history A_CI nomination,
not every A_CI parameter setting, arbitrary graph, or global CI branch.
The exact C_OS and A_OS accepted declarations remain unchanged.

Sources are the [generic specification](../../../specs/grc-v4-spec.md),
[V4 interface](../../../specs/grc-common-interface-v4-ext.md),
[initializer](../../../specs/grc-v4-a-initializer-spec.md),
[event contract](../../../specs/grc-v4-topology-event-spec.md),
[fixture catalog](../../../specs/grc-v4-conformance-fixtures.json),
[paper](../../investigations/grc9v4-constitutive-design/drafts/2026-09-GRC-V4.md)
and typed investigation traces. None is amended by this review.

## Integrated assessment

| Obligation | Assessment |
| --- | --- |
| `G2-EXACT-PRODUCT` | All 28 required catalog IDs resolve exactly once: eight common, nine Candidate A, one CI and ten lifecycle. The accepted [local product](./P9-7.7-A_CI-LocalProduct.json) supplies 21 rows; the accepted [crossings](./P9-7.7-A_CI-Crossings.json) supply the seven other obligations. |
| `G2-ORDERED-ENDPOINTS` | Satisfied for the finite, explicitly identified crossing population below. Both roles, exact identities, W/Z dispositions, losses, charge, reset, lineage and complete-publication rejection remain bound. Endpoint acceptance is not all-pairs support. |
| `G2-INTEGRATED-REVIEW` | PASS proposal combining that retained numerical/lifecycle product with one new [public-interface method](./P9-7.7-A_CI-G2Interface.json), current source checks and fresh typed provenance. Explicit G2 acceptance is still required. |

The CI evidence is not borrowed OS evidence. It contains independent implicit
root equations, trial-current-dependent W-hat with nonzero gamma, a local
self-map/contraction certificate and joint residual, fixed incoming W through
the trials, one continuity update and one post-continuity log writer. Written
W is readmitted at restart and not fed back into the same beat. This establishes
the tested local branch, not global root existence/uniqueness or a parameter
sweep. Chi/zeta controls retain their separate complete identities. The
one-iteration zero-tolerance failure is labeled root construction, not a failed
public lifecycle operation. Injected faults retain their test-only labels.

### Ordered-endpoint applicability

- Retained A_OS→nominated A_CI is a positive exact **target** witness; nominated
  A_CI→A_PC is a positive exact **source** witness. New A_PC→A_CI preserves W
  and explicitly loses the distinct nonzero carriers in both current/reset roles.
- A_CI→C_OS explicitly loses W. This locally admitted C_OS crossing endpoint
  is not the accepted C_OS G2 declaration; it gains no public support here.
- The nomination does not select the optional incoming C→A initializer.
  A well-formed request rejects at admission without publication. This is the
  required unavailable-policy boundary, not a successful C→A operation.
- Positive C→A and the mapped event use the distinct initializer-enabled A_CI
  `grcv4-profile-sha256:7e90f24e123cbaa2571aa5071489a8b4fdec815098925cba51ed01be09b1fe2b`.
  It is not proposed support. The event starts at the nomination, applies the
  declared resource map independently to both roles, initializes their W,
  receipts explicit history loss, and verifies reset/restore/archive behavior.
- PC↔CI+PC cannot contain this nonpersistent nomination. Separate accepted
  witnesses document those classes without earning A_CI execution credit.
- The current-as-reset readmission control passes; the otherwise matching
  reset-only rejection preserves the complete publication and ledger.

All seven classes have explicit dispositions. No applicable promised positive
operation is replaced with a negative, and no unsupported initializer or
unknown ordered pair is inferred. The crossing fixtures' near-equilibrium
resources are distinct from the local numerical campaign's state inputs;
matching profile IDs do not equate those input populations.

### Interface and claim status

The added native method exercises `GRCV4.from_config`, `GRCModel`, detached
configuration, immutable parameters, unchanged state assignment, local lookup,
missing defaults, invalid run counts and request layers, malformed config,
strict/external zero-duration agreement and interrupted-iterator commit
preservation. Local lookup works while global discovery excludes A_CI.
Zero duration provides no additional positive-duration CI solver evidence.

Twelve contract and three debt queries use the current forensic API. The CI
root/contraction and older D10.2 associations retain their reported
`indeterminate_requires_review` boundaries. Optional initializer authority,
forward verification obligations and unavailable numeric abundance are not
promoted by runtime success. Source/edge refs and trace digests remain in the
integrated record. This is bounded implementation conformance, not a rewrite
of scientific claim status.

## Shared G2 plumbing

[ProfileG2Registry.json](./ProfileG2Registry.json) records C_OS and A_OS as
accepted, and A_CI as proposed. One shared validator checks pinned record
identities, exact resolved declarations, explicit authority ceilings and
discovery agreement for every accepted entry. Historical acceptance adapters
preserve existing files, hashes and source-reuse evidence. New acceptances
use `phase9_exact_profile_g2_acceptance_v1`; no A_CI acceptance record is created.

The common schema binds the exact declaration and additional support, reviewed
Git subject, JSON/text hashes, release identities and predecessor support, with
explicit false G3/aggregate/all-pairs flags and empty runtime/specialization
permissions. A pinned registry entry is still required: dropping an acceptance
file into the repository cannot grant support. The ordered registry preserves
a review's predecessor support across later acceptances. Scientific review
checkers remain profile-specific and separate from this acceptance adapter.

API, notebook and HTTP status expose `profile_g2` and `a_ci_g2_review`. The
browser validates them against the checked-in registry projection, served as
`g2-registry.js`, rather than trusting self-rehashed incoming data. New profile
acceptance needs data registration and its own scientific review, not a new
acceptance function or G2 browser branch. Bounded local/crossing evidence views
remain separate. Failed status clears both accepted and proposed projections.

## Validation and decision

The retained facade capture passed one method in 17.664 seconds. An earlier
17.657-second development capture also passed; it was superseded before
publication when the checker was adjusted to preserve predecessor support.
Only the final capture is cited; neither run adds positive-duration evidence.
The six accepted local/crossing methods were not rerun or rewritten.

Focused validation covers the 12 integrated-proposal mutation controls, shared
registry/acceptance/discovery tests, unchanged historical evidence and runtime
permission logic, and one actual current status carried through notebook,
HTTP and browser. Transport reuse is not multiple independent executions.
The six initial registry tests plus integrated-review test passed in 3.821
seconds. The actual status/transport test passed in 70.780 seconds, including
36 coherently rehashed browser rejection controls, the served roster asset
and decision-table rendering/clearing. A final seven-test registry suite passed
in 3.806 seconds after moving per-profile metrics into the pinned roster;
that narrow follow-up checks shared Python/browser projection without another
full status or numerical run. Registry counts are checked against each bound
review rather than assuming every future profile has 28 obligations.

The integrated record digest is
`2a1211974eaa047456c9a8f791c501e4c3087d2d2037ff1e8bf577b38ae093ae`.

From the repository root:

```bash
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_a_ci_g2.py
```

Default checks retained evidence. `--emit` reconstructs the integrated review;
`--capture-interface` emits a distinct new run to stdout without overwriting
the retained capture. These do not accept G2.

**Next decision:** independently review and accept or return this exact G2
proposal. Until accepted, public support remains exactly C_OS and A_OS.
Eight other-profile G2 decisions (including A_CI), aggregate P9-7.7 and G3
remain separate. No production numerical code or older version is changed.
