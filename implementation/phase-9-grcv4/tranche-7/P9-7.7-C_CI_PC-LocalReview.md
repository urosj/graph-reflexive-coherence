# P9-7.7 — C_CI+PC local reconciliation

Status: local reconciliation implemented and verified, pending review and
bounded acceptance; G2 remains separate. The exact original P9-7.1 nomination is
`grcv4-profile-sha256:3a7a084788c59a55b4232fb98e9c5529c1aa4c7c71074c9d3288982fa2fd2a5b`.

The local product covers 26 catalog cells: eight common, 13 Candidate C,
`CI-PC-SAME-SOURCE`, and four fixed-profile lifecycle cells. Seven crossing
cells remain. Shared executions, provisional stages and control declarations
retain their own scope; they are not added together as independent runs.

## Scientific evidence

- Use the exact nomination and a same-profile signed-history companion. Old
  C/Z remain frozen during the joint solve. No W_A, independently written sector
  or mobility state is introduced. Candidate C's realization still owns Z.
- Independently solve the literal geometry/current equations. Compare the
  selected source with both immediate geometry and the one prospective ZOH
  write. Old-Z-only PC geometry and a second post-continuity source must be
  numerically distinguishable and rejected as substitutes.
- At every recorded consumed-root trial, reconstruct the selector, selected
  sector, retained Hodge, potential, direct baseline, retained/physical
  identification, total current and read-back using independent dense equations.
  The baseline changes across trial geometry; a reference-only baseline is not
  valid evidence for the selected coupled state.
- Current, reset and restart each have their own joint root and complete C
  reconstruction. Check continuity from the consumed numerical current and ZOH
  from its selected source. Count zero W writers, one continuity evaluation and
  one Z writer. Reconstructed restart source is not the writer input.
- Retain the local reference-ball contraction certificate, admitted residual,
  iteration budget and uniform carrier-source slack. A separately declared
  exhausted-budget control rejects without fallback. Gain two denotes the
  constant-source law, not a measured settled trajectory or amplitude equivalence.
- Fixed-stage checks retain distinct structural-Hodge/mobility constructors,
  strict selector gap and boundary rejection, typed physical identification,
  and retained versus physical conditioning. Baseline derivative/covariance
  checks use the nominal stratum plus a separately declared noncommuting,
  nonzero-deformation three-node fixed-stage control. That control is not a
  complete coupled trajectory or additional nomination execution.
- Separate chi-zero and zeta-zero declarations retain negative nonzero Z:
  the structural source vanishes, while the coupled geometry still contains
  old Z and the carrier undergoes exponential release, not reset/drop.
  The one-chi control fixes input current explicitly to avoid conflating gate
  multiplicity with the changed current response.

The nomination has `kappa_M_C=0`; its baseline nevertheless depends on the
retained geometry at each trial. No global root uniqueness, indefinite
base-chart invariance, matched-forcing trajectory contraction, arbitrary-graph
coverage, migration capability or specialization conformance is inferred.

## Lifecycle and failure pressure

Common facade checks cover ordinary/zero/negative duration, domain/nonfinite
faults, detached stale-observable pressure, malformed cache inputs and postsolve
charge failure. Injected faults are labeled test-only, not naturally observed
solver failures. Reset readmission rejects before the consumed solve/writer;
restart readmission rejects after the consumed solve and prospective Z write.
Both preserve the complete scientific/lifecycle publication.

Save/load and replay retain exact snapshots/results and receipt parents. Reset
after ordinary steps uses independently supplied reset C/Z without rewinding
time or charge. Persistent receipt ledger and emitted delta remain distinct.
Coherently rehashed missing-parent chains reject. Exported and duplicated
current/reset histories and receipt/commit ledgers are detached.

## Sources, integrity and access

Sources are the [V4 spec](../../../specs/grc-v4-spec.md),
[catalog](../../../specs/grc-v4-conformance-fixtures.json),
[common-interface extension](../../../specs/grc-common-interface-v4-ext.md),
[paper](../../investigations/grc9v4-constitutive-design/drafts/2026-09-GRC-V4.md)
§10.6, and accepted [Tranche 6 composition evidence](../tranche-6/P9-6.3ab-Review.md).
The current forensic API binds 13 contract traces: CI+PC parent/C composition,
C contraction and ablations; carrier writer/release/matched-forcing; D11 C
reference/mobility/baseline/derivative/covariance; and the receipt-parent ceiling.
Source classifications and support limits are preserved, not promoted by a
runtime test. The accepted C composition/contraction contracts supply the
coupled-root authority.

The [retained run](P9-7.7-C_CI_PC-LocalProduct.json) binds current sources,
nomination, predecessor, test roster and content-addressed preimages. Its checker
re-evaluates the independent equations and rejects rehashed alterations to
history, source/geometry, trial/role reconstruction, writer counts, controls,
acceptance or scientific scope.

The new `c_ci_pc_local_product` view uses the existing registry/materializer and
generic Python/API/notebook/browser rendering. It grants no G2 support:
C_OS/A_OS/A_CI/C_CI/A_PC/C_PC/A_CI+PC remain the seven accepted declarations.

The five native methods passed in 104.078 seconds. The record digest is
`abc34f0442d9e2bc1aeecb18c946e7fd894070a4be7a9894d6a79593fd23d5f6`.
All 47 rehashed evidence mutations rejected (263.980 seconds), and 11 shared
registry/materializer tests passed (9.480 seconds). One real status evaluation,
reused across notebook/API/browser transports, passed in 709.240 seconds with
149 browser rejection controls. It verifies retained evidence, not a rerun of
the native numerical campaign.

That surface review exposed an older hard-coded next-gate narrative naming only
four accepted profiles. The display now derives accepted families and local/
crossing counts from checked registry views. One focused test of the actual
projection and its call site passed (0.011 seconds), including later crossing
reconciliation without a stale remaining-cells claim. This display-only change
was checked separately after the full status run; that run is not relabeled as
an execution of the later text projection. No production numerical code changed.

From repository root, inspect retained evidence without a numerical rerun:

```bash
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_c_ci_pc_local.py
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python -m unittest test_p977_c_ci_pc_local_evidence -v
```

Use `--run` only for a deliberately new execution; stdout is a new record.
The subsequent [crossing reconciliation](P9-7.7-C_CI_PC-CrossingReview.md)
addresses all seven remaining cells. Combined bounded acceptance and integrated
G2 remain separate; the original local record and its scope are unchanged.
