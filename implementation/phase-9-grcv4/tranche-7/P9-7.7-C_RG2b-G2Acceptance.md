# P9-G2[C_RG2b] — exact declaration accepted

The user accepted the reviewed G2 proposal on 2026-09-15 with “accept and commit”.
The [decision record](P9-7.7-C_RG2b-G2Acceptance.json) binds checkpoint `796229e`,
the original review text, 33-cell product and facade evidence. Those records
remain unchanged, including their execution-time pending flags.

Public discovery adds exactly
`grcv4-profile-sha256:413497bec4f219ec402d82d5cd2aced01dca25a58d2ab906c348472b98d596b0`.
Ten exact declarations are accepted. The shared registry adapter/materializer
projects this decision through API, notebook and browser views.

Acceptance preserves complete Candidate C reference/mobility/baseline authority,
separate current/reset resources and absence of W_A/Z_4 history. The section is
frozen-completion-relative and Lipschitz-only, with finite approximation error
and a fixed positive beat. The fixed-stratum C derivative is not an RG section
derivative. K state admission does not imply K-minus ordinary-entry admission.
No C1 debt resolution, arbitrary positive dt, indefinite chart invariance,
formation authentication or classical section spectrum is claimed.

Scalar witnesses, controls, companion graphs and initializer/event targets are
not added support. All unreviewed ordered pairs, aggregate P9-7.7, G3 and
specialization remain separate. No new runtime iterations are authorized.

The [finite source-reuse record](P9-7.7-C_RG2b-G2SourceReuse.json) pins exact
before/after hashes for discovery declarations/assertions and source comparison
changes. It does not waive arbitrary drift, alter numerical laws or rewrite
accepted executions. Prior acceptance and review records remain byte-identical
to the checkpoint. The work manifest preserves iteration owners and permissions.

Verification: 17 of 19 focused methods passed in the initial 146.882-second
batch. Two shared tests retained the nine-declaration expectation; after
updating their exact roster/count and synthetic predecessor fixture, both
passed in a targeted 4.527-second rerun. All 19 focused methods are covered,
including 42 rehashed interface/proposal controls. Three native discovery
regressions also passed (1.840 seconds), as did the phase boundary and
portability checks. No numerical campaign was rerun.

Focused verification command from repository root:

```bash
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python -m unittest test_p977_c_rg2b_g2_acceptance test_p977_c_rg2b_g2_surfaces test_p977_c_rg2b_g2.ReviewTests test_profile_g2_registry test_profile_materializers -v
```
