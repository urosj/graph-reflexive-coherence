# P9-7.1 — Generic lifecycle integration

Status: **accepted by the user on 2026-09-11**, including the audit corrections,
through authorization of this commit. Acceptance covers the ten concrete
ordinary-lifecycle scopes below; it does not grant new G2/G3 or crossing support.
Branch: `impl/phase-9-grcv4-tranche-7`, based on `b45d0af`.

The user authorized the complete parent, not just the A_OS scheduling hint in
the Tranche 6 handoff. This review covers ten exact family fixtures: A/C × OS,
CI, RG2b, PC, CI+PC. Historical C_OS acceptance is reused without duplicate
credit; the other nine children are accepted against this new evidence.

The original execution records and their checker retain their pre-acceptance
flags. This later acceptance entry does not rewrite either run or its source
identities. Scoped verification and actual API/browser boundary checks passed
after the corrections; all numerical realization sources remain unchanged.

## Authority and implementation

The sources are [generic specification §7](../../../specs/grc-v4-spec.md),
[common-interface extension](../../../specs/grc-common-interface-v4-ext.md),
[paper](../../investigations/grc9v4-constitutive-design/drafts/2026-09-GRC-V4.md),
the accepted D10.2 snapshot/reset and atomicity contracts, and the accepted
P9 receipt-parent successor. Typed forensic contract queries preserve their
original support dispositions; they do not themselves certify execution.

One `GRCV4Operation` owns immutable publication, ledger and commit preimages.
The historical `CandidateCOSOperation` import remains an alias. Existing
Tranche 4–6 provisional numerical owners perform actual current/reset admission,
the single ordinary resource write, any W/Z writes and final reconstruction.
No new numerical solver or physical law is introduced.

- A retains C/W; C retains only C; PC-bearing profiles also retain independent Z.
- CI roots, RG2b sections, PC geometry and the C derived sector are reconstructed,
  not serialized as causal state. Both live and reduced reset are readmitted.
- A requires the explicit differential-reference payload already bound by its
  complete profile. This records a supplied initializer/backend, not formation.
- C_OS retains its closed v3 snapshot. Other profiles use the closed generic v1
  envelope with the A backend field (null for C). No implicit layout conversion.
- Reset restores all current C/W/Z from the reduced baseline without rewinding
  the live clock or ledger. Rebase changes only the baseline. Both append receipts.
- Ordinary receipt history policies identify unchanged coordinate placement,
  not unchanged writer/reset values: the actual endpoint authority digests bind
  those values. No mapped history transport or information-loss claim is made.
- Newly integrated profiles reject crossing registries/archives and do not
  advertise migration/events. Their actual crossing work belongs to P9-7.2.

## Evidence and limits

The original retained run passed all **26 tests**: two named lifecycle/pressure methods
per family and six selected legacy compatibility methods. There were no failures,
errors or skips; sources remained unchanged during execution. The existing
loaded-source guard checked origins and live code against the repository.

The read-only successor check passed with ten exact fixture reconstructions,
five rejected misleading-record controls and 454 unchanged predecessor files.
All 34 browser-validator tests passed. Actual API/browser identity, fail-closed
support cleanup, notebook status-cell/stale-pressure isolation and the real
status-only notebook runner passed. No full numerical, performance, historical
verification or desktop/mobile browser campaign was rerun.

From the repository root:

```sh
PYTHONPATH=src:. .venv/bin/python implementation/phase-9-grcv4/verification/verify_p971_lifecycle.py --check
```

The explicit `--run --output <new-repository-relative-json-path>` mode executes
the same focused roster and refuses to overwrite a retained record. It uses
the repository `.venv`; dependency versions and the lockfile are recorded.

[The record](./P9-7.1-Lifecycle.json) binds exact input recipes, graph/profile/
reference/backend identities, distinct reset authority, execution results and
original execution source hashes. The focused tests exercise save/load/replay, reset after
ordinary evolution, rebase, duplicate independence, receipt parent ownership,
hostile rehashed imports, current-only assignment, reset-only failure and atomic
publication controls for each family. Existing C_OS tests cover compatibility.
Fresh-process load is additionally exercised for A_CI_PC (explicit backend and
both histories) and graph C_RG2b; every family executes actual file save/load.

Numerical Tranche 5/6 sources and accepted records remain byte-exact. Their
scientific oracles and original accepted runs are preserved, not rerun under a
new name. The small graph RG2b cases stay in their declared entry chart; no
indefinite entry-chart invariance, general performance envelope, optional
32-vertex run, formation, migration/event closure or wider G2/G3 is claimed.

All paths and reconstruction commands are repository-relative. A rerun is new
evidence, never a replacement for the retained original execution record.

Development corrections: the shared OS diagnostic initially assumed C's selector
path metadata also existed on A; that field is now C-specific. A PC-bearing
reset fixture initially exceeded its declared W chart, so its distinct reset
was moved inside the chart while keeping explicit invalid-reset pressure.
Hostile negative-history imports use schema-independent canonical hashing,
so the receiver—not a schema-validating or small-dyadic fixture builder—rejects them.
These are implementation/test corrections, not changes to accepted physics.
Code review also caught an error-classification regression: post-solve resource
failures must retain the successful candidate disposition. Solver-failure mapping
is now limited to the candidate-solve stage; the original native C_OS resource/
overflow/charge regression is included in the focused compatibility selection.
RG2b's frozen-completion duration check also precedes the provisional owner's
numerical error handler. The lifecycle adapter converts that typed declaration
failure into a noncommitting admission result; both A/C wrong-beat controls
preserve the entire owned tuple. No new completion duration is inferred.

## Independent audit corrections

The [independent pressure report](./P9-7.1-AuditPressure.json) held acceptance
for two lifecycle defects and identified a diagnostic labeling correction.
Its isolated ledger/numerical executions are **not** a native facade campaign;
the supplied seven native regression proposals were compiled but unexecuted.
Those proposals now live in `P971AuditRegressions` in
[`tests/models/test_grc_v4_generic_lifecycle.py`](../../../tests/models/test_grc_v4_generic_lifecycle.py).
External harness filenames in the pressure report are historical audit metadata,
not required repository inputs. No attachment-directory dependency is retained.

| Finding | Correction and native regression |
| --- | --- |
| F1: RG2b admitted poststate cannot be restored/read | Separate state readmission on the declared core chart K from ordinary-step entry on K_minus. Reconstruct current **and reset** sections and native currents without invoking a writer. A/C boundary steps exit K_minus into K; load/save/duplicate/read/assignment/rebase/reset remain possible. Another ordinary beat still rejects atomically. |
| F2a: zero-duration history can change science | When a previous clock exists, unchanged index requires unchanged time **and the operation's own source/target scientific identity**. Test coherently rehashed imports across all ten families, with lawful unreceipted assignment followed by a zero step as a positive control. |
| F2b: RG2b history ignores frozen duration | Positive historical advancement must equal the exact input sum of previous time and the frozen beat, rounded once to binary64. Test both candidates' hostile graph clocks and scalar/graph positive beats absorbed by a large represented time. No origin clock or cross-operation scientific adjacency is invented. |
| Non-OS provenance label | Use `commit_reconstruction` for reconstructed-current diagnostics and the zero-step authoritative-current stage. OS retains `commit_reference_readmission`. |
| Additional native rollback pressure | Through the A_OS public owner with an existing ledger, the accepted W=4 to W=2 singularity rejects at final reconstruction with `valid_root`, one failure receipt and byte-identical prestate/ledger. |

Additional charge-preserving, coherently rehashed current/reset coordinates
outside K remain rejected by constructor/import/assignment. Valid core state
admission does not weaken ordinary entry, including the existing zero-step
entry semantics, or imply unlimited chart invariance. All numerical owners,
specifications, paper, investigation authority and predecessor evidence remain
unchanged. The independent audit's F2a was also reproduced natively before the
fix on C_OS: the coherently rewritten history loaded instead of rejecting.

The correction capture is [P9-7.1-AuditFollowup.json](./P9-7.1-AuditFollowup.json).
It runs nine native audit methods plus six selected compatibility methods—not
another full 26-method campaign. Current inputs, boundary outputs, hostile
clocks, rollback identities and loaded source hashes are bound separately.
The original 26-method record is not overwritten or rehashed as a new run.
[Compact reverse source edits](./P9-7.1-OriginalSources.json) recover its exact
three changed source files; all other original source bindings remain exact.
The current `--check` validates both subjects and rejects missing coverage,
missing boundary witnesses or premature acceptance without numerical reruns.

To reproduce only the correction capture from the repository root:

```sh
PYTHONPATH=src:. .venv/bin/python implementation/phase-9-grcv4/verification/verify_p971_lifecycle.py --audit-run --output <new-repository-relative-json-path>
```

Correction execution: **15/15 passed**, no failures/errors/skips, in 648.476
seconds with unchanged source bindings throughout. This includes all seven
supplied native proposals and two additional domain/rounding methods. No new G2/G3 support,
formation claim or migration/event generalization is inferred by this follow-up.
