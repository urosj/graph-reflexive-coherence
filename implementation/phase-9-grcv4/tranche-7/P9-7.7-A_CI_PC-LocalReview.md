# P9-7.7 — exact A_CI+PC local reconciliation

Status: implemented and verified; pending independent review and user acceptance.
Crossing reconciliation, bounded acceptance and integrated G2 remain separate.

## Subject and scope

The original ten-family review nominates exactly
`grcv4-profile-sha256:5f2f848af0f482699ac6cb88e4e1bd1a66458774bac2c3cc6df9f74cc47d7689`.
The local product covers 21 catalog cells: eight common, eight Candidate A,
one CI+PC realization and four fixed-profile lifecycle cells. Seven crossing
cells remain: A migration-history receipt, all migration classes, history
disposition, reset after migration/event, target-readmission failure and
whole-lifecycle tuple mapping. Shared observations are not independent runs.

## Scientific checks

Sources are the [V4 specification](../../../specs/grc-v4-spec.md),
[fixture catalog](../../../specs/grc-v4-conformance-fixtures.json),
[common-interface extension](../../../specs/grc-common-interface-v4-ext.md),
and the [paper's CI+PC contract](../../investigations/grc9v4-constitutive-design/drafts/2026-09-GRC-V4.md#d8-coupled-implicit-plus-persistent-carrier-realization).
Current typed forensic queries retain composition/root, parent realization,
ZOH, release, ablation and receipt-parent ceilings with source/edge references
and trace digests. Passing does not promote their claim classifications.

The current/root equations are calculated independently of production solvers.
The selected root obeys the literal unit immediate plus unit retained geometry
law. Its one selected source is held for one ZOH write; a later source is not
substituted. Every trial retains old C/W/Z and rebuilds baseline and W-hat.
Only after continuity does the separate A writer refresh descriptors and W.
The identity-bearing composition gain is two; no CI/PC amplitude equivalence
or optimal weighting is inferred. The constant-source gain-two limit follows
from the checked unit-plus-unit geometry and ZOH laws; this is not a measured
steady-state trajectory campaign.

A same-declaration signed-carrier companion makes old Z and the difference
between selected and post-continuity sources observable. Separate chi-zero and
zeta-zero declarations show native nonzero exponential release from negative Z,
not reset/drop. They are controls, not new supported profiles. Reset and restart
roots are independently checked against their own authoritative states.

Bounds retain their local-reference-ball and compact-envelope meaning. A
separately declared exhausted-budget control rejects without fallback; it is
root-construction pressure, not a facade execution for the nomination. No
global uniqueness, indefinite base-chart invariance, matched-forcing trajectory
contraction, all-graph coverage or new G3 authority is claimed.

## Lifecycle and evidence

Common facade calls cover ordinary/zero/negative duration, domain/nonfinite
consumed-solve faults, postsolve charge failure and stale exported observables.
Injected reset/final readmission failures preserve the whole owned publication:
reset fails before a writer, final admission after one prospective Z writer.
Fault injection is labeled, not presented as a naturally arising solver defect.

Save/load, exact replay, reset after ordinary steps, independent duplicate/rebase,
detached W/Z/reset/ledger exports and coherently rehashed missing-parent rejection
run on this exact A_CI+PC declaration. Earlier PC assertions are reused as code,
not as evidence. Persistent receipts remain distinct from emitted deltas.

The [retained capture](P9-7.7-A_CI_PC-LocalProduct.json) has record digest
`456ba01d2b25c9d035a40081f56d3afc482775fd8d0ebf5a9b89b64ac3f5938d`.
The retained checker validates source identities, nomination, test roster,
content-addressed preimages, independent numerical equations, both admission
roles and authority ceilings. Rehashed mutation tests target these boundaries.
The registry adds one local materializer/view without profile-specific Python
dispatch or browser branches. Existing six exact G2 declarations stay accepted;
A_CI+PC is not published as supported.

Verification passed: five native methods; 33 rehashed evidence mutations;
11 shared registry/materializer tests; and one actual status check reused for
notebook, HTTP and browser transports (386.232 seconds). Browser pressure rejects
125 altered payloads across registered reconciliation and acceptance views.
The final surface check verifies retained evidence, not another native campaign.

From repository root:

```bash
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_a_ci_pc_local.py
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python -m unittest test_p977_a_ci_pc_local_evidence -v
```

The first command checks retained evidence and the predecessor chain. Capture
mode checks exact predecessor input identities; the shared status check performs
the full ancestry validation once. Development corrections were confined to
nested-certificate JSON conversion and distinguishing the admitted numerical
root from the tighter independent root when checking the W writer.

Use `--run` only for a deliberately
new native capture to stdout; a rerun does not replace the historical record.
The subsequent [crossing product](P9-7.7-A_CI_PC-CrossingReview.md) reconciles
those seven cells. This local execution record and its historical flags remain
unchanged. Combined bounded review/acceptance and integrated G2 remain separate.
