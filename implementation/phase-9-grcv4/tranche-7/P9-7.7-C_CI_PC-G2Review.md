# P9-G2[C_CI+PC] integrated review

Verdict: **PASS proposal for the exact declared scope**, pending explicit G2
acceptance. This integrated review is not an independent audit or a G2 decision.
The user accepted the bounded local/crossing result first, committed as `6ab241f`.
Public support remains exactly C_OS/A_OS/A_CI/C_CI/A_PC/C_PC/A_CI+PC.

Proposed complete profile:
`grcv4-profile-sha256:3a7a084788c59a55b4232fb98e9c5529c1aa4c7c71074c9d3288982fa2fd2a5b`.

The [machine-readable review](P9-7.7-C_CI_PC-G2Review.json) binds the accepted
checkpoint, exact nomination, source/release identities, all 33 catalog cells,
ordered applicability, facade supplement and typed contract/debt traces.
Five local methods and four crossing methods are reused, not rerun or presented
as a new independent numerical campaign. Its digest is
`863730d90209e4813075c7b517a68df3e9587fe87c1ef4b04638a03f4a340858`.

## Three G2 obligations

| Obligation | Integrated result |
| --- | --- |
| `G2-EXACT-PRODUCT` | 33 unique catalog IDs resolve to 26 local rows and seven crossing dispositions. Controls and provisional stages keep their original scope. |
| `G2-ORDERED-ENDPOINTS` | Exact C_CI↔nomination and C_PC↔nomination distinguish fresh Z initialization, explicit drop and exact carrier preservation. A→C loses W/Z; C→the separate selected A target initializes W and loses Z, not candidate history. |
| `G2-INTEGRATED-REVIEW` | Specs, paper, typed contracts/debts, independent equations, lifecycle evidence and the narrow public facade agree for this declaration. Historical scientific claim status is not promoted. |

Sources are the [V4 specification](../../../specs/grc-v4-spec.md),
[catalog](../../../specs/grc-v4-conformance-fixtures.json),
[common-interface extension](../../../specs/grc-common-interface-v4-ext.md),
unchanged [common interface](../../../specs/grc-common-interface.md),
[initializer](../../../specs/grc-v4-a-initializer-spec.md) and
[event](../../../specs/grc-v4-topology-event-spec.md) specifications, and the
[substrate paper](../../investigations/grc9v4-constitutive-design/drafts/2026-09-GRC-V4.md),
especially its C-CI+PC contract and Appendix D.8. The
[local review](P9-7.7-C_CI_PC-LocalReview.md) and accepted
[crossing review](P9-7.7-C_CI_PC-CrossingReview.md) contain the detailed evidence.

Current forensic queries retain 22 contract and four debt traces: CI+PC
composition/parent/ablations and C contraction; carrier writer/release and
matched-forcing; D11-C reference/mobility/baseline/derivative/covariance; optional
initializer, receipt-parent, abundance and lifecycle contracts; plus the
initializer/parent/abundance/baseline-authority debt lifecycles. Classifications,
support dispositions, optional-policy boundaries, source/edge references and
trace identities are retained. Design resolution and forward verification
routing are not backward runtime evidence. This review does not rewrite claims.

## Coupled and ordered-scope limits

- Trials freeze old C/Z. Every consumed trial and current/reset/restart root
  rebuilds the full Candidate C selector, retained Hodge, potential, direct
  baseline and read-back chain. One selected source drives immediate geometry
  and the prospective ZOH carrier write; no post-continuity source substitutes
  for it. Candidate C has neither W state nor an A-style W writer.
- The unit-plus-unit, gain-two identity is the checked constant-source law,
  not a measured settled trajectory, amplitude equivalence or optimal weight.
  Root uniqueness is limited to the admitted local domain. No global root,
  indefinite base-chart invariance, matched-forcing trajectory contraction,
  formed-branch stability or arbitrary-graph claim follows.
- Structural Hodge and fixed transport mobility retain distinct authority.
  Zero nominal `kappa_M_C` does not remove baseline geometry dependence. Dense
  derivative/covariance controls, signed-carrier companions and zero-gate
  controls retain their separate declarations; they are not extra nomination
  trajectories or additional G2 support.
- C_CI→nomination rederives C and initializes zero Z without loss. The reverse
  rederives C and explicitly drops Z. C_PC↔nomination preserves Z only under
  exact carrier-contract identity; changed `tau_PC` rejects without a silent
  lossy fallback. Native exponential release is neither reset nor drop.
- A_CI+PC→nomination explicitly loses W and resets/loses Z; no W leaks into C.
  Nomination→the separate initializer-selected A_CI+PC target independently
  initializes W for current/reset and resets/loses Z. There was no source W to
  lose. This target differs from the accepted A_CI+PC nomination and is not
  proposed for G2.
- The renamed-edge C-CI+PC event rebuilds the complete target reference map,
  swaps resources with increment `(-1/8, 0)`, rederives C and zeros Z with
  explicit carrier loss. It does not invent candidate-history loss. Zero Z
  retains the immediate source and measurable geometry in both roles.
  Its target differs from both the nomination and the selected A initializer
  target. Target readmission, archive rejection, replay/reset and persistent
  ledger ownership remain bound to the executed maps.
- Reset-only target-chart rejection preserves the whole publication despite
  a passing current-as-reset control. The nonpersistent→nonpersistent witness
  remains separate, not nomination execution. Endpoint support does not grant
  all-pairs capability. Aggregate P9-7.7, G3, specialization and RG2b decisions
  remain separate.

## Facade supplement and access

The [one-method interface capture](P9-7.7-C_CI_PC-G2Interface.json) passed in
18.301 seconds. It checks detached C/Z configuration and reset carrier,
immutable parameters, unchanged assignment, local versus public discovery,
missing defaults, malformed inputs, absence of any A backend, strict/external
zero-duration equivalence and preservation of the prior commit when an iterator
fails. Positive numerical-step credit remains in the accepted local run.

One proposed `exact_profile_v1` registry row and one dependent G2 materializer
use the existing Python/API/notebook/browser path. The UX must distinguish
seven accepted declarations from the C_CI+PC proposal. Production discovery,
runtime permissions and original accepted evidence are unchanged.

Verification passed: 14 focused methods in 10.664 seconds (two review methods
with 38 rehashed interface/proposal mutations, plus 12 shared registry and
materializer methods). One actual status run, reused across notebook/API/browser,
passed in 749.839 seconds, including 172 browser rejection controls.
No accepted local/crossing numerical campaign was rerun, and no G2 acceptance
was inferred. The source/record bindings and accepted checkpoint remain exact.

From repository root, inspect the proposal and its retained evidence:

```bash
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_c_ci_pc_g2.py
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python -m unittest test_p977_c_ci_pc_g2.ReviewTests -v
```

`--capture-interface` deliberately emits a new facade run; it is not required
to check this record. Next decision: accept or return this exact G2 proposal.
