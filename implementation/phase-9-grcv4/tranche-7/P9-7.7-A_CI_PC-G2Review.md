# P9-G2[A_CI+PC] integrated review

Verdict: **PASS proposal for the exact declared scope**, pending explicit G2
acceptance. This is an integrated review, not an independent audit or G2 decision.
The user accepted the bounded local/crossing result first, committed as `d41e1c6`.
Public support remains exactly C_OS/A_OS/A_CI/C_CI/A_PC/C_PC.

Proposed complete profile:
`grcv4-profile-sha256:5f2f848af0f482699ac6cb88e4e1bd1a66458774bac2c3cc6df9f74cc47d7689`.

The [machine-readable review](P9-7.7-A_CI_PC-G2Review.json) binds the accepted
checkpoint, exact nomination, current source/release identities, all 28 catalog
cells, ordered applicability, facade supplement and typed contract/debt traces.
Five local methods and four crossing methods are reused, not rerun or presented
as a new independent numerical campaign. Its digest is
`abb89b7309b9978c107fb43b76ee4d0d3c9d3640543d5746d1c869ed27f669f6`.

## Three G2 obligations

| Obligation | Integrated result |
| --- | --- |
| `G2-EXACT-PRODUCT` | 28 unique catalog IDs resolve to 21 local rows and seven crossing dispositions. Controls and provisional stages keep their original scope. |
| `G2-ORDERED-ENDPOINTS` | Exact A_CI↔nomination and A_PC↔nomination witnesses distinguish fresh carrier initialization, explicit drop and exact preservation. Candidate loss, initializer and event targets retain their separate dispositions. |
| `G2-INTEGRATED-REVIEW` | Specs, paper, typed contracts/debts, independent equations, lifecycle evidence and the narrow public facade agree for this declaration. Historical scientific claim status is not promoted. |

Sources are the [V4 specification](../../../specs/grc-v4-spec.md),
[common-interface extension](../../../specs/grc-common-interface-v4-ext.md),
unchanged common interface, initializer/event specifications and the
[substrate paper](../../investigations/grc9v4-constitutive-design/drafts/2026-09-GRC-V4.md),
particularly its CI+PC and A-CI+PC contracts. The
[local review](P9-7.7-A_CI_PC-LocalReview.md) and accepted
[crossing review](P9-7.7-A_CI_PC-CrossingReview.md) contain the detailed evidence.

Current forensic queries retain 18 contract and three debt traces: CI+PC
composition/root/parent/ablations and A contraction; carrier writer/release and
matched-forcing; optional initializer, receipt-parent, abundance and lifecycle
contracts, plus the initializer/parent/abundance debt lifecycles. Preserve their
classifications, `indeterminate_requires_review`, optional-policy boundaries,
source/edge references and trace identities. A design resolution is not backward
evidence of runtime conformance; G2 does not rewrite historical claims.

## Coupled and ordered-scope limits

- Trials retain old C/W/Z. The selected implicit-root source drives both the
  immediate geometry and one prospective ZOH carrier write. It is not replaced
  by the post-continuity source. W has its separate post-continuity refresh/write.
  Current/reset/restart roots use their own complete authoritative operands.
- The unit-plus-unit, gain-two identity is the checked constant-source law,
  not a measured settled trajectory, amplitude equivalence or optimal weight.
  Root uniqueness is bounded to the admitted local reference ball; the compact
  carrier envelope does not prove global uniqueness, indefinite base-chart
  invariance, matched-forcing trajectory contraction or arbitrary-graph coverage.
- Supplied positive W is not authenticated formation. Zero-gate controls and
  the signed-carrier companion keep their exact declared scope. Native release
  is exponential decay, not event reset or migration drop.
- A_CI→nomination preserves W and initializes zero Z without loss. The reverse
  preserves W and explicitly drops Z. Exact A_PC↔nomination preserves both W/Z
  only under exact carrier-contract identity; a changed `tau_PC` rejects.
  Nomination→C_CI explicitly loses both histories, with no target carrier slot.
- C→the nomination rejects an unselected initializer. The retained positive
  C_PC→initializer-selected A_CI+PC target is a different declaration, not new
  support. The event uses yet another selected target (`gamma=2`, tolerance
  `1e-14`) to make its immediate geometry numerically discriminating. Both target
  identities are bound separately; neither is proposed for G2.
- The event independently maps current/reset resources, initializes W, and
  zeros Z with explicit W/Z losses. Zero Z does not remove its immediate source.
  Coupled readmission, archive rejection, exact migration replay and reset retain
  full ownership. Reset-only C_CI+PC target-chart rejection preserves the whole
  publication despite a passing current-as-reset control.
- The nonpersistent→nonpersistent witness is separate, not nomination execution.
  Endpoint support does not authorize all ordered pairs. G3, specialization,
  aggregate P9-7.7 and the other profile decisions remain separate.

## Facade supplement and access

The [one-method interface capture](P9-7.7-A_CI_PC-G2Interface.json) passed in
18.289 seconds. It checks detached C/Z configuration and reset carrier,
immutable parameters, unchanged assignment, local versus public discovery,
missing defaults/backend, malformed inputs, strict/external zero-duration
equivalence, and preservation of the prior commit when an iterator fails.
Positive numerical-step credit remains in the accepted local run.

One proposed `exact_profile_v1` registry row and one dependent G2 materializer
use the existing Python/API/notebook/browser path. The UX must distinguish six
accepted declarations from the unaccepted A_CI+PC proposal. Production discovery,
runtime permissions and original accepted evidence remain unchanged.

Verification passed: 13 focused methods in 9.021 seconds (two review methods
with 33 rehashed evidence mutations, plus 11 shared registry/materializer
methods). One real status run, reused across notebook/API/browser transports,
passed in 411.937 seconds, including 140 browser mutation controls. No accepted
local/crossing numerical campaign was rerun, and no G2 acceptance was inferred.

From repository root, inspect the proposal and its retained evidence:

```bash
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_a_ci_pc_g2.py
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python -m unittest test_p977_a_ci_pc_g2.ReviewTests -v
```

`--capture-interface` is a deliberately new facade run; it is not required to
check this record. Next decision: accept or return this exact G2 proposal.
