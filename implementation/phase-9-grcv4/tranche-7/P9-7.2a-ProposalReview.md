# P9-7.2a — initializer integration into the proposal

Status: **proposal revision accepted by the user** through the commit-and-continue
request on 2026-09-11. Its exact bytes below remain the accepted review subject;
the candidate's original header is not retroactively rewritten. Paper propagation
is authorized next; specification and runtime work remain separate.
The source/tool admission was accepted and committed at `f36b3ba` following
design acceptance at `49b83ba`. This step changes the
[GRCV4 proposal](../../investigations/grc9v4-constitutive-design/drafts/GRCV4-proposal.md),
not the paper, V4 specification release or runtime.

## Source fidelity and editorial changes

The investigation spine is the admitted
[initializer authority](../../investigations/grc9v4-constitutive-design/decisions/P9CandidateAInitializerReferencePassAuthority.json),
queried through the existing current forensic API. Its projection digest is
`fa59abda356446daca516b33028a07a48ec63aaacb8f961cd274a425dca48dd1`.
Contract, debt, claim and object traces retain source/edge witnesses.
The predecessor D10.2 initializer contract retains `indeterminate_requires_review`;
the new contract's required support is optional design authority, not runtime proof.

| Proposal surface | Integration and retained boundary |
| --- | --- |
| Header/control rules | Separately admitted P9 authority; older tranche audit notes and D10/D11 census retain historical subjects |
| §8.11, §12.5 | Inherited initializer role versus the newly selected producer; migration classes are not executed C→A evidence |
| §12.6.1 | Fixed target inputs, fresh differentials, auxiliary `G_W(C,0)`, reference potential/flux, full final `G_W`; no auxiliary total-current admission or implicit neutrality |
| §12.6.2 | Binary64 rounding/floor/range rules; unit-vertex/WLS/site scope; acyclic static policy → profile → invocation identities; payload/release binding remains specification work |
| §12.6.3 | Independent current/reset construction, separate W/Z history channels, fixed target charts, final readmission, archived versus evolved W and F1/F2 atomicity |
| §12.6.4 | Producer choice resolved; five-realization positive evidence and aggregate review pending; auxiliary-singular/final-regular fixture is design pressure, not migration execution |
| §14.4, §15.2, App. E.8, G.9 | Exact GRC9V3 remains separate; optional claim, design-resolved debt and forward obligation have explicit successor attribution |

The proposal candidate SHA-256 is
`09c080fd835d096372cff6204e42ddf0baba2b2da674fee905c7b5df2c83ed84`.
The scoped checker pins this review subject and the forensic projection. Those
identity checks detect drift; they do not prove semantic fidelity or constitute
independent prose acceptance. No D10/D11/P9 decision, accepted design document,
historical claim row or execution record is rewritten.

## Draft versus released source

The original released proposal is retained in Git at `f36b3ba`, with SHA-256
`6a1935c9158e3391e8ed1ad7ab9cc891f4aad1ce2fbd12da89aa8b6469a5b7c0`.
The unchanged executable release remains
`grcv4-spec-release-sha256:e2acd9df0cc02c5fd4bbed4989ff5d7da3a819adeb2950d922b8a6ef4bf35f24`.

`verify_p972a_proposal.py` verifies that release's exact accepted bytes and
recomputes its identity from the retained member bindings. Only the proposal
member is resolved from its explicit Git subject. Every other source/member,
source-manifest row, creation tool, packaged asset and codec pin remains bound
to current exact bytes. The original builder is unchanged and can reconstruct
its output at that original checkout. This current check is byte/identity
verification, not a new generator execution, release, numerical run or acceptance.

Normal verification and API/notebook/browser status use this review-stage
route. The existing migration `--check` also checks the candidate and preserves
the 25/17-test subjects, their 440/442 source bindings and 7.1 predecessor.
Current paper edits remain prohibited. No new runtime permission, wider G2/G3,
generic-event support or automatic GRC9V3 binding is introduced.

The five focused methods in
[`test_p972a_proposal.py`](../verification/test_p972a_proposal.py) cover:
allowed draft evolution without release/acceptance promotion; draft/authority
rehashing; unintended paper/spec/source/generator edits; release/package/codec
tampering; missing or substituted historical proposal bytes. They run without
model execution. A full clone contains the Git subject; shallow/export-only
checkouts must obtain it explicitly. No zip or replacement evidence is created.

Scoped verification, from the repository root:

```sh
.venv/bin/python implementation/phase-9-grcv4/verification/test_p972a_proposal.py -v
PYTHONPATH=src:. .venv/bin/python implementation/phase-9-grcv4/verification/verify_p972a_proposal.py --check
```

Next: propagate this accepted proposal revision to the paper for review.
Specification payload/release work follows paper review; producer and
positive C→A implementation follow that specification authority.
