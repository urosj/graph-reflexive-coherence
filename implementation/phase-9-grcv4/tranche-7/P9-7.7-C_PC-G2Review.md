# P9-G2[C_PC] integrated review

Verdict: **PASS proposal for the exact declared scope**, pending explicit G2
acceptance. This is an integrated review, not an independent audit or a G2
decision. The user accepted the bounded local/crossing result first, committed
as `b90bb9e`. Public support remains C_OS/A_OS/A_CI/C_CI/A_PC.

Proposed complete profile:
`grcv4-profile-sha256:6105daf6f5111fdc51640194298b1b8398d608684791050d85b696bcd681f64f`.

The [machine-readable review](./P9-7.7-C_PC-G2Review.json) binds the exact
nomination, current source/release identities, accepted bounded reconciliation,
all 33 catalog cells, ordered applicability, facade supplement and typed
contract/debt traces. Five local methods and three crossing methods are reused,
not rerun or presented as another independent numerical campaign.

## Three G2 obligations

| Obligation | Integrated result |
| --- | --- |
| `G2-EXACT-PRODUCT` | 33 unique catalog IDs resolve to 26 local rows and seven crossing dispositions. Separate zero/derivative controls are not nomination executions; provisional stages are not public commits. |
| `G2-ORDERED-ENDPOINTS` | Exact C_PC↔C_CI+PC contracts preserve both-role Z. Candidate changes, explicit carrier loss/reset and separate nonpersistent/initializer/event endpoints retain their individual dispositions. |
| `G2-INTEGRATED-REVIEW` | Specs, paper, typed contracts/debts, native lifecycle evidence, independent equations and a narrow public facade agree for this declaration. Claim classifications and forward obligations are not promoted. |

Sources are the [V4 specification](../../../specs/grc-v4-spec.md),
[common-interface extension](../../../specs/grc-common-interface-v4-ext.md),
unchanged common interface, initializer/event specifications and the
[substrate paper](../../investigations/grc9v4-constitutive-design/drafts/2026-09-GRC-V4.md),
particularly its PC and C_PC contracts. The [local review](./P9-7.7-C_PC-LocalReview.md)
and accepted [crossing review](./P9-7.7-C_PC-CrossingReview.md) carry the detailed
execution evidence.

Current forensic queries retain 19 contract and four debt traces: PC parent,
writer, release and matched-forcing; D11-C reference field, mobility, baseline,
derivative and covariance; initializer, receipt-parent, abundance and lifecycle
contracts, plus their relevant debt lifecycles. Preserve the original
`indeterminate_requires_review` and optional-policy boundaries, source references
and trace identities. A design resolution is not backward evidence of runtime
conformance, and G2 does not rewrite those historical claim statuses.

## C_PC scientific and ordered-scope limits

- Reconstruct geometry from old committed Z, then rebuild the complete C
  selector/Hodge/potential/baseline/current/read-back chain before continuity.
  Hold that source through one ZOH write. Rebuild the full C chain for both
  independent target roles before publication; no post-continuity source
  refresh feeds the same beat's held write.
- Candidate C has no W state or A-history writer. Its persistent realization
  does own Z. Mobility has its own D11-C authority; it cannot be identified with
  structural Hodge. Nominal `kappa_M_C=0` does not remove the baseline's retained
  geometry dependence. The noncommuting dense derivative control is separate
  fixed-stage evidence, not an executed nomination trajectory.
- Native zero-source release is exponential decay, not reset, migration drop
  or event-driven zeroing. The bounded source/carrier envelope does not prove
  matched-forcing contraction, indefinite base-chart invariance, endpoint
  hysteresis or an arbitrary-graph/parameter result. PC is not Candidate B.
- Exact C_PC↔C_CI+PC witnesses preserve distinct nonzero live/reset carriers
  under exact contract identity. A changed `tau_PC` rejects instead of silently
  resetting. The nonpersistent→nonpersistent witness has separate endpoints
  and supplies no positive C_PC execution credit.
- A→C loses W and resets Z with separate loss channels. C_PC→the selected A_PC
  initializer target has no source W to lose; it independently initializes W
  for both roles while explicitly losing/resetting the carrier. This is not
  permission for any A target, nor a new supported initializer declaration.
- The renamed-edge C event carries a complete target `W_C_tr` map, independent
  affine current/reset resources and zero target Z. Both C chains readmit
  before commit. It has carrier loss but no candidate-history loss. Missing
  archives reject; reset and exact replay retain full lifecycle ownership.
  A separate reset-only target-chart failure preserves the whole source
  publication despite a passing current-as-reset control.
- The event target, initializer target and control declarations are not
  proposed additions. Endpoint support does not authorize unreviewed pairs.
  G3, GRC9V4 specialization, aggregate P9-7.7 and other profile decisions remain
  separate; this proposal creates no new runtime permission.

## Facade supplement and access

The [one-method facade capture](./P9-7.7-C_PC-G2Interface.json) passed in
13.852 seconds. It checks detached C/Z configuration, immutable resolved
parameters, unchanged assignment, local versus public discovery, missing
defaults and malformed inputs, Candidate C without an A backend,
strict/external zero-duration equivalence, and preservation of the prior commit
when a request iterator fails. Distinct current/reset Z and reset authority
survive the sequence. Positive numerical-step credit stays in the local run.

One proposed `exact_profile_v1` registry row and one dependent G2 materializer
use the existing Python/API/notebook/browser path. The UX distinguishes five
accepted declarations from the unaccepted C_PC proposal without new
profile-specific production dispatch or rendering code. Original accepted
execution, review and acceptance records are untouched.

## Decision and reproduction

Validation passed: 20 rehashed proposal mutations, 10 interface mutations and
11 shared registry/materializer tests. One frozen-input status evaluation,
reused across notebook, HTTP and browser transports, passed in 242.771 seconds
with 120 browser rejection controls. It renders five accepted declarations,
the accepted bounded C_PC reconciliation and the separate unaccepted G2 proposal.
No local or crossing numerical campaign was rerun.

Accept or return this exact G2 proposal. A later acceptance would add only the
nominated C_PC declaration, not the separate targets/controls or all crossings.

From repository root:

```bash
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_c_pc_g2.py
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python -m unittest test_p977_c_pc_g2.ReviewTests -v
```

`--emit` reconstructs the review without a numerical campaign rerun.
`--capture-interface` deliberately emits a new facade execution on stdout;
neither command replaces the retained evidence.
