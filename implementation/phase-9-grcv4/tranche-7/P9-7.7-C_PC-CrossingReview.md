# P9-7.7 — C_PC crossing reconciliation

Status: bounded local/crossing reconciliation accepted by the user on 2026-09-14.
The explicit “accept, commit then continue with g2” decision covers all 33
catalog cells: 26 local cells and seven crossing dispositions. Original execution
bytes and flags are unchanged. This does not accept C_PC G2, G3, all-pairs or
aggregate P9-7.7; public support remains C_OS/A_OS/A_CI/C_CI/A_PC.

Exact nomination:
`grcv4-profile-sha256:6105daf6f5111fdc51640194298b1b8398d608684791050d85b696bcd681f64f`.
Together with the [26-cell local product](./P9-7.7-C_PC-LocalReview.md), this
reconciliation addresses the seven remaining catalog cells. The
[crossing capture](./P9-7.7-C_PC-Crossings.json) distinguishes retained executed
witnesses from new execution. It does not claim every ordered profile pair.

## Ordered migration evidence

Seven source-bound aliases retain original execution identities, actual
endpoint declarations, current/reset state, receipt deltas and loss channels.
Historical receipts do not record a solver disposition; none is inferred.

| Migration class | Witness | Exact C_PC scope |
| --- | --- | --- |
| Nonpersistent → nonpersistent | `migration/C_NH_NH` | Separate C_OS → C_CI endpoints; no C_PC execution credit |
| Nonpersistent → persistent | `migration/C_NH_PC` | Target; fresh zero Z, no candidate history or loss |
| Persistent → nonpersistent | `migration/C_PC_NH` | Source; explicit carrier loss |
| PC → CI+PC | `migration/C_PC_CIPC` | Source; exact carrier contract, both-role Z preserved |
| CI+PC → PC | `migration/C_CIPC_PC` | Target; exact carrier contract, both-role Z preserved |
| A → C | `migration/A_C_PC` | Target; candidate-history loss and whole-carrier reset/loss |
| C → A | `initializer/A_PC` | Source; independently initialized W, carrier loss only; separately declared A target |

These witnesses come from the [accepted migration capture](./P9-7.2a-Migrations.json)
and the [initializer capture](./P9-7.2a-InitializerRuntime.json), linked by each alias's exact repository-relative
source pointer. The checker independently verifies both-role transport and
the C→A reference-pass equations without rerunning the old lifecycle campaigns.
The initializer target is not the public A_PC nomination or added C_PC support.

## New bounded execution

Three passing native methods (47.840 seconds) produce four cases:

- A same-candidate PC→CI+PC migration with changed `tau_PC` rejects at
  admission, preserving the entire owned publication. Exact preservation
  cannot silently fall back to a carrier reset.
- A renamed-edge C_PC reconstruction event applies an explicit affine C map
  independently to distinct live/reset resources. Its complete `W_C_tr` uses
  the target edge ID. Each target C chain is checked against independent dense
  equations: selector, sector, retained Hodge, potential, baseline,
  retained/physical identification, total current and read-back.
  Both roles readmit before publication; target Z is zero and source Z is
  distinct and nonzero in both roles. There is no W state, A initializer or
  candidate-history loss. Whole-carrier reset/loss is explicit, not native
  exponential release or a preserving representation change.
- Identity-resource reconstruction into a tighter PC resource chart has a
  passing current-as-reset control and a failing reset-only counterpart.
  Both source roles fit radius 4; current fits target radius 3 while the
  distinct reset does not. Failure occurs at target readmission, before
  publication validation, with the complete original C/Z/lifecycle owner
  unchanged. This is a declared reconstruction policy, not a way around the
  exact-contract rule for preserving migration.

The mapped event also checks exact snapshot replay, rejection of a missing
transition archive, independent reset without rewinding charge/time, receipt
delta versus persistent ledger, and an unaffected duplicate after reset.
The graph witness is one renamed edge with a nontrivial resource map, not an
arbitrary-topology or parameter sweep. GRC9 specialization remains outside scope.

During evidence-pressure development, two proposed alterations matched the
actual values (zero reset current and reference Hodge 2), so they were no-ops.
The mutation tests now use distinct values and assert that each alteration
really changes the record. No production change or tolerance relaxation was
needed, and the successful native capture was not replaced.

## Catalog and authority boundaries

The seven reconciled cells are `C-LIFECYCLE-REFERENCE-MAP`,
`RESET-AFTER-MIGRATION`, `RESET-AFTER-EVENT`, `WHOLE-LIFECYCLE-TUPLE-MAP`,
`ALL-MIGRATION-CLASSES`, `HISTORY-DISPOSITION`, and
`TARGET-READMISSION-FAILURE`. The record pins each cell's actual witnesses.

Sources remain the [V4 specification](../../../specs/grc-v4-spec.md),
[common-interface extension](../../../specs/grc-common-interface-v4-ext.md),
[event contract](../../../specs/grc-v4-topology-event-spec.md),
[paper](../../investigations/grc9v4-constitutive-design/drafts/2026-09-GRC-V4.md)
and current typed forensic traces. PC, D11-C, receipt-parent and initializer
authority preserve their original claim/support classifications and trace
digests. Neither aliases nor new tests promote scientific authority.

The shared registry/materializer exposes `c_pc_crossings` through the existing
API, notebook and browser view without profile-specific dispatch. Public
support remains C_OS/A_OS/A_CI/C_CI/A_PC. The subsequent bounded-acceptance
projection records the user's decision without changing these execution flags;
integrated C_PC G2 review and acceptance remain separate.

Verification passed: three native methods/four cases, 37 rehashed evidence
mutations, 11 registry/materializer tests, and one real status check reused
across notebook, HTTP and browser transports. Browser verification includes
110 rejected payload alterations across the registered views. The historical
migration campaigns and local numerical suite were not rerun.

From repository root:

```bash
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_c_pc_crossings.py
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python -m unittest test_p977_c_pc_crossing_evidence -v
```

The checker validates retained evidence. `--run` deliberately produces a new
execution record on stdout and does not replace the retained capture.
