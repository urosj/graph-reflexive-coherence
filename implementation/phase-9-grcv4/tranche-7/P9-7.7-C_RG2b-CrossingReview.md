# P9-7.7 — C_RG2b crossing reconciliation

Status: bounded local/crossing reconciliation accepted by the user on 2026-09-15.
The explicit “accept and commit, then continue with G2” decision accepts the
26 local cells and seven bounded crossing obligations. Original execution
records and flags remain unchanged. This does not accept G2, all-pairs,
aggregate P9-7.7 or G3; nine accepted G2 declarations are unchanged.

This completes the seven crossing obligations beside the
[26-cell local product](P9-7.7-C_RG2b-LocalReview.md). The exact original graph
nomination remains
`grcv4-profile-sha256:413497bec4f219ec402d82d5cd2aced01dca25a58d2ab906c348472b98d596b0`.

## Evidence scope

Nine new cases in three native methods cover five migration routes, one mapped
event and three target-admission controls. They do not assert all ordered pairs
or grant support to companion declarations.

| Migration class | Bounded witness |
| --- | --- |
| Same-candidate nonhistory → nonhistory | Graph C_OS → exact C_RG2b |
| Same-candidate nonhistory → history | Exact C_RG2b → graph C_PC; fresh zero carrier, no loss |
| Same-candidate history → nonhistory | Graph C_PC → exact C_RG2b; explicit nonzero carrier loss |
| PC → CI+PC | Retained `migration/C_PC_CIPC`, separate scalar endpoints |
| CI+PC → PC | Retained `migration/C_CIPC_PC`, separate scalar endpoints |
| A → C | Graph A_OS → exact C_RG2b; explicit independently distinct W-history loss |
| C → A | Exact C_RG2b → separately initializer-selected graph A_OS; independent reference passes, no source W to lose |

The two retained witnesses preserve their source record/pointer, endpoint and
receipt identities, reset outcome and unavailable solver disposition. Neither
is relabeled as execution on the RG2b nomination.

Every new successful migration checks both roles before atomic publication,
independent current equations, history channels, ledger versus emitted delta,
unchanged clock/charge, exact replay and reset/restore. Candidate C targets
reconstruct the complete selector/sector/Hodge/potential/baseline/current/readback
chain. RG2b target sections use the frozen positive beat even though lifecycle
readmission has dt=0; independent inverse transforms check the finite section
within its contraction-tail/error allowance. No ordinary A writer runs during
a crossing. Fixed-completion Lipschitz authority is not promoted to C1 or a CI root.

The mapped C→C event renames all graph coordinates, reverses the resource tuple
and adds a declared 1/128 charge increment. Its target reference map covers all
three renamed edges. Independently mapped current/reset resources are each
fully reconstructed before publication. No W initializer or carrier is created,
and no history loss is invented. Snapshot archive integrity, duplication and
reset are checked. This is one graph-generic affine event, not GRC9 mechanics.

The admission contrast holds current resources fixed and changes only reset:
a passing current-as-reset control, a charge-preserving reset outside target K
that fails before publication, and a reset inside K but outside K-minus. The
last admits migration and reset but rejects ordinary entry for both zero and
the frozen positive duration, preserving the full publication. State admission
does not authorize an ordinary step.

## Validation

The final source-bound capture passed three native methods covering nine cases
in 177.258 seconds. Its record digest is
`f6b717563826c8af1d333390b22cfaf7c6967ff01e5636a7b9b90dc7aff6feaf`.

All 14 focused evidence/registry/materializer methods passed in 126.533 seconds,
including 56 rehashed evidence mutations and 11 browser rejection controls.
The three altered-preimage controls use the runtime canonical codec and verify
all object hashes before testing rejection, rather than relying on hash errors.
The actual bound crossing checker, generic rendering, discovery, dependency
order and failed-refresh cleanup were exercised. The phase boundary,
repository-relative references and local links passed.

No production code, scientific contract, numerical tolerance, existing accepted
record or public-support declaration changed. There are now 33 reconciled
C_RG2b catalog cells at their stated bounded scopes. The user decision above
accepts that reconciliation, not G2, all-pairs or aggregate/G3 closure.

## Claims, records and reproduction

Sources are the [catalog](../../../specs/grc-v4-conformance-fixtures.json),
[V4 spec](../../../specs/grc-v4-spec.md),
[initializer spec](../../../specs/grc-v4-a-initializer-spec.md),
[event spec](../../../specs/grc-v4-topology-event-spec.md),
[paper](../../investigations/grc9v4-constitutive-design/drafts/2026-09-GRC-V4.md)
and the pinned local/predecessor records. Current forensic provenance retains
the RG2b, C baseline/reference/mobility, initializer and receipt-parent claim
classifications, source/edge references and trace digests. Forward verification
obligations are not retroactively promoted by this bounded run.

The [crossing record](P9-7.7-C_RG2b-Crossings.json) binds the local product,
exact source hashes, test roster, environment, content-addressed operation
preimages, retained aliases and seven-cell mapping.

Run from the repository root:

```bash
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_c_rg2b_crossings.py
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_c_rg2b_crossings.py --run
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python -m unittest test_p977_c_rg2b_crossing_evidence test_profile_g2_registry test_profile_materializers -v
```

Default mode validates retained evidence. `--run` emits a fresh record to stdout
without overwriting the original. The shared registry/materializer exposes
`c_rg2b_crossings` through API/notebook/browser without bespoke dispatch or a new
acceptance layer. Earlier local and historical numerical campaigns are reused,
not rerun.
