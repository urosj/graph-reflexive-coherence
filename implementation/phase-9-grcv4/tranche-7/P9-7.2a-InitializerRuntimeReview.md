# P9-7.2a initializer runtime review

Status: **implementation accepted by the user** through the accept-and-commit
request on 2026-09-11. The retained focused capture passed **25/25**.
Aggregate P9-7.2a review remains separate from this implementation acceptance
and from acceptance of the specification at `f7962e4`.

This review supplies the later acceptance disposition for execution record
`446e37a1764f8863f973ad5443ccde3c68d4eecdc07242e3d7c1f64f82f8be61`.
Its capture-time `verified_pending_review` / `user_accepted: false` fields stay
unchanged; they are not a revocation of this subsequent user decision.

## Scope

The accepted [supplement](../../../specs/grc-v4-a-initializer-spec.md) now has
an [additive release](../../../specs/grc-v4-a-initializer-release.json), packaged
schema and explicit codec selection. The predecessor release, base schema,
original asset index, old codec pins, accepted proposal/paper and investigation
rows remain unchanged. Accepted spec bytes retain their drafting-stage labels;
the [spec review](P9-7.2a-SpecificationReview.md) records their later acceptance.

`CandidateAReferencePass` computes the fresh fixed-weight differential,
auxiliary conductance, rounded positive mobility, full-component potential,
reference flux and original unfloored final conductance law. It never constructs
total current at the auxiliary W. `CandidateAReferencePassPair` invokes it
independently for current/reset and recomputes retained outputs on import.

The existing migration owner now maps C→A using this pair, admits both final
targets and atomically publishes current/reset, reference/backend ownership,
receipts, commits and crossing archives. The new v2 receipt binds the pair;
the new snapshot layout reconstructs archived crossings, not an initializer
constraint on ordinarily evolved W. Other transitions carry a null pair.
The ordinary A writer and all five realization owners admit the new history
policy without changing their numerical laws. The old explicit-flux constructor
continues to mean exactly what it did before this implementation.

## Focused evidence

The [new execution record](P9-7.2a-InitializerRuntime.json) binds exact source
and loaded-module hashes, environment, accepted document subjects, test IDs,
input/target references, construction pairs, endpoints and actual receipts.
It contains no external input paths or archive. Test fixtures and the runner
are part of its source bindings; rerunning produces a new execution record.

The bounded roster has 18 new methods and seven changed-path compatibility
guards. It covers:

- Independent rational WLS elimination and constitutive arithmetic on a
  nontrivial graph, all three channels and both gamma signs, parallel edges,
  loops, an isolate, vertex/edge permutations and signed reorientation.
- Active negative-gamma graph migration and return to C with the initializer
  proof retained. C_OS/C_PC source differences do not change the target pair.
- Whole-expression rounding, huge-channel cancellation, an active auxiliary
  floor with an unfloored final exponent, subnormal W, canonical flux zero,
  mobility underflow and stage-specific overflow through final mobility.
- The actual auxiliary-singular/final-regular producer and migration regression;
  identical operands execute twice with equal values and distinct role IDs.
- Positive C→A through A_OS, A_CI, A_PC, A_CI_PC and A_RG2b, distinct reset W,
  directional carrier loss/zeroing, no candidate-history loss, and unchanged
  C/charge/clock. Each runs continuation, reset, duplication and restoration;
  A_OS also runs save/load in a fresh process.
- Real reset-only construction failure, fixed-chart rejection, programmer and
  late-publication failures, rehashed output/intermediate/receipt forgeries,
  missing/duplicate/extraneous pairs, and old/unknown release-layout rejection.
- Selected prior migrations and F1/F2 mapper/archived/repeated/prospective
  commitment guards. The old negative C→A test still rejects the unadmitted
  explicit-flux policy. No full historical campaign is rerun.

Separately, 17 document/release/specification checks and four read-only evidence
and UX methods pass. The latter execute the actual notebook status cell, HTTP
handler and browser validator, including four rehashed overclaim/permission
controls. Browser and runtime registries agree on 47 eligible/59 total paths.

Development checks found two integration omissions (the OS history-policy gate
and successful-receipt union dispatch); both were corrected before capture.
The singularity test also needed its declared finite solver tolerance instead
of the raw zero-tolerance fixture. These were implementation/test setup issues,
not changes to the accepted scientific rule or output-dependent chart repairs.
An initial 25/25 run could not serialize its evidence because the recorder
queried uninstalled optional SciPy metadata. The recorder now captures explicit
dependency absence before execution; the retained JSON is a subsequent new run,
not reconstructed output attributed to that earlier run.

## Checking and continuation

From the repository root:

```sh
PYTHONPATH=src:. .venv/bin/python implementation/phase-9-grcv4/verification/verify_p972a_migrations.py --check
```

This checks the additive package, bound new evidence, accepted sources and exact
old 25/17-test subjects without numerical execution. The accepted predecessor
bytes remain recoverable from Git; current changes are limited to the explicit
initializer runtime path set and bound by the new record, not a blanket runtime
exemption. API/notebook/browser status exposes `initializer_runtime` separately
from immutable forensic design traces. Missing or stale evidence fails closed.

For a deliberately new run, use `verify_p972a_initializer_runtime.py --run` with
the same interpreter/PYTHONPATH. It emits JSON to stdout and progress to stderr;
retain it under a new repository-relative name if needed. Do not overwrite the
original accepted evidence. Platform `math.exp` is environment-bound, not a
promise of bitwise equality across arbitrary systems.

Next: review the new execution and aggregate all seven migration classes.
This implementation does not itself accept aggregate 7.2a, open generic events
(7.2b), widen G2/G3 or public support, establish native formation, or supply the
separate exact GRC9V3 specialization initializer. The supported public singleton
remains the accepted exact C_OS profile.
