# P9-G2[C_CI] integrated review

Verdict: **PASS proposal for the exact declared scope**, pending review and
explicit G2 acceptance. This is not an independent audit or a G2 decision.
The bounded reconciliation was accepted and committed as `565418a` first.
Public discovery still contains only exact C_OS, A_OS and A_CI declarations.

Proposed complete profile:
`grcv4-profile-sha256:a56ef981821478cc50a3551a914dd6240e0dc62c9ca69d52305bd59a3405f69e`.

[Machine-readable review](./P9-7.7-C_CI-G2Review.json), digest
`56edc8d0a725a693a0363bf66f23bdcbe000357403928f9fd84da991c542bedc`,
binds the exact nomination, current scientific sources/releases, accepted
bounded reconciliation, all 33 catalog obligations, ordered applicability,
interface supplement and typed contract/debt traces.

## Three G2 obligations

| Obligation | Integrated result |
| --- | --- |
| `G2-EXACT-PRODUCT` | All 33 unique catalog IDs resolve to 26 local rows and seven crossing dispositions. Exact controls and provisional/fixed-stage observations remain labeled; they are not new supported profiles or additional committed operations. |
| `G2-ORDERED-ENDPOINTS` | Exact incoming/outgoing evidence, separate PC/CI+PC applicability, selected outgoing A initializer and renamed C event target are explicitly distinguished. No arbitrary-pair support follows from profile acceptance. |
| `G2-INTEGRATED-REVIEW` | Specs, paper, claim/debt traces, native lifecycle results, retained numerical observations and public facade agree for the bounded declaration. Source obligations and scientific authority classes are not promoted by passing this review. |

Sources are the [V4 specification](../../../specs/grc-v4-spec.md), its
[common-interface extension](../../../specs/grc-common-interface-v4-ext.md),
the unchanged common interface, initializer/event contracts, and the
[substrate paper](../../investigations/grc9v4-constitutive-design/drafts/2026-09-GRC-V4.md),
particularly the Candidate C baseline and §10.2 CI contract. Typed forensic
queries cover the D11 reference field, distinct mobility/Hodge constructors,
baseline/derivative/covariance/lifecycle/realization coverage, C CI root and
selection contracts, optional initializer, receipt-parent and abundance
contracts and their debts. D10.2 indeterminate/review dispositions and forward
verification obligations remain intact.

The [local review](./P9-7.7-C_CI-LocalReview.md) and
[crossing review](./P9-7.7-C_CI-CrossingReview.md) retain the detailed scientific
and lifecycle evidence. Four local methods and three crossing methods are
reused, not rerun or counted as a second independent campaign.

## Essential scope limits

- The nomination has zero mobility deformation. The nonzero-deformation,
  noncommuting derivative control is a separate exact declaration.
- The CI evidence certifies the admitted local reference ball, not global
  root existence/uniqueness, arbitrary seeds or parameter/graph sweeps.
- Dense conditioning counterexamples remain explicitly algebra-only controls.
- Incoming A_CI_PC → C_CI loses both histories; C_PC → C_CI loses carrier
  history only. Outgoing C_CI → initializer-enabled A_OS initializes W without
  source-history loss. Candidate and carrier channels are never conflated.
- The initializer-enabled A_OS and renamed-edge C_CI event targets are not
  proposed additions to public support. Separate PC-pair witnesses are not
  executions of the nominated nonpersistent C_CI profile.
- G3, GRC9V4 specialization, aggregate P9-7.7 and all other profile decisions
  remain separate. No implementation permission is added.

## Public-facade supplement

[One focused interface method](./P9-7.7-C_CI-G2Interface.json), digest
`bee5b94261934457045d3f83f8d29b298c436e1baf103bbbcb824d2906080eb9`,
passed against the exact C_CI declaration. It checks detached configuration,
immutable parameters and unchanged assignment, local versus public discovery,
missing defaults and malformed inputs, C operation without an A differential
backend, strict/external zero-duration request equivalence, and preservation
of a prior commit when a request iterator fails. The zero-duration check adds
no positive-step numerical credit; that remains in the local product.

## Fourth-profile dispatch

The Python status entry point now uses the pinned registry's ordered
`materializers` table for local → bounded crossing → G2 checks. Dependencies,
module paths, current source hashes, import origins and exact returned views
are checked; module names are never accepted from incoming status data.
Scientific checkers retain their separate schemas and equations.

The same table supplies bounded views and G2 review inputs. Results are kept
private until materialization succeeds, and the entry point tracks the produced
keys for generic cleanup after later failures. Thus adding this fourth G2
profile requires no family-specific imports, payload dictionaries, review
seed or cleanup list in the status entry point. Historical acceptance records
and runtime/scientific sources remain unchanged.

Validation is deliberately bounded: retained-review mutations, dependency and
source/origin pressure, ordered dispatch and partial-result rejection, the
actual cleanup loop with a synthetic late-failure payload, and one real status
reused for notebook/HTTP/browser rendering and rejection controls. Synthetic
orchestration checks do not count as numerical evidence.

## Decision and reproduction

Accept or return this exact G2 proposal. Acceptance would add only the nominated
C_CI declaration; it is not inferred from the prior bounded acceptance.

From the repository root, inspect the retained proposal without rerunning the
numerical campaign:

```bash
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_c_ci_g2.py
```

`--emit` reconstructs the review from current bound evidence;
`--capture-interface` emits a new facade-only run without replacing the retained
capture. Existing acceptance and execution records are not rewritten.
