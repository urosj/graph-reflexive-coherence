# P9-G2[A_RG2b] — exact declaration accepted

The user accepted the reviewed G2 proposal on 2026-09-15 with “accept and commit”.
The [decision record](P9-7.7-A_RG2b-G2Acceptance.json) binds the reviewed
checkpoint `504859f`, original review text, 28-cell product and facade evidence.
Those records remain unchanged, including their execution-time pending flags.

Public discovery now adds exactly
`grcv4-profile-sha256:12abb2946bfaa616df2a42bd571732e2be3000736f5078d45f8dbf53682b212b`.
Nine exact declarations are accepted. The existing shared adapter/materializer
projects this decision through API, notebook and browser views.

Acceptance preserves the frozen-completion-relative bounded Lipschitz section,
finite approximation error and fixed positive beat. State readmission on K
does not imply ordinary-entry admission on K-minus. The separate scalar
witnesses, zero controls, initializer/event targets and arbitrary graph/parameter
declarations are not added support. The C1 debt is not resolved; no classical
derivative/spectrum, CI equivalence, indefinite chart invariance or formation
authentication is claimed. C_RG2b, all unreviewed pairs, disabled specialization
reductions, aggregate P9-7.7 and G3 remain separate. No new runtime iterations
or specialization support sets are authorized.

The [finite source-reuse record](P9-7.7-A_RG2b-G2SourceReuse.json) pins exact
before/after hashes for discovery declarations/assertions and retained-source
projection changes. It does not waive arbitrary drift, change numerical laws,
rewrite accepted executions or claim a numerical rerun. Prior acceptance and
review records remain byte-identical to the checkpoint.

Verification: all 17 focused acceptance/registry/materializer methods passed
in 53.938 seconds, and all three native profile-discovery regression methods
passed in 1.832 seconds. The work manifest mirrors the accepted nine-profile
set and the exact changed discovery-source hashes; existing iteration owners
and permissions are unchanged. No numerical campaign was rerun.

Focused verification commands from repository root:

```bash
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python -m unittest test_p977_a_rg2b_g2_acceptance test_p977_a_rg2b_g2_surfaces test_profile_g2_registry test_profile_materializers -v
```
