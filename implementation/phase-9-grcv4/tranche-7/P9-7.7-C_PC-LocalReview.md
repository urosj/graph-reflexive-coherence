# P9-7.7 — C_PC local reconciliation

Status: bounded local/crossing reconciliation accepted by the user on 2026-09-14.
The [crossing review](./P9-7.7-C_PC-CrossingReview.md) records the combined
33-cell decision. Original run bytes/flags are unchanged. No C_PC G2 acceptance
or public-support addition.

The exact original 7.1 nomination is
`grcv4-profile-sha256:6105daf6f5111fdc51640194298b1b8398d608684791050d85b696bcd681f64f`.
The [retained native capture](./P9-7.7-C_PC-LocalProduct.json) covers 26 local
catalog cells through five passing methods (74.273 seconds): eight common,
13 Candidate C, PC-ZOH, and four fixed-profile lifecycle cells. Seven crossing
obligations were separate at capture. The subsequent
[crossing reconciliation](./P9-7.7-C_PC-CrossingReview.md) supplies those seven
cells without changing this run's bytes or flags. The user subsequently accepted
the combined bounded result; integrated G2 remains separate.

## Scientific evidence

- Reconstruct geometry from old committed Z; independently check the selector,
  selected sector, retained Hodge, potential, baseline current, retained/physical
  identification, total current and read-back. The structural source is computed
  there and held through continuity and the one exponential ZOH write.
- The exact nomination's seed and a same-profile signed-history companion
  distinguish old geometry from both reference and prospective geometry. Live
  and reset Z are independently supplied. Dense equations and a 2000-digit
  scalar ODE oracle check current, source, updated C and updated Z.
- Reconstruct and independently check the entire C chain again for reset and
  post-write current. Their currents and geometries are not borrowed from the
  consumed state. Post-continuity baseline/source changes do not become a
  second writer source or feed the same beat's consumed current.
- Count zero A-history writers, one continuity evaluation and one Z writer.
  Candidate C has no W_A, selector or mobility state. Its realization does have
  authoritative Z; the C-only candidate contract must not erase that carrier.
  Attempted A-history admission rejects.
- Separate chi-zero and zeta-zero declarations retain the direct baseline and
  produce zero structural source. Starting from nonzero negative Z, their
  provisional native steps show nonzero exponential release, not reset/drop.
  They are not additional supported profiles or nomination executions.
- Fixed-stage checks distinguish mobility from structural Hodge, strict selector
  gaps from selector-boundary rejection, typed physical identification, and
  retained versus physical conditioning. Baseline derivative/covariance is
  checked on the nominal stratum and a separately declared nonzero-deformation,
  noncommuting three-node control. The latter is fixed-stage evidence, not a
  complete PC trajectory or nomination execution.

The nomination has `kappa_M_C=0`. Its direct baseline still consumes the
retained geometry reconstructed from Z. No matched-forcing trajectory
contraction, indefinite base-chart invariance, endpoint hysteresis, all-graph
coverage or CI+PC/RG2b result is inferred. The envelope bounds and observed
source/domain checks retain their bounded meaning; PC is not Candidate B.

## Lifecycle and negative pressure

Common facade cases cover ordinary/zero/negative duration, current-stage
domain/nonfinite faults, detached stale-observable pressure, malformed cache
input and postsolve charge failure. Fault injection is explicitly labeled;
these controls are not claims of naturally occurring solver failures.

Reset readmission rejects before a consumed solve/writer, with no solver
outcome; final readmission rejects after a valid consumed solve and one
prospective carrier write. Both injected failures preserve the entire owned
scientific/lifecycle publication, including C and Z.

Save/load and replay preserve exact results and snapshots. Reset after ordinary
steps uses independent reset C/Z without rewinding time or charge. Persistent
ledger is distinct from emitted delta. Coherently rehashed missing-parent
receipts reject, and exported/duplicated state, reset carrier, receipt/commit ledger
mutations do not alter the original owner.

## Sources, integrity and access

Sources are the [V4 specification](../../../specs/grc-v4-spec.md),
[catalog](../../../specs/grc-v4-conformance-fixtures.json),
[common-interface extension](../../../specs/grc-common-interface-v4-ext.md),
the [paper's PC contract](../../investigations/grc9v4-constitutive-design/drafts/2026-09-GRC-V4.md#105-persistent-carrier--pc),
and accepted Tranche 6 PC evidence. Current typed forensic queries retain PC
parent/writer/release/matched-forcing provenance, D11 reference/mobility/baseline/
derivative/covariance contracts and the parent-receipt ceiling. Original claim
classifications, source references and trace digests are not promoted by passing.

The checker binds current source bytes, exact nomination/predecessor, test
roster, content-addressed preimages, actual dispositions, independent equations,
both-role admission, history ownership and scope ceilings. Rehashed mutation
pressure covers numerical substitutions, lost roles, writer counts, invented
candidate state, release/drop confusion, authority changes and support widening.

The `c_pc_local_product` row uses the existing pinned registry/materializer and
shared API/notebook/browser presentation, with no new profile dispatch branch.
Public support remains exactly C_OS/A_OS/A_CI/C_CI/A_PC. The original local view
retains its pending-review flags; the crossing view separately projects the
subsequent combined bounded acceptance.

Verification passed: five native methods, 37 rehashed evidence mutations,
11 shared registry/materializer tests, and one real status check reused across
notebook, HTTP and browser transports. Browser checks cover the C_PC pending
row and 105 rejected payload alterations across registered evidence/acceptance
views. The surface check validates retained evidence; it does not rerun the
native numerical suite.

From repository root, check retained evidence without a numerical rerun:

```bash
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_c_pc_local.py
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python -m unittest test_p977_c_pc_local_evidence -v
```

Use `--run` on the checker only for a deliberately new execution; stdout is a
new record, not a replacement for this capture. With the subsequent crossings
and bounded acceptance complete, next is integrated C_PC G2 review.
