# P9-7.7 — A_CI crossing reconciliation

Status: **bounded local/crossing reconciliation accepted by the user on
2026-09-14**: “i'd accept A_CI so that we can later do A_CI G2 review.”
This accepts the 21 local cells and seven crossing evidence/dispositions,
including their explicit endpoint limits. It does not assert an independent
audit or accept A_CI G2. Original execution flags remain capture-time facts.
Together with the [21-cell local product](./P9-7.7-A_CI-LocalReview.md), every
catalog row now has bounded evidence or an explicit endpoint disposition.
Integrated applicability/public-facade G2 review and a separate G2 decision
remain. Public support is still the exact accepted C_OS/A_OS pair; G3 and
aggregate P9-7.7 remain open.

The nomination stays
`grcv4-profile-sha256:16ed65f7f65d4716e1be3e384f6fa0f957d26dd7b7a3f7e1b43ad1aa3f250946`.
The new crossings use the same complete profile with the explicitly captured,
near-equilibrium distinct current/reset inputs of the accepted migration
fixtures. A profile ID is not a substitute for those state/reference inputs.

## Retained evidence and new execution

The [crossing record](./P9-7.7-A_CI-Crossings.json) references five unchanged
retained cases. A_OS→A_CI has the exact nominated **target**, and A_CI→A_PC
has the exact nominated **source**. Their role-specific history, ordered
receipt endpoints, emitted delta and reset values are reused, not rerun.
The old receipts do not record a solver outcome; the projection says so
explicitly rather than inventing one. PC↔CI+PC and the positive C→initializer-A_CI
case remain separate-profile witnesses, not borrowed nomination credit.

Three focused methods add six cases:

- **A_PC→A_CI:** preserve each role's C and W, explicitly lose nonzero,
  distinct current/reset Z, then reset and restore the complete snapshot.
- **A_CI→C_OS:** preserve both C roles, explicitly lose candidate W, retain
  absent Z, then reset and restore. Clock, index and target charge stay fixed.
- **Mapped A_CI→initializer-enabled A_CI event:** independently calculate the
  resource swap plus 1/8 increment for both roles and use the independent
  reference-pass oracle for their distinct target W. Verify explicit W loss,
  absent carrier, charge increment, unchanged clock/index, reset, archive and
  ledger retention, and rejection of a missing transition preimage.
- **Reset-only target-readmission failure and successful control:** nominated
  A_CI→A_PC with identical live authority and target but different reset W.
  The failing reset rejects at `target_readmission`, before publication; the
  current-as-reset control commits. Whole publication/payload stay unchanged
  on failure, and its emitted failure is not appended to the persistent ledger.
- **Incoming C→nominated A_CI rejection:** a well-formed request declares the
  initializer while targeting the nomination that does not select it. Admission
  rejects without mutating publication. This is not positive initializer coverage.

The event target is separately identified as
`grcv4-profile-sha256:7e90f24e123cbaa2571aa5071489a8b4fdec815098925cba51ed01be09b1fe2b`.
It does not replace the nomination or acquire the nomination's local-product
credit. This finite affine event is not all maps, all graphs or GRC9 expansion.
No production code, scientific source, paper, specification or old run changed.

The C_OS endpoint/control is
`grcv4-profile-sha256:ac3101442242ef71e4d39bcf8e52e288c7ee5f5c3a7dc76de9e405fb72a735e2`,
not the globally accepted C_OS declaration ending in `2f59b6d`. The PC endpoint
is likewise a locally admitted target, not globally accepted support. Exact
A_CI source/target credit does not grant the other endpoints G2 or establish
crossings between every pair of publicly supported declarations.

## Seven obligations and applicability

| Catalog row | Evidence/disposition |
| --- | --- |
| `A-MIGRATION-HISTORY-RECEIPT` | Retained incoming A_OS→A_CI and outgoing A_CI→A_PC; new incoming PC and outgoing C crossings. |
| `RESET-AFTER-MIGRATION` | Retained reset endpoints and new explicit reset/restore checks. |
| `RESET-AFTER-EVENT` | Mapped event followed by target reset; both independently initialized roles retained. |
| `WHOLE-LIFECYCLE-TUPLE-MAP` | Exact affine resources, independent W, no Z, target reference, charge, clock/index, archive and ledger. |
| `ALL-MIGRATION-CLASSES` | Seven-class endpoint matrix below; not an all-pairs execution claim. |
| `HISTORY-DISPOSITION` | W and Z independently preserved, initialized, lost or absent under the selected policy. |
| `TARGET-READMISSION-FAILURE` | Reset-only rejection versus a passing current-as-reset control for the same target. |

| Migration class | Exact disposition |
| --- | --- |
| Same candidate NP→NP | Retained A_OS→nominated A_CI: positive exact target. |
| Same candidate NP→P | Retained nominated A_CI→A_PC: positive exact source. |
| Same candidate P→NP | New A_PC→nominated A_CI: positive exact target. |
| PC→CI+PC | Retained separate pair; neither endpoint is A_CI. |
| CI+PC→PC | Retained reverse separate pair; neither endpoint is A_CI. |
| A→C | New nominated A_CI→C_OS: positive exact source. |
| C→A | New rejection at the exact nominated target; retained positive uses the different initializer-enabled A_CI. |

The nominal history policy accepts supplied reference history, not the
C→A producer. No positive incoming C→this nomination is claimed. The separate
PC pairs are not relabeled as A_CI executions. Integrated review must accept
or return these scoped dispositions; this reconciliation cannot waive a
mandatory positive row. If the support set must include a positive initializer
target, that different identity needs its own product and nomination review.

## Authority, validation and reproduction

The local CI root/contraction and parent-ceiling traces are retained alongside
a fresh typed `P9-EC-A-INITIALIZER-REFERENCE-PASS` provenance trace. They preserve
their classifications, support dispositions, source/edge references and digests.
The optional initializer remains a selected endpoint policy, not a universal
default. Forward verification obligations do not become backward evidence.

All three focused methods passed in **92.433 seconds**, with no failures,
errors or skips. No retained migrations or local numerical methods were rerun.
Record digest:
`a5d8a9ea17843359b57e094cacb7f8d0dde1fa9ab46480f1eae6632acd2aba80`.

From the repository root:

```bash
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_a_ci_crossings.py
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python -m unittest test_p977_a_ci_crossing_evidence test_p977_a_ci_surfaces -v
```

The default validates retained evidence; `--run` emits a distinct new run to
stdout without overwriting this one. Source/environment bindings, full
content-addressed pre/post/reset snapshots, requests and results accompany
the aliases. All paths are repository-relative. The status API, notebook and
browser expose `a_ci_crossings` separately from the unchanged historical local
view. Transport tests reuse one real checked status instead of repeatedly
validating the entire predecessor chain. Opening status never runs numerics.

Verification passed: **22 rehashed evidence controls** (18.947 seconds) and
the one-check API/notebook/HTTP/browser test with **16 browser controls**
(66.977 seconds). Whitespace and machine-local-path checks passed. No broad
numerical suite or original local-product method was rerun for these checks.

Next: perform integrated A_CI G2 applicability and public-facade review, then
obtain a separate G2 decision. The user decision above accepts the bounded
reconciliation; neither the original execution nor that decision grants G2.
