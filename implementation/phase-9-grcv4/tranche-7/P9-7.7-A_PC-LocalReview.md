# P9-7.7 — A_PC local reconciliation

Status: four-method native capture passed; local product pending independent
review and user acceptance. No new G2 support.

The subject is the original 7.1 A_PC nomination
`grcv4-profile-sha256:058ae6b1f923c85952ffdfa083af74e3b56dd450f309190f307c3ea56ac2aa75`.
Its unchanged profile has independently declared live/reset C, W and Z. A
signed nonzero-carrier companion changes authoritative state, not profile
identity. Chi-zero and zeta-zero controls are distinct parameter declarations.

## Bounded scope

Reconcile 21 local catalog cells through four focused methods: eight common,
eight Candidate A, PC-ZOH, and four fixed-profile lifecycle rows. Seven ordered
crossing obligations remain in this local record. They are now separately
reconciled in the [crossing review](./P9-7.7-A_PC-CrossingReview.md), preserving
this original execution record unchanged. Neither is an integrated G2 proposal
or acceptance.

- Read geometry from old committed Z; calculate current and the held structural
  source there. Compare with independent dense equations.
- Perform one continuity evaluation, one refreshed logarithmic W update, and
  one exponential Z update using the held prestate source. The W writer still
  carries old Z; prospective W/Z never feed the consumed same-beat current.
- Independently check reset and post-write native readmission, including both
  histories and their reconstructed geometry.
- Check zero-source exponential release from nonzero Z for both zero controls.
  Native release is neither reset nor migration-driven history removal.
- Verify public common operations, full rollback after a postsolve charge
  fault, reset rejection before writers, and final readmission rejection after
  the prospective carrier write. Injected faults are labeled test controls,
  not evidence of naturally occurring solver failure.
- Verify save/load/replay, persistent ledger versus emitted delta, previous
  primary receipt ownership, independent reset, and detached duplicate C/W/Z.

The original nomination starts with supplied W and zero Z. This is not evidence
of authenticated W formation. The carrier envelope/domain certificate does not
establish matched-forcing trajectory contraction, indefinite base-chart
invariance, or every profile parameter/graph. PC is not Candidate B.

## Authority and reproduction

Sources are the [specification](../../../specs/grc-v4-spec.md), its
[catalog](../../../specs/grc-v4-conformance-fixtures.json), the
[paper](../../investigations/grc9v4-constitutive-design/drafts/2026-09-GRC-V4.md),
and accepted [PC numerical evidence](../tranche-6/P9-6.2ab-Review.md).
Typed PC parent, ZOH writer, release and matched-forcing provenance traces retain
`indeterminate_requires_review`; runtime tests do not promote that classification.
The parent-receipt ceiling remains separately traced. Old evidence is unchanged.

From repository root:

```bash
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_a_pc_local.py
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python -m unittest test_p977_a_pc_local_evidence -v
```

The checker validates retained results and independently recalculates their
equations without executing the native campaign. `--run` emits a fresh execution
record to stdout, never replacing the retained run silently. No larger parameter
sweep or historical numerical campaign is required for this local reconciliation.

The [retained execution](./P9-7.7-A_PC-LocalProduct.json) contains 21 result rows,
content-addressed operands/snapshots, four methods with no failures or skips, and
two explicitly injected readmission rejection sequences. Its digest is
`fb13a73a10e3ffb7cfde778ce86b89e238ae0c893eb11f5f0ed5d8352c2ff6e7`.
The successful native capture took 81 seconds. Initial development corrected a
test expectation: reset rejection reports no consumed solver disposition,
whereas final readmission rejection preserves the consumed `valid_root`; both
retain the `no_admitted_root` failure code and their distinct stages. No runtime
code or scientific criterion was changed.

`a_pc_local_product` is exposed by the existing registry-driven
API/notebook/browser path using one local materializer and pinned view, without
new profile-specific dispatch or rendering branches. The four accepted G2
declarations and historical records remain unchanged. Review this bounded local
product next, then reconcile its seven ordered crossing obligations.

Validation passed: 24 rehashed retained-evidence mutations; 12 local-registry,
shared dispatch and G2 boundary tests; and one actual status reused across
API/notebook/HTTP/browser with 85 browser rejection controls (139 seconds).
The native campaign was not rerun for transport or display validation.
Repository-relative references and diff-whitespace checks passed. No production
source, historical evidence, accepted support or runtime permission changed.
