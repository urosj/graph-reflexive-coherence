# P9-7.7 — A_CI+PC crossing reconciliation

Status: bounded local/crossing reconciliation accepted by the user on 2026-09-14.
The explicit “accept, commit and then continue with G2” decision covers all
21 local cells and seven crossing dispositions. Original execution records and
their flags remain unchanged. This does not accept A_CI+PC G2, all-pairs,
aggregate P9-7.7 or G3. Existing six exact G2 declarations are unchanged.

The [local product](P9-7.7-A_CI_PC-LocalReview.md) covers 21 cells. This
[crossing record](P9-7.7-A_CI_PC-Crossings.json) addresses the remaining seven:
A migration-history receipt, reset after migration/event, whole-lifecycle tuple
mapping, migration classes, history disposition and target-readmission failure.
The exact nomination remains
`grcv4-profile-sha256:5f2f848af0f482699ac6cb88e4e1bd1a66458774bac2c3cc6df9f74cc47d7689`.
Class reconciliation does not mean universal positive execution of all pairs.

## Ordered evidence

Five retained aliases preserve original record identities, receipt deltas,
ordered endpoints and two-role maps. Three directly involve the nomination;
the nonpersistent pair and positive initializer target remain separate.

| Class | Evidence and boundary |
| --- | --- |
| Nonpersistent → nonpersistent | Retained A_OS → A_CI; separate endpoints, no A_CI+PC execution credit. |
| Nonpersistent → persistent | New A_CI → exact A_CI+PC; preserve W, initialize zero Z, no loss. |
| Persistent → nonpersistent | New exact A_CI+PC → A_CI; preserve W, explicitly lose Z. |
| PC → CI+PC | Retained exact A_PC → nomination; preserve W/Z under exact carrier-contract identity. |
| CI+PC → PC | Reverse retained nomination → exact A_PC; new altered-carrier-contract request must reject. |
| A → C | Retained nomination → C_CI; explicit candidate and carrier loss, with no target carrier slot. |
| C → A | New operation rejects the unselected initializer on the nomination. Retained C_PC → initializer-selected A_CI+PC is a different declaration, not nomination support. |

The preserving PC pair has distinct nonzero live/reset carriers. Its exact
graph/K4/weights/context/common/candidate/geometry/carrier identity is checked in
both directions. Dropping Z into a nonpersistent realization, resetting a
target carrier with declared loss, and native exponential release are distinct.
Retained migration receipts do not record a solver disposition; none is inferred.

## New native cases

Four focused methods produce seven cases, passing in 123.639 seconds:

- Same-candidate A_CI↔A_CI+PC migration independently checks both current/reset
  C/W/Z maps and target implicit roots. Incoming carrier initialization loses
  nothing; outgoing carrier removal is explicit loss. Replay is exact. Reset
  after migration preserves its independently mapped authority, time, charge,
  archives and persistent ledger, not merely the emitted receipt delta.
- A selected affine event swaps resources and adds `(-1/8, 0)`. It independently
  initializes W for both roles, explicitly loses source W/Z and sets target Z
  to zero. The distinct event target selects initializer `gamma=2` and root
  tolerance `1e-14`, making immediate geometry measurable. It is neither the
  nomination nor the separately retained positive C→A target. Independent
  coupled equations check current, geometry and same-root structural source:
  zero carrier does not erase the immediate source. The event changes charge
  by -1/8 but not time. Missing archives reject; reset preserves mapped authority.
- A current-as-reset control passes a C_CI+PC target resource radius 3. An
  otherwise identical source-admissible reset `(3, 1)` alone exceeds that chart.
  The failure occurs at target readmission before publication, preserving the
  whole owned scientific/lifecycle state. Both source roles fit radius 4.
- C_PC→nomination rejects its unselected initializer at actual operation
  admission. Nomination→A_PC with changed `tau_PC` also rejects at admission:
  exact carrier preservation cannot silently become reset. Both failures keep
  the complete publication unchanged.

## Authority, integrity and access

Sources remain the [V4 specification](../../../specs/grc-v4-spec.md),
[catalog](../../../specs/grc-v4-conformance-fixtures.json),
[common-interface extension](../../../specs/grc-common-interface-v4-ext.md),
the [paper](../../investigations/grc9v4-constitutive-design/drafts/2026-09-GRC-V4.md),
and retained migration/initializer evidence. The typed forensic API supplies
the local composition/root/carrier/receipt ceilings plus the optional accepted
reference-pass initializer contract. Classifications, source/edge references
and trace digests are retained; execution does not promote scientific authority.

The checker binds exact sources, local predecessor, test roster, content-addressed
snapshots/requests/results, two-role equations, losses and ordered scope.
Rehashed mutations pressure missing cases/classes, borrowed endpoints, false
fixed-reference geometry, erased losses, reset/readmission substitution and
acceptance/support widening. One registry materializer depends on
`a_ci_pc_local_product`; generic API/notebook/browser dispatch needs no new branch.

The capture digest is
`484beef4f47d73f0c255c50c5299fcb79179dfa59813a2dde1a2a868e461b4e5`.
All 39 rehashed evidence mutations and 11 shared registry/materializer tests
passed. One actual status check, reused across notebook/HTTP/browser, passed in
397.728 seconds with the pending A_CI+PC crossing row and 130 browser rejection
controls. The surface check validates retained evidence; it does not rerun native
campaigns. Initial fixture probes selected the separate event declaration so
its immediate geometry was numerically discriminating, rather than crediting
an effect below the standard target's solver tolerance.

From repository root:

```bash
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_a_ci_pc_crossings.py
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python -m unittest test_p977_a_ci_pc_crossing_evidence -v
```

Default verification checks retained evidence. `--run` emits a deliberately new
native record to stdout; it does not replace the historical run. Capture checks
the exact local preimages/equations; the shared status path checks full ancestry
once. The next step after this bounded acceptance is separate integrated G2.
No all-pairs, aggregate P9-7.7 or G3 closure is claimed.
