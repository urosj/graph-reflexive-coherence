# P9-7.7 — A_RG2b integrated G2 review

Status: **PASS proposal, pending separate G2 acceptance**. The user accepted bounded local/crossing
reconciliation at commit `5ea4213`; this is the subsequent, separate G2 review.
No G2 acceptance, discovery update, G3 or aggregate P9-7.7 closure is inferred.

The only proposed additional declaration is
`grcv4-profile-sha256:12abb2946bfaa616df2a42bd571732e2be3000736f5078d45f8dbf53682b212b`.
It is the original four-node, three-edge A_RG2b graph nomination, with nonuniform
weights, supplied current/reset W, and its exact frozen completion. A family
label does not authorize arbitrary parameters, graphs or completions.

## Evidence reconciliation

The [integrated record](P9-7.7-A_RG2b-G2Review.json) binds all 28 catalog cells:

| Obligation | Evidence and boundary |
| --- | --- |
| `G2-EXACT-PRODUCT` | [21-cell local product](P9-7.7-A_RG2b-LocalProduct.json): independent inverse/current/continuity/log-writer equations; finite section error; separate current/reset/restart reconstruction; failures, replay and ownership. Chi-zero and zeta-zero controls are separate declarations. |
| `G2-ORDERED-ENDPOINTS` | [Seven crossing cells](P9-7.7-A_RG2b-Crossings.json): nine native cases and four separately scoped retained aliases. Exact graph migration endpoints, two-role section/current readmission, explicit W/Z dispositions, event initialization, reset/replay and K/K-minus pressure. Not all-pairs coverage. |
| `G2-INTEGRATED-REVIEW` | Accepted checkpoint bindings, current forensic authority/debt traces, a complete evidence-reference product and one [facade supplement](P9-7.7-A_RG2b-G2Interface.json). Existing numerical campaigns are checked as retained evidence, not rerun. |

Reuse the four local methods and three crossing methods. The generalized
[Tranche 6 graph result](../tranche-6/P9-6.4c-Review.md), its
[d follow-up](../tranche-6/P9-6.4d-AuditFollowup.json) and
[accepted routing](../tranche-6/P9-6.5-RealizationRouting.json) remain source
context; this exact-profile decision neither repeats their campaigns nor
converts generalized evaluator coverage into arbitrary-graph G2 support.

The facade supplement executes only zero-duration requests. It checks detached
C/W configuration, immutable parameters, required A backend, strict/external
request equivalence, missing-default/wrong-layer rejection, local discovery,
and preservation of a committed prefix when a request iterator raises.
Current/reset W stay distinct, Z stays absent, and zero request duration does
not select a new zero-beat completion. Positive ordinary evolution remains in
the accepted local evidence. Solver status `valid_root` does not mean that
RG2b has become a CI realization.

## Ordered scope and scientific limits

- Graph A_OS→nomination preserves W; nomination→graph A_PC initializes zero Z
  without loss; graph A_PC→nomination explicitly loses Z while preserving W.
- Nomination→graph C_OS explicitly loses candidate W, with no carrier to lose.
- PC↔CI+PC witnesses and the older scalar A_RG2b target are separate endpoints,
  not executions of this graph nomination.
- C→the exact nomination rejects an unselected initializer. The retained
  positive C→A initializer is a separate scalar declaration, not extra support.
- The mapped event leaves the nomination for a separately named graph with an
  initializer-selected policy and its own frozen W chart centered on one.
  Both W roles are independently initialized, with explicit source W loss.
  That event target is not the nomination or the retained scalar target, and
  neither is proposed for G2 here.
- State readmission on K is not ordinary-entry admission on K-minus. A
  reset-only out-of-K target rejects before publication; a K-only target can
  readmit/reset but cannot silently pass ordinary entry.

The admitted section is bounded and Lipschitz, relative to the exact frozen
completion and fixed positive beat, with certified finite approximation error.
It is reconstructed geometry, never persistent history. No classical Jacobian,
spectrum, C1 theorem, arbitrary positive step duration, CI equivalence,
indefinite base-chart invariance or formation authentication is claimed.
Disabled GRC9 specialization reductions remain outside this generic decision.

## Authority and debt

Sources are the [V4 spec](../../../specs/grc-v4-spec.md#reconstructed-geometry-rg2b),
[common-interface extension](../../../specs/grc-common-interface-v4-ext.md),
[paper §10.4](../../investigations/grc9v4-constitutive-design/drafts/2026-09-GRC-V4.md#104-reconstructed-geometry--rg2b),
and current typed forensic queries. The integrated record preserves all trace
classifications, source/edge references and digests. D10.2
`indeterminate_requires_review` support is not rewritten by passing tests.

`GTRS-RG-DEBT-C1-SECTION-REGULARITY` remains **narrowed**, not resolved. Its
activation condition is a derivative or spectrum claim; this proposal excludes
those claims. Initializer, receipt-parent and abundance debt histories retain
their design/forward-verification distinctions. G2 is an exact implementation
conformance decision, not a promotion of those historical scientific claims.

## Verification and next decision

The single native facade method passed in 22.610 seconds. The retained
interface digest is
`6cc88513539a610126cdb35a3bf8a54be0c664ea0418a07aa2442a697f1f981f`.
The integrated review digest is
`71c459901d2590022d44775b8101ac08a2b52e15334032fd8bd4b1735cb84b1f`.
It preserves 15 contract traces and four debt traces.

All 15 focused review/registry/materializer methods passed in 29.800 seconds,
including 14 interface and 25 proposal mutation controls. The actual
maintenance-bound G2 checker rebuilt the proposal from accepted evidence;
the shared browser projection and eight-profile discovery checks also passed.
The acceptance-only checkpoint had separately passed seven focused methods
in 16.708 seconds. No full status chain or earlier numerical campaign was rerun.

Development corrected one facade test expectation: RG2b rejects a missing
typed A differential reference with `TypeError`, not the CI fixture's
`ValueError`. The test now requires that specific exception and message;
no production validation or numerical criterion changed.

From repository root:

```bash
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_a_rg2b_g2.py
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python -m unittest test_p977_a_rg2b_g2.ReviewTests test_p977_a_rg2b_g2_surfaces test_profile_g2_registry test_profile_materializers -v
```

`--emit` reconstructs the integrated review without numerical campaigns;
`--capture-interface` emits a distinct new facade run to stdout. Neither
overwrites retained evidence. The shared registry/materializer exposes the
proposal through the existing API, notebook and browser projections; there is
no new family-specific dispatch or renderer.

Next decision: accept or return this exact G2 proposal. Until acceptance, the
eight accepted declarations remain unchanged. C_RG2b, all unreviewed pairs,
G3/specialization and aggregate P9-7.7 remain separate.
