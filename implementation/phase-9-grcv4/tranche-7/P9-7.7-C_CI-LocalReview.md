# P9-7.7 C_CI local reconciliation

Status: implemented and validated; pending independent review/user acceptance.
This is the local continuation, not C_CI G2 or aggregate P9-7.7 closure.

Subsequent decision: the user accepted the combined bounded local/crossing
reconciliation on 2026-09-14, as recorded in the
[crossing review](./P9-7.7-C_CI-CrossingReview.md). Original capture flags remain
unchanged. Integrated G2 review and G2 acceptance are separate.

Exact nomination:
`grcv4-profile-sha256:a56ef981821478cc50a3551a914dd6240e0dc62c9ca69d52305bd59a3405f69e`.
It is the original C_CI declaration in [P9-7.1](./P9-7.1-Lifecycle.json),
not a replacement inferred from a family label. Public support remains exactly
C_OS, A_OS and A_CI.

## Evidence and scope

[Retained execution](./P9-7.7-C_CI-LocalProduct.json), digest
`156ddbcd49040082d992d7c0c8532a6beb93409812e65f518ceab812f078fbfa`,
records four passing methods, no failures/errors/skips, 26 catalog rows,
content-addressed input/output preimages, source/checker identities,
environment versions and seven typed forensic traces. Shared executions are
referenced by multiple obligations, not counted as independent runs.

| Local obligations | Evidence |
| --- | --- |
| Eight common cases | Native positive/zero/negative-duration operations, fresh-cache consumption, explicit test-only domain/nonfinite/postsolve-charge fault controls, whole-publication rollback. The charge/atomic rows share one execution. |
| Twelve fixed-stage C cases | Stable-edge reference map and separate Hodge/mobility identities; independent selector, retained Hodge, potential, baseline, physical current and read-back; strict gap/boundary, Q_C typing, conditioning, C-only authority, derivative/covariance, zero deformation and chi/zeta controls. Fixed-stage and algebra-only checks are not relabeled as lifecycle executions. |
| C post-continuity rederivation and CI branch | Independent 200-digit scalar bisection versus the production fixed-point root; local contraction/displacement/residual bounds; independent dense equations at every observed live CI trial; one continuity update, no writer/carrier writes; separately admitted live/reset/restart roots. One-iteration zero-tolerance control rejects without fallback. |
| Four fixed-profile lifecycle cases | Save/load/replay equality, reset after ordinary steps from independently declared reset resources, persistent receipt ancestry and coherent missing-parent rejection, duplication/export independence. |

The nomination has `kappa_M_C=0`, so its retained Hodge equals the stage Hodge
and its mobility remains independent of Hodge. It does **not** supply a
nonzero-deformation runtime witness. A separately identified three-vertex,
two-edge, noncommuting-Hodge control with nonzero deformation checks the
baseline derivative against analytical equations and fresh finite differences.
Signed vertex/edge reversal checks baseline and derivative covariance. These
controls do not earn additional supported complete-profile declarations.

Likewise, nominal one-dimensional conditioning certificates are supplemented
by an explicitly algebra-only two-dimensional similarity counterexample;
this is not a dense physical-conditioning failure executed by the nomination.
Zero chi/zeta controls have their own profile identities and one-evaluation
CI roots equal to their baseline. The one-chi check holds J fixed rather than
mistaking the changed solved current for a second chi factor.

The CI certificate establishes only the admitted local reference ball, not
global existence, uniqueness, arbitrary-seed convergence or parameter sweeps.
Forensic provenance preserves the D10.2 indeterminate/review dispositions and
D11 claim boundaries; this local record does not promote source obligations
to completed runtime conformance.

## Remaining work

This section describes the local capture's boundary. The subsequent
[crossing reconciliation](./P9-7.7-C_CI-CrossingReview.md) supplies the seven
dispositions below; combined review/acceptance and integrated G2 remain separate.

Seven catalog obligations remain for crossing reconciliation:

- `C-LIFECYCLE-REFERENCE-MAP`
- `ALL-MIGRATION-CLASSES`
- `HISTORY-DISPOSITION`
- `RESET-AFTER-EVENT`
- `RESET-AFTER-MIGRATION`
- `TARGET-READMISSION-FAILURE`
- `WHOLE-LIFECYCLE-TUPLE-MAP`

Preserve exact incoming/outgoing endpoint and applicability distinctions when
reusing accepted crossing witnesses. Reconcile/review that bounded scope
before the integrated G2 product/facade review and explicit acceptance. No
all-pairs, G3 or specialization support follows from this local result.

## Reproduction and presentation

From the repository root, inspect the retained evidence without rerunning the
numerical campaign:

```bash
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_c_ci_local.py
```

Append `--run` to produce a new execution record on stdout. It does not replace
the retained run. The checker hashes all retained objects, verifies the exact
nomination, current sources and authority, checks operation/result boundaries,
and compares retained numerical observations with independent equations.
Pressure tests mutate and rehash the record; source drift, borrowed profiles,
altered derivatives/CI staging, and premature acceptance/support fail closed.

Development smoke caught a test-only import of a nonexistent read-back helper;
the fixture now uses the public `read_back` method. A successful capture was
repeated because its console transfer was truncated; only the complete retained
capture above is cited. No production code or numerical tolerance was changed.

The current API/notebook/browser expose `c_ci_local_product`. The existing
[registry](./ProfileG2Registry.json) now also pins bounded reconciliation
summaries for shared Python/browser checking and a browser evidence table.
Scientific validators and historical record shapes remain separate; the
registry adds neither acceptance nor execution credit. Surface tests use one
actual checked status and reuse it for notebook/HTTP transport and browser
rejection/rendering checks.
