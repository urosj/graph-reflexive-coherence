# P9-7.7 — C_RG2b integrated G2 review

Status: PASS proposal, pending separate G2 acceptance. The user accepted bounded
local/crossing reconciliation at `a168b7d`; this is the subsequent G2 review.
Nine accepted G2 declarations remain unchanged. No aggregate P9-7.7, G3 or
specialization closure follows from this proposal.

The only proposed additional declaration is
`grcv4-profile-sha256:413497bec4f219ec402d82d5cd2aced01dca25a58d2ab906c348472b98d596b0`:
the original four-node, three-edge C_RG2b nomination, with nonuniform reference
weights, nonzero C deformation/current coupling and its frozen completion.
Current/reset resource roles are independently declared. Neither W history nor
a persistent carrier is authoritative state for this profile. A family label
does not authorize arbitrary parameters, graphs or completions.

## Evidence reconciliation

The [integrated record](P9-7.7-C_RG2b-G2Review.json) binds all 33 catalog cells.

| Obligation | Evidence and boundary |
| --- | --- |
| `G2-EXACT-PRODUCT` | [26 local cells](P9-7.7-C_RG2b-LocalProduct.json): complete C chain, finite section/inverse and continuity equations, current/reset/restart reconstruction, failures, replay and ownership. Fixed-stratum derivative, chi/zeta and zero-deformation controls retain their separate scopes. |
| `G2-ORDERED-ENDPOINTS` | [Seven crossing cells](P9-7.7-C_RG2b-Crossings.json): nine native cases and two separate retained PC↔CI+PC witnesses. Exact graph endpoints, complete two-role C reference/section readmission, independent C→A initialization, W/Z loss, event/reset/replay and K/K-minus pressure; not all-pairs coverage. |
| `G2-INTEGRATED-REVIEW` | Accepted checkpoint bindings, current typed authority/debt traces, complete evidence references and one [facade supplement](P9-7.7-C_RG2b-G2Interface.json). Existing numerical campaigns are checked as retained evidence, not rerun. |

Reuse five local and three crossing methods. The generalized
[Tranche 6 graph result](../tranche-6/P9-6.4c-Review.md), its
[follow-up](../tranche-6/P9-6.4d-AuditFollowup.json) and
[routing](../tranche-6/P9-6.5-RealizationRouting.json) remain source context,
not arbitrary-graph G2 support.

The single facade supplement uses zero-duration requests. It checks detached
C/reset configuration, immutable parameters, Candidate C without an A backend,
strict/external request equivalence, missing-default/wrong-layer rejection,
local versus public discovery, and preservation of a committed prefix when an
iterator raises. W/Z remain absent. A zero request duration does not select a
new zero-beat completion; positive evolution remains in the local product.

## Ordered scope and scientific limits

Graph C_OS→nomination rederives C surfaces; nomination→graph C_PC initializes
zero Z without loss; graph C_PC→nomination explicitly loses nonzero Z. Graph
A_OS→nomination explicitly loses independently distinct W histories. Conversely,
nomination→an initializer-selected graph A_OS initializes both W roles without
source history loss. That A target is a separate declaration, not proposed G2
support. The two scalar PC↔CI+PC witnesses are not nomination executions.

The C→C mapped event uses a separate renamed-graph target with a complete
three-edge reference map and frozen completion. Both resource roles are mapped
and reconstructed; no W initializer, carrier or history loss is invented. That
event target is distinct from both the nomination and the A initializer target,
and is not proposed for G2 here. No GRC9 mechanics are inferred.

State readmission on K is distinct from ordinary entry on K-minus. A reset-only
out-of-K target rejects atomically; a K-only target can readmit/reset but cannot
pass ordinary entry, including zero-duration entry. Only the exact frozen
positive beat is admitted for ordinary evolution.

The RG section remains bounded, completion-relative and Lipschitz-only, with
certified finite approximation error. The local C baseline derivative is a
fixed-stratum candidate calculation, not a derivative of that section. Frozen
mobility authority is not transferred to geometry. No C1/spectrum theorem,
arbitrary positive duration, CI equivalence, indefinite base-chart invariance,
formation authentication or persistent section history is claimed. Disabled
GRC9 specialization reductions remain outside this generic proposal.

## Authority and debt

Sources are the [V4 specification](../../../specs/grc-v4-spec.md),
[common-interface extension](../../../specs/grc-common-interface-v4-ext.md),
[paper](../../investigations/grc9v4-constitutive-design/drafts/2026-09-GRC-V4.md)
and current typed forensic queries. Twenty contract traces retain C reference,
mobility, baseline/derivative/covariance, RG2b, initializer, receipt-parent,
abundance and lifecycle authority, including classifications, source/edge
references and trace digests. Passing tests does not rewrite D10.2's
`indeterminate_requires_review` support or promote forward obligations.

Four debt traces preserve their histories. `GTRS-RG-DEBT-C1-SECTION-REGULARITY`
remains narrowed, not resolved; a section derivative or spectrum claim would
activate it. Initializer, receipt-parent and abundance design/implementation
distinctions remain intact. G2 is an exact implementation-conformance decision,
not a retrospective promotion of scientific claims.

## Reproduction and next decision

The single native facade method passed. All 15 focused review, registry,
materializer and surface methods passed in 36.965 seconds, including 42 rehashed
interface/proposal mutations. The actual G2 checker and shared browser projection
preserve nine accepted declarations plus this one proposal. Phase boundary,
portability and local-link checks passed; no earlier numerical campaign was rerun.

Interface digest:
`778740d222c33bb5f24b7890b5ffbd1235068cf67e55025b47d5900a51dc8ed0`.
Integrated review digest:
`321ce3bf367100425800218d8cf6aca15a549f9be7bb3ec46b35f046474f5c8f`.

From the repository root:

```bash
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_c_rg2b_g2.py
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_c_rg2b_g2.py --capture-interface
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python -m unittest test_p977_c_rg2b_g2.ReviewTests test_p977_c_rg2b_g2_surfaces test_profile_g2_registry test_profile_materializers -v
```

Default mode rebuilds/checks the retained review without rerunning numerical
campaigns. `--emit` emits a reconstructed review; `--capture-interface` emits a
fresh supplement, never overwriting the original. The shared registry exposes
this as a proposed `exact_profile_v1` row with no acceptance record, through the
existing API/notebook/browser materializer. Accept or return this exact G2
proposal separately; discovery and aggregate gates remain unchanged until then.
