# P9-7.7 — A_OS local-product reconciliation

Status: bounded local evidence accepted with the crossing reconciliation on
2026-09-13 through “accept and commit.” A_OS G2 and aggregate P9-7.7 remain held.
No new public support or G3 admission is granted. The original execution flags
remain capture-time facts; see the linked crossing review for scoped acceptance.

Subsequent progress: the [crossing reconciliation](./P9-7.7-A_OS-CrossingReview.md)
addresses the seven rows listed below with explicit endpoint dispositions.
This local record and its coverage-time status remain unchanged.

## Nomination and concrete progress

Keep the original A_OS nomination:

`grcv4-profile-sha256:e4c04a83240a33d77c50a26ca6effbb6ce142774bd01f0966861dd517a94e2c4`.

The [initial review](./P9-7.7-Review.md) and its JSON remain the initial coverage
snapshot. This [separate execution](./P9-7.7-A_OS-LocalProduct.json) supplies
21 local catalog rows: eight common, eight Candidate A, OS one-pass, and four
fixed-profile lifecycle rows. These are not 21 independent test runs: three
focused methods share executions explicitly where the same result discharges
multiple assertions. Seven crossing-related rows remain unresolved.

The exact retained 7.1 initial inputs supply distinct live/reset C and W, graph,
reference, backend and parameters. The [accepted 5.3 numerical tests](../tranche-5/P9-5.3-Review.md)
already contain the independent rational one-edge and logarithmic-writer oracles.
Reuse those equations and the existing catalog result recorder. New focused
execution supplies the missing local input/output/result linkage; it is not a
rerun of the broad Tranche 5 or 7 campaigns, nor replacement of their records.

The initializer/event A_OS target is a genuinely different profile. Compared
with this nomination, it changes alpha from 0 to -0.34657359027997264, beta and
gamma from 0 to 0.001, and the lifecycle history policy from supplied reference
history to target reference-pass initialization. Its identity cannot be copied
onto this seed, or vice versa. Those crossings remain separate work.

## Verified local cases

| Group | Catalog cases | Actual evidence boundary |
| --- | --- | --- |
| Common, 8 | Ordinary step; invalid domain; nonfinite; charge mismatch; stale cache; zero/negative duration; atomic failure | Native public operations, whole-publication comparison and emitted delta. Domain/nonfinite and postsolve charge controls use labeled test-only fault injection, not fabricated natural numerical failures. |
| Candidate A, 8 | Positive W admission; exact pre-read W-hat; chi/zeta zero; post-continuity refresh; log writer; no same-beat new-W read; initialization versus formation | Independent scalar/log equations, actual predictor/corrector/writer/final values and stage inputs; native zero-gain control operations retain their different profile IDs. |
| OS, 1 | One pass | One predictor/corrector, one continuity evaluation and one writer, with independently computed geometry/current/split residual. |
| Local lifecycle, 4 | Snapshot/load/replay; reset after ordinary; receipt ownership; duplicate independence | Actual save/load and exact result replay, distinct reset authority, previous-primary parents with a coherently rehashed rejection, and independent clone rebase/export mutation pressure. |

The fixed-stage rows are explicitly `not_invoked`, with no operation result,
commit or solver status invented for the provisional calculation. Observations
retain both numerical expectations and actual stage outputs. The nominated
alpha/beta/gamma are zero: its conductance is constant, but descriptors are
rebuilt from post-continuity C and the writer changes incoming W. This finite
point does not claim nonzero-gain descriptor sensitivity or general parameter
coverage; broader numerical controls retain their existing independent subjects.

Chi-zero and zeta-zero rows record the actual control identity in the required
profile/result fields. A separate nomination link and an exact one-parameter
comparison establish their control relationship. Neither control becomes a
supported profile. Supplied initial W is not evidence of physical formation or
the separate target initializer contract.

Common charge mismatch and atomic failure reuse one execution, explicitly
labeled. Replay and receipt ownership likewise share a result. Failed attempts
retain one emitted failure receipt without appending it to the persistent ledger.
The recorder retains content-addressed pre/post snapshots, requests, results,
required catalog fields, observations and the exact current source bindings.

## Remaining exact-endpoint work

The seven still-open A_OS catalog rows are:

- `A-MIGRATION-HISTORY-RECEIPT`;
- `RESET-AFTER-MIGRATION`;
- `RESET-AFTER-EVENT`;
- `WHOLE-LIFECYCLE-TUPLE-MAP`;
- `ALL-MIGRATION-CLASSES`;
- `HISTORY-DISPOSITION`;
- `TARGET-READMISSION-FAILURE`.

Reconcile the retained ordered migration/event witnesses against the nominated
source and explicitly selected target/control population. Keep positive,
negative and justified nonapplicable dispositions separate, including distinct
current/reset authority, W versus Z history and reset-only target admission.
Capture only missing exact assertions. Do not treat a target's different
complete-profile ID as a production defect or silently change the nomination.
After this work, integrated public-facade/G2 review and user acceptance remain
necessary. Other family gates and G3 are unchanged.

## Reproduction and checks

From the repository root with `.venv`:

```bash
PYTHONPATH=src:.:implementation/phase-9-grcv4/verification .venv/bin/python implementation/phase-9-grcv4/verification/verify_p977_a_os_local.py
```

The default checks retained identities, exact profile/control relationships,
catalog fields, canonical preimages, operation/solver dispositions and ledger
deltas without numerical reruns. `--run` emits a new execution to stdout and
does not overwrite the retained run. Source and environment identities are
captured. Temporary save/load paths are disposable test data, not evidence
references. Existing source claims, specs, production code and original
accepted runs are unchanged.

The first development invocation also discovered the imported C recorder's
test class. The import was corrected to expose only the new A_OS test class;
the retained runner names its three methods explicitly. Those incidental C
checks supply no new credit here.

The retained three-method run passed in 86 seconds with no failures, errors or
skips. Digest: `21328fd2ae791fdc875a5f874153e876ec2a94d690cf42da014df20f9d3eada8`.
The separate read-only evidence check passed all twelve rehashed mutation controls.
The focused API/notebook/HTTP/browser integration check also passed, including
46 browser rejection controls (six added for this local-product view). These
checks validate retained evidence and status agreement without numerical reruns;
local review, crossing closure and G2 acceptance remain separate.
