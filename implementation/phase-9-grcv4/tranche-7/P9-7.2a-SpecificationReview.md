# P9-7.2a — initializer specification propagation

Status: **specification accepted by the user** through the commit-and-implement
request. Exact reviewed spec bytes are retained, including drafting-stage status
text; this record supplies their acceptance disposition. Implementation is now
authorized, with its execution evidence and aggregate acceptance still separate.
The user accepted the exact paper through the commit-and-continue request;
`7d45218` records acceptance. This step binds it in the
[V4 supplement](../../../specs/grc-v4-a-initializer-spec.md),
[closed schema](../../../specs/grc-v4-a-initializer-schema.json) and
[wire examples](../../../specs/grc-v4-a-initializer-vectors.json), with links and
applicability notices in the generic spec, V4 interface extension and registry.
Only those six specification files differ from the accepted release.

## Source and translation

The accepted paper SHA remains
`6060e28698c07068f91a5e40d5335d1cf8ae2c4292baf3a7c817a36f1ede57a9`;
the accepted proposal at `448e420` remains
`09c080fd835d096372cff6204e42ddf0baba2b2da674fee905c7b5df2c83ed84`.
Both are checked at their accepted Git subjects. This is engineering binding
of their chosen rule, not a new scientific selection or edits to those documents.

The current forensic API retains projection
`fa59abda356446daca516b33028a07a48ec63aaacb8f961cd274a425dca48dd1`.
The new contract trace is
`7edb0289f4d9a3ab7ec3e9c8d068185239039e01212425862434ffafb2dded59`;
the debt trace is
`f9f9c2222d790f9750e5e38c1ede674445da24a722721ecc83f92d6742f745f2`.
They preserve source/edge witnesses and optional-design versus forward-runtime
status. The historical initializer's current-context trace is
`78c3881967ea65ea8396861dfdacacd7064dfa4b440e6d3ff2a66381ea8e5e35`,
still `indeterminate_requires_review`. Historical source flags are not rewritten
to make this specification candidate look accepted or executed.

| Binding | Source and engineering consequence |
| --- | --- |
| One pass and exact rounding | Paper §§12.6.1–12.6.2; unchanged full G_W, no auxiliary full-current/domain admission or floored-product shortcut |
| Static before output identity | §12.6.2; existing history/descriptor/site profile fields bind fixed policy and recipes, then complete profile precedes constructions |
| Closed current/reset records | §§12.6.2–12.6.3; embedded reference/differential preimages, explicit roles/C/W, nullable recomputed derived intermediates |
| Pair and receipt linkage | §12.6.3; request names static policy, new v2 receipt binds output-bearing pair after construction; no profile cycle |
| Snapshot and restoration | §12.6.3; explicit new layout/transition pair field, archived endpoint replay, no fresh-initializer constraint on evolved W |
| Release and evidence limits | §§12.6.2–12.6.4; new codec/release must be explicit, no old-record relabel or inferred positive migration |

The schema is additive; the released base schema and existing definitions are
unchanged. Payload fields, version names, digest domains and linkage are the
engineering choices submitted for review. The new snapshot layout reuses the
existing top-level fields, with closed schemas for snapshot, receipt envelope
and transition rows including their pair field; production decoding and
archive/receipt agreement remain implementation.

## Focused verification and preserved evidence

The existing `verify_p972a_proposal.py` checks exact candidate hashes, the
accepted source documents and unchanged forensic projection. Only the released
generic/interface specs and registry join the proposal/paper as historical
document subjects at `f36b3ba`. Every other release input, base schema, packaged
asset and codec pin is still current-byte exact. The release/source manifest
is not regenerated. Current draft drift and missing/substituted historical
subjects hold instead of being silently accepted.

The focused tests cover document/source/release separation plus schema closure,
unknown/old policies, bad numerical wire values, reference/profile identity
and array dimensions, current/reset roles, acyclic IDs, canonical byte vectors,
directional history and old-receipt rejection. Deliberately wrong but coherently
hashed derived arithmetic is explicitly **not authenticated** by wire tests:
actual recomputation, endpoint linkage, final readmission and rollback need the
future producer/lifecycle tests. The synthetic receipt's placeholder endpoints
are not a successful migration record. No numerical campaign is run here.

Commands from repository root:

```sh
.venv/bin/python implementation/phase-9-grcv4/verification/test_p972a_proposal.py -v
PYTHONPATH=src:. .venv/bin/python implementation/phase-9-grcv4/verification/test_p972a_specification.py -v
PYTHONPATH=src:. .venv/bin/python implementation/phase-9-grcv4/verification/verify_p972a_proposal.py --check
```

Original 25/17-test records and the 7.1 predecessor are retained at their exact
source subjects, not rerun or reclassified. The accepted paper/proposal,
scientific authority records, older families, GRC9V4 specialization, `src/`,
`tests/` and current packaged release are untouched.

Next: explicit successor release/codec
binding and the common producer plus five positive A target migration paths.
Aggregate P9-7.2a, wider G2/G3 and generic events remain separate.
