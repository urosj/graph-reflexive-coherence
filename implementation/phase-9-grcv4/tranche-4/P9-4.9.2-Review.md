# P9-4.9.2 — receipt-parent implementation

Status: implemented; source propagation, runtime regression, boundary pressure,
API/notebook and new parent browser views verified. User implementation review pending.
The user accepted the bounded authority and requested its full implementation.
This review does not accept G2 or authorize a new profile.

## Source to implementation

The [accepted authority/debt record](../../investigations/grc9v4-constitutive-design/decisions/P9ReceiptParentAuthority.json)
closes `G2-COS-PARENTS`' missing design rule under
`P9-4.9.2-CL-N-001`, not by retroactively interpreting D10.2. The three
`P9-EC-RECEIPT-PARENT-{CHAIN,ADMISSION,CEILING}` contracts are queryable in the
side-tool's current context with source/edge witnesses and trace digests.
The historical D11 graph remains byte-exact. Its four bearing D10.2 contracts
retain their original support dispositions.

Propagation order was accepted investigation authority → current forensic
admission → proposal §12.5.1 → paper §12.5.1 → V4 interface/family specs and
successor release → C_OS runtime. The release binds the current paper/proposal,
specs, parent authority and compact graph admission. Older family specs and
runtime code are unchanged. Historical receipts, runs and acceptance records
are not rewritten or rehashed.

Every successful operation's receipts now name the previous successful primary,
or no parent for the first group. Ordinary steps no longer skip intervening
reset/rebase/migration/event commits. Auxiliaries share that same parent.
Publication and restore validate the complete partition and content/commit
bindings before any state becomes observable. Failure and unreceipted operations
leave the parent head unchanged. C_OS snapshots use explicit v3 layout, policy
and release declarations; historical v1/v2 layouts reject without conversion.

The existing Phase 9 browser has a working claim/debt/object/contract selector;
the notebook executes the same typed traces. API admission is fail-closed,
including coherently rehashed source changes. Browser/notebook clear stale output.

## Validation and limits

The [execution record](P9-4.9.2-ExecutionRecord.json) binds the
[original runtime run](../evidence/P9-4.9.2/parent-rule/run.json) and
[source-bound claim/debt/contract traces](../evidence/P9-4.9.2/parent-rule/inputs.json).
The fixed combined run passed **140/140**, with zero failures, errors or skips,
440 input/checker source bindings, exact discovered/executed roster coverage,
and unchanged before/after disk and loaded-code attribution. Elapsed time was
1888.891 seconds. No historical run was replaced.

Additional checks passed: five source/graph/notebook/propagation tests, 30 Node
component tests, and the exact published mapped-event runtime test. That event's
request and `event_id` remain unchanged under the successor release. All 242
current boundary-pressure cases passed; API/notebook results were byte-identical.
Both desktop/mobile parent-authority selectors passed, including all six typed
traces and clearing stale results. The normal command below runs the complete
18-case current browser suite together with historical compatibility and
retained-runtime source checks. Its generated `verification-v3.json` is bound
to its own exact inputs; this review is not a substitute for that live check.

Historical D11 Python/Node/browser checks use accepted commit
`f1817b8cf41e439cbb18ad82dfab6b39a77ae43d`, matching the original dependency
hashes. A fresh clone reconstructs ignored browser data from the tracked,
hash-verified records. Rebuilds must leave accepted records byte-identical.
This checks the historical subject, not the current parent overlay. The current
graph, API, notebook and browser are checked separately on current inputs.

```bash
.venv/bin/python implementation/phase-9-grcv4/verification/verify_p9492_parents.py --check
.venv/bin/python implementation/investigations/grc9v4-constitutive-design/tools/exploratory-side-tool/tool/scripts/run.py verify-iteration9
```

For a new numerical rerun, use the run record's environment and `--capture`
command with a fresh generated destination. To inspect the original result,
read its retained record and source-bound test assertions without rerunning.

The symbolic checker covers nine valid and sixteen malformed graph projections.
Runtime pressure covers all primary kinds as roots, interleaving/zero-duration
operations, crossings, nonwriting/failure controls, shared-prefix replay,
explicit snapshot admission, coherent wrong-parent/auxiliary hashes and atomic
rejection before publication. Actual self/cyclic content-ID references are not
claimed to have been coherently rehashed: that would require a hash fixed point.
The graph-shape controls and real malformed-ID admission tests are distinct.

The exact-source runtime capture reuses `_p941_execute`: historical Git pins
the predecessor test roster; added parent tests are explicit; source bytes,
loaded method/code slots and executed IDs are checked before/after. Input and
checker hashes, dependency versions, outcomes and replay command are retained
in the run record. A rerun uses a fresh generated directory and never replaces
the original output. Hash linkage is not hostile-interpreter attestation.

Development corrections: the new test initially used `migrate` instead of the
existing `migrate_profile` method; fixed before final execution. The first capture
preflight required importing the shared transport-test module for the existing
source checker; no runtime tests executed in that failed preflight. These routine
causes are recorded here, without a separate failure archive or process.
Integrated boundary pressure also exposed a historical V4-freeze test that
needed to target the still-frozen common interface, and source/test permission
set aliasing that could open the facade source through its test owner. The
successor now copies the permission set before adding its exact owner;
`parent_cannot_open_facade` guards that boundary. Neither correction changes
the retained runtime inputs or results.
The API route also exposed an import-context dependency; release validation now
uses the builder's own CLI without changing a caller's global import paths.
Browser fixtures were aligned with the new permission fields and explicit
successor/historical-crosswalk label. An initial browser run passed 16/18; its
two failures were the old status-label assertions, corrected for the final run.
A development pressure run invalidated by concurrent verifier edits was
discarded; the subsequent 242-case result used unchanged inputs. The final
normal command checks the finished input set again, without rerunning the
140-test numerical capture. No older execution is relabeled as current.

## Remaining closure

P9-4.9.1 public facade and P9-4.9.3 final complete-profile fixture product remain
pending, followed by the single P9-4.8B integrated review. The original P9-4.8
HOLD is historical and unchanged. No complete-profile nomination, accepted
generic runtime support, GRC9V4 support or G2 acceptance is added here.
The C_OS internal parent-policy obligation is the only runtime scope of this work.
