# P9-4.6 audit correction within the P9-4.7 batch

Status: accepted by the user after correction. The combined independent audit
explicitly closes all three prior findings. See the [combined audit](../evidence/P9-4.7b/audit-followup/inputs.json).

The supplied audit is preserved verbatim as `audit_markdown` in
[inputs.json](../evidence/P9-4.6/audit-followup/inputs.json). Its main finding was
that reset/rebase could publish correctly hashed receipts whose semantics would
fail later restoration. The original [review](P9-4.6-Review.md),
[execution record](P9-4.6-ExecutionRecord.json) and 76-test run remain unchanged,
so their original commit-time and validation-time claims remain reconstructible.
This follow-up records the later correction, not retroactive acceptance.

The live publication owner now applies the same semantic ledger checker as
restoration before publishing reset/rebase. It additionally binds receipt and
commit fields to the actual source/target authority, charge and clock. The
shared gate also covers ordinary steps and the new registered crossings.
Schema-valid semantic corruption is distinguished from a helper throwing.

`CandidateCOSAuditCorrectionTests` in
[test_grc_v4_lifecycle.py](../../../tests/models/test_grc_v4_lifecycle.py) supplies:

- 14 successful-factory corruption controls across reset/rebase: wrong baseline,
  authority, parent, history slots, charge delta, clock and operation kind.
  Every case preserves the whole state/reference/commit snapshot; a subsequent
  valid rebase/reset restores normally.
- Four successful-return current/clock corruption controls on an empty ledger,
  where no preceding commit can constrain the administrative target.
- The exact three audit clock cases: starting at `0`, `0.125`, and `1e308`,
  commit zero duration, then `5e-324`, and restore the resulting snapshot.
- Assignment after a historic commit followed by snapshot continuation replay,
  explicitly allowing last historical target identity to differ from live C.

These methods enter the retained source-bound P9-4.7 batch run, including the
accepted negative clock regression. The old supplementary clock checks are
superseded by that executed record; the historical `_P946_METHODS` roster and
run are preserved. See [P9-4.7a execution](P9-4.7a-ExecutionRecord.json).

`P9-7.1-C_OS` aliases only P9-4.6's bounded snapshot/restoration/continuation,
reset/rebase, assignment and independence evidence. It creates no P9-4.7a
integrated failure/receipt-ownership/replay credit. Replay means restoring the
same current snapshot and applying the same future inputs; the commit ledger
cannot reconstruct unreceipted assignments from origin.

The audit author lacked the original review in their supplied package. The
repository link above supplies that review, and the batch authorization verifies
its original Git binding along with the P9-4.6 execution/run records. This
remedies repository navigation without attributing a review-file check to the
auditor that they did not perform.

Historical source replay uses the compact [reverse patches](../evidence/P9-4.7b/pre-release-source/inputs.json); both old run manifests were reconstructed exactly. The original records retain their historical dispositions.
