# P9-4.9.1a — Abundance availability closure

2026-09-09. **Design accepted by the user; implementation completed and focused
verification passed, pending implementation review. G2 remains held.**

## Result and authority

The [accepted decision](../../investigations/grc9v4-constitutive-design/decisions/P9AbundanceInterfaceAuthority.json)
admits `P9-4.9.1a-CL-N-001` and resolves `P9-4.9.1a-DEBT-ABUNDANCE` at bounded
design scope. Three required-support contracts own availability, observation
identity and failure/claim ceilings. The seven-node append-only extension retains
every earlier graph node/edge and source classification. It is available through
the forensic API, the notebook's abundance cell and **Load abundance authority**
in the Phase 9 browser; no new numerical scenario is claimed.

Propagation is source → identical proposal/paper §14.2.1 → V4 interface, generic
and GRC9V4 specs → successor release → C_OS projection/tests. Common/V3/GRC9V3
specs and legacy runtime are unchanged. No numeric definition or detector is
admitted. Enabled C_OS always publishes:

```json
{
  "abundance": null,
  "abundance_status": "unavailable_no_admitted_definition",
  "abundance_observation": null
}
```

No `v4_abundance_diagnostic` capability is advertised. A private, stateless
projection validator enforces the protocol before the existing sole lifecycle
publication. Synthetic declared-definition tuples exercise future numeric shape
rules; no public receiver accepts such tuples, detector injection or hidden
configuration. The diagnostic adds no scientific/profile/reset/receipt coordinate.
The v3 snapshot layout and parent policy remain unchanged; stale specification
releases are rejected, not silently upgraded.

## Focused evidence

[Original successor run](../evidence/P9-4.9.1a/abundance/run.json): **11 tests passed,
zero failures/errors/skips**, 102.976 seconds, 472 source bindings. The
[execution record](P9-4.9.1a-ExecutionRecord.json) pins that run. Six public-facade
tests and five source/UX tests ran once after capture setup was corrected.

| Obligation | Executed coverage |
| --- | --- |
| Required triplet and truthful capability | Real construction, observation, zero/positive steps, assignment, rebase/reset, copy/restore and actual profile/graph crossings. |
| Null versus zero versus failure | Actual unavailable projection; synthetic available zero and contradictory value/status/metadata/capability controls; typed projection exceptions preserve full prestate. |
| Observed state and release identity | Synthetic target-versus-source, stale stage/model/definition/detector, missing/wrong release and unchanged-model/different-diagnostic controls; current-release restore and old-release rejection. |
| No scientific feedback | Detached output mutation does not change state or later availability; prepublication and read-only failures preserve complete snapshot and receipt ledger. Existing charge/crossing/parent assertions remain exercised. |
| Accepted authority and source coherence | Append-only graph, required support and exact source/edge/trace identities, proposal/paper equality, V4 source links, no numeric admission, unchanged older specs. |
| Actual tool access | Executed notebook cell and real HTTP handler match the API; failures clear output. Actual browser validator consumes API traces and rejects numeric/G2/identity overclaims. No socket or desktop/mobile campaign required. |

Numeric controls are **protocol validation**, not execution of a production
abundance functional, proof of a detector's totality, or GRC9V4 conformance.
Totality and typed evaluation failure are obligations of any future separately
admitted detector; the current branch runs none.

Two capture preparations stopped before tests: one used a test-module alias
outside the existing capture's audited package namespaces; the other omitted
an explicit lifecycle-test preload. Corrected the capture setup without changing
the accepted capture engine. No numerical result was discarded or replaced.

Supplementary lean checks passed: the five existing parent-authority regressions,
31 status-component tests, successor release rebuild, current boundary and
retained-run inspection, and the scoped notebook status/stale-pressure control.
The held-status test fixture initially retained the new abundance authority
field; removing it from the synthetic held fixture fixed that expectation.
No runtime change or numerical rerun was needed for that test correction.

## Reproduction and remaining work

From the repository root, use the existing `.venv`:

```bash
.venv/bin/python implementation/phase-9-grcv4/verification/verify_p9491a_abundance.py --check
.venv/bin/python implementation/phase-9-grcv4/verification/audit_phase9_implementation.py --boundary-only
```

For a deliberate rerun, use `--capture` with a fresh repository-relative
directory under the side-tool's ignored `tool/generated/`. The retained run's
command and source bindings identify its exact inputs. A rerun is a new record.
The original 140-test parent and 14-test facade runs remain at their Git subjects
`d8f26d9` and `7905e7e`; their bytes and source bindings are checked there, not
relabeled as current execution. Full historical/numerical/pressure/browser
campaigns were not rerun for this closure.

P9-4.9.1a implementation permission covers only six existing lifecycle/codec/test/
packaged-release paths: 29 ready leaves, unchanged 29 eligible runtime paths.
Accepted generic/specialization support sets remain empty. Next is **final
P9-4.9.3 fixture reconciliation**, including the outstanding exact dimension
vector and full profile/pair population, then **one P9-4.8B G2 review**. Do not
repeat the initial inventory, claim those cases from these focused tests, or
relabel the historical P9-4.8 HOLD.
