# P9-4.8 continuation handoff

Prepared 2026-09-09. This is the current restart point for Phase 9 on another
machine or in a fresh conversation. It records implementation state, the latest
correction to the review, and remaining work; it grants no scientific acceptance.

## Checkout and acceptance state

Branch: `impl/phase-9-grcv4-tranche-4`.
Accepted runtime base: `12411282422ba3cd9e91ce93051fe3470b9bf084`
(`Accept P9-4.6 and P9-4.7 lifecycle corrections`). Tranches 1–3 and
P9-4.1–P9-4.7b are accepted within their recorded scopes. The full audit of
P9-4.6 and the combined P9-4.7 corrections are closed in that commit. The
[batch acceptance record](P9-4.7ab-AuthorizationRecord.json) binds the closure.

The user intends to commit the P9-4.8 files **after this handoff is written**.
At preparation time they are working-tree changes, with no P9-4.8 commit yet.
On the destination machine, use the later branch tip that contains this file
and all changes listed below; checking out the accepted runtime base alone
does not recover this review. Keep full Git history for historical acceptance
and source reconstruction. The future handoff/review commit does not itself
mean G2 or P9-4.8 has been accepted.

**P9-4.8 is an unfinished gate review with a draft HOLD. P9-G2[C_OS] has not
passed.** P9-7.7-C_OS aliases this work without another execution credit.
Accepted generic-runtime and specialization support sets remain empty.
The runtime permission adapter still exposes 26 ready leaves and 29 eligible
runtime paths; this review adds only explicit review-maintenance paths.

## Correction that must survive the conversation

The first P9-4.8 response described the review as complete and identified three
gaps. The user reasonably asked how that could happen after following every
checklist leaf. Inspection showed that completion accounting had allowed
requirements to remain deferred under narrower accepted scopes. The assistant
also overstated the fixture finding. The review, index, plan and checklist have
now been qualified accordingly:

1. **Public interface: existing integration work was left unfinished.**
   [P9-2.6](../tranche-2/P9-2.6-Review.md) assigned the actual facade/admission/step
   work to P9-4.4/P9-4.5/P9-4.7a. Those consumers implemented the numerical
   pipeline and internal receiver, while continuing to exclude the full public
   facade. An explicit closure task was not carried forward. The user did not
   skip a checklist leaf.
2. **Receipt parents: a known authority obligation remained deferred.**
   [P9-2.4](../tranche-2/P9-2.4-Review.md) assigned scope/order resolution to
   P9-7.6. The registered C_OS child described a narrower ledger/duplication
   scope. P9-4.7 acceptance established a tested local convention while
   expressly excluding general parent-DAG conformance. That difference must
   be reconciled with the parent obligation before claiming closure.
3. **Fixtures: finish P9-4.8's reconciliation before declaring missing behavior.**
   A missing consolidated per-profile mapping is not a runtime defect. The
   initial stale-cache finding used only the cache-attribute exclusion test.
   Existing second-beat reconstruction evidence was not adequately considered.
   This is a review correction, not a newly imposed scientific requirement.

The two existing unresolved integration/authority obligations still support a
draft HOLD. Do not describe the three findings as three demonstrated runtime
bugs, regard the current checker as a scientific acceptance decision, or repeat
the initial claim that the fixture review is finished.

## Read these sources first

- [Draft P9-4.8 review](P9-4.8-Review.md) and
  [gate record / 33-case index](P9-4.8-GateReview.json).
- [Implementation checklist](../../Phase-9-GRCV4-ImplementationChecklist.md)
  and [plan](../../Phase-9-GRCV4-ImplementationPlan.md), especially P9-2.6,
  P9-4.4–P9-4.8 and the parent/child P9-7.6 obligations.
- [Generic specification](../../../specs/grc-v4-spec.md),
  [common interface](../../../specs/grc-common-interface.md),
  [V4 extension](../../../specs/grc-common-interface-v4-ext.md),
  [fixture catalog](../../../specs/grc-v4-conformance-fixtures.json),
  [vectors](../../../specs/grc-v4-conformance-vectors.json), and
  [contract schema](../../../specs/grc-v4-contract-schema.json).
- [Paper](../../investigations/grc9v4-constitutive-design/drafts/2026-09-GRC-V4.md),
  especially lifecycle §12 and Appendix E, plus the accepted design records
  reached through the [side-tool query guide](../../investigations/grc9v4-constitutive-design/tools/exploratory-side-tool/docs/AgenticQueryGuide.md).
  Specs, paper and accepted side-tool/design authority jointly govern the work.
- [P9-4.7a review](P9-4.7a-Review.md), [P9-4.7b review](P9-4.7b-Review.md),
  and their execution records for the exact claims already established.

The active [specification release](../../../specs/grc-v4-specification-release.json)
is `grcv4-spec-release-sha256:7b8b4d4e32e48fd35f70421cce7f547eebb21dd81389764061efe6e1a8c19886`.
The mapped-vector correction is accepted. Avoid casually editing frozen
specification assets or their generator: the generator is release-bound.

## Concrete next work

Start with evidence reconciliation and explicit assignment of the existing
closure obligations. A new process framework or repeated full regression is
not needed to establish what is missing.

**Facade.** [grc_v4.py](../../../src/pygrc/models/grc_v4.py) contains request
records. [CandidateCOSOperation](../../../src/pygrc/models/grc_v4_lifecycle.py)
implements the bounded C_OS owner but does not inherit `GRCModel`. It has actual
step_v4, snapshot/restoration, assignment, reset/rebase, duplication and
migration/event behavior. Missing public integration includes from_config,
get_params, capabilities, stage-tagged observable computation, common step/run,
run_v4 and exact profile/model discovery. Read the common signatures and
projection rules; method-name overlap is insufficient. Reuse the existing sole
publication owner. Exercise request defaults, zero/subnormal/negative/extreme
durations, restored instances, capability truth and common projections. Earlier
foundation no-facade/export assertions are stage-local and must evolve when the
real facade is implemented. The empty public registry is appropriate before G2;
it is not an independent defect.

**Parent authority.** The accepted side-tool registry's
`D10.2-EC-PARENT-L-ORDERED-RECEIPTS` at
`/normative_equation_contract_registry/50` in the
[D10.2 record](../../investigations/grc9v4-constitutive-design/decisions/D10_2FullSubstrateProvenanceAndPromotionAudit.json)
requires ordered source/target profile identities. It does not settle generic
receipt-parent scope/order. The paper and interface distinguish this from
acyclic receipt→commit→envelope hashing. The tested current convention uses the
previous ordinary primary for ordinary steps, the latest primary for
administrative/crossing operations, and shared parents for all four receipts.
Resolve the outstanding authority question through the existing design/spec
route; do not silently assume intra-commit-only or all-history parents. Then
pressure historical/intra-commit, missing/foreign/forward/self/cyclic,
duplicate and reordered references. Preserve the distinction between restore
plus identical future inputs and authenticated historical execution:
unreceipted `set_state` is lawful.

**Fixtures.** The index covers 8 common, 14 Candidate C, 1 OS and 10 lifecycle
catalog cases; its rows currently point to scoped tests, not a complete set of
new conformance results. Nominate actual complete profile identities and
reconcile applicable existing evidence under their exact graph, reference,
parameter, context and domain inputs. Parameter changes and ordered migration
pairs retain separate identities. Inspect each claimed gap before adding tests.
The two most immediate reconciliation questions are:

- `COMMON-STALE-CACHE`: in
  [test_grc_v4_lifecycle.py](../../../tests/models/test_grc_v4_lifecycle.py), inspect
  `CandidateCOSOperationTests.test_two_beats_use_actual_request_poststate_ledger_and_reference`
  together with `test_no_cache_input_and_immutable_detached_observations` and the
  geometry/stage stale-provenance tests. The former checks the actual second
  predictor's current, request, ledger and reference geometry, and asserts a
  committed beat. Determine whether the combined evidence establishes the
  catalog's rebuild-before-consumption outcome for the nominated public
  operation. A missing index entry alone cannot justify a new persistent cache
  or a claim that runtime consumes stale values.
- `SEMANTIC-REJECT-RESOURCE-TRANSFORM-DIMENSIONS`: the index currently distinguishes
  synthetic crossing dimension/order failures from execution of the exact
  published semantic row. Resolve its required layer and exact-input credit;
  do not count schema validity alone as semantic admission evidence.

The corrected mandatory generic mapped event already has exact-input execution
credit; retain it. All seven migration classes have dispositions in the review:
positive same-graph C_OS parameter migration, applicable unsupported-target
negatives, and explicit deferral where sources/targets are not implemented.
Unavailable source construction is not an executed migration-source test.
Candidate A, other realization positives, GRC9 expansion/recursive/metamorphic
cases and the 40 disabled-compatibility cells do not block the generic C_OS
positive scope. Broader GRC9/K4 vector-builder defects remain separately
inventoried; do not reopen the corrected generic row on that basis.

After closing demonstrated obligations, complete P9-4.8 and issue a fresh G2
verdict. A_OS and P9-G3[C_OS] remain behind G2 and a subsequent route review.
This handoff requests neither their implementation nor automatic acceptance.

## Existing execution evidence and inexpensive checks

Reuse the retained runs and their original environment/source metadata:

| Record | Established execution |
| --- | --- |
| [P9-4.7a batch-final](../evidence/P9-4.7a/batch-final/run.json) | 135 methods; original bounded lifecycle batch. |
| [P9-4.7b audit-followup](../evidence/P9-4.7b/audit-followup/run.json) | 35 methods; combined audit correction pressure. |
| [P9-4.7b authoritative-vector](../evidence/P9-4.7b/authoritative-vector/run.json) | 50 methods; exact corrected mapped request/event plus scoped regression. |

The earlier two runs have distinct historical source overlays, recoverable
through Git and [compact reverse patches](../evidence/P9-4.7b/pre-release-source/inputs.json).
Do not relabel them current-source reruns. The last run's 443-file scientific
source manifest matched the unchanged runtime during P9-4.8 verification.

The [review checker](../verification/verify_p948_review.py) verifies the catalog
roster, source/evidence hashes, referenced methods, actual missing facade
methods, exact crossing identities, and seven mechanical counterexamples to
misleading acceptance claims. It has **not** rerun numerical tests and cannot
determine the scientific sufficiency of a test or select a parent convention.
It intentionally checks this draft's HOLD and source snapshot. Future legitimate
runtime changes require a successor review/checker update; do not treat its
hard-coded HOLD or old source hash as an immutable scientific requirement.

P9-4.8 checks passed before handoff: 33 indexed cases, 443 source files, seven
rejected mutation controls, the current implementation boundary, Ruff, relative
links and `git diff --check`. The handoff changes only review/navigation text
and its bindings; the final handoff check repeats this small scope.

## Environment and restart commands

The commands below use a POSIX shell and require Git, uv and a compatible host
Python. Run from the repository root. Use the root `.venv` for both the implementation
and side tool; rebuild it locally rather than copying another machine's venv.
The [tool bootstrap](../../investigations/grc9v4-constitutive-design/tools/exploratory-side-tool/README.md)
uses host Python only to create/re-enter that environment. Python 3.11+ is
declared; this review used Python 3.12.3, NumPy 2.4.6, jsonschema 4.26.0 and
rfc8785 0.1.4. The actual review environment is also in the gate record.

```sh
git branch --show-current
git status --short
git merge-base --is-ancestor 12411282422ba3cd9e91ce93051fe3470b9bf084 HEAD
GRCV4_TOOL=implementation/investigations/grc9v4-constitutive-design/tools/exploratory-side-tool/tool
python3 "$GRCV4_TOOL/scripts/bootstrap.py" --python-only
uv sync --locked --extra v4 --extra dev
.venv/bin/python implementation/phase-9-grcv4/verification/verify_p948_review.py
```

Bootstrap alone does not install the repository's V4 extra. `uv sync` uses the
committed root lockfile; do not regenerate it merely to prepare a new machine.
These provisioning commands are instructions for the destination, not a claim
that a new-machine installation was executed during this handoff. If browser
work is actually needed, run the full bootstrap under `.venv`; Node/npm/browser
state stays tool-local. Do not bootstrap or rerun the full browser suite just
for this document or a scientific coverage index.

For a read-only current permission check, use the existing policy directly:

```sh
.venv/bin/python - <<'PY'
import sys
from pathlib import Path
sys.path.insert(0, 'implementation/phase-9-grcv4/verification')
import phase9_implementation_policy as policy
policy.current_boundary(Path.cwd())
ready, owners = policy.leaf_permissions(Path.cwd())
print(len(ready), sum(bool(set(ready) & leaves) for leaves in owners.values()))
PY
```

Expected current counts: `26 29`. Generated API/notebook/browser receipts may
be stale or absent on a fresh checkout. They are not substitutes for the above
source check. Run the broader tool verification when changing its actual
permissions or presentation behavior, rather than automatically during handoff.

## Files to carry in the handoff commit

All paths below are relative to the repository root:

- `implementation/Phase-9-GRCV4-Handoff.md`: current entry point; older sections
  are explicitly historical.
- `implementation/Phase-9-GRCV4-ImplementationPlan.md` and
  `implementation/Phase-9-GRCV4-ImplementationChecklist.md`: qualified draft
  status and corrected stale P9-4.6/4.7 audit navigation.
- `implementation/phase-9-grcv4/tranche-4/P9-4.8-Review.md`,
  `P9-4.8-GateReview.json`, and this `P9-4.8-Handoff.md`: review, evidence index,
  mechanical check results, interpretation correction and continuation context.
- `implementation/phase-9-grcv4/verification/verify_p948_review.py`:
  bounded draft-review checker.
- `implementation/phase-9-grcv4/verification/phase9_implementation_policy.py`
  and `Phase9ImplementationBoundary.json`: explicit review-maintenance paths
  and final artifact bindings. No new runtime leaf permissions.

No runtime implementation, tests, accepted earlier evidence, spec assets, or
side-tool scientific claims were changed during P9-4.8. The pending review and
handoff files must be included in the intended commit; ordinary `git diff`
omits untracked new files. No external audit attachments or machine-local
Downloads files are required to resume.

## Working discipline

The user prioritizes correctness, source detail, edge cases and reconstruction.
They also explicitly requested less overhead after very long P9-4.6/4.7 and
acceptance turns. Keep scientific analysis and implementation pressure rigorous;
reuse established execution evidence and keep bookkeeping proportional.
Use targeted tests for changed behavior, then broader integration checks where
actual shared changes justify them. Avoid repeated full suites during acceptance,
custom capture machinery for each leaf, copied source archives, and repeated
hash updates while text is still changing. Finalize text and bindings together.
Keep references repository-relative and published historical evidence stable.
The [prospective evidence workflow](../tranche-1/P9-1.9-EvidenceHandoff.md#prospective-evidence-workflow)
already records this standard; no replacement evidence system is needed.
