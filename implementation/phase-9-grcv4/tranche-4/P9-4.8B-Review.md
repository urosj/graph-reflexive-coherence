# P9-4.8B — integrated C_OS G2 review

**Review verdict: PASS for the nominated exact scope; user acceptance pending.**
P9-G2[C_OS] is not yet accepted. Global generic-runtime and specialization
support remain empty. No A_OS, GRC9V4, G3 or other implementation permission
opens through this review. P9-7.7-C_OS aliases this result, without a second
execution credit.

Reviewed implementation: Git `c01526c`, where the user's commit instruction
accepted P9-4.9.3. The preceding bounded parent, facade and abundance work was
accepted in `d8f26d9`, `7905e7e` and `1f5f5e9`. Their validation-time records keep
their original dispositions; these later acceptance acts do not rewrite them.
The original [P9-4.8 HOLD](P9-4.8-Review.md),
[gate record](P9-4.8-GateReview.json) and checker remain historical and unchanged.

## Scope and evidence assessment

The [successor record](P9-4.8B-GateReview.json) binds the exact current release,
paper, proposal, specifications, retained executions, reviewed source and typed
forensic traces. The governing source is the [generic specification](../../../specs/grc-v4-spec.md),
the inherited [common interface](../../../specs/grc-common-interface.md) with its
[V4 extension](../../../specs/grc-common-interface-v4-ext.md), and the accepted
paper/investigation authority reached through those links and the forensic API.
No mathematical requirement or old-version contract is changed for this review.

The proposed support set contains exactly:

`grcv4-profile-sha256:a6b853ee382895eb78b1a7955a0df22f95d68b27cb0f762503e8c424c2f59b6d`.

Its full resolved parameters, graph/reference/context/domain and current/reset
preimages are retained in the [P9-4.9.3 run](../evidence/P9-4.9.3/complete-profile/run.json).
This is the nonempty dyadic C_OS nomination, **not all C_OS configurations**.
The family-level gate name is shorthand for that exact reviewed population.
Zero-control variants, coordinate witnesses and registered crossing targets
retain their actual identities and evidence limits; they are not additional
proposed G2 support. Future consumers needing other complete profiles must
establish those profiles' applicable conformance, not borrow this identity.

| Original obligation | Integrated assessment | Evidence |
| --- | --- | --- |
| G2-COS-INTERFACE | Satisfied for the nomination. The real GRCModel facade delegates to the sole C_OS owner; strict/external requests, defaults, immutable protocols, observation stages, lifecycle operations and truthful discovery are covered. The formerly undefined abundance placeholder is replaced by the accepted unavailable triplet. | [Facade](P9-4.9.1-Review.md), [abundance](P9-4.9.1a-Review.md), current public fixture results. |
| G2-COS-PARENTS | Satisfied for the nomination. The accepted successor chooses the previous successful primary for every operation group; runtime publication/restoration enforce that policy. This is new explicit authority, not an inferred D10.2 rule or authentication of uninterrupted history. | [Parent decision and execution](P9-4.9.2-Review.md), current receipt/crossing regression and typed forensic debt/contract traces. |
| G2-COS-FIXTURES | Satisfied for the nomination. The 33-row product is non-vacuous and bound to complete identities, actual inputs/results and evidence layers. Both outstanding exact-credit questions are closed, without waiving a mandatory C_OS row. | [Fixture assessment](P9-4.9.3-Review.md), [execution index](P9-4.9.3-ExecutionRecord.json), retained 22-test run. |

The 33 catalog rows comprise 8 common, 14 Candidate C, 1 OS and 10 lifecycle
rows. Fixed-stage evaluations do not pretend to be committed operations.
Test-only domain/nonfinite/charge injections are labelled, while the native
adverse-profile regressions are separately attributed. Multiple requirements
may cite the same actual execution; row counts are not extra run counts.

The public second beat establishes rebuild-before-consumption, with fresh
current/request/ledger/reference and the correct parent. The exact published
3-by-2 dimension input reaches the semantic diagnostic and then the public typed,
atomic rejection; its dimension-valid control is not blanket target admission.
The mandatory published mapped event executes unchanged, with event ID
`grc-event-sha256:583d7337ff5d3910fb189ee1aad0f81485a4722bdb889d5623e82828a759807c`.
Its source/target pair is distinct from the nomination's same-graph migration
pair, and neither is replaced by a declaration seed or another solver profile.

I reviewed the separately recorded seven migration-class dispositions, seven
schema/semantic negatives, distinct live/reset and reset-only rejection cases,
two history channels, mixed crossings, charge/increment edge cases and coherent
receipt mutations. Unavailable A/persistent sources are not executed migrations.
The three-node witness retains algebra credit, and foundation identity/result
vectors retain codec/comparator credit. Historical preimplementation holds are
interpreted against later scoped evidence, not deleted or copied as current
failures. Other realizations, GRC9 expansion/recursion/port cases, hierarchy,
disabled compatibility and cross-implementation charge claims remain outside
this proposed acceptance. None is used to waive an applicable C_OS requirement.

## Authority and reuse boundaries

Current `contract_provenance` and `debt_lifecycle` queries retain source refs,
edge refs, classifications, support dispositions and trace digests in the
successor record. The six parent/abundance contracts are required-support
successors. The older D10.2 ordered-receipt/atomicity/snapshot contracts retain
`indeterminate_requires_review`; the source debts retain bounded-design status
and their forward rows. This phase review assesses the implemented nominated
scope; it does not mutate those historical source statements into global closure.
The D11-C current contract remains separately source-bound.

The 22-test run belongs to its original 473-file scientific/checker subject and
is reused without rerunning it. Of those bindings, 471 remain byte-exact,
including all numerical runtime/tests and the original fixture checker. Two
status-only corrections are verified as exact replacements against `c01526c`:
the browser's ready-leaf list and the API's next-work message. Both old/new
hashes are retained in the successor record, with fresh surface checks rather
than relabeling the original capture. Any other captured-source change rejects.
The 140-test parent, 14-test facade and 11-test
abundance runs are checked at their original Git subjects; they are not summed
or presented as fresh current-tree executions. Reuse is justified by source
comparison: the facade is unchanged since its accepted implementation, the
codec since abundance acceptance, and the only subsequent lifecycle definition
changes are the exact dimension guard and its call from `_affine_resource`,
exercised in P9-4.9.3. Earlier parent/observation/publication definitions remain
unchanged. Hash linkage is reproducible source attribution, not hostile-host
attestation or proof of every possible state.

## Verification

The existing normal verification route dispatches to the
[successor review checker](../verification/verify_p948b_review.py), which reuses
the unchanged fixture-product checks and original accepted evidence. The lean
review command is:

```sh
.venv/bin/python implementation/phase-9-grcv4/verification/verify_p948b_review.py --check
```

It checks the current boundary/release, unchanged execution bindings, typed
authority, exact review inputs and decision consistency. It also executes the
existing API/notebook current-status cells and the actual authority
notebook/HTTP/browser-validator controls. It does not run numerical tests,
historical campaigns or a desktop/mobile browser campaign. Existing status
surfaces are corrected to display current readiness and pending review; no new
authority endpoint or accepted-support claim is added.

Controls include a valid complete PASS **proposal**, a valid HOLD with an open
obligation, and thirteen coherently rehashed invalid proposals: missing or
duplicate rows, empty/family population, unreviewed proposed support, borrowed
mapped identity, erased parent debt, promoted historical support, erased/open
obligations, premature acceptance/registry updates and stale release. Thus a
checker pass is neither a hard-coded HOLD nor an acceptance act, and it does
not replace the substantive evidence assessment above.
Three additional source-reuse controls reject unreviewed changes to either
status file or the numerical facade; the exception is not a file-wide waiver.

### Full-path follow-up and harness correction

The first full normal invocation passed the current release, historical
predecessor replay and complete parent/abundance authority suites, then stopped
at the permission-pressure suite's initial valid-current-tree control. Its
disposable checkout used lifecycle baseline `f10d105` with current files
overlaid. That HEAD cannot contain the required later accepted parent
`d8f26d9` or abundance `1f5f5e9` commits. Both are ancestors of the real reviewed
HEAD. This is a test-fixture defect, not a numerical failure or authority waiver.

The [pressure harness](../verification/test_phase9_g1.py) now uses the reviewed
HEAD for current-file candidates; historical authorization baselines remain
unchanged. Two actual detached-HEAD controls preserve current files but remove
parent or abundance acceptance ancestry. Each must reach its specific real
Git ancestry rejection, and the candidate HEAD/files must be restored.

That correction reached 245/246 pressure passes, including both ancestry
negatives. The sole remaining failure was another fixture assumption: the
export-owner probe appended a second lazy hook to the now-integrated current
file. It now constructs one addition from the frozen pre-integration baseline,
as the dependency probe already does. An explicit duplicate-hook negative
preserves rejection of that invalid input; no export policy or runtime changes.
The failed run remains distinct from the corrected pressure execution.

Read-only inspection before the next stage also exposed a stale 29-leaf
expectation in both its test and the shipped browser status validator. The API
already exposes the accepted fixture leaf (30 leaves / 29 paths). The validator
and synthetic fixture now require that exact list, reject a missing or duplicate
fixture leaf and reject P9-4.8B runtime permission. The surface test also feeds
the real API payload through the shipped browser validator, preventing two
matching synthetic fixtures from hiding API/UI drift. API navigation now names
the pending P9-4.8B decision; support and gate state remain unchanged. The
interrupted pressure rerun earned no execution credit.

Resume the failed and unexecuted stages in order, without repeating the passed
historical/authority stages or numerical evidence:

```sh
.venv/bin/python implementation/phase-9-grcv4/verification/test_phase9_g1.py
.venv/bin/python implementation/investigations/grc9v4-constitutive-design/tools/exploratory-side-tool/tool/scripts/test_phase9_g1_surfaces.py
.venv/bin/python implementation/phase-9-grcv4/verification/verify_p948b_review.py --check
```

The resumed verification also checks the pressure report's current policy/tree
and digest, zero failures and unchanged boundary across those stages, matching
the normal verifier's final checks. This is continuation across attempts,
not a claim that the initial full invocation passed. Generated pressure and
surface results retain their own run identities; no numerical evidence is replaced.

## Decision still required

Accept or return this scoped G2 review. Only a subsequent accepted positive
review may update the exact accepted support/discovery and gate state. Keep
G2 closed and the registries empty until that action; do not infer it from
acceptance of P9-4.9.3 or from this checker's PASS output. No new numerical work
is proposed merely to repeat this review. A_OS and G3 require separate
continuation/entry review after any G2 acceptance.
