# P9-4.9.3 — complete-profile fixture reconciliation

This is the fixture closure submission for review, **not P9-G2[C_OS]
acceptance**. P9-4.8B is next; accepted generic and specialization support remain
empty. The original [P9-4.8 HOLD](P9-4.8-GateReview.json), its checker and all
accepted runs remain unchanged. The initial [inventory](P9-4.9.3-EvidenceInventory.md)
is historical; this document resolves its fixture work, not its separate gate
decision.

## Exact population and evidence

Nominate one complete profile:
`grcv4-profile-sha256:a6b853ee382895eb78b1a7955a0df22f95d68b27cb0f762503e8c424c2f59b6d`.
Its resolved-parameter identity is
`grcv4-params-sha256:81b0876e6852f2b08755f08cf932abf08d95a4763b43fc14bc607f45a5fde48f`.
This is the existing `dyadic_fixture()`: vertices `u,v`, oriented edge `e:u→v`,
reference Hodge 2, mobility 1, constant-zero context, strict-gap/SPD domain,
`tau_C=0`, `chi_C=1`, `zeta_C=3`, `kappa_M_C=kappa_H=0`, and literal zero solve
and charge tolerances. Full parameters, graph, K4, reference weights, context,
current/reset states, receipt ledgers and all identity preimages are retained
as content-addressed objects in the [run](../evidence/P9-4.9.3/complete-profile/run.json).
No unspecified family-wide parameter range is nominated.

The [execution index](P9-4.9.3-ExecutionRecord.json) identifies the run, source,
environment, exact row population and reused evidence. Its `fixture_results`
contain all ten catalog-required fields, actual request/result objects, both
whole snapshots, observation witnesses and explicit execution layers.
`complete_profile_id` and `active_model_identity` identify the **source**;
`target_active_model_identity` separately identifies the actual poststate.
The checker recomputes complete-profile identities from the retained parameters.

| Required group | Nomination product | Evidence boundary |
| --- | --- | --- |
| Common | 8 × 1 | Actual public operations. Domain/nonfinite/charge controls patch internal test boundaries only; native adverse-profile regressions retain separate evidence. |
| Candidate C | 14 × 1 | Exact fixed-stage equations/typing/controls and public post-continuity/crossing observations. A fixed-stage row has `not_invoked`, no commit and no operation receipts; it is not a fabricated transition. |
| OS | 1 × 1 | Public operation, counted predictor/corrector and single resource write, retained split residual. |
| Lifecycle | 10 × 1 | Actual public ordinary/reset/restore/duplicate/migration/event behavior; ordered target identities and reset observations remain separate. |

Two additional rows execute the exact published dimension negative and mapped
event. They do not enlarge the nominated product. Rows sharing an operation
(e.g. charge rejection/atomic failure, or mapped tuple/history/reset) cite that
same result object; they are not additional runs. No mandatory C_OS catalog row
is waived. Changed `chi_C`/`zeta_C` controls retain their own complete-profile
preimages and explicitly carry **no nominated support**. Crossing targets have
their actual registered identities, not the source identity or a family alias.

The scalar profile does not provide a nontrivial dense conditioning contrast
or nonlinear Hodge-deformation derivative. Those retain separately attributed
component evidence and targeted regressions; their passes are not relabeled
as execution of this profile. At the nominated point, `cond(Q_C)=1` and all
conditioning certificates are checked. The `chi_C=1/2` control makes an accidental
second chi gate observable (`J=16`, explicit read `8`).

## Demonstrated gaps closed

**Stale cache:** the actual public second beat consumes current `(2.5,1.5)`,
the second request and duration, prior ledger and reconstructed reference
geometry. Its corrector current is `2`, not the prior beat's `4`; the resulting
resource is `(2,2)`, and parents use the preceding successful primary. A cache
field is rejected before operation. This earns the catalog's committed
`cache_rebuilt_before_consumer` outcome. No persistent cache was introduced.

**Exact dimensions:** the published input is decoded unchanged, including its
policy, source/target orders, six coefficients and two-entry increment. The
production semantic guard raises `resource_transform_dimension_mismatch`.
The public operation translates that into the existing typed
`invalid_topology_event` receipt, with the semantic diagnostic in its message,
null solver disposition and identical whole pre/post state. The three-entry
zero-increment control passes dimensions only; it is not target admission.
Graph-order mismatches and exact charge-form conservation retain their separate
guards. No receipt enum, mathematical law, specification or legacy version changes.

**Derivative exactness:** arbitrary Hodge perturbations are unsuitable for the
nominated zero-tolerance solver because their inverses may not be exactly
representable. The test uses an exact Hodge secant within the strict stratum
(baseline is linear in Hodge at `kappa_M_C=0`), a centered resource derivative,
and signed-edge/vertex covariance. The combined derivative is `-3`. No tolerance
or profile identity is silently relaxed. General smooth nonlinear derivatives
remain independently tested at their own profile.

## Ordered crossings and all migration classes

The nominated source migrates to the original same-graph `os_fixture` target
with `chi_C=zeta_C=kappa_H=0`; both full identities and preimages are in the
migration row. This pair is **distinct** from the published mapped-event pair:

- Source: `grcv4-profile-sha256:4a631bc25ab15c5b10e06588ff58aa6a802fd9a2749a0dd1909ffd3eae535ed8`.
- Target: `grcv4-profile-sha256:f7b4023ed543c496fa693e8a1bbd701d357495374b415602fcc6b4774caffb7b`.
- Event: `grc-event-sha256:583d7337ff5d3910fb189ee1aad0f81485a4722bdb889d5623e82828a759807c`.

The exact published mapped request is now also executed through `GRCV4`, with
resource/reset `(1,2,0.5)` and charge target `3.5`. The prior 50-method
[authoritative run](../evidence/P9-4.7b/authoritative-vector/run.json) remains the
original execution. Neither its earlier reidentified companion nor the frozen
declaration seed substitutes for this row. The separate generic event used by
the nomination maps distinct live/reset states to `(1,3,0.5)` / `(3,1,0.5)`.

| Migration class | Current disposition |
| --- | --- |
| same_candidate_nonhistory_to_nonhistory | Positive exact ordered C_OS pair and public target reset; unregistered targets reject. |
| same_candidate_nonhistory_to_history | Actual C_OS rejects unregistered C_PC/C_CI+PC targets; positive class deferred. |
| same_candidate_history_to_nonhistory | No implemented persistent source; no executed source crossing. |
| PC_to_CI_PC | No implemented persistent source; positive class deferred. |
| CI_PC_to_PC | No implemented persistent source; positive class deferred. |
| A_to_C | No implemented A source; no executed source crossing. |
| C_to_A | Actual C_OS rejects unregistered A targets; positive class deferred. |

Candidate and carrier histories remain separate (`rederived` versus
`not_applicable`, no hidden persistent state). Public event pressure maps a
valid live state to `(1,1,0)` while the reset would become `(3,-1,0)` and rejects
the whole operation. Selected crossing regressions retain singular-current and
singular-reset distinction, stable graph order, negative/absorbed increments,
unrepresentable charge deltas and coherently rehashed receipt failures. Mixed
graph/profile continuation remains in P9-4.7's source-exact evidence. Replay is
**restore plus identical future inputs**, not authentication of an earlier
trajectory: lawful unreceipted assignment remains tested.

## Seven schema/semantic negatives and historical holds

| Vector | Current credit |
| --- | --- |
| SCHEMA-REJECT-PROFILE-FIELD-MISMATCH | Exact codec/schema negative; selected current codec regression. |
| SCHEMA-REJECT-HISTORY-SUBJECT-MISMATCH | Separate published input in that codec regression. |
| SEMANTIC-REJECT-RESOURCE-TRANSFORM-DIMENSIONS | Exact semantic diagnostic plus actual atomic public rejection, as above. |
| SEMANTIC-REJECT-WCTR-EDGE-SET-MISMATCH | Published GRC9-shaped input consumed by the reference validator; current targeted regression. No GRC9 event credit. Generic C_OS target references are independently exercised. |
| SEMANTIC-REJECT-PROFILE-PARAMS-HASH-MISMATCH | Exact profile semantic negative; current targeted regression. |
| SEMANTIC-REJECT-RESOURCE-DISTRIBUTION-SUM | GRC9 expansion-only; deferred, not a generic C_OS blocker. |
| SEMANTIC-REJECT-GROWTH-PHASE-REMAINDER | GRC9 expansion-only; deferred, not a generic C_OS blocker. |

`C-D11-T3A-THREE-NODE-ALGEBRA-WITNESS` keeps algebra-only credit from P9-4.2.
Canonicalization/identity/subdigest/result/failure vectors keep foundation
schema/codec/comparator credit. They are not public lifecycle executions.

Reconcile the published preimplementation `coverage_holds` without rewriting
them: per-realization steps, lifecycle, generic mapped events, immutability and
runtime receipts now have the exact scoped C_OS evidence above and preceding
accepted leaves. Cross-implementation charge conformance remains broader than
these binary64 arithmetic tests. Candidate A, other positive realizations,
hierarchy/child stabilization, GRC9 expansion/deep recursion/metamorphic/port
work and all 40 disabled-compatibility cells remain deferred. The separately
inventoried GRC9/K4 builder issue is not fixed or hidden here.

## Verification and continuation

The [focused runner](../verification/verify_p9493_fixtures.py) uses the existing
exact-source capture, not a second execution-evidence framework. Its fixed
roster and content-addressed fixture objects are retained in one portable JSON
run; no source ZIP or machine-local path is needed. Environment and dependencies
are recorded, so a rerun is identifiable as a new run. Keep full Git history
for older accepted subjects. Current inspection:

```sh
.venv/bin/python implementation/phase-9-grcv4/verification/verify_p9493_fixtures.py --check
.venv/bin/python implementation/phase-9-grcv4/verification/audit_phase9_implementation.py --boundary-only
```

Both commands are nonnumerical. The normal full verification route now dispatches
to this successor and checks original abundance/facade/parent evidence at
`1f5f5e9` / `7905e7e` / `d8f26d9`, rather than asking their historical runs to
match changed source bytes. No claim graph/API/notebook/browser behavior changed;
their accepted authority checks are reused, not represented as fresh UX tests.
Readiness adds only P9-4.9.3 on the existing lifecycle/test paths: 30 ready leaves,
unchanged 29 eligible runtime paths. A valid fixture-product control and rejected
missing/duplicate/empty/family/borrowed-identity/changed-result/G2 mutations are
not a positive gate review.

P9-4.8B must review this nomination, fixture sufficiency and the integrated
facade/parent/abundance results together. Reuse this final run; do not rerun it
merely to review it. Its decision and any later user acceptance remain separate.
P9-7.7-C_OS stays an alias without duplicate credit. Do not start A_OS or GRC9.
