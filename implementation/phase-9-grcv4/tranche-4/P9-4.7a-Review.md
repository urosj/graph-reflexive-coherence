# P9-4.7a — C_OS failure, receipt ownership and replay pressure

Status: accepted by the user after correction. The combined independent audit
recommends ACCEPT for this leaf and closes the P9-4.6 correction debt.
The [audit input](../evidence/P9-4.7b/audit-followup/inputs.json) is retained.
P9-4.7b's mandatory mapped row now passes under its accepted successor release;
its [review](P9-4.7b-Review.md) records that separate correction and validation.
The batch does not grant a runtime conformance support set.

## Contract and interpretation

Controlling sources: the [specification](../../../specs/grc-v4-spec.md)'s
complete-step, lifecycle, serialization and errors sections; the
[interface extension](../../../specs/grc-common-interface-v4-ext.md)'s result,
receipt and atomicity contracts; paper §§11–12 and E.3–E.5/E.11–E.12; and the
accepted side-tool provenance for `L-ATOMICITY` and `L-ORDERED-RECEIPTS`.

The commit owner now checks requested clock progression, preservation of
reference/context/reset/Q/receipt ownership, and exact scientific identity at
zero duration before publishing a numerical target. Matching state, result,
receipt and commit hashes cannot substitute for these transition postconditions.
The four invalid-transition characterizations retained from P9-2.4/P9-2.5 now
run through the actual owner with explicitly injected producer faults. Each
must reject before publication; an unmodified live operation is the positive
control. Native clock/resource results separately use rational expectations.

This bounded receiver keeps the established parent convention: ordinary steps
name the preceding ordinary primary receipt; administrative and crossing operations name the
latest primary receipt. All four receipts in an operation share its parent set.
The persistent ordered ledger supplies the full operation order. Parents resolve
to earlier receipts in this same ledger; foreign, missing, duplicate, reordered
or inappropriate parents reject. The frozen common schema does not prescribe
this local convention or authorize importing other conventions automatically.
Historical state digests remain content commitments, not proof that external
historical computation occurred. Current-state assignment can lawfully interrupt
a chain of equal target/source authoritative values without a new step receipt.

## Pressure index

`CandidateCOSReplayTests` in
[test_grc_v4_lifecycle.py](../../../tests/models/test_grc_v4_lifecycle.py) adds:

- rational live resource/clock expectations, strict zero, minimum positive
  duration and large clocks;
- four coherently formed invalid numerical targets and lifecycle replacement
  controls, rejected before publication;
- native typed failures with a populated ledger, distinct current/reset states,
  and successful recovery equivalent to an untouched duplicate;
- failure-receipt exclusion from persistent history;
- interleaved ordinary/reset/rebase deltas and the exact parent convention;
- missing/foreign/reordered parents with recomputed hashes;
- save/load replay at every checkpoint of an operation sequence with literal
  dyadic resource expectations;
- independence from mutable result projections and old observables; and
- injected programmer exceptions at receipt, result, binding and publication
  preparation, preserving the complete snapshot and reference identity.

The [P9-4.6 audit follow-up](P9-4.6-AuditFollowup.md) adds shared semantic
prepublication validation, 14 correctly hashed administrative corruption controls,
four administrative target corruptions with an empty ledger, the three exact
zero-then-minimum-positive clock cases, and unreceipted assignment
followed by snapshot continuation replay. The P9-7.1-C_OS alias creates no credit
for this stronger P9-4.7a slice.

Previously accepted ordinary-operation, snapshot/reset, request, codec and state
tests remain shared regressions. The [execution record](P9-4.7a-ExecutionRecord.json)
binds the final source and one retained batch run. Native observations and
injected controls retain separate meanings; test counts do not create independent
audit acceptance. Use the full Git base and the four exact source overlays named
by the run, recovering their historical bytes through the
[reverse patches](../evidence/P9-4.7b/pre-release-source/inputs.json); verify its
scoped manifest and run the recorded module command in a
fresh generated destination. The relocation check uses a separate full-history
checkout with spaces, a fresh interpreter, seed 947 and one BLAS/OpenMP thread.
It reuses installed locked dependencies; it is not a fresh-install or second-
platform claim. The run checks source slots, loaded code, exact executed roster,
outcomes and unchanged source bytes. Committed source is recovered through Git,
not copied into the evidence folder.

The established local parent rule is tested, but full generic parent-DAG
conformance remains unresolved by this bounded implementation. The exact-input mapped-vector correction is recorded in
[P9-4.7b](P9-4.7b-Review.md). P9-4.8 and G2 have not been started or accepted.
