I've now read all the relevant artifacts: the guide itself, the P9-1.6 design doc and execution record, both successor verifiers (`audit_phase9.py` v1 and `audit_phase9_successor.py` v2), the shared policy module (`phase9_policy.py`), both test suites (13 dispatch tests + 74 pressure cases), the boundary/successor policies, the P9-1.7/1.8 records/scenarios/review, and the access guide.

One important scope observation up front: **the work on disk has grown well beyond the guide's stated P9-1.6 scope.** The guide is titled "P9-1.6 — Successor Verification Pressure Guide" and defines its own scope as *"successor verification and routing from the normal tool entry point,"* explicitly separating out P9-1.7 (adversarial mutation-policy), P9-1.8 (tool/API/notebook/browser surfaces), and P9-1.9 (accepted P9-G1 authority). The implementation now bundles a full v2 successor verifier, a 74-case pressure matrix, dependency/composition validators, an optional-completion gate, and API/notebook/browser/scenario surfaces — i.e., it covers not just the guide's P9-1.6 but also its P9-1.7 and P9-1.8 leaves. That is more than required, not less.

I did **not** execute any code (that would write generated receipts into `tool/generated/phase9-verification/`), so passing claims below are drawn from the artifacts plus static reasoning about the code, not an independent re-run.

## Directive-by-directive coverage

The guide's 13 sections map onto the implementation as follows:

**§1 Central acceptance question** — Covered. `active_phase.py` selects the successor purely by opening-file presence or Git ancestry (never a flag/branch); `run.py` routes `verify-post-d10-specifications` through that selector; both dedicated (`verify-phase9`) and normal entry converge on the same v2 verifier. The claim ceiling is stated explicitly in every receipt: `verification_of_planning_and_frozen_authority_not_runtime_conformance`.

**§2 Source basis / evidence ceiling (7 requirements)** — All covered. Historical audits run unchanged in a disposable shared clone byte-verified against git blobs (§2.1–2.2); the current tree is verified through content-bound policies (§2.3); authorization is never inferred from name/flag/old acceptance (§2.4, reinforced by `CLI_has_no_authorizing_flag`); P9-1.1 crosswalk rerun preserves 41 claims/183 contracts/31 debts/18 obligations (§2.5); both policies assert `accepted_generic_runtime_support == []` and empty specialization sets (§2.6); P9-TOOL-001 stays held until dependent verification closes (§2.7).

**§3 Historical-vs-current subject distinction + 4 pressure cases** — Covered. Receipts retain verifier identity, baseline/release identity, evaluated-tree identity, dirty/staged/untracked scope, authority chain, and conclusion scope. Frozen-file corruption is rejected (`changed_frozen_spec`), the old historical false-flag is preserved as a correct fact while current review is admitted, mixed-revision byte drift declines attestation, and missing historical material raises via `ancestor()` rather than skipping.

**§4 No error-suppression fallback + 8 double-checks** — Covered well. Mode is resolved from validated authority *before* any execution; `run_logged` requires exit 0 (a green string cannot override a non-zero exit — directly tested by `test_success_marker_does_not_override_failed_command`); no try/except legacy fallback exists; dedicated and normal entry evaluate the same policy version; verification does not repair manifests or regenerate hashes.

**§5 No self-authorization + 7 pressure cases** — Covered. The fixed `EFFECT` dict cannot grant runtime authority; rehashing digests fails because an external anchor (release_id + predecessor_digest) is required (`self_rehashed_runtime_approval` rejected); each predecessor is checked via `git_exact`; a future gate ID without its acceptance record is rejected by the fixed status field; contradictory planning+runtime records are rejected.

**§6 Precise mutation scope + matrix** — Covered thoroughly. `grc_v4.py`, `__init__.py`, and `pyproject.toml` are explicitly protected with additive-only integration rules; frozen auditors/specs can't be edited to pass; untracked/ignored/staged additions are all included in scope (`validate_tree` uses `ls-tree` + `ls-files --cached --others`); deletions, symlink escapes, and mode changes are rejected (`deleted_frozen_spec`, `protected_symlink_rejected`, `protected_mode_rejected`).

**§7 Protect claim meaning not just counts + 8 mutation table** — Covered via preservation. `validate_composition` checks parent/evidence-alias/status semantics (not totals), C_OS dependency closure stays isolated from unrelated profile barriers, unexecuted exact profiles remain unsupported (`exact_accepted_profile_ids == []`), and deferred obligations stay `planned_not_executed` rather than being converted to non-applicable. The specific "conditional→normative" and "indeterminate→supported" claim-class mutations are D10.2-crosswalk concerns that P9-1.6/1.7 deliberately leave untouched (preserved), so they're covered by non-mutation + the retained 15 pending obligations.

**§8 Prove real entry ran successor + minimum end-to-end evidence** — Mostly covered. The original phase-envelope failure is documented; dispatch tests confirm normal and dedicated routes select the same verifier; protected-input mutations in an isolated clone are verified to reject for the intended reason (`test_phase9_dispatch.py`). Two caveats: (a) I could not independently re-run the real normal entry point due to the no-file constraint, so "it actually passed on this tree" is self-reported in `P9-1.6-ExecutionRecord.json` and statically consistent but not freshly confirmed; (b) per-case "which check was reached" is implied by the ValueError message rather than recorded as an explicit field — acceptable for planning phase but not a perfect match to §8's ideal.

**§9 Staleness/caching/side effects** — Covered. `current_boundary` re-runs policy+tree validation after execution and asserts `after == boundary` (TOCTOU + side-effect guard); receipts are bound to inputs via `policy_digest`; the access guide documents that a matching *recorded* pass is a prior run for current inputs, not a signed attestation.

**§10 Three combined outlier tests** — **Partial gap.** The guide explicitly says these belong *after* isolated one-fault cases and "expose errors that a collection of isolated unit tests can miss." The pressure matrix is overwhelmingly single-fault (74 isolated cases). I did not find explicit combined-fault scenarios matching A (legit + illegitimate together), B (self-consistent but unauthorized promotion with all digests regenerated), or C (green history + stale current report with unchanged counts). This is the most notable coverage shortfall.

**§11 Reporting boundaries / closure language** — Covered. Separate conclusions are presented for historical release validation, historical auditor validation, current-tree identity/scope, successor authority-chain, normal-entry routing, claim/support/debt preservation, P9-1.7 status, P9-1.8 status, runtime authorization, and both support sets. The pre-P9-1.9 boundary block (`P9-G1: pending; runtime_authorized: false; accepted_generic_runtime_support: []; admitted_specialization_support_sets: []`) is stated verbatim in the docs and records.

**§12 Evidence to retain (per-case fields)** — Partial. The aggregate receipts retain command-level evidence (labels, exit codes, code_head, historical_revision, release_id, tree scope). Per-pressure-case, the results include case_id, review_pressure, expected/observed/passed/scope but omit some of the guide's ideal fields: base-fixture identity per case, explicit "which check was reached," verifier/dispatcher identities per case, and a protected pre/post state comparison per case. Reasonable for planning phase; not a complete match to §12's checklist.

**§13 Priority summary (7 highest-value checks)** — All seven are demonstrably addressed: no broad fallback (§4), no self-authorized promotion even after rehashing (§5), separate historical/current evaluation (§3), exact mutation scope across committed/staged/unstaged/untracked (§6), unchanged support semantics despite fixed counts (§7), real normal-entry execution with attributable negatives (§8/§10), and no stale/side-effect/status projection (§9).

## Gaps and caveats

1. **Scope growth beyond the guide.** The implementation is really a bundled P9-1.6 + 1.7 + 1.8 effort (v2 successor, 74-case pressure matrix, dependency/composition/optional-completion validators, API/notebook/browser/scenario surfaces). This exceeds the guide's P9-1.6 mandate rather than failing to meet it.

2. **§10 combined-fault tests missing.** The guide calls these out specifically as catching errors isolated unit tests miss; only single-fault cases are present.

3. **§8/§12 per-case evidence granularity.** "Which check was reached" and pre/post protected-state comparisons aren't recorded per case at the level §8/§12 describe.

4. **Naming convergence worth noting.** `active_phase.py`'s `PHASE9_AUDITOR`, `run.py`'s `verify-phase9`, and both entry routes all resolve to `audit_phase9_successor.py` (v2), which supersedes the v1 `audit_phase9.py`. The v1 auditor bytes (`baba1eb0…`) and its policy digest (`8282d8e0…`) are pinned in `phase9_policy.py`'s predecessor snapshot, so backward compatibility is maintained — but a reader glancing at P9-1.6's own record (which references the v1 file) can be momentarily confused about which verifier is actually live.

5. **Not independently executed.** Due to the no-file-change constraint I did not run `test_phase9_dispatch.py`, `test_phase9_pressure.py`, or the normal entry point. The artifacts claim 13/13 and 74/74 passes; the code logic statically supports those claims, but they are self-reported, not freshly confirmed by me this session.

## Bottom line

**Yes — P9-1.6 (as implemented, now bundled with P9-1.7 and P9-1.8) covers essentially all of the guide's directives.** Every one of the guide's 13 sections has a corresponding implementation mechanism, and all seven §13 highest-value checks are demonstrably addressed. The coverage is actually broader than the guide required (it absorbed the guide's own P9-1.7/1.8 leaves). The substantive gaps are narrow: the three combined-outlier tests (§10) appear absent, and per-case evidence granularity (§8/§12) is lighter than the guide's ideal. Both are planning-phase refinements rather than structural failures — nothing in the guide's core concern (no false acceptance, no false rejection, correct subjects under correct authority, no over-claiming) is left uncovered.


----

I've now read the P9-1.7 guide in full and have all the implementation artifacts from context (the v2 successor verifier, shared `phase9_policy.py`, 74-case pressure matrix + results, dependency/composition/optional-completion validators, scenarios register, execution record, access guide). Here is my assessment of how well P9-1.7 covers its own guide's directives.

## Overall shape

The implementation treats P9-1.7 as the **adversarial mutation-and-authority leaf** and bundles it with P9-1.8 surface reconciliation — exactly matching the guide's scope ("challenge the successor verifier and actual normal-entry route"). The core deliverable is `test_phase9_pressure.py` (74 cases, all passing) driven by `phase9_policy.py`, which reuses the byte-frozen P9-1.6 auditor as its trusted predecessor anchor. This is a strong structural match to the guide's central question in §1: accept exactly the reviewed changes, withhold when necessary, and preserve verification-vs-permission.

## Directive-by-directive coverage

**§1 Central question + paired cases** — **Covered.** The matrix has explicit admit/reject pairs (legitimate_current_planning ↔ planning_rejects_*, future_explicit_approval ↔ self_declared_runtime_no_external_anchor). No accept-all or reject-all: 30 admit / 44 reject, both directions exercised.

**§2 Entry/binding + protect live checkout + bound threat model** — **Covered well.** `phase9_policy.py.predecessor()` binds the exact P9-1.6 candidate verifier (byte-frozen via SHA-256 guard), dispatcher, release, historical baseline, scope policy, and trust anchor before any mutation. The pressure test runs entirely inside a disposable `tempfile` clone (`grcv4-p917-pressure-*`) — it never mutates the live checkout except to *read* it in `original_checkout_still_planning`. Synthetic future approvals are written only into the scratch repo, never the live policy (§2's "Do not add hypothetical runtime permissions to the real Phase-9 gate" is honored).

**§3 State-and-permission matrix (12 rows)** — **Nearly fully covered.** Mapping each row:
- Review-positive → `legitimate_current_planning` (admit) ✓
- Premature `grc_v4.py` before P9-G1 → `planning_rejects_src_pygrc_models_grc_v4.py` (reject) ✓
- Premature V4 test → `planning_rejects_tests_models_test_grc_v4.py` (reject) ✓
- Future-positive under trusted simulated P9-G1 → `future_explicit_approval_exact_targets` (admit, scoped `synthetic_future_approval_not_live_authority`) ✓
- Frozen-byte mutation → `changed_frozen_spec`/`deleted_frozen_spec` (reject) ✓
- Additive-path misuse → `legacy_dependency_not_additive`/`legacy_export_rebound` (reject) ✓
- Unauthorized committed change + clean worktree → `validate_tree` compares accepted→current via `ls-tree`, and `assume_unchanged_cannot_hide_bytes` rejects it ✓
- Authorized dirty review uncommitted → untracked additions validated by exact bytes, not rejected for Git status ✓
- Missing historical input → `ancestor()` raises (unverifiable, not passed) ✓
- Rehashed forged grant → `rehashed_policy_*`/`self_rehashed_runtime_approval` (reject) ✓
- Wrong context (other release/baseline/predecessor) → `runtime_wrong_target_*` + stale-predecessor checks (reject) ✓
- Mixed change → structurally enforced by whole-scope acceptance failing on any forbidden byte; no explicit single "legit+forbidden together" case (see §8 gap).

**§4 Mutation coverage beyond ordinary edits** — **Strongly covered.** This is the implementation's best section. §4.1 (committed/staged/unstaged/deletions/renames/untracked, file-type/symlink change, untracked build output handling) maps to: `validate_tree` uses `ls-tree -r -z` + `ls-files --cached --others`, plus `protected_symlink_rejected`, `protected_mode_rejected`, `assume_unchanged_cannot_hide_bytes`, `staged_runtime_addition_rejected`, `ignored_runtime_addition_rejected`. §4.2 bypass probes (all six) are individually present. §4.3 "test what actually executes" — historical auditors are byte-verified against git blobs and run from the disposable clone, exposing accidental relative-path imports.

**§5 Authority/forgery/staleness** — **Mostly covered.**
- §5.1 (candidate records can't grant trust; rehashing; cyclic predecessor; missing acceptance; conflicting successors) — rehashing is extensively tested (`rehashed_policy_*`, `self_rehashed_runtime_approval`). **But explicit "cyclic predecessor chain" and "conflicting candidate successors" cases are absent** — the policy checks specific fields (status/active_phase) rather than modeling a predecessor graph, so those two named probes aren't exercised.
- §5.2 (old false flag vs new false grant) — **Covered structurally.** Historical `implementation_authorized=false` audits run unchanged; future-positive admits under a separate externally-supplied anchor. Both subjects/times preserved rather than Boolean-resolved. Not a single explicit paired test, but the architecture enforces it.
- §5.3 (cached success / TOCTOU) — **Covered.** `current_boundary()` re-runs policy+tree validation after execution and asserts `after == boundary`; stale receipts are documented as prior-run evidence, not fresh passes.

**§6 Claim drift without count drift (8 mutations)** — **Partial.** This is the most notable coverage shortfall. Mapping:
- Conditional→normative → **not directly tested** (validate_composition checks status/evidence_alias/dependency but not claim-class promotion).
- `indeterminate_requires_review`→supported → **not directly tested** (a P9-1.5 crosswalk concern; the guide says test it "if a semantic-projection checker is also claimed," and P9-1.7 doesn't claim one for this, so defensible but unaddressed).
- Candidate C optional→universal → `universal_optional_completion` (reject) ✓
- GRC9-only→generic specialization leakage → **not directly tested** (C_OS closure isolation is checked, which is related but different).
- 40-cell compatibility duplicate → `matching_count_child_substitution` partially covers "same count, different content" ✓ partial
- Planned C_OS→accepted runtime support → `unexecuted_support_set`/`unexecuted_exact_profile` (reject) ✓
- Evidence alias counted twice → `forged_evidence_alias` (reject) ✓
- Unimplemented obligation→nonapplicable → status==planned_not_executed check + `universal_optional_completion` ✓
- Deferred RG2b→scientifically rejected → **not tested**

So ~4 of 8 are directly covered, ~1 partial, ~3 absent. The guide frames these as "recommended," and notes the semantic-projection test is conditional on claiming such a checker — so this is a defensible scoping rather than a structural failure, but it's the clearest gap against the letter of §6.

**§7 Discrimination** — **Mostly covered.**
- §7.1 three-outcome recording (candidate decision / assertion match / project effect) — recorded as observed/passed/scope; "project effect" is implied by top-level `runtime_authorized:false` and scope labels but not a per-case field. Partial.
- §7.2 positive/negative pairs ✓, **but the explicit "deliberately break one protection in a disposable checker copy and confirm its test fails" (inertia/no-false-negative check) is absent.** The guide specifically recommends this to prove tests aren't inert — not present. Notable gap.
- §7.3 avoid self-grading — well covered; expected decisions come from the hardcoded `case()` table, not computed by the same predicate under test.
- §7.4 exercise normal route — routing is tested (`test_phase9_dispatch.py`), and `audit_phase9_successor.py` invokes the pressure matrix as a subprocess, but I could not confirm an end-to-end run through `run.py verify-phase9` this session (no-file-change constraint). Partial.

**§8 Three combined outliers + fourth P9-1.7/1.8 boundary case** — **Partial.** The three combined-outlier tests (A: legit+protected corruption together; B: self-consistent forged successor + runtime addition; C: valid baseline then stale report reuse) are single-fault only, same limitation noted in the P9-1.6 session. **However**, the guide's *new* fourth outlier — "a negative test rejects its candidate but a consumer renders 'tree verified'; P9-1.7 supplies the fixture, P9-1.8 checks the projection" — **is covered** via P9-1.8's `P9-UX-REJECTION` scenario (`verification.test.mjs`: authority promotion, contradictory disposition, digest drift rejected). That's a genuine cross-leaf coverage win.

**§9 Evidence package + bounded completion** — **Lighter than ideal.** Per-case the results retain case_id, review_pressure, expected/observed/passed/scope. Missing §9's ideal fields: exact mutation/diff, decisive check trace (beyond the ValueError message), protected pre/post manifests per case, run identity, residual limitation per case. Reasonable for planning phase but not a complete match.

**§10 Execution batches** — **Implicit.** The six `P9-REVIEW-PRESSURE-8.x` groupings in the scenarios register approximate the guide's three-batch structure (baseline/permission; file scope/frozen/trust/semantic; stale/normal-route/evidence), but there's no explicit three-batch separation with independently retained per-batch results — it's one aggregate report. Partial.

## Gaps summary (in rough order of significance)

1. **§7.2 inertia check missing** — the guide explicitly recommends breaking a protection in a disposable checker copy to confirm tests aren't inert; no such negative-control test exists. This is arguably the most important omission, since it's the standard way to prove a rejection test actually guards something.
2. **§6 claim-class drift under-tested** — conditional→normative, indeterminate→supported, GRC9-only→generic, and deferred-RG2b→rejected mutations aren't exercised; only dependency/evidence/completion semantics are validated. Defensible scoping (guide makes semantic-projection testing conditional), but the letter of §6 is not met.
3. **§5.1 named probes absent** — explicit cyclic-predecessor-chain and conflicting-successcases cases aren't present.
4. **§8 combined-outlier tests absent** — only single-fault cases (same as P9-1.6's §10). The fourth boundary outlier is covered via P9-1.8, which partially compensates.
5. **§7.1/§9 per-case evidence granularity lighter than ideal** — no explicit "project effect" field, no per-case mutation diff / pre/post manifests / run identity / residual limitation.
6. **§10 batching implicit** — six-pressure grouping rather than three-explicit-batch separation with retained results.

## Bottom line

**P9-1.7 covers the guide's core contract strongly and exceeds it in several areas.** The mutation-scope coverage (§4), threat-model discipline (§2), rehash/forgery resistance (§5.1), staleness/TOCTOU (§5.3), and non-self-grading design (§7.3) are all genuinely well-implemented, and the paired positive/negative structure (§1, §7.2) is present. The implementation earns the guide's proposed closeout statement: under the named verifier, trust model, and input envelope, the reviewed cases produce the expected scoped decisions including normal-entry routing — without earning P9-1.8/P9-G1/runtime/conformance.

The substantive gaps are concentrated in **§6 (claim-class semantic drift)** and **§7.2 (inertia/negative-control test)**, with §8 combined-outliers and §5.1 named probes as secondary misses. None of these undermine the guide's central concern — that the successor route accepts exactly its authority permits and rejects everything else under correct authority without over-claiming. They are refinements that would strengthen adversarial confidence but aren't required to satisfy the leaf's core purpose. As with P9-1.6, I did not execute any code this session, so the 74/74 pass claim is self-reported in `P9-1.7-PressureResults.json` and statically consistent with the code, not freshly confirmed by me.

----

I've read the P9-1.8 guide in full and all its surface artifacts: the API module (`phase9_verification.py`), the surface test harness (`test_phase9_surfaces.py`), notebook runner, server, browser spec + web files (index.html, verification.js), plus the scenarios register and access guide. Here is my assessment of how well P9-1.8 covers its own guide's directives.

## Overall shape

The implementation treats P9-1.8 as a **cross-surface projection-consistency leaf**: one read-only `verification_status` API, an executable notebook that byte-compares against it, a separate read-only browser server, and a Node/browser test suite — all sharing the same bound payload and claim ceiling. This matches the guide's central question (§1) precisely: *"Can every affected consumer present or export the verified result without changing its subject, scope, freshness, authority, evidence kind, or claim ceiling?"* The implementation is deliberately minimal (no new frontend, no redesign of the D11 explorer), which aligns with §2's "affected surfaces" framing rather than a rewrite-everything approach.

## Directive-by-directive coverage

**§1 Central question + affected-surface inventory** — **Partially covered.** The scenarios register links all six P9-REVIEW-PRESSURE-8.x items and four access scenarios (API/notebook/browser/rejection) to executables, and every surface shares `verification_status`. But the guide §2 explicitly wants an *inventory table* recording each surface's input contract, what it executes/displays, its output/export, and review owner. The implementation has scenario entries but not that structured inventory — no explicit "input contract → output → review owner" per surface. Defensible scoping (the guide says the fields are advisory), but the letter of §2 isn't fully met.

**§3 Multidimensional result** — **Well covered.** `verification_status`'s payload preserves: schema/output_class, runtime_authorized + its source, P9_G1_accepted, accepted_generic_runtime_support/admitted_specialization_support_sets (both empty), current_boundary, recorded_full_verification (evidence kind: not_current vs recorded_pass_matching_current_inputs), policy_digest, source_refs with sha256, separate leaf iterations, next_gate, and claim_ceiling. This maps almost 1:1 onto the guide's required multidimensional fields (§3). The `output_class` label `implementation_verification_status_not_forensic_trace` is a deliberate semantic boundary, not a cosmetic tag.

**§4 Shared scenario corpus** — **Covered.** All three surfaces project the same decision-relevant content through one API; the scenarios register documents each scenario's "meaning that must survive projection." The guide notes not every frontend must execute every verifier directly — here the browser is a read-only live consumer of the same `verification_status`, which is exactly the intended architecture.

**§5 Highest-risk case: negative test's green status** — **Partially covered (this is the key gap).** The guide §5 demands an end-to-end projection chain: *unauthorized mutation → verifier rejection → passing negative-test assertion → API/notebook/browser projection → export, with the candidate staying rejected at every stage.* 

The implementation tests failure-closing well but not this specific cross-surface projection of a P9-1.7 negative result:
- The API injects `forged source binding`/`missing authority`/`subprocess error` and confirms `failed_closed` + `not_current` ✓ (failure path)
- It tests stale/forged/malformed receipts → held, not promoted ✓
- The browser test "forged runtime payload and changed digest fail closed" rejects `{runtime_authorized: true}` and `{claim_ceiling: 'forged'}` ✓
- **But** there is no case that runs an actual P9-1.7 negative-test-rejected candidate through all three surfaces in sequence to confirm the *green harness assertion* never becomes a *red candidate admission* at each projection stage. The failure-closing is tested; the specific "negative test green status must not become candidate admitted" projection across every surface is not run end-to-end.

**§6 Semantic conversion prevention:**
- **§6.1 No status substitution** — **Strongly covered.** `checkedStatus` in verification.js enforces `runtime_authorized === false`, `P9_G1_accepted === false`, empty support arrays, and rejects unknown boundary/recorded dispositions; the API injects missing authority → `failed_closed`. False/missing/unrecognized values are all tested. Transport-success (200 OK) is explicitly decoupled from scientific pass.
- **§6.2 Keep accepted authority and forensic support separate** — **Covered by labeling/separation, not projection.** The `output_class` label preserves the P9-REVIEW-001 distinction, but this surface intentionally does *not* project D10.2 `indeterminate_requires_review` dispositions at all (it's a governance status, not a forensic trace). So §6.2 is honored by keeping the surfaces separate rather than by actively preserving those dispositions in projection. Defensible — the guide says "preserve both statements," and here it's preserved as "this surface doesn't touch that claim."
- **§6.3 Counts retain denominator and evidence kind** — **Covered.** `claim_ceiling` explicitly states *"Current planning integrity and recorded tool execution are not scientific authority or runtime conformance."* No counts are presented as executed evidence; the 41/80/183 headline isn't even projected here (it belongs to P9-1.6). Good.

**§7 Freshness:**
- **§7.1 Notebook freshness** — **Covered by design, not by explicit rerun-after-mutation test.** `run_phase9_notebook.py` executes real cells and asserts `observed == verification_status(root)`, leaving saved outputs empty (no stale output to project). But it doesn't explicitly "change a source file and rerun; previous cell success must not be presented." The defensive-by-design approach (empty outputs) largely satisfies the intent, but the guide's specific freshness-after-mutation probe isn't run.
- **§7.2 Browser/API freshness** — **Well covered.** `refreshGeneration` counter prevents an older async response from overwriting a newer one; `cache: no-store`; the browser test "refresh failure removes stale success and disables download" covers failed refresh + stale-success removal. The guide's "request A, switch to B, let A finish last" maps directly onto the generation guard.
- **§7.3 Hash/schema fidelity** — **Well covered.** `checkedStatus` validates 64-hex policy_digest and source_refs sha256; distinct false/absent values are preserved (any non-false runtime flag throws); unknown dispositions throw rather than defaulting to success.
- **§7.4 No hidden read-side writers** — **Well covered.** The server is GET-only (`do_POST` → 405), exposes no arbitrary repo-read route (browser test confirms `/pyproject.toml` → 404, `/api/run-verification` → 404), and the API/runner only write to `tool/generated/phase9-verification/`. No protected path is rewritten.

**§8 Tool documentation and checklist propagation** — **Covered.** The Phase9VerificationGuide.md access guide distinguishes historical/current/adversarial/surface/reviewer/runtime, documents the actual normal command (`verify-phase9`), its input identities, expected scoped conclusions, and known limitations. P9-1.7/1.8 records update without checking off P9-1.9 or unrelated runtime work. The guide's "documentation-only rewording that changes authority status is a semantic change" concern is honored — the labels are governance-status, not scientific claims.

**§9 Artifact and regression expectations** — **Covered.** Scenarios register retains the corpus; browser test binds screenshots to structured results *and* asserts `JSON.parse(download) === api` (exact export equality); Node tests assert 6/6 assertions executed; API checks = 13. Regression scope includes existing D11 suites via `verify-iteration9`. The guide's "schema-valid JSON doesn't prove a visible label preserves meaning" concern is addressed by the browser test checking both the DOM labels and the downloaded JSON against the same payload.

**§10 Completion and shared issue reconciliation** — **Covered.** P9-1.7/1.8-VerificationReview.md carries a handoff-style statement (P9-1.6 route verified / P9-1.7 matrix reconciled / P9-1.8 projections reconciled / P9-TOOL-001 closure or remaining conditions stated / P9-G1 still required / runtime support unchanged). It names remaining conditions rather than erasing the historical failure, matching §10's requirement.

## Gaps summary (in rough order of significance)

1. **§5 negative-test-green-status projection not run end-to-end across surfaces.** This is the guide's explicitly highest-risk case ("the highest-risk case: the negative test's green status"). Failure-closing is tested, but no case runs an actual P9-1.7 rejected candidate through API → notebook → browser → export to confirm the green harness assertion never becomes a red candidate admission at each projection stage. The most significant gap against the letter of §5.
2. **§2 no explicit surface applicability inventory.** No structured input-contract/output/review-owner table per surface — scenario entries exist but not the full inventory structure.
3. **§7.1 notebook freshness-after-mutation** is defensive-by-design (empty outputs) rather than an explicit rerun-after-change probe.
4. **§6.2 indeterminate disposition preservation** is honored by separation/labeling, not by actively projecting those dispositions — defensible but the guide's "preserve both statements" is satisfied structurally rather than in projection.

## Bottom line

**P9-1.8 covers the guide's core contract strongly and matches its intent closely.** The highest-value concerns — no status substitution (§6.1), freshness/async-race protection (§7.2), hash/schema fidelity (§7.3), no hidden read-side writers (§7.4), exact export-to-API equality (§9), and explicit P9-TOOL-001 closure conditions (§10) — are all genuinely well-implemented, and the minimal "affected surfaces only" architecture aligns with §2's guidance rather than over-reaching. The implementation earns the guide's proposed closeout statement: affected operational and presentation surfaces preserve the same bound verification conclusions, negative-test subjects, authority distinctions, source semantics, freshness limitations, and evidence ceilings — without granting P9-G1/runtime/conformance/promotion.

The substantive gap is concentrated in **§5** (the explicitly highest-risk case), where failure-closing is tested but the specific cross-surface projection of a green-negative-test-into-red-candidate-admission chain isn't run end-to-end through all surfaces. Secondary gaps are §2's missing inventory and §7.1's defensive-by-design freshness. None undermine the leaf's central purpose — that no surface overclaims subject, scope, freshness, authority, evidence kind, or claim ceiling. As with P9-1.6 and P9-1.7, I did not execute any code this session (the browser test requires a live server + Playwright), so the pass claims in `P9-1.7-PressureResults.json` and the surface harness are self-reported and statically consistent with the code, not freshly confirmed by me.

