# P9-1.7 and P9-1.8 — successor pressure and accessible verification

Status: implemented and verified; user review and P9-G1 pending.

The entry request authorizes these two verification leaves, not runtime work.
They share fixtures and a release-bound successor policy but retain
[separate execution results](./P9-1.7-1.8-ExecutionRecord.json).

## Authority and retained evidence

The accepted specifications are the normative source. The paper and accepted
investigation claims remain explanatory/provenance authority. The source
crosswalk is rerun through the forensic API; this batch adds no scientific
claim, contract, runtime profile, or debt discharge. All fifteen pending
source obligations and five implementation follow-ups remain pending.

The original P9-1.1–P9-1.5 records and P9-1.6 auditor/policy/review/result are
unchanged. Because P9-1.6 was already prepared but not committed on entry,
its exact thirteen-file candidate is retained in the
[predecessor snapshot](../verification/history/P9-1.6-ArtifactSnapshot.json).
Each row includes complete UTF-8 bytes and SHA-256, with a pinned aggregate
digest. The original bindings can therefore be reconstructed without
rewriting the old result to describe this later tree. Seven routing/progress
files have explicit successor bindings; the remaining P9-1.6 files stay exact.
The pressure suite reconstructs that original tree and reruns all thirteen
unchanged P9-1.6 dispatch/authority tests before overlaying the V2 candidate.

The [V2 policy](../verification/Phase9VerificationSuccessor.json) is a
planning-only verification candidate. A green flag, self-declared phase,
filename or locally rehashed runtime declaration cannot supply user approval.
Hashes are integrity bindings, not signatures or independent scientific proof.

## P9-1.7: actual byte and authority pressure

The new validator checks all protected baseline files directly against their
Git blobs and modes, plus the exact new-path/content roster. Staged additions,
ignored additions, deleted files, symlink escapes and `assume-unchanged` cannot
hide unauthorized source, test, specification or dependency changes.

The [retained pressure matrix](./P9-1.7-PressureResults.json) is reproduced by
the executable test, not accepted by counting or reading a green marker.
It covers every `P9-REVIEW-PRESSURE-8.1`–`8.6` assignment:

| Review item | Pressure |
| --- | --- |
| 8.1 | Planning success cannot admit source/test/dependency changes or install runtime authority. |
| 8.2 | The legitimate current planning tree and unchanged predecessor pass; an independently pinned synthetic future approval admits exact new V4 targets and additive integration. |
| 8.3 | Missing/substituted children, forged evidence aliases, unresolved dependencies and cycles reject, independently of matching counts. |
| 8.4 | C_OS keeps its own dependency closure without unrelated A/PC/RG2b barriers; unexecuted exact profiles remain unsupported. |
| 8.5 | Optional completion is conditional on advertisement or explicit handoff and its own exact-profile evidence. Completion off never waives mandatory mechanics, topology, resource/charge, reset, receipt, delegate or compatibility leaves. |
| 8.6 | Frozen bytes/modes, deleted authority, stale hashes, contradictory/rehashed status, protected-path wrong content and invalid runtime targets fail closed. |

Runtime-positive cases operate only on isolated synthetic authority fixtures.
The live policy has no trusted runtime approval. A real successor, its exact
allowed mutation scope and acceptance bindings must still be recorded and
dispatched under P9-1.9 before implementation. These tests demonstrate the
policy component, not present or future user acceptance.

## P9-1.8: API, notebook, browser and scenarios

The separate [access guide](../../investigations/grc9v4-constitutive-design/tools/exploratory-side-tool/docs/Phase9VerificationGuide.md)
provides runnable CLI commands, API import, notebook and browser URL.
All three surfaces share `verification_status`; no browser-only authority
logic substitutes for the current-tree validator. The page refreshes current
checks and downloads source-bound status JSON. The notebook's actual code
cells execute and reproduce the same API payload. Desktop and mobile checks
cover live identity, failed refresh, forged/stale responses and read-only
routes. UI digests are planning-status integrity, not a runtime codec claim.

The [scenario register](./P9-1.7-1.8-Scenarios.json) connects pressures to real
executables. Normal verification additionally preserves the existing D11
API/notebook/browser suites and byte-exact rebuilds. The separate Phase 9
surface avoids altering the frozen scientific explorer or promoting the
still-candidate ET-C11 UX.

## Verification and next gate

Execution passes: 106 pressure cases across all six review assignments,
13 API checks, ten cross-surface checks, two real notebook code cells, eight new Node tests and fourteen
desktop/mobile browser tests. Actual-byte checks protect 712 baseline files.
The normal verifier also passes the four historical audits, the current
accepted-release/source/architecture checks, 17 existing Python commands,
20 existing Node tests, 16 existing browser tests and the byte-exact D11
rebuilds. Exact terminal summaries and separate leaf dispositions are retained
in the result. No previously failing scientific assertion was weakened.
Generated logs, receipts and screenshots live only under the ignored
`tool/generated/phase9-verification/` tree.

P9-1.9, P9-G1, ET-C11 UX human acceptance, all runtime gates and advertised
support remain pending. No `src/`, runtime `tests/`, dependency declaration,
older family, frozen specification, paper or scientific source is changed.

## Supplied pressure-guide corrections

The [closure register](./P9-1.6-1.8-AuditClosure.json) binds all three guides
and the static quick audit as engineering review inputs, not new authority.
The quick audit inspected the earlier 74-case preparation; the pre-correction
candidate here contained 79. P9-1.7/P9-1.8 were explicitly requested separately
from P9-1.6, so batching is not inferred scope or automatic acceptance.

Every case now separates candidate decision, harness assertion and project
effect, retaining actual rejection reason, decisive rule/trace, input and
verifier identities, exact mutation preimages and protected before/after
manifests. Actual commands retain output and exit code; in-process checks are
identified as such, not assigned a fabricated subprocess status. Three batches
retain independent results. Raw run archives include failures and successes.
The tracked pressure file is a stable coverage index; raw run evidence binds
the current policy and complete input manifest, avoiding a circular hash.

Added probes cover combined legitimate/forbidden edits, fully rehashed forged
runtime promotion, stale current subjects with unchanged counters, committed
clean unauthorized trees, unsupported cyclic/self-referential authority,
conflicting successors, and deliberate checker breakage. Ten source-admitted
projection mutations exercise classification, support, profile scope,
specialization, exact forty-cell identity, aliases, obligations, scheduling
and unknown-ID preservation beyond hash checks.

The first stricter run exposed three old profile-barrier negatives that failed
at a cycle/missing-node check rather than the intended scope rule. Independent
synthetic providers now let each case reach the scope comparison; expectation
matching was not weakened to accept the unrelated errors.

The [surface inventory](./P9-1.8-SurfaceInventory.json) states actual input,
execution/display, export and owner contracts, including unchanged historical
surfaces. The real normal-entry rejection is projected through API, actual
notebook cells, browser badges and JSON export with `candidate: rejected` and
`assertion: passed` preserved. A separately trusted future fixture remains
admitted only in simulation. Notebook mutation/checkout/out-of-order controls,
reversed browser response order, stale refresh, exact full IDs and source
meaning are tested. Accepted specifications and 152/183 indeterminate forensic
contract associations are shown together without promotion or loss of authority.
