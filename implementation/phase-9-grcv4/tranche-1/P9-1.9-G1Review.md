# P9-1.9 — accepted P9-G1 implementation authority

Status: user acceptance recorded; bounded implementation authorized. No runtime
code or model tests are added in this iteration.

## Accepted decision and sources

The user answered **“yes, i accept”** to acceptance of P9-1.4–P9-1.8 and the
reviewed V4-only modules, tests, packaged assets and two additive integration
points. The [acceptance record](./P9-1.9-G1Acceptance.json) binds that exchange,
the accepted specification release and review evidence at commit `6e0a507`.
P9-1.1–P9-1.3 retain their earlier separate acceptance. All historical review,
opening, audit and scientific-source bytes retain their as-recorded states.

The accepted specifications remain primary, with the paper and investigation
claims through the frozen [source crosswalk](./P9-1.1-SourceCrosswalk.json).
This step adds implementation permission, not scientific authority. The
152/183 indeterminate forensic contract associations are not upgraded.

## Exact scope and remaining gates

The record enumerates 43 targets: seventeen reviewed modules and their test
counterparts, two independent harness/oracle files, five packaged-asset files,
and the two additive integration files. The ten GRC9V4 module/test targets
remain held until an accepted exact-consumed-set P9-G3 successor exists.
Generic foundation work may begin under the reviewed per-iteration entry and
dependency rules: P9-2.1 and P9-2.2 are ready, with eleven eligible runtime
paths owned by those leaves. Listing a later module does not make it ready.
Later generic leaves require their accepted predecessors; integration remains
owned by P9-2.6. Existing families, legacy tests,
specifications, paper/proposal and accepted claim/debt records stay protected.

`src/pygrc/models/__init__.py` may only preserve its existing AST and append the
reviewed lazy GRCV4 export; importing old families must not load optional V4
dependencies. `pyproject.toml` may add only the V4 optional dependency extra
and asset-package data. Package/version compatibility and installation remain
implementation follow-through, not claims made by this record. No dependency
is installed here. Packaged schema/release/detached-digest bytes must equal
the frozen sources; asset loading and index validation belong to P9-2.2/2.6.

G1 is permission to implement; G2 is exact-profile conformance, G3 admits a
specialization support set, and G4 is release/handoff. G2/G3/G4 remain pending,
accepted support sets remain empty, and all fifteen pending source obligations
and five implementation follow-ups retain their evidence requirements. No
debt is discharged and ET-C11 UX human acceptance remains unchanged.

## Successor verification and future work

The [implementation boundary](../verification/Phase9ImplementationBoundary.json)
binds current verification/progress artifacts. A separately pinned acceptance
digest establishes scope; changing a work manifest or recomputing its digest
cannot install acceptance, enlarge the target roster or grant profile support.
Hashes are integrity checks, not signatures against an attacker who controls
the verifier and all local files.

The [runtime work manifest](../runtime/RuntimeWorkManifest.json) starts empty.
Future authorized work binds each concrete path, current SHA-256 and registered
leaf ID there. Exact new runtime paths are admitted only when registered;
unchanged integration files need no entry. Source crosswalk groups and the
accepted ownership register determine each target's eligible owning leaves;
a candidate cannot borrow a ready leaf's ID. The current accepted execution
prerequisite is G1 only. Later leaf acceptances enter through the existing
controlled per-leaf successor/review process, not a work manifest flag; they
do not require reaccepting G1 or obtaining approval per helper/function.
Evidence uses the reviewed per-leaf
run directory and seven filenames; new leaf execution/review records use
explicit leaf-derived names. Unknown leaves/paths, altered frozen files,
symlinks, changed modes and content/binding mismatches reject. Execution records
cannot delete or rename published runtime files under an add/update grant.
Published run evidence, including failures, is immutable; a new run gets a new
directory rather than overwriting the earlier output. These checks read Git
history as well as the index, so staging a deletion cannot hide it. Execution records
cannot grant G2/G3 support. Maintenance document changes require refreshed
boundary bindings; new scope or gate authority requires its own reviewed successor.

The two verification commands share the same active dispatcher. Deleting G1
records does not restore planning authority. `--boundary-only` is a diagnostic
scope selector, not a bypass or full-verification claim. Full verification
replays the unmodified V2 verifier on its accepted commit, including its 106
pressure cases, source/architecture checks, actual notebook/API/Node checks
and all four earlier historical audits. Those results remain about that exact
historical subject. Current G1 pressure and API/notebook/Node checks run against
the new subject; the normal command additionally retains D11 and browser checks.

The P9-1.9 request authorizes publication and verification maintenance separately
from the runtime grant it records. The actual acceptance subject is the reviewed
package at `6e0a507` and its forward scope, not a retrospective claim that the
user inspected the newly written V3 code. V3's exact code and presentation
bindings are separately tested as the implementation of that accepted decision.

## Scenarios and evidence

The supplied [P9-1.9 pressure guide](../verification/inputs/P9-1.9-G1-Acceptance-And-Scoped-Authorization-Pressure-Guide.md)
is retained byte-for-byte as engineering guidance, not acceptance or a new
schema mandate. The execution record maps all sixteen suggested outlier rows
to current tests, exact historical reuse, or explicit nonapplicability. It also
reconciles every consumed Tranche-1 leaf individually. The guide exposed the
need to enforce owning-leaf readiness as well as path/G3 scope; those are now
separate executable checks, including refusal to borrow a ready leaf's ID.

| Prerequisite | Exact accepted subject | Remaining condition / G1 effect |
| --- | --- | --- |
| P9-1.1 | `6e0a507`, `P9-1.1-SourceCrosswalk.json`; earlier 1.1–1.3 acceptance record | Source associations retain their original claim/support ceilings. |
| P9-1.2 | `6e0a507`, `P9-1.2-DebtInventory.json`; earlier 1.1–1.3 acceptance record | All 31 debt records retain exact source dispositions; no G1 discharge. |
| P9-1.3 | `6e0a507`, `P9-1.3-VerificationRouting.json`; earlier 1.1–1.3 acceptance record | Fifteen forward evidence obligations remain routed, not G1 blockers. |
| P9-1.4 | `6e0a507`, corrected support/dependency register; explicit current acceptance | Ready leaves derive from accepted edges; no all-profile barrier. |
| P9-1.5 | `6e0a507`, corrected ownership/baseline register; explicit current acceptance | Integration, packaging, independent oracles and five runtime follow-ups remain required. |
| P9-1.6 | `6e0a507`, original execution record and immutable candidate snapshot; explicit current acceptance | Original planning routing fix retained on its historical subject. |
| P9-1.7 | `6e0a507`, separate leaf result and 106-case coverage; explicit current acceptance | Replayed against the exact V2 checker; new G1 checker gets its own pressure. |
| P9-1.8 | `6e0a507`, separate leaf result, surface inventory and audit closure; explicit current acceptance | Historical surfaces retain their subject; current G1 representation is tested separately. |

Full path hashes and acceptance references are in the machine-readable
acceptance and execution records; mutable filenames or checklist marks alone
are not used as prerequisites. `P9-TOOL-001` is closed through the combined
normal-route, pressure and surface evidence, while its original failure remains
in the unchanged opening. Tranche 1 is reconciled as the review/authorization
tranche; this does not close Phase 9 or any runtime conformance gate.

[Executable G1 pressure](../verification/test_phase9_g1.py) covers accepted
scope and normal routing, authorized generic additions, additive dependency
and lazy export controls, immutable assets, forged/stale/missing acceptance,
unregistered paths/leaves, premature specialization, content/mode/symlink
mutations, combined faults and support promotion. All mutations occur in
disposable checkouts. Each result retains its exact mutation bytes, intended
rejection, observed decision, read-only check trace and restoration identity.
Negative-test success stays distinct from candidate admission and live effect.

Current API, executable notebook and desktop/mobile browser/export show
`P9_G1_accepted: true` and `runtime_authorized: true` for bounded implementation,
with empty support sets. Missing or stale authority withholds permission;
refresh failure clears an earlier accepted badge. The independent probe panel
cannot create acceptance. Historical planning checkouts still report false.

Raw runs, predecessor receipts and surface/browser outputs are retained under
the ignored `tool/generated/phase9-verification/` tree. The
[execution record](./P9-1.9-ExecutionRecord.json) gives commands, result scope,
remaining work and the next permitted step. No runtime implementation is
inferred from a successful verification run.
